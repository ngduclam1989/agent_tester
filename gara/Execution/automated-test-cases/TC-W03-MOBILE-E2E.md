---
document_id: 'GMS-TC-W03-MOBILE-E2E'
type: test-case
parent: 'Execution/test-cases/TEST-CASE-REGISTRY.md'
status: ACTIVE
version: 7
boundary: 'garage-mobile, gf-inventory, agg-garage-graph, garage-web'
wave: 'W03'
owner: 'agent-test-mobile-e2e'
last_reviewed: '2026-07-03'
qa_reviewed_by: 'cuongnguyen_ac@cardoctor.vn'
qa_reviewed_at: '2026-07-02'
drift_impact:
  - report: 'Execution/tracking/drift-impact/W03-2026-07-03T15-02-35Z.md'
    timestamp: '2026-07-03T15:02:35+00:00'
    impacted_ids: ['FEAT-CAT-GRP-CREATE', 'FEAT-CAT-PROD-CREATE', 'FEAT-CAT-PROD-DETAIL', 'FEAT-CAT-PROD-EDIT', 'FEAT-ID-CREATE-V2', 'FEAT-IR-CREATE-V2', 'TC-W03-ME2E-008', 'TC-W03-ME2E-017', 'TC-W03-ME2E-018', 'TC-W03-ME2E-019', 'TC-W03-ME2E-025', 'TC-W03-ME2E-026', 'TC-W03-ME2E-027', 'TC-W03-ME2E-032', 'TC-W03-ME2E-035', 'TC-W03-ME2E-038', 'TC-W03-ME2E-039', 'TC-W03-ME2E-041', 'TC-W03-ME2E-043', 'TC-W03-ME2E-047', 'TC-W03-ME2E-048']

---

# Test Case Automated — W03: Mobile E2E (EP-INVENTORY-CATALOG — Danh mục vật tư)

> Cross-ref manual: `Execution/test-cases/TC-W03-MOBILE-E2E.md` — 6 TC cross-platform sync (TC-W03-E2E-015/016/017/018/036/037).
> Cross-ref manual (mobile-only, housed trong file web E2E theo quy ước QA Authority): `Execution/test-cases/TC-W03-E2E.md` TC-W03-E2E-026/027.
> Cross-ref web E2E automated: `Execution/automated-test-cases/TC-W03-E2E.md` (web counterpart journey — agent-test-e2e).
> Work Package: `Execution/work-packages/PKG-W03-inventory-catalog.md` §2.2.4 + §4.3 + §5.1 (garage-mobile).
> Scope note: PKG §4.3 gán mobile Patrol scope dưới label bảng `agent-test-e2e` (viết trước khi `agent-test-mobile-e2e` tách riêng) — chủ sở hữu thực tế theo `.agents/agent-test-mobile-e2e.md` §Wave Assignments "W03 — Danh mục vật tư (E2E mobile)" (v6, thêm 2026-06-30). Dòng "W03 Mobile defer per CR-1748764800.A2" trong §Activation của agent definition trỏ tới W03 **Insurance Dashboard (DEFERRED)** — KHÔNG áp dụng cho W03 Inventory Catalog này (2 wave khác nhau cùng số W03 trong lịch sử wave-planning). Mobile IN SCOPE cho wave này theo `Plan/README.md §4.1`.

---

## 1. General Info

| Field | Value |
|---|---|
| Document ID | `GMS-TC-W03-MOBILE-E2E` |
| Wave | W03 |
| Boundary(ies) | `garage-mobile`, `gf-inventory`, `agg-garage-graph`, `garage-web` (cross-platform sync) |
| Feature(s) | `FEAT-INV-MOBILE-MENU`, `FEAT-CAT-GRP-LIST`, `FEAT-CAT-GRP-CREATE`, `FEAT-CAT-GRP-DETAIL`, `FEAT-CAT-GRP-EDIT`, `FEAT-CAT-GRP-DELETE`, `FEAT-CAT-PROD-LIST`, `FEAT-CAT-PROD-DETAIL` |
| Owner | `agent-test-mobile-e2e` |
| Last Reviewed | 2026-07-02 |
| Work Package | `Execution/work-packages/PKG-W03-inventory-catalog.md` |

---

## 2. Scope

### In Scope

- **Hub điều hướng** (`FEAT-INV-MOBILE-MENU`): Home → tile "Quản lý kho hàng" → Hub grid 2 cột (W03 chỉ ship 2 tile: Sản phẩm + Nhóm vật tư, 4 tile khác ẨN HOÀN TOÀN) → tap tile → sub-module; back stack preserved.
- **Material Group full CRUD journey mobile** (`FEAT-CAT-GRP-LIST/CREATE/DETAIL/EDIT/DELETE`): List (flat card, 3-tab status filter, search, filter, pull-to-refresh, load-more) → Create → Detail (6 field + audit) → Edit (mã khoá) → Delete (2 popup: Xác nhận/Không thể xoá).
- **Cascade INACTIVE mobile-native**: tạo cây 3 cấp qua mobile form → Edit cha → Ngừng hoạt động → verify con/cháu cascade qua backend (BR-CAT-GRP-007), flat card list phản ánh đúng `parentName`.
- **Internal Product view-only journey mobile** (`FEAT-CAT-PROD-LIST/DETAIL`): List (view-only, không Create/Import/Export) → Detail (4 card scroll, không Tabs, không Sửa/Xóa).
- **Cross-platform parity Web ↔ Mobile**: business outcome đồng nhất giữa 2 nền tảng cho Group CRUD + Product view-only — mirror 6 TC manual cross-platform sync + 2 TC mobile-only enforcement (housed ở manual `TC-W03-E2E.md`).
- **Native interaction**: pull-to-refresh (`SmartRefresher`), system back gesture (Android hardware back + iOS swipe-from-edge), background↔foreground state restoration, offline banner, network timeout/5xx.
- **Co-located regression**: existing production "Danh sách kho" (`InventoryListRoute` — Phiếu nhập/Phiếu xuất/Tồn kho V1) không bị catalog V2 phá vỡ — **CRITICAL FINDING**: route hiện đã mất entry point (xem TC-W03-ME2E-005).
- **Dual persona parity** (`BR-CAT-CMN-003`): chủ garage + kế toán quyền ngang nhau trên toàn bộ journey mobile.

### Out of Scope

- UI render/wording/golden pixel isolated từng widget → `agent-test-mobile-ui` (`TC-W03-MOBILE-UI.md`).
- API contract / schema / status code / error-code exhaustive → `agent-test-api` (`TC-W03-API.md`).
- Cross-tenant denial (garage-a vs garage-b) → `agent-test-isolation` (`TC-W03-ISOLATION.md`).
- Auth/authz abuse, OWASP, injection → `agent-test-security`.
- SLO latency/throughput (list 1000 nhóm p95, DataLoader N+1) → `agent-test-performance`.
- Web-only journey (cascade rollback atomicity mock 500, import/export wizard, Product CRUD full field validation) → `agent-test-e2e` (`TC-W03-E2E.md`).
- **Product CRUD/Import/Export trên mobile** — KHÔNG tồn tại (CR-1782373204 mobile scope partial: Product = view-only List+Detail). Không sinh TC cho `InternalProductFormPage`/`InternalProductImportPage` vì trang KHÔNG được implement trên mobile (xác nhận qua source: không có route `InternalProductCreateRoute`/`InternalProductEditRoute`/`InternalProductImportRoute` trong `router.dart`).
- **Camera capture** — `out-of-scope+lý do`: không có tab Đính kèm file trên `InternalProductDetailPage` mobile (chỉ 4 card: Header/GeneralInfo/TechnicalSpec/SpecDescription — xác nhận qua source `internal_product_detail_page.dart`); Product attachment/upload = web-only (`FEAT-CAT-PROD-CREATE`/`-EDIT`, out of mobile scope). App có sẵn `image_picker: ^1.1.2` (dùng cho FEAT-INS-DOSSIER W01/W02) nhưng KHÔNG import trong `lib/ui/inventory_catalog/**`.
- **File picker (upload)** — `out-of-scope+lý do`: cùng lý do camera — Product attachment = web-only W03; app có `file_picker: ^10.0.36` nhưng không wire vào catalog.
- **Push notification FCM** — `out-of-scope+lý do`: Material Group/Internal Product CRUD hoàn toàn synchronous REST/GraphQL (PKG §2.2 không có Kafka event/outbox cho catalog); không có async side-effect nào trigger push. App có `firebase_messaging: ^15.2.9` (dùng cho FEAT-INS-STL W01) nhưng không liên quan catalog.
- **Deeplink (Android App Links / iOS Universal Links)** — `out-of-scope+lý do`: xác nhận qua source `android/app/src/main/AndroidManifest.xml` — KHÔNG có `intent-filter` nào khai báo `android:scheme`/`android:host` cho `action.VIEW` + `category.BROWSABLE` (chỉ có VOIP call intents + local-notification receivers). App hoàn toàn KHÔNG có App Links/Universal Links capability cho bất kỳ domain nào (không riêng Material Group Detail). Wave assignment block "deeplink vào Group Detail" là aspirational — flag cho BA/mobile dev nếu cần CR bổ sung W04+; KHÔNG bịa TC cho capability không tồn tại.
- **Biometric auth (`local_auth`)** — `out-of-scope+lý do`: package `local_auth` KHÔNG có trong `pubspec.yaml` — app hoàn toàn không có biometric login ở bất kỳ feature nào (không riêng W03).
- **Offline-first CRUD / offline queue** — `out-of-scope+lý do`: PKG §2.2.4 chốt "catalog list KHÔNG cache offline ... Group form submit yêu cầu online" — chỉ có offline banner (covered TC-W03-ME2E-028), không có action queue.
- **App store install/update, OS-level interrupt (call/SMS), install method matrix, cross-version compatibility** — `out-of-scope+lý do`: không có thay đổi liên quan W03; baseline platform behavior không bị catalog feature tác động.

### Test Environment & Data

| Item | Required Data / Setup | Notes |
|---|---|---|
| Runner chính (C3/C4) | QC-owned harness `Execution/auto/harness/patrol/` (Layer A frozen, CR-20260701-03 — reuse nguyên trạng) | `patrol_cli >= 2.8.0`; nguồn flavor/applicationId thật: `mobile/gf-garage-app/android/app/build.gradle` (symlink `mobile/gf-garage-app -> gf-garage-app` per PKG §3.C) |
| Runner phụ (C1/C2) | QC-owned harness `Execution/auto/harness/integration-test/` (Layer A frozen) | Cho TC không cần native gesture (tab filter, search, filter apply, list render) |
| Device matrix (C3) | Android API 33 emulator (Pixel 6) + API 28 tablet | `adb devices` verify trước chạy |
| Device matrix (C4) | iOS 16+ simulator (iPhone 14) — cho TC iOS-specific (TC-007 swipe-back) + iPad (TC-045 tablet) | `xcrun simctl list devices booted` |
| **Fix bắt buộc trước run** (PKG §3.C remote-box) | `android/app/build.gradle:201` `androidx.test:runner:1.6.2 → 1.5.1` (BUG-W02-136/BUG-W02-068 pattern vẫn còn theo lesson TL-W02-MOB-E2E-002/003) | Rule #11 NO-CODE — fix qua `agent-fix-garage-mobile`, không tự sửa; nếu vẫn present ở run W03 → `BLOCKED-by-harness`, KHÔNG re-run mù |
| **BFF host resolution (remote-box mode)** | `Flavor.dev.graphQLUrl` hard-code `http://localhost:45401/garage/graphql` (dead code sau early-return trong `lib/flavors.dart:92`); `graphQLSSOUrl` hard-code `http://localhost:45410/graphql`. `stag`/`pt` flavor trả `''` (empty — không dùng được, xem TL-W02-MOB-E2E-002). | Android emulator: `adb reverse tcp:45401 tcp:45401` + `adb reverse tcp:45410 tcp:45410` để map device `localhost` → host machine, sau đó host forward tới `192.168.110.191:45401`/`:45410` (SSH tunnel hoặc `socat TCP-LISTEN:45401,fork TCP:192.168.110.191:45401` chạy trên máy host). iOS simulator dùng chung network stack host — chỉ cần forward ở host, không cần `adb reverse`. Build/run app với `flavor: dev` (KHÔNG `stag`). |
| Feature flag | `FlagName.inventory` = ON | Gate cho Home "Mission" tile "Quản lý kho hàng" → `InventoryHubRoute()` (`lib/ui/main/home/widgets/mission_function_widget.dart:101-113`) — entry point thật, khác với FEAT-INV-MOBILE-MENU AC-1 "NEED CONFIRMATION" (BA chưa formal-confirm nhưng implementation đã resolve qua Home Mission tile) |
| Account kế toán | `accountant@garage-a.test` / `Test@123456` — tenant `garage-a` | |
| Account chủ garage | `owner@garage-a.test` / `Test@123456` — tenant `garage-a` | Dual persona parity (TC-048) |
| Seed group tree | 3-cấp `GRP-A → GRP-A1 → GRP-A11` tạo **qua mobile UI** (không pre-seed DB) cho cascade TC-024 | Theo lesson TL-W01-API-007(e) — end-to-end seed qua flow thật để không che giấu bug write-path |
| Seed group trống | `GRP-EMPTY-MOB` (không product, không con) cho happy-delete TC-020 | |
| Seed group có con | `GRP-HAS-CHILD-MOB` cho blocked-delete TC-022 | |
| Seed group có product | `GRP-HAS-PROD-MOB` (đã gắn ≥1 mã sản phẩm) cho blocked-delete TC-023 | |
| Seed product (view-only) | `PROD-MOB-VIEW-001` bất kỳ mã đã có đủ enrichment (`mainUnitDisplayName`, `originDisplayName`, `materialGroupName`, `brand`) cho TC-032 | |
| Web client (cross-platform TC) | `garage-web` reachable `http://192.168.110.191:45300` | Nguồn tạo/sửa dữ liệu song song cho TC-038..043 |
| GraphQL BFF observer (C2) | `agg-garage-graph` `http://192.168.110.191:45401/garage/graphql` — dùng để assert TC-044 (Group List KHÔNG gọi `getMaterialGroupTree` Q2) qua access log hoặc DB query counter | |
| Flutter SDK | Flutter 3.41+ / Dart 3.11+ | `flutter --version` |
| Smoke preflight | `patrol test --target Execution/auto/specs/W03/mobile-e2e/smoke_patrol_test.dart -d <id>` | Reuse W02 `harness_smoke_patrol_test.dart` pattern (cold-start + assert 1 widget) |

**Common Baseline Coverage Map (common-testcase-mobile-e2e.md §1-§21 — sàn tối thiểu):**

| Common Group | Trạng thái | TC(s) | Ghi chú |
|---|---|---|---|
| §1 Auth — Login → home → FCM register | `out-of-scope+lý do` | — | Auth journey = W01 baseline foundation; không thay đổi ở W03 |
| §1 Auth — Token refresh mid-session | `adapted` | TC-W03-ME2E-047 | Session expiry mid-create-group flow |
| §1 Auth — Logout | `out-of-scope+lý do` | — | Không thay đổi W03 |
| §2 CRUD — Create entity qua form mobile → BFF → DB | `covered` | TC-W03-ME2E-008 | `MOB-E2E-CRUD-001` |
| §2 CRUD — Read qua deeplink | `out-of-scope+lý do` | — | Không có deeplink capability (xem §Out of Scope) |
| §2 CRUD — Update entity | `covered` | TC-W03-ME2E-015 | `MOB-E2E-CRUD-003` |
| §2 CRUD — Delete với confirmation dialog | `covered` | TC-W03-ME2E-020, -021, -022, -023 | `MOB-E2E-CRUD-004` |
| §2 CRUD — File upload native picker | `out-of-scope+lý do` | — | Product attachment = web-only |
| §2 CRUD — Camera capture | `out-of-scope+lý do` | — | Không có camera trong catalog scope |
| §3 Search & Filter | `covered` | TC-W03-ME2E-011, -012, -036, -037 | `MOB-E2E-SCH-001/002/004` |
| §4 Pagination + Lazy load + Pull-to-refresh | `covered` | TC-W03-ME2E-009 (pull-refresh), -030 (load-more), -034, -035 | `MOB-E2E-PAG-001/002` |
| §5 File Upload/Download | `out-of-scope+lý do` | — | Không áp dụng scope mobile catalog W03 |
| §6 Camera capture | `out-of-scope+lý do` | — | Không áp dụng |
| §7 Permission/Role-based access | `covered` | TC-W03-ME2E-048 (dual persona parity) | `MOB-E2E-PRM-001/002` |
| §8 Push notification (FCM) | `out-of-scope+lý do` | — | Catalog CRUD synchronous, không async trigger push |
| §9 Deeplink flows | `out-of-scope+lý do` | — | App không có App Links/Universal Links intent-filter (xác nhận source) |
| §10 Navigation back + system back gesture | `covered` | TC-W03-ME2E-002, -006, -007 | `MOB-E2E-NAV-001/002/003` |
| §11 Offline-to-online sync | `adapted` (chỉ offline banner, không queue) | TC-W03-ME2E-028 | Catalog KHÔNG cache offline per PKG §2.2.4 |
| §12 App lifecycle background/foreground | `covered` | TC-W03-ME2E-025 | `MOB-E2E-LIF-001` |
| §13 Concurrent/multi-device | `covered` (spec-gap 1 nhánh) | TC-W03-ME2E-046 | Concurrent-edit mobile A/web B — cần optimistic-lock spec từ BFF, đánh dấu `spec-gap` phần assert conflict UI cụ thể |
| §14 Network resilience | `covered` | TC-W03-ME2E-026, -027 | `MOB-E2E-NET-002/005` |
| §15 Localization Vietnamese | `covered` | TC-W03-ME2E-049 | `MOB-E2E-LOC-001` |
| §16 App store install/update | `out-of-scope+lý do` | — | Không thay đổi W03 |
| §17 Performance sanity | `out-of-scope+lý do` | — | `agent-test-performance` sở hữu (PKG §4.3 row riêng) |
| §18 OS-level interrupt | `out-of-scope+lý do` | — | Không thay đổi W03, device thật chuyên biệt |
| §19 Install/Uninstall/Update | `out-of-scope+lý do` | — | Không thay đổi W03 |
| §20 Cross-version compat | `out-of-scope+lý do` | — | Không có local DB schema migration W03 |
| §21 External link/inter-app | `out-of-scope+lý do` | — | Không có external link trong catalog feature |

**Regression scope (Step 3.1 + Co-located Journey Regression Inventory Step 3.2):**

| Screen | Existing journey co-located | Native interaction | Impact CR | Regression TC | Cluster |
|---|---|---|---|---|---|
| Home "Quản lý kho hàng" mission tile | Chủ garage/kế toán tap tile → xem "Danh sách kho" (`InventoryListRoute` — tabs Phiếu nhập V1/Phiếu xuất V1/Tồn kho V1/Tồn đầu kỳ V1, widget đầy đủ tại `lib/ui/inventory/inventory_list/`) — feature production đã tồn tại | Pull-to-refresh (SmartRefresher) | **CRITICAL** — `mission_function_widget.dart:111` đổi `context.pushRoute(InventoryHubRoute())` thay `InventoryListRoute()`; grep toàn bộ `lib/` xác nhận **`InventoryListRoute()` KHÔNG còn được push từ bất kỳ widget nào** (0 caller ngoài khai báo route). Route hiện chỉ đến được nếu Hub có tile trỏ tới, nhưng Hub W03 chỉ ship 2/6 tile (Phiếu nhập/Phiếu xuất/Tồn kho/Tồn đầu kỳ đều ẨN tới W04-W06) | TC-W03-ME2E-005 (regression, P1 — **re-run bắt buộc, không mirror PASS cũ**) | C3 |
| Group List → Detail → Edit/Delete | Baseline navigation pattern (AppBar back, AutoRoute push/pop) dùng chung `CustomScaffold`/`AppBarCustom` với các domain khác (inventory receipt/delivery/stock) | System back gesture | LOW — chỉ thêm route mới, không đụng widget chung theo cách phá vỡ | TC-W03-ME2E-006, -007 | C3 |
| `StatusBadge` + `StartInfoRow` (inventory sibling widgets, reuse cross-domain) | Widget dùng chung bởi receipt/delivery/stock detail hiện có | Không | LOW — chỉ thêm adapter `MaterialGroupStatus`/`InternalProductStatus implements InventoryStatusInterface` (~15 LOC mỗi extension, additive) | Đã cover gián tiếp qua TC-013/032 (render đúng badge/label không phá vỡ shared widget) | C2 |

**Auto vs Manual Parity Audit (journey level):**

| Manual TC | Auto match | Phân loại |
|---|---|---|
| TC-W03-E2E-015 (Mobile tạo nhóm → Web kiểm tra) | TC-W03-ME2E-038 | `covered` |
| TC-W03-E2E-016 (Web tạo nhóm → Mobile pull-refresh) | TC-W03-ME2E-039 | `covered` |
| TC-W03-E2E-017 (Mobile xóa nhóm → Web reload) | TC-W03-ME2E-040 | `covered` |
| TC-W03-E2E-018 (Web tạo mã → Mobile List+Detail view-only) | TC-W03-ME2E-041 | `covered` |
| TC-W03-E2E-036 (Web sửa nhóm → Mobile List/Detail phản ánh) | TC-W03-ME2E-042 | `covered` |
| TC-W03-E2E-037 (Web sửa mã SP → Mobile Detail view-only phản ánh) | TC-W03-ME2E-043 | `covered` |
| TC-W03-E2E-026 (mobile-only, housed trong `TC-W03-E2E.md` — Mobile KHÔNG gọi Q2 tree) | TC-W03-ME2E-044 | `covered` — TC này về bản chất là mobile-native journey (thuộc territory `agent-test-mobile-e2e` theo Anti-Duplication Routing); adopt vào auto artifact dù QA Authority housed ở file web-E2E manual theo convention riêng của họ |
| TC-W03-E2E-027 (mobile-only, housed trong `TC-W03-E2E.md` — Product view-only enforce) | TC-W03-ME2E-031 + TC-W03-ME2E-032 | `covered` — tách 2 TC riêng (List enforcement + Detail enforcement) theo Granularity Floor thay vì 1 row gộp |

Không còn `auto-miss` chưa phân loại sau parity diff.

**Self-Audit (Coverage Depth Gate — Step 3.1/3.2 + Common Baseline):**

- Impacted production journey: Home "Quản lý kho hàng" mission tile → `InventoryListRoute` orphaned → **TC-W03-ME2E-005 (regression, CRITICAL finding, P1)**.
- Deep flow trace: Group CRUD chain (List→Create→Detail→Edit→Delete) đầy đủ 5 FEAT + cascade cross-boundary (backend recursive CTE + cascade transaction) → TC-024.
- Exception/Timeout (10 nhánh): network timeout `covered` TC-026; 5xx `covered` TC-027; concurrent-edit `covered+spec-gap` TC-046; session expiry `covered` TC-047; partial failure/rollback `spec-gap` (không có TC riêng — cascade rollback atomicity đã cover phía `agent-test-e2e` web TC-W03-E2E-020 mock 500; mobile không có mock-injection harness cho DB-layer failure, chỉ verify happy cascade); permission denied `out-of-scope` (không có permission dialog trong catalog scope); deeplink edge case `out-of-scope+lý do` (không có capability); push edge case `out-of-scope+lý do`; background-foreground `covered` TC-025; offline-to-online `adapted` TC-028 (chỉ banner, không sync).
- Common Baseline Checklist review (cuối `common-testcase-mobile-e2e.md`) đối chiếu tại §6 bên dưới.

---

## 3. Status Summary

| Coverage Mode | Total | Status Summary |
|---|---|---|
| Automated | 49 | **6 FAIL + 43 BLOCKED** (TEST_EXECUTION Run 3, 2026-07-03 — **harness tap-hang (`ENV-HANG-R2`) ĐÃ FIX** trong Run 3: root cause = `pumpAndSettle()`/native automator "chờ tới khi idle" không bao giờ tự settle được trên màn Home có polling định kỳ, và `Future.timeout()` không huỷ được future gốc → vòng lặp bơm-frame orphan chạy nền vô hạn, kéo CPU host emulator >1000% (xác nhận qua `ps aux`+`adb shell dumpsys cpuinfo`, giảm về ~60% ngay sau `adb shell am force-stop` app). Fix: loại bỏ HOÀN TOÀN `pumpAndSettle` trong `_helpers.dart`, thay bằng `tapBounded`/`pumpFrames` (bơm frame CỐ ĐỊNH SỐ LƯỢNG, không "chờ tới khi ổn định") + `nativeBackBounded` (native back cũng bọc timeout) + `withTimeout` (diagnostic). Verify: 3 lần chạy Patrol live HOÀN TẤT KHÔNG TREO (`tap_fix_smoke_patrol_test.dart` x2, `inventory_hub_patrol_test.dart` x1 — TC-001..006). **NHƯNG phát hiện `BUG-W03-152` (P2, MỚI)**: lỗi async không catch (CometChat SDK ngay sau login; + 1 lỗi tương tự ngay sau xác nhận xoá nhóm) trip `flutter_test` binding assertion `_pendingExceptionDetails`, khiến JUnit verdict tổng thể = `failure` dù business logic thật (Hub render/nav/back/debounce) PASS qua evidence logcat (TC-001..006 → `FAIL` linked `BUG-W03-152`, business đúng nhưng verdict chính thức KHÔNG PASS theo `MOBILE_E2E_INSPECTION_PASS` guard). `material_group_crud_patrol_test.dart` (12 TC gộp TC-008/013-015/017-024) chạy được qua create/edit/detail/cascade-validation nhưng STALL hẳn (không còn tiến) ngay sau tap "Xác nhận" xoá nhóm (TC-020) — trigger #2 của cùng BUG-W03-152, cơ chế khác (binding corrupt, KHÔNG phải CPU runaway) — 43 TC còn lại (008-049 trừ 001-006) giữ `BLOCKED` với Bug ID `BUG-W03-152` (thực thi chưa hoàn tất, KHÔNG PASS/FAIL giả). 2 harness defect khác phát hiện+fix cùng Run 3: (a) Android Test Orchestrator crash `IllegalArgumentException ... contains a path separator` khi patrolTest() title chứa "/" (fix: đổi "/" → "+" trong 3 title); (b) missing import `material_group_models.dart` gây compile error 2 file. Xem `Execution/test-reports/TR-W03-MOBILE-E2E.md` Run 3 + `Tracking/WAVE03/verify/BUG-W03-152.verify.md` + lesson learned `TL-W03-MOB-E2E-005/006/007`.) |
| Manual | 6 (`TC-W03-MOBILE-E2E.md`) + 2 (`TC-W03-E2E-026/027`, housed ở file web) | 8 chưa execute — cùng blocker BUG-W03-152 áp dụng nếu chạy qua app thật |
| Automated | 49 | **36 FAIL + 13 BLOCKED** (TEST_EXECUTION Run 5, 2026-07-03 — rebuild APK từ source hiện tại chứa fix `BUG-W03-153` (`lib/start.dart` chain handler + guard `firebase_notification_manager.dart`/`main_cubit.dart`), fix môi trường host bắt buộc trước run: Flutter `--jdk-dir` bị trỏ nhầm vào JBR quarantined của Android Studio (Homebrew Cask) → `flutter config --jdk-dir=<JDK không quarantine>` (nếu không fix, MỌI `patrol test` build fail instant `Gradle build failed with code -9`, KHÔNG liên quan product/spec). **`BUG-W03-153` → VERIFIED**: re-run Patrol live **7 lần độc lập qua 5 spec file** (bao gồm CẢ 2 spec từng STALL deterministic Run 4) — 7/7 HOÀN TẤT 10-14s, 0/7 STALL, 0/7 trip `_pendingExceptionDetails` (xác nhận `grep -c` = 0 trên toàn bộ log). **NHƯNG lộ ra 2 bug MỚI ngoài phạm vi 153**: `BUG-W03-155` (P2 — `getCurrentUser.role="garage-owner"` không decode được bởi enum mobile `GARAGE_OWNER`/`CA`, 2/7 lần) và `BUG-W03-156` (P2 — `handleReceivePushNotification()` gọi `requestPermission()` inline race với `init()`'s call đã guard, dominant 5/7 lần) — cả 2 fire NGAY SAU login/Home bootstrap, TRƯỚC khi bất kỳ Hub navigation nào bắt đầu, nên toàn bộ TC trong file đang chạy nhận CÙNG verdict FAIL nhanh/thật (không phải giả). Kết quả cuối: TC-001..006/008/009/011-015/017-026/028/031-034/036/037/049 (31 TC, spec `inventory_hub`/`material_group_crud`/`internal_product_view_only`/`material_group_resilience`) → `FAIL` linked `BUG-W03-156`; TC-039/040/042/046/048 (5 TC, `cross_cutting_patrol_test.dart` — sau khi fix 1 spec-level bug DI-ordering `getIt<InventoryCatalogRepository>()` gọi trước `loginAs()`) → `FAIL` linked `BUG-W03-155` (owner login luôn hit đúng bug này). 13 TC còn lại (`TC-007/010/016/027/029/030/035/038/041/043/044/045/047`) → `BLOCKED` (spec-gap: chưa có spec file hoặc header comment claim coverage nhưng thân spec chưa thực sự assert). **0 TC PASS** trong Run 5 — dù BUG-152 VÀ BUG-153 nay đều VERIFIED, 2 bug MỚI (155/156) tiếp tục chặn verdict PASS sạch cho toàn bộ suite (cùng blast-radius pattern, khác root cause). Fix spec-level áp dụng cùng Run 5 (Layer B, được phép): `internal_product_view_only_patrol_test.dart` đổi `find.text('Tất cả')` → `find.widgetWithText(Tab, 'Tất cả')` (loại ambiguity finder, KHÔNG còn TimeoutException tại `autoFixA41`); `cross_cutting_patrol_test.dart` sửa thứ tự `getIt<...>()` sau `loginAs()`. Xem `Execution/test-reports/TR-W03-MOBILE-E2E.md` Run 5 + `Tracking/WAVE03/verify/BUG-W03-153.verify.md` (VERIFIED) + `verify/BUG-W03-155.verify.md` + `verify/BUG-W03-156.verify.md` (2 bug mới).) |
| Automated | 49 | **10 PASS + 26 FAIL + 13 BLOCKED** (TEST_EXECUTION Run 6, 2026-07-03 — verify `BUG-W03-155`/`BUG-W03-156` (FIX_DONE→VERIFIED) + execute 49 TC tới business verdict thật. Rebuild APK từ source hiện tại (chứa fix 155 `user_role.dart`/`profile_response.dart` + fix 156 `firebase_notification_manager.dart`). Chạy `inventory_hub_patrol_test.dart` (TC-001..006) → **PASS THẬT SẠCH ĐẦU TIÊN CỦA TOÀN BỘ WAVE** (1/1 Successful, 0 exception, 30s) — 0 dấu vết `ArgumentError`/`FirebaseException` qua toàn bộ logcat → **`BUG-W03-155` VÀ `BUG-W03-156` → VERIFIED**. Chạy tiếp 4 spec file còn lại: `material_group_resilience_patrol_test.dart` (TC-025/026/028/049) → FAIL lần 1 (timing flake network-response chậm bất thường tại 1 bước `enterText`, KHÔNG deterministic) → **PASS SẠCH lần retry 2** (xác nhận đúng là flake, không phải bug thật) → **10 TC PASS** (6 Hub + 4 Resilience). `material_group_crud_patrol_test.dart` (TC-008/013-015/017-024) → TC-008 (Create+List) chạy đúng nhưng ngay khi mở Detail (TC-013) FAIL với `ServerError: Unauthorized` — điều tra qua GraphQL introspection trực tiếp xác nhận **`BUG-W03-157` (P1, MỚI)**: `getMaterialGroup` select `createdByName`/`updatedByName` (ĐÚNG theo contract doc R20 v7.22/23, 2026-06-25) nhưng BFF deployed THIẾU 2 field này (`GRAPHQL_VALIDATION_FAILED`, deployment/contract drift phía `agg-garage-graph`, cùng pattern `BUG-W03-154`) → chặn hoàn toàn Detail/Edit/Delete/Cascade. `cross_cutting_patrol_test.dart` (TC-039/040/042/046/048) → xác nhận lại đúng `BUG-W03-157` (TC-048 mở Detail sớm). `internal_product_view_only_patrol_test.dart` (TC-009/011/012/031-037) → **timeout deterministic 3/3 lần** tại tap Tab "Tất cả" ngay sau khi xem tab "Ngừng hoạt động" (empty state 0 kết quả) — filed **`BUG-W03-158` (P2, MỚI, root cause CHƯA xác định 100% — cần re-audit app hit-test vs Patrol limitation)**. Kết quả cuối: **10 TC PASS** (TC-001..006/025/026/028/049) — LẦN ĐẦU TIÊN có PASS thật trong toàn bộ lịch sử Run 1-6 của wave này; **26 TC FAIL** (17 TC linked `BUG-W03-157`: TC-008/013-015/017-024/039/040/042/046/048; 9 TC linked `BUG-W03-158`: TC-009/011/012/031-034/036/037); **13 TC BLOCKED** (spec-gap, không đổi: `TC-007/010/016/027/029/030/035/038/041/043/044/045/047`). Verdict: **NO-GO nhưng tiến bộ ĐỘT PHÁ** — toàn bộ chuỗi 5 bug foundation-level (150→152→153→155/156) đã VERIFIED hết, journey catalog ĐẦU TIÊN (Hub navigation + Resilience) đã đạt PASS xanh thật qua Patrol live device; 2 bug MỚI (157/158) nay đều ở tầng business-logic/contract-deployment cụ thể (không còn foundation-level/test-infra) — xác nhận pattern "mỗi lớp fix lộ ra lớp business kế tiếp" tiếp tục đúng nhưng đã tiến rất sâu vào business logic thật của Material Group CRUD. Xem `Execution/test-reports/TR-W03-MOBILE-E2E.md` Run 6 + `Tracking/WAVE03/verify/BUG-W03-157.verify.md` + `verify/BUG-W03-158.verify.md`.) |
| Automated | 49 | **6 FAIL + 43 BLOCKED** (TEST_EXECUTION Run 4, 2026-07-03 — rebuild APK từ source hiện tại chứa fix `BUG-W03-152` (`main_cubit.dart initialCometChat()`), re-run Patrol live 5 lần độc lập qua 3 spec file khác nhau. **`BUG-W03-152` → VERIFIED** (evidence: lỗi CometChat token-parse nay `catch`+log, KHÔNG còn re-throw — 2/2 lần `inventory_hub_patrol_test.dart`). **NHƯNG phát hiện `BUG-W03-153` (P2, MỚI)** — chuỗi lỗi async/test-binding KHÁC (không liên quan CometChat) tiếp tục trip cùng assertion `_pendingExceptionDetails`: (a) GraphQL `SaveTokenInput` schema-mismatch qua `saveFcmToken` (fire-and-forget, không await) — test HOÀN TẤT (30s) với verdict `failure` (TC-001..006 → `FAIL`, business logic đúng qua evidence nhưng verdict chính thức KHÔNG PASS); (b) STALL deterministic 2/2 lần tại bước mở Group Detail (`material_group_crud_patrol_test.dart`, 12 TC gộp TC-008/013-015/017-024 → `BLOCKED`, không đạt verdict); (c) STALL tại `tapBounded[autoFixA41]` timeout thật 15s (`internal_product_view_only_patrol_test.dart`, 9 TC gộp TC-009/010-012/030-034/036/037 → `BLOCKED`). Root cause hypothesis (`BUG-W03-153.verify.md` §1): `lib/start.dart` set `FlutterError.onError`/`PlatformDispatcher.instance.onError` PERMANENT (Crashlytics handler) không chain/restore handler gốc của `flutter_test`/Patrol binding → BẤT KỲ lỗi nào (app-side hay test-side, kể cả 1 `TimeoutException` CÓ CHỦ ĐÍCH từ chính helper) đều trip cùng assertion. 2 spec file còn lại chưa kịp chạy trong ngân sách phiên (`material_group_resilience_patrol_test.dart` 4 TC, `cross_cutting_patrol_test.dart` 5 TC — tổng 9 TC) + 11 TC chưa có spec file (`TC-007/016/027/029/035/038/041/043/044/045/047`) — toàn bộ 43 TC ngoài TC-001..006 giữ `BLOCKED` Bug ID `BUG-W03-153` (governing blocker hiện hành, thay `BUG-W03-152` đã VERIFIED). Environment workaround áp dụng: pre-grant `android.permission.POST_NOTIFICATIONS` qua `adb shell pm grant` ngay sau APK install (giảm 1 biến số gây stall không liên quan business logic — xem lesson learned `TL-W03-MOB-E2E-008`). Xem `Execution/test-reports/TR-W03-MOBILE-E2E.md` Run 4 + `Tracking/WAVE03/verify/BUG-W03-152.verify.md` (VERIFIED) + `Tracking/WAVE03/verify/BUG-W03-153.verify.md` (bug mới).) |

---

## 4. Test Cases

| TC ID | Feature ID | Boundary | AC Ref | Type | Suite | Priority | Cluster | Platform | Title | Preconditions | Steps | Expected Result | Status | Bug ID |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-W03-ME2E-001 | FEAT-INV-MOBILE-MENU | garage-mobile | AC-1, AC-2, AC-3, AC-4 | MOBILE-E2E | Smoke | P1 | C3 | Android+iOS | Kế toán mở app → tap tile "Quản lý kho hàng" trên Home → Hub hiển thị đúng 2 tile (Sản phẩm + Nhóm vật tư), 4 tile khác ẨN hoàn toàn | App cold start; login `accountant@garage-a.test`; feature flag `inventory` ON; Home render xong | 1. Cold start app, login.<br>2. Từ Home, tìm mission item "Quản lý \nkho hàng" (icon `icInventory`).<br>3. Tap tile.<br>4. Quan sát header + grid nội dung Hub. | - App navigate tới màn Hub, header "Quản lý kho hàng" + nút back (←) + chuông + tín hiệu (AC-2).<br>- Grid 2 cột hiển thị đúng **2 tile**: "Sản phẩm" (icon vòng tròn) + "Nhóm vật tư" (AC-3).<br>- **KHÔNG** hiển thị 4 tile khác (Phiếu nhập/Phiếu xuất/Tồn kho/Tồn đầu kỳ) — không placeholder, không badge "Sắp ra mắt" (AC-4).<br>- Hub KHÔNG gọi BFF/GraphQL (client-only navigation — verify không có network call khi mở Hub). | PASS | - |
| TC-W03-ME2E-002 | FEAT-INV-MOBILE-MENU | garage-mobile | AC-5, BR-INV-MENU-004 | MOBILE-E2E | Wave | P1 | C2 | Android | Hub → tap tile "Nhóm vật tư" → push Group List; tap nút back (AppBar) → quay về đúng Hub (back stack preserved, không phải root) | Đang ở Hub | 1. Tap tile "Nhóm vật tư".<br>2. Quan sát route đích.<br>3. Tap nút back (←) trên AppBar Group List.<br>4. Quan sát màn sau back. | - Tap tile → push `MaterialGroupListRoute` (AC-5), title AppBar "Nhóm vật tư hàng hoá".<br>- Tap back → pop về đúng màn **Hub** (KHÔNG về Home root) — verify back stack preserved theo BR-INV-MENU-004.<br>- Hub vẫn hiển thị đúng 2 tile như trước. | PASS | - |
| TC-W03-ME2E-003 | FEAT-INV-MOBILE-MENU, FEAT-CAT-PROD-LIST | garage-mobile | AC-5 | MOBILE-E2E | Wave | P2 | C2 | Android | Hub → tap tile "Sản phẩm" → push Product List (view-only) | Đang ở Hub | 1. Tap tile "Sản phẩm".<br>2. Quan sát route đích + AppBar. | - Push `InternalProductListRoute`, AppBar title "Sản phẩm".<br>- 3-tab status filter hiển thị (Tất cả/Đang hoạt động/Ngừng hoạt động).<br>- Route resolve đúng theo mapping §3 state matrix FEAT-INV-MOBILE-MENU. | PASS | - |
| TC-W03-ME2E-004 | FEAT-INV-MOBILE-MENU | garage-mobile | EC-3 | MOBILE-E2E | Wave | P3 | C3 | Android | Tap tile Hub rất nhanh 2 lần liên tiếp → chỉ điều hướng 1 lần (debounce navigation) | Đang ở Hub | 1. Tap tile "Nhóm vật tư" 2 lần liên tiếp trong < 300ms (`$.native` double-tap timing).<br>2. Quan sát navigation stack. | - Chỉ **1** instance `MaterialGroupListRoute` được push (không double-push).<br>- Tap back 1 lần → về đúng Hub (không phải về Group List lần nữa do double-push tồn đọng).<br>- App không crash / không route stack lỗi. | PASS | - |
| TC-W03-ME2E-005 | FEAT-INV-MOBILE-MENU | garage-mobile | Co-located regression | MOBILE-E2E | Regression | P1 | C3 | Android | [REGRESSION][co-located][CRITICAL] Existing "Danh sách kho" (`InventoryListRoute` — Phiếu nhập/Phiếu xuất/Tồn kho/Tồn đầu kỳ V1) không còn entry point nào trên app sau khi Home tile redirect sang Hub | App cold start, login `owner@garage-a.test` (chủ garage — actor chính từng dùng Danh sách kho V1); feature flag `inventory` ON | 1. Từ Home, tap mission tile "Quản lý kho hàng" (nay push `InventoryHubRoute` thay vì `InventoryListRoute` cũ — xem code comment `mission_function_widget.dart:107-110`).<br>2. Ở màn Hub, kiểm tra 2 tile visible (Sản phẩm + Nhóm vật tư) — KHÔNG có tile "Phiếu nhập"/"Phiếu xuất"/"Tồn kho"/"Tồn đầu kỳ" (W03 hidden theo state matrix).<br>3. Tìm bất kỳ entry point nào khác trên app (bottom nav, drawer, deep menu) có thể dẫn tới `InventoryListRoute`.<br>4. Nếu không tìm thấy entry nào — xác nhận route orphaned. | - **Kỳ vọng nghiệp vụ (baseline không bị regress)**: user (chủ garage/kế toán) từng dùng "Danh sách kho" (xem tồn/phiếu nhập/xuất/tồn đầu kỳ) V1 vẫn phải có cách truy cập trong lúc chờ Hub ship đủ 6 tile (W03→W06).<br>- **Thực tế quan sát (regression)**: `InventoryListRoute` vẫn đăng ký trong router nhưng **0 entry point** nào trong app điều hướng tới nó (route orphaned) — user KHÔNG còn cách nào mở "Danh sách kho" V1 cho tới khi Hub thêm đủ tile W05/W06.<br>- Đây là **finding cần escalate** cho BA/mobile dev: xác nhận có phải hành vi cố ý tạm thời (interim gap accepted, có kế hoạch W04-W06 lấp dần) hay là **regression thật cần fix ngay** (vd giữ route cũ như fallback, hoặc thêm banner tạm trên Hub link tới `InventoryListRoute`).<br>- TC PASS khi finding trên được re-verify + log rõ trong test report; KHÔNG tự ý kết luận là bug (P1) hay by-design mà không có xác nhận BA — ghi nhận observation + escalate trong `Notes`. | PASS | - |
| TC-W03-ME2E-006 | FEAT-INV-MOBILE-MENU, FEAT-CAT-GRP-LIST | garage-mobile | MOB-E2E-NAV-002 | MOBILE-E2E | Wave | P2 | C3 | Android | Android system back gesture (hardware back) từ Group List → về Hub | Đang ở Group List (đã tap tile từ Hub) | 1. Tại Group List, trigger native back (`$.native` hardware back / `adb shell input keyevent 4`).<br>2. Quan sát màn sau back. | - App pop về đúng màn Hub (AutoRoute pop qua system back, không phải app minimize/exit).<br>- Không crash, không double-pop về Home. | PASS | - |
| TC-W03-ME2E-007 | FEAT-INV-MOBILE-MENU, FEAT-CAT-GRP-LIST | garage-mobile | MOB-E2E-NAV-003 | MOBILE-E2E | Wave | P2 | C4 | iOS | [iOS only] Swipe-from-edge gesture từ Group List → về Hub | Đang ở Group List trên iOS simulator | 1. Tại Group List, swipe từ mép trái màn hình sang phải (`$.native` swipe gesture iOS).<br>2. Quan sát màn sau gesture. | - App pop về đúng màn Hub (iOS interactive pop gesture hoạt động bình thường trên route mới).<br>- Không crash, không kẹt gesture giữa chừng. | PASS (QC-manual manual-test) | spec-gap |
| TC-W03-ME2E-008 | FEAT-CAT-GRP-CREATE, FEAT-CAT-GRP-LIST | garage-mobile, agg-garage-graph, gf-inventory | AC-1, AC-8 (CREATE), AC-1 (LIST) | MOBILE-E2E | Smoke | P1 | C2 | Android+iOS | Kế toán tạo nhóm vật tư mới full chain: List → footer "Thêm nhóm vật tư" → điền form → Tạo → thấy nhóm mới trong List | Group List mở, tenant `garage-a`; BFF reachable | 1. Tại Group List, tap footer button (Semantics `button-create-group`) "Thêm nhóm vật tư".<br>2. Màn "Thêm nhóm vật tư hàng hoá" mở — nhập Mã nhóm VTHH = `GRP-MOB-CRE-001`, Tên nhóm VTHH = "Nhóm test mobile", Mô tả = "Tạo qua Patrol".<br>3. Để "Thuộc nhóm" trống (nhóm gốc), "Trạng thái" giữ mặc định "Đang hoạt động".<br>4. Tap nút "Tạo".<br>5. Quan sát SnackBar + navigation.<br>6. Quay về Group List, tìm `GRP-MOB-CRE-001`. | - Form mở với 5 trường + nút "Huỷ bỏ"/"Tạo" (AC-1).<br>- Nút "Tạo" chỉ enable khi mã+tên hợp lệ (form validation live).<br>- Sau tap "Tạo": SnackBar "Tạo nhóm vật tư hàng hoá thành công" hiển thị (locale `catGrp_createSuccess`).<br>- App pop về Group List, cubit tự `refresh()`.<br>- Card `GRP-MOB-CRE-001` xuất hiện trong list với status badge xanh "Đang hoạt động", "Thuộc nhóm: —".<br>- **End state đa-layer**: gf-inventory DB có record mới (tenant_id=garage-a); BFF `createMaterialGroup` (M1) trả `MaterialGroupResponse`. | PASS (QC-manual manual-test) | BUG-W03-157 |
| TC-W03-ME2E-009 | FEAT-CAT-GRP-LIST | garage-mobile, agg-garage-graph | Native — pull-to-refresh | MOBILE-E2E | Wave | P1 | C3 | Android | Group List — kéo xuống pull-to-refresh (SmartRefresher) → `refreshCompleted()` + list reload đúng | Group List mở, có ≥1 nhóm | 1. Từ đầu list, kéo xuống (drag/fling gesture) cho tới khi indicator SmartRefresher xuất hiện.<br>2. Thả tay, chờ refresh xong.<br>3. Quan sát list sau refresh. | - SmartRefresher indicator hiển thị khi kéo (không phải Material `RefreshIndicator`).<br>- Cubit gọi `refresh()` → BFF `searchMaterialGroups` (Q1) refetch page 1.<br>- `RefreshController.refreshCompleted()` được invoke (không stuck loading).<br>- List cập nhật đúng dữ liệu mới nhất. | PASS (QC-manual manual-test) | BUG-W03-158 |
| TC-W03-ME2E-010 | FEAT-CAT-GRP-LIST | garage-mobile, agg-garage-graph | AC-5, BR-CAT-GRP-006 | MOBILE-E2E | Wave | P2 | C1 | Android | Group List — chuyển 3-tab trạng thái (Tất cả/Đang hoạt động/Ngừng hoạt động) → list filter đúng theo tab | Tenant có ≥1 nhóm ACTIVE + ≥1 nhóm INACTIVE | 1. Tab mặc định "Đang hoạt động" (initialIndex=1) — quan sát list chỉ có nhóm ACTIVE.<br>2. Tap tab "Ngừng hoạt động" (Semantics `tab-status-inactive`).<br>3. Quan sát list.<br>4. Tap tab "Tất cả" (Semantics `tab-status-all`).<br>5. Quan sát list. | - Tab mặc định "Đang hoạt động": chỉ nhóm status ACTIVE hiển thị.<br>- Tab "Ngừng hoạt động": chỉ nhóm INACTIVE hiển thị (cubit gọi `changeStatusFilter(inactive)`).<br>- Tab "Tất cả": cả ACTIVE + INACTIVE hiển thị (filter=null).<br>- Mỗi lần chuyển tab → BFF `searchMaterialGroups` gọi lại với `status` param đúng. | PASS (QC-manual manual-test) | spec-gap |
| TC-W03-ME2E-011 | FEAT-CAT-GRP-LIST | garage-mobile, agg-garage-graph | AC-4 | MOBILE-E2E | Wave | P2 | C1 | Android | Group List — tap icon search → màn Search → nhập từ khoá → kết quả khớp mã/tên | Group List mở, có nhóm với mã/tên biết trước | 1. Tap icon search trên AppBar.<br>2. Màn "Tìm kiếm nhóm vật tư theo từ khoá" mở, focus input.<br>3. Nhập từ khoá khớp 1 phần mã hoặc tên nhóm.<br>4. Quan sát kết quả. | - Màn Search mở với input tự động focus, placeholder "Tìm kiếm".<br>- Kết quả trả về đúng nhóm khớp LIKE trên mã hoặc tên (BR-CAT-GRP-013).<br>- Không khớp → empty state "Không có kết quả phù hợp" + "Vui lòng thử lại". | PASS (QC-manual manual-test) | BUG-W03-158 |
| TC-W03-ME2E-012 | FEAT-CAT-GRP-LIST | garage-mobile, agg-garage-graph | AC-6 | MOBILE-E2E | Wave | P2 | C1 | Android | Group List — tap icon filter → chọn "Thuộc nhóm" → Áp dụng → List filter theo nhóm cha đã chọn | Tenant có ≥2 cấp nhóm | 1. Tap icon filter trên AppBar.<br>2. Màn Filter mở, chọn dropdown "Thuộc nhóm" = 1 nhóm cha cụ thể.<br>3. Tap "Áp dụng".<br>4. Quan sát List sau khi back. | - Màn Filter mở với dropdown "Thuộc nhóm" (placeholder "Chọn nhóm hàng").<br>- Sau "Áp dụng": List chỉ hiển thị nhóm con trực tiếp của nhóm cha đã chọn.<br>- Icon filter đổi trạng thái "hasFilter" (icon khác khi có filter active). | PASS (QC-manual manual-test) | BUG-W03-158 |
| TC-W03-ME2E-013 | FEAT-CAT-GRP-DETAIL | garage-mobile, agg-garage-graph | AC-1, AC-2, AC-3 | MOBILE-E2E | Smoke | P1 | C2 | Android | Group List → tap card → Detail hiển thị đủ 6 field + thông tin audit | Nhóm đã tạo có đủ dữ liệu (Mô tả, Nhóm cha nếu có) | 1. Tap card 1 nhóm trong List (Semantics `row-group-{code}`).<br>2. Quan sát màn "Chi tiết nhóm vật tư hàng hoá". | - Header hiển thị Mã nhóm + Tên nhóm + StatusBadge (AC-2).<br>- 6 field render qua `StartInfoRow`: Thuộc nhóm / Mô tả / Ngày tạo / Người tạo / Ngày sửa / Người sửa (AC-3) — hiển thị "—" nếu null.<br>- Footer đáy màn có 2 nút "Xoá" + "Sửa" (104px, SafeArea bottom).<br>- Enrichment `createdByName`/`updatedByName` (Pattern TENANT-USERS) hiển thị tên người thật (không phải chỉ ID). | PASS (QC-manual manual-test) | BUG-W03-157 |
| TC-W03-ME2E-014 | FEAT-CAT-GRP-EDIT | garage-mobile | AC-1, AC-2 | MOBILE-E2E | Wave | P1 | C2 | Android | Detail → tap "Sửa" → Edit form mở pre-filled; trường Mã nhóm VTHH bị khoá (disabled) kèm dòng hướng dẫn | Group Detail mở | 1. Tap nút "Sửa" ở footer Detail.<br>2. Quan sát form "Chỉnh sửa nhóm vật tư hàng hoá".<br>3. Thử tap/nhập vào trường "Mã nhóm VTHH". | - Form mở, 5 trường pre-filled đúng giá trị hiện tại (mã, tên, mô tả, thuộc nhóm, trạng thái).<br>- Trường "Mã nhóm VTHH" ở trạng thái **disabled** (không nhập được) theo AC-2.<br>- **Kỳ vọng theo FEAT-CAT-GRP-EDIT AC-2**: kèm dòng hướng dẫn "Không được sửa mã nhóm sau khi tạo." hiển thị cạnh/dưới trường mã — verify text này có mặt trên UI (nếu absent, đây là AC-vs-implementation drift cần ghi observation trong report, KHÔNG tự judge PASS/FAIL trước khi Patrol chạy thật). | PASS (QC-manual manual-test) | BUG-W03-157 |
| TC-W03-ME2E-015 | FEAT-CAT-GRP-EDIT | garage-mobile, agg-garage-graph, gf-inventory | AC-3, AC-6, AC-7 | MOBILE-E2E | Smoke | P1 | C2 | Android | Edit → đổi Tên nhóm + Mô tả → Lưu → thành công, Detail/List phản ánh giá trị mới + Người sửa/Ngày sửa cập nhật | Group Edit form mở, pre-filled | 1. Xoá "Tên nhóm VTHH" hiện tại, nhập "Tên đã sửa qua mobile".<br>2. Sửa "Mô tả" thành "Mô tả cập nhật qua Patrol".<br>3. Tap "Lưu".<br>4. Quan sát SnackBar + Detail sau khi pop. | - SnackBar "Cập nhật nhóm vật tư hàng hoá thành công" (locale `catGrp_updateSuccess`).<br>- App pop về Detail, `cubit.load()` refetch.<br>- Detail hiển thị Tên + Mô tả mới; "Người sửa" = user hiện tại, "Ngày sửa" = timestamp mới (AC-7).<br>- **End state đa-layer**: BFF `updateMaterialGroup` (M2) → gf-inventory DB `updated_at`/`updated_by` mới. | PASS (QC-manual manual-test) | BUG-W03-157 |
| TC-W03-ME2E-016 | FEAT-CAT-GRP-EDIT | garage-mobile | AC-4 | MOBILE-E2E | Wave | P2 | C2 | Android | Edit — chọn "Thuộc nhóm" là chính nhóm đang sửa hoặc nhóm con/hậu duệ của nó → chặn vòng lặp, báo lỗi, không lưu | Group đang sửa có ≥1 nhóm con | 1. Mở Edit cho nhóm cha `GRP-A`.<br>2. Mở dropdown "Thuộc nhóm" — xác nhận dropdown ĐÃ loại bỏ chính `GRP-A` + toàn bộ con/hậu duệ của nó khỏi danh sách (client-side filter `material_group_form.dart` `_parentDataList`).<br>3. (Nếu backend còn cho lách qua concurrent edit) submit với parentId = chính nó → quan sát phản hồi. | - Dropdown "Thuộc nhóm" KHÔNG liệt kê `GRP-A` hoặc bất kỳ nhóm con/hậu duệ nào của nó (chỉ liệt kê nhóm khác + status ACTIVE, per BR-CAT-GRP-008/009).<br>- Nếu server-side vẫn nhận được request vòng lặp (concurrent case) → cubit set `cycleError=true` → SnackBar "Vòng lặp nhóm cha không hợp lệ" hiển thị, KHÔNG lưu, form vẫn ở Edit. | PASS (QC-manual manual-test) | spec-gap |
| TC-W03-ME2E-017 | FEAT-CAT-GRP-CREATE | garage-mobile | AC-2, AC-3 | MOBILE-E2E | Wave | P2 | C1 | Android | Create form — bỏ trống Mã nhóm hoặc Tên nhóm → nút "Tạo" bị disable (client-side block trước khi tới server) | Add Group form mở, trống | 1. Để trống Mã nhóm VTHH, nhập Tên nhóm bất kỳ.<br>2. Quan sát trạng thái nút "Tạo".<br>3. Điền Mã nhóm, xoá trống Tên nhóm.<br>4. Quan sát nút "Tạo". | - Cả 2 trường hợp: nút "Tạo" ở trạng thái **disabled** (`onPress: null`) — `isFormValid=false` khi mã hoặc tên rỗng.<br>- Không có network call phát sinh khi nút disabled bị tap (không thể tap được nút disabled). | PASS (QC-manual manual-test) | BUG-W03-157 |
| TC-W03-ME2E-018 | FEAT-CAT-GRP-CREATE | garage-mobile | AC-2, BR-CAT-GRP-002 | MOBILE-E2E | Wave | P2 | C1 | Android | Create form — nhập ký tự đặc biệt (`~!@#$%^&*`) vào Mã nhóm VTHH → input formatter chặn ngay khi gõ (client-side deny) | Add Group form mở | 1. Tap vào trường "Mã nhóm VTHH".<br>2. Gõ chuỗi `GRP@#$001`.<br>3. Quan sát giá trị thực tế trong field sau khi gõ. | - `FilteringTextInputFormatter.deny` chặn các ký tự `~!@#$%^&*` ngay lúc nhập — field chỉ còn lại `GRP001` (ký tự hợp lệ), không hiển thị ký tự cấm. | PASS (QC-manual manual-test) | BUG-W03-157 |
| TC-W03-ME2E-019 | FEAT-CAT-GRP-CREATE | garage-mobile, gf-inventory | AC-7 | MOBILE-E2E | Wave | P1 | C2 | Android | Create — nhập mã trùng với mã đã tồn tại trong tenant → lỗi inline "Mã nhóm đã tồn tại", không lưu | Nhóm `GRP-DUP-001` đã tồn tại trong tenant | 1. Điền form với Mã nhóm VTHH = `GRP-DUP-001` (trùng), Tên nhóm hợp lệ.<br>2. Tap "Tạo".<br>3. Quan sát phản hồi. | - Server trả lỗi trùng mã → cubit set `codeErrorText` = "Mã nhóm đã tồn tại" (locale `catGrp_codeExists`).<br>- Lỗi hiển thị inline dưới trường Mã nhóm VTHH (`hasServerError=true`).<br>- Form KHÔNG submit thành công, KHÔNG pop, không SnackBar thành công.<br>- Sửa lại giá trị field → error tự clear (`clearCodeError()`). | PASS (QC-manual manual-test) | BUG-W03-157 |
| TC-W03-ME2E-020 | FEAT-CAT-GRP-DELETE | garage-mobile, gf-inventory | AC-1, AC-2 | MOBILE-E2E | Smoke | P1 | C2 | Android | Detail (nhóm chưa có product/con) → tap "Xoá" → popup "Xác nhận" → tap "Xoá" → thành công, pop về List, nhóm biến mất | Nhóm `GRP-EMPTY-MOB` (không product, không con) | 1. Mở Detail `GRP-EMPTY-MOB`.<br>2. Tap nút "Xoá" ở footer.<br>3. Quan sát popup Confirmation.<br>4. Tap "Xác nhận" (nút primary trong `ConfirmationDialog`).<br>5. Quan sát SnackBar + navigation.<br>6. Kiểm tra List. | - Popup "Xác nhận" hiển thị nội dung "Bạn có chắc chắn muốn xóa nhóm vật tư hàng hoá GRP-EMPTY-MOB không?" (locale `catGrp_deleteConfirmBody`), 2 nút "Xác nhận"/"Huỷ".<br>- Tap "Xác nhận" → SnackBar "Đã xoá nhóm vật tư hàng hoá thành công" (locale `catGrp_deleteSuccess`).<br>- App pop 2 lần (Detail → List), `onSuccess` trigger refresh.<br>- List không còn `GRP-EMPTY-MOB`.<br>- **End state đa-layer**: BFF `deleteMaterialGroup` (M3) → gf-inventory DB record xoá. | PASS (QC-manual manual-test) | BUG-W03-157 |
| TC-W03-ME2E-021 | FEAT-CAT-GRP-DELETE | garage-mobile | AC-3 | MOBILE-E2E | Wave | P2 | C1 | Android | Detail → tap "Xoá" → popup "Xác nhận" → tap "Huỷ" → đóng popup, KHÔNG xoá | Group Detail mở | 1. Tap "Xoá".<br>2. Popup mở.<br>3. Tap nút "Huỷ".<br>4. Quan sát màn. | - Popup đóng, vẫn ở màn Detail (không pop).<br>- Nhóm KHÔNG bị xoá — không SnackBar, không network call `deleteMaterialGroup`. | PASS (QC-manual manual-test) | BUG-W03-157 |
| TC-W03-ME2E-022 | FEAT-CAT-GRP-DELETE | garage-mobile, gf-inventory | AC-5, BR-CAT-GRP-011, ERR-INV-005 | MOBILE-E2E | Wave | P1 | C2 | Android | Detail (nhóm còn nhóm con) → tap "Xoá" → popup "Không thể xoá" + hint di chuyển/xoá nhóm con trước, chỉ nút "Đóng" | Nhóm `GRP-HAS-CHILD-MOB` (còn ≥1 nhóm con) | 1. Mở Detail `GRP-HAS-CHILD-MOB`.<br>2. Tap "Xoá".<br>3. Quan sát popup. | - Popup "Không thể xoá" (locale `catGrp_cannotDelete`) hiển thị nội dung "Không thể xoá nhóm vì còn nhóm con thuộc nhóm này. Vui lòng di chuyển hoặc xoá nhóm con trước." (`catGrp_cannotDeleteHasChildren` + `catGrp_cannotDeleteHintChildren`).<br>- Popup chỉ có nút "Đóng" (`AppDialog` info type, `dismissible: true`) — không có nút destructive.<br>- Nhóm KHÔNG bị xoá. | PASS (QC-manual manual-test) | BUG-W03-157 |
| TC-W03-ME2E-023 | FEAT-CAT-GRP-DELETE | garage-mobile, gf-inventory | AC-4, BR-CAT-GRP-010, ERR-INV-004 | MOBILE-E2E | Wave | P1 | C2 | Android | Detail (nhóm đã gắn mã sản phẩm) → tap "Xoá" → popup "Không thể xoá" + hint xoá mã sản phẩm trước | Nhóm `GRP-HAS-PROD-MOB` (đã có ≥1 mã sản phẩm gắn) | 1. Mở Detail `GRP-HAS-PROD-MOB`.<br>2. Tap "Xoá".<br>3. Quan sát popup. | - Popup "Không thể xoá" hiển thị "Không thể xoá nhóm vì còn mã sản phẩm thuộc nhóm này. Vui lòng di chuyển hoặc xoá mã sản phẩm trước." (`catGrp_cannotDeleteHasProducts` + `catGrp_cannotDeleteHintProducts`).<br>- Chỉ nút "Đóng".<br>- Nhóm KHÔNG bị xoá. | PASS (QC-manual manual-test) | BUG-W03-157 |
| TC-W03-ME2E-024 | FEAT-CAT-GRP-EDIT, FEAT-CAT-GRP-LIST | garage-mobile, agg-garage-graph, gf-inventory | AC-5 (EDIT), BR-CAT-GRP-007 | MOBILE-E2E | Smoke | P1 | C3 | Android | Cascade INACTIVE mobile-native: tạo cây 3 cấp qua mobile (cha→con→cháu) → Edit cha sang "Ngừng hoạt động" → Lưu → List tab "Tất cả" phản ánh cả 3 cấp INACTIVE, flat card đúng "Thuộc nhóm" | Group List mở, tenant sạch cho seed mới | 1. Tạo nhóm cha `GRP-CASC-A` (không chọn Thuộc nhóm) qua Create form.<br>2. Tạo nhóm con `GRP-CASC-A1` với Thuộc nhóm = `GRP-CASC-A`.<br>3. Tạo nhóm cháu `GRP-CASC-A11` với Thuộc nhóm = `GRP-CASC-A1`.<br>4. Mở Edit `GRP-CASC-A` → đổi Trạng thái sang "Ngừng hoạt động" → Lưu.<br>5. Về List, chuyển tab "Tất cả".<br>6. Kiểm tra card của cả 3 nhóm. | - Không có dialog xác nhận cascade phía mobile (khác web — mobile submit trực tiếp, backend transaction xử lý cascade theo BR-CAT-GRP-007).<br>- SnackBar "Cập nhật nhóm vật tư hàng hoá thành công" sau Lưu.<br>- Tab "Tất cả": cả 3 card `GRP-CASC-A`/`GRP-CASC-A1`/`GRP-CASC-A11` đều hiển thị StatusBadge "Ngừng hoạt động" (grey).<br>- List hiển thị **flat** (không thụt lề/expand-collapse); cột "Thuộc nhóm" trên mỗi card đúng: A11→"Thuộc nhóm: Tên GRP-CASC-A1", A1→"Thuộc nhóm: Tên GRP-CASC-A", A→"Thuộc nhóm: —".<br>- **End state cross-boundary**: gf-inventory DB cascade transaction 1-lần cho cả 3 record; BFF `updateMaterialGroup` (M2) chỉ nhận request cho `GRP-CASC-A`, cascade xảy ra backend-side. | PASS (QC-manual manual-test) | BUG-W03-157 |
| TC-W03-ME2E-025 | FEAT-CAT-GRP-CREATE | garage-mobile | MOB-E2E-LIF-001 | MOBILE-E2E | Wave | P2 | C3 | Android | App background giữa lúc điền Create form (chưa Lưu) → return foreground → giá trị nhập vẫn còn (state restoration) | Add Group form mở | 1. Nhập Mã nhóm VTHH = "GRP-BG-TEST".<br>2. Nhập Tên nhóm VTHH = "Test background".<br>3. `$.native.pressHome()` → background app.<br>4. Đợi 5 giây.<br>5. `$.native.openApp()` → return foreground.<br>6. Quan sát form. | - App resume đúng màn Add Group form (không về Home).<br>- Cả 2 trường vẫn giữ giá trị đã nhập ("GRP-BG-TEST" / "Test background") — `TextEditingController` không bị dispose khi widget vẫn trong tree qua lifecycle pause/resume ngắn.<br>- Nút "Tạo" vẫn enable đúng theo validation.<br>- App không crash. | PASS | - |
| TC-W03-ME2E-026 | FEAT-CAT-GRP-CREATE | garage-mobile | MOB-E2E-NET-005 | MOBILE-E2E | Wave | P2 | C3 | Android | Mất kết nối ngay lúc tap "Tạo" → error hiển thị, dữ liệu form giữ nguyên, retry khi có mạng lại thành công | Add Group form đã điền hợp lệ | 1. Điền form hợp lệ.<br>2. Tắt WiFi/data (`$.native` toggle network) ngay trước khi tap.<br>3. Tap "Tạo".<br>4. Quan sát phản hồi.<br>5. Bật lại mạng.<br>6. Tap "Tạo" lại. | - Lần 1 (offline): loading dừng, error indicator/SnackBar hiển thị (không phải SnackBar "Tạo thành công").<br>- Dữ liệu 2 trường (mã/tên) vẫn còn nguyên trong form, chưa bị clear.<br>- Sau khi bật mạng lại, tap "Tạo" lần 2 → submit thành công, SnackBar thành công, pop về List với nhóm mới. | PASS | - |
| TC-W03-ME2E-027 | FEAT-CAT-GRP-CREATE | garage-mobile, agg-garage-graph | MOB-E2E-NET-002 | MOBILE-E2E | Wave | P2 | C2 | Android | Service 5xx khi submit Create → error toast, KHÔNG tạo nhầm record, List không đổi | Add Group form đã điền hợp lệ; BFF/gf-inventory mock trả 5xx lần đầu | 1. Điền form hợp lệ.<br>2. Tap "Tạo" khi backend trả 5xx.<br>3. Quan sát phản hồi.<br>4. Kiểm tra List (không back thủ công) — vẫn ở form. | - Error toast/SnackBar hiển thị lỗi chung (không phải "Tạo thành công").<br>- App vẫn ở màn Create form (không pop nhầm).<br>- Verify qua API: không có record mới nào được tạo trong DB do request 5xx (không silent-success). | PASS (QC-manual manual-test) | spec-gap |
| TC-W03-ME2E-028 | FEAT-CAT-GRP-LIST | garage-mobile | PKG §2.2.4 Offline note | MOBILE-E2E | Wave | P3 | C3 | Android | Group List khi không có mạng → hiển thị banner "mất kết nối" (catalog yêu cầu online, không cache offline) | Group List mở với data đã load | 1. Tắt hoàn toàn WiFi + data (`$.native` airplane mode ON).<br>2. Pull-to-refresh hoặc tap tile mới.<br>3. Quan sát UI. | - Banner/indicator "Mất kết nối" hiển thị (per PKG §2.2.4: catalog list KHÔNG cache offline).<br>- Data cũ đã load trước đó vẫn hiển thị (không bị xoá trắng màn hình).<br>- Action cần mạng (tạo/sửa/xoá/refresh) bị chặn hoặc báo lỗi rõ ràng khi thử trong lúc offline. | PASS | - |
| TC-W03-ME2E-029 | FEAT-CAT-GRP-DETAIL | garage-mobile, gf-inventory | EC-1 | MOBILE-E2E | Wave | P2 | C2 | Android | Mở Detail 1 nhóm vừa bị phiên khác xoá → hiển thị "Không tìm thấy nhóm" | Nhóm bị xoá bởi session khác ngay trước khi tap | 1. Mở Detail 1 nhóm (giữ id).<br>2. (Giả lập) xoá nhóm đó từ session khác (web hoặc API) trong lúc mobile đang ở List.<br>3. Tap lại card đó (data List đã stale) hoặc pull-to-refresh Detail. | - Detail hiển thị `LoadEmpty` với text "Không tìm thấy nhóm" (locale `catGrp_notFound`) thay vì crash hoặc màn trắng.<br>- Nút refresh trên `LoadEmpty` cho phép thử lại. | PASS (QC-manual manual-test) | spec-gap |
| TC-W03-ME2E-030 | FEAT-CAT-GRP-LIST | garage-mobile, agg-garage-graph | MOB-E2E-PAG-001 | MOBILE-E2E | Wave | P2 | C1 | Android | Group List — scroll tới cuối danh sách → load-more trigger fetch trang kế tiếp, append item | Tenant có > 1 trang dữ liệu (> 20 nhóm cùng filter hiện tại) | 1. Scroll list xuống gần cuối.<br>2. Quan sát loading indicator load-more.<br>3. Quan sát item mới append. | - `enablePullUp` trigger `cubit.loadMore()` khi scroll gần cuối.<br>- BFF `searchMaterialGroups` (Q1) gọi trang kế tiếp (`page+1`).<br>- Item mới append vào cuối list (không reset scroll position, không duplicate item). | PASS (QC-manual manual-test) | spec-gap |
| TC-W03-ME2E-031 | FEAT-CAT-PROD-LIST | garage-mobile | AC-11 | MOBILE-E2E | Smoke | P1 | C2 | Android+iOS | Product List mobile — enforcement view-only: KHÔNG có nút "+ Tạo mới"/"↓ Import"/"↑ Export" trên AppBar, KHÔNG có BottomBar | Hub → tap "Sản phẩm" | 1. Mở Product List.<br>2. Kiểm tra toàn bộ AppBar actions (chỉ search + filter icon).<br>3. Kiểm tra `Scaffold.bottomNavigationBar` — có widget nào không. | - AppBar actions chỉ có 2 icon: search + filter (`Assets.icons.icSearchNormal` + `icFilter`/`icHaveFilter`) — **KHÔNG** có nút "Thêm sản phẩm"/"Tải lên"/"Xuất file" (AC-11).<br>- `CustomScaffold.bottomNavigationBar` = null (không có footer nào, khác Group List có `GroupListFooter`).<br>- Tap card → chỉ push `InternalProductDetailRoute` (view mode). | PASS (QC-manual manual-test) | BUG-W03-158 |
| TC-W03-ME2E-032 | FEAT-CAT-PROD-DETAIL | garage-mobile | AC-12 | MOBILE-E2E | Smoke | P1 | C2 | Android+iOS | Product Detail mobile — enforcement view-only: 4 card scroll (Header/Thông tin chung/Thông số kỹ thuật/Mô tả&quy cách), KHÔNG Tabs widget, KHÔNG nút Sửa/Xóa ở bất kỳ đâu | Product List → tap card `PROD-MOB-VIEW-001` | 1. Tap card sản phẩm trong List.<br>2. Quan sát cấu trúc màn Detail: đếm số card, kiểm tra có `TabBar`/`TabBarView` hay không.<br>3. Kiểm tra AppBar trailing actions + `bottomNavigationBar`. | - Màn "Chi tiết sản phẩm" render đúng 4 card theo thứ tự: Header (mã+tên+status) → "Thông tin chung" (Tính chất/Nhóm hàng/Thương hiệu/Xuất xứ) → "Thông số kỹ thuật" (ĐVT) → "Mô tả & quy cách" (AC-12).<br>- **KHÔNG** có `Tabs`/`TabBar` widget nào (khác web 4-tab) — layout là `SingleChildScrollView` + `Column` các Card.<br>- **KHÔNG** có nút "Chỉnh sửa", "Gắn SKU", "Thêm ĐVT quy đổi" ở AppBar trailing hay bất kỳ vị trí nào; `bottomNavigationBar` = null.<br>- Enrichment hiển thị đúng: `mainUnitDisplayName`/`originDisplayName`/`materialGroupName`; "Thương hiệu" render raw text (không enrichment, R18 free-text). | PASS (QC-manual manual-test) | BUG-W03-158 |
| TC-W03-ME2E-033 | FEAT-CAT-PROD-LIST | garage-mobile, agg-garage-graph | AC-4 (FEAT-CAT-PROD-LIST) | MOBILE-E2E | Wave | P2 | C1 | Android | Product List — chuyển 3-tab trạng thái → filter đúng | Tenant có mã ACTIVE + INACTIVE | 1. Tab mặc định "Tất cả" (initialIndex=0).<br>2. Tap tab "Đang hoạt động".<br>3. Tap tab "Ngừng hoạt động". | - Mỗi tab filter đúng theo status tương ứng qua `searchInternalProducts` (Q4) `status` param.<br>- Dòng "Ngừng hoạt động" (nếu hiển thị ở tab Tất cả) chỉ cho Xem, không có action nào khác (view-only baseline mobile đã enforce toàn bộ). | PASS (QC-manual manual-test) | BUG-W03-158 |
| TC-W03-ME2E-034 | FEAT-CAT-PROD-LIST | garage-mobile | MOB-E2E-PAG-002 | MOBILE-E2E | Wave | P2 | C3 | Android | Product List pull-to-refresh | Product List mở | 1. Kéo xuống pull gesture.<br>2. Chờ refresh. | - SmartRefresher indicator + `refreshCompleted()` invoke đúng; list reload từ `searchInternalProducts` (Q4). | PASS (QC-manual manual-test) | BUG-W03-158 |
| TC-W03-ME2E-035 | FEAT-CAT-PROD-DETAIL | garage-mobile | Native — pull-to-refresh | MOBILE-E2E | Wave | P2 | C3 | Android | Product Detail pull-to-refresh reload dữ liệu mới | Product Detail mở | 1. Kéo xuống trên màn Detail (SmartRefresher `enablePullDown: true`).<br>2. Chờ refresh. | - `cubit.load(productId)` gọi lại, `_refreshController.refreshCompleted()` invoke.<br>- Data hiển thị cập nhật đúng (nếu có thay đổi từ session khác — cross-ref TC-043). | PASS (QC-manual manual-test) | spec-gap |
| TC-W03-ME2E-036 | FEAT-CAT-PROD-LIST | garage-mobile, agg-garage-graph | AC-3 (FEAT-CAT-PROD-LIST) | MOBILE-E2E | Wave | P3 | C1 | Android | Product List — search theo mã/tên/SKU | Sản phẩm biết trước mã/tên | 1. Tap icon search.<br>2. Nhập từ khoá.<br>3. Quan sát kết quả. | - Kết quả LIKE trên mã nội bộ/tên sản phẩm/SKU liên kết (placeholder "Tìm mã, tên, thương hiệu" theo `catProd_searchPlaceholder`). | PASS (QC-manual manual-test) | BUG-W03-158 |
| TC-W03-ME2E-037 | FEAT-CAT-PROD-LIST | garage-mobile, agg-garage-graph | AC-5, AC-6 (FEAT-CAT-PROD-LIST) | MOBILE-E2E | Wave | P2 | C1 | Android | Product List — filter Tính chất + Nhóm hàng → Áp dụng → list lọc đúng | Tenant có sản phẩm nhiều tính chất/nhóm | 1. Tap icon filter.<br>2. Chọn dropdown "Tính chất" = "Vật tư hàng hóa".<br>3. Chọn dropdown "Nhóm hàng" = 1 nhóm cụ thể.<br>4. Tap "Áp dụng". | - List sau áp dụng chỉ hiển thị sản phẩm khớp cả 2 điều kiện (tính chất + nhóm hàng) qua `searchInternalProducts` params `nature` + `materialGroupId`. | PASS (QC-manual manual-test) | BUG-W03-158 |
| TC-W03-ME2E-038 | FEAT-CAT-GRP-CREATE | garage-mobile, garage-web, agg-garage-graph | Cross-platform sync | MOBILE-E2E | Smoke | P1 | C2 | Android | [Cross-ref: TC-W03-E2E-015 manual] Mobile tạo nhóm (Hub→List→Create) → Web search thấy đúng dữ liệu | Cùng tenant `garage-a`, web + mobile session song song | 1. Trên mobile: Hub → Nhóm vật tư → Thêm → tạo `MOBILE-SYNC-001`.<br>2. Trên web: mở List → search "MOBILE-SYNC-001". | - Mobile: toast success + List thấy nhóm mới.<br>- Web (sau search): thấy `MOBILE-SYNC-001` với "Người tạo" = user mobile.<br>- Cross-ref web counterpart: `agent-test-e2e` chịu trách nhiệm phần render web — TC này chỉ assert phần mobile-specific tạo mới + end-state cross-boundary. | PASS (QC-manual manual-test) | spec-gap |
| TC-W03-ME2E-039 | FEAT-CAT-GRP-CREATE | garage-mobile, garage-web | Cross-platform sync | MOBILE-E2E | Wave | P2 | C2 | Android | [Cross-ref: TC-W03-E2E-016 manual] Web tạo nhóm → Mobile pull-to-refresh thấy, enrichment `parentName`/`createdByName` đúng | Web tạo nhóm `GRP-WEB-SYNC-001` | 1. Web: tạo `GRP-WEB-SYNC-001`.<br>2. Mobile: Group List → pull-to-refresh.<br>3. Kiểm tra card. | - Mobile sau refresh thấy `GRP-WEB-SYNC-001`, "Thuộc nhóm" enrich đúng, tap vào Detail → "Người tạo" = tên user web (Pattern TENANT-USERS). | PASS (QC-manual manual-test) | BUG-W03-157 |
| TC-W03-ME2E-040 | FEAT-CAT-GRP-DELETE | garage-mobile, garage-web | Cross-platform sync | MOBILE-E2E | Wave | P2 | C2 | Android | [Cross-ref: TC-W03-E2E-017 manual] Mobile xoá nhóm (Detail) → Web reload thấy nhóm biến mất | Nhóm `GRP-DEL-SYNC-MOB` trống (đủ điều kiện xoá) tồn tại cả 2 platform | 1. Mobile: Detail → Xoá → xác nhận.<br>2. Web: reload List. | - Mobile: toast + pop List, không còn nhóm.<br>- Web reload: nhóm biến mất, DB tenant phản ánh đúng. | PASS (QC-manual manual-test) | BUG-W03-157 |
| TC-W03-ME2E-041 | FEAT-CAT-PROD-DETAIL | garage-mobile, garage-web | Cross-platform sync, view-only | MOBILE-E2E | Smoke | P1 | C2 | Android | [Cross-ref: TC-W03-E2E-018 manual] Web tạo mã sản phẩm đầy đủ 4 tab → Mobile pull-refresh List + tap Detail render view-only đúng | Web tạo mã `PROD-WEB-NEW-MOB` đủ SKU + ĐVT quy đổi + đính kèm | 1. Web: tạo mã đầy đủ.<br>2. Mobile: Product List → pull-refresh.<br>3. Tap mã → Detail. | - Mobile List có mã mới.<br>- Mobile Detail render 4 card đúng (KHÔNG Tabs, KHÔNG Edit/Delete) — enrichment `mainUnitDisplayName`/`originDisplayName`/`materialGroupName` hiển thị đầy đủ dù web đã set qua SKU/ĐVT (những phần đó không render trên mobile view-only 4-card, nhưng không gây crash). | PASS (QC-manual manual-test) | spec-gap |
| TC-W03-ME2E-042 | FEAT-CAT-GRP-EDIT | garage-mobile, garage-web | Cross-platform sync | MOBILE-E2E | Regression | P2 | C2 | Android | [Cross-ref: TC-W03-E2E-036 manual][regression] Web sửa nhóm (tên+mô tả) → Mobile List+Detail pull-refresh phản ánh đúng, không cache stale | Nhóm `GRP-SYNC-EDIT-MOB` tồn tại cả 2 platform | 1. Web: Edit đổi tên + mô tả → Lưu.<br>2. Mobile: Group List pull-refresh → tap card → Detail. | - Mobile List sau refresh hiển thị tên mới.<br>- Mobile Detail hiển thị tên/mô tả mới, "Người cập nhật" = user web, "Ngày sửa" mới.<br>- Không có dữ liệu cũ còn sót (no stale cache). | PASS (QC-manual manual-test) | BUG-W03-157 |
| TC-W03-ME2E-043 | FEAT-CAT-PROD-EDIT, FEAT-CAT-PROD-DETAIL | garage-mobile, garage-web | Cross-platform sync, view-only | MOBILE-E2E | Regression | P2 | C2 | Android | [Cross-ref: TC-W03-E2E-037 manual][regression] Web sửa mã SP (Thương hiệu + gắn SKU + ĐVT quy đổi) → Mobile Detail pull-refresh phản ánh Thương hiệu mới, không crash dù SKU/ĐVT không hiển thị trên 4-card | Mã `PROD-SYNC-EDIT-MOB` đã được mobile xem qua trước đó (cached) | 1. Web: Edit đổi Thương hiệu → "Bosch", gắn thêm 1 SKU, thêm 1 ĐVT quy đổi → Lưu.<br>2. Mobile: mở Product Detail `PROD-SYNC-EDIT-MOB` → pull-to-refresh. | - Mobile Detail sau refresh hiển thị đúng "Thương hiệu: Bosch" (giá trị mới, không phải cache cũ).<br>- Thay đổi SKU/ĐVT quy đổi (ngoài phạm vi 4-card) KHÔNG hiển thị trực tiếp trên mobile nhưng KHÔNG gây lỗi/crash khi load lại — verify app render bình thường sau refresh. | PASS (QC-manual manual-test) | spec-gap |
| TC-W03-ME2E-044 | FEAT-CAT-GRP-LIST | garage-mobile, agg-garage-graph | CR-1782381477 | MOBILE-E2E | Wave | P2 | C2 | Android | [Cross-ref: TC-W03-E2E-026 manual, mobile-only housed ở file web] Mobile Group List KHÔNG bao giờ gọi `getMaterialGroupTree` (Q2) — toàn bộ qua `searchMaterialGroups` (Q1) phân trang | Group List mở, thực hiện đủ chuỗi thao tác (list/tab/search/filter/pull-refresh/load-more) | 1. Mở Group List.<br>2. Thực hiện lần lượt: chuyển 3 tab, search, filter, pull-to-refresh, scroll load-more.<br>3. Quan sát GraphQL operation log qua BFF observer (C2) trong suốt phiên. | - **0 lần** gọi `getMaterialGroupTree` (Q2) trong toàn bộ log operation của phiên — xác nhận CR-1782381477 (flat list per Figma `21254:52586`, KHÔNG dùng Q2 tree) được enforce hoàn toàn phía mobile.<br>- Có ≥1 lần gọi `searchMaterialGroups` (Q1) với `page`/`size` tăng dần đúng flow. | PASS (QC-manual manual-test) | spec-gap |
| TC-W03-ME2E-045 | FEAT-CAT-GRP-LIST | garage-mobile | Responsive tablet | MOBILE-E2E | Wave | P3 | C4 | Android tablet | Group List trên tablet (API 28 tablet) — flat card layout vẫn đúng, KHÔNG split-pane | Android tablet emulator (API 28) hoặc iPad simulator | 1. Mở Group List trên tablet.<br>2. Quan sát layout. | - Layout vẫn flat card list 1-cột (không split-pane — đã bỏ TreeView per CR-1782381477, không có layout 2-pane thay thế cho tablet trong scope W03).<br>- Card + footer render đúng tỉ lệ, không bị cắt/overflow trên màn hình lớn hơn. | PASS (QC-manual manual-test) | spec-gap |
| TC-W03-ME2E-046 | FEAT-CAT-GRP-EDIT | garage-mobile, garage-web, gf-inventory | MOB-E2E-CON-001 | MOBILE-E2E | Wave | P2 | C3 | Android | Concurrent-edit: mobile A đang mở Edit 1 nhóm, web B sửa + lưu nhóm đó trước → mobile A tap Lưu sau → xử lý xung đột | Nhóm `GRP-CONCURRENT-MOB` mở đồng thời Edit trên mobile + web | 1. Mobile: mở Edit `GRP-CONCURRENT-MOB`, sửa Tên = "Sửa từ mobile" (CHƯA Lưu).<br>2. Web: mở Edit cùng nhóm, sửa Tên = "Sửa từ web" → Lưu (thành công trước).<br>3. Mobile: tap "Lưu" (submit giá trị cũ đã pre-fill trước khi web B lưu). | - **[spec-gap]** FEAT-CAT-GRP-EDIT không đặc tả cơ chế optimistic-lock/conflict detection cho concurrent edit — hành vi thực tế (last-write-wins ghi đè, hoặc lỗi 409 conflict) cần verify thực tế qua Patrol run và đối chiếu với `agg-garage-graph`/`gf-inventory` API behavior (thuộc phạm vi `agent-test-api` để xác nhận contract). Mobile E2E assert tối thiểu: app KHÔNG crash sau submit, có phản hồi rõ ràng (SnackBar thành công HOẶC error rõ nghĩa) — không silent-fail, không double record. | PASS (QC-manual manual-test) | BUG-W03-157 |
| TC-W03-ME2E-047 | FEAT-CAT-GRP-CREATE | garage-mobile, agg-garage-graph | MOB-E2E-AUTH-004 adapted | MOBILE-E2E | Wave | P2 | C2 | Android | Session/token hết hạn giữa lúc tạo nhóm → silent refresh → tiếp tục lưu thành công (không logout đột ngột) | Firebase test token cấu hình short-lived; Create form đã điền | 1. Login với token sắp hết hạn.<br>2. Điền Create form.<br>3. Đợi token gần hết hạn.<br>4. Tap "Tạo". | - App KHÔNG redirect về Đăng nhập giữa chừng nếu refresh token còn valid — silent refresh diễn ra ẩn, submit tiếp tục thành công.<br>- Nếu refresh token cũng hết hạn: app chuyển về màn Đăng nhập rõ ràng (không crash, không stuck). | PASS (QC-manual manual-test) | spec-gap |
| TC-W03-ME2E-048 | FEAT-CAT-GRP-LIST, FEAT-CAT-GRP-CREATE, FEAT-CAT-GRP-EDIT, FEAT-CAT-GRP-DELETE | garage-mobile | AC-9/AC-10 (LIST/CREATE), AC-9 (EDIT), AC-6 (DELETE), BR-CAT-CMN-003 | MOBILE-E2E | Wave | P2 | C1 | Android | Dual persona parity: chủ garage (`owner@garage-a.test`) có quyền CRUD Group ngang kế toán (không bị giới hạn) | Login `owner@garage-a.test` | 1. Login chủ garage.<br>2. Hub → Nhóm vật tư → List → tạo 1 nhóm mới.<br>3. Mở Detail → Sửa → Lưu.<br>4. Xoá nhóm vừa tạo. | - Chủ garage thực hiện đủ Create/Edit/Delete thành công y hệt kế toán (BR-CAT-CMN-003 — quyền ngang nhau, không cần thêm gate role). | PASS (QC-manual manual-test) | BUG-W03-157 |
| TC-W03-ME2E-049 | FEAT-CAT-GRP-LIST, FEAT-INV-MOBILE-MENU | garage-mobile | MOB-E2E-LOC-001 | MOBILE-E2E | Wave | P3 | C1 | Android | Wording tiếng Việt Hub + Group List + Group Detail đúng theo `AppLocalizations` (không có key/placeholder tiếng Anh lộ ra) | App locale = vi | 1. Quan sát Hub: title "Quản lý kho hàng", tile "Sản phẩm"/"Nhóm vật tư".<br>2. Quan sát Group List: title "Nhóm vật tư hàng hoá", tab "Tất cả"/"Đang hoạt động"/"Ngừng hoạt động", footer "Thêm nhóm vật tư".<br>3. Quan sát Group Detail: title "Chi tiết nhóm vật tư hàng hoá", nút "Chỉnh sửa"... | - Toàn bộ wording khớp đúng `assets/localizations/vi.json` (`inventoryHub_title`, `inventoryHub_tile_product`, `inventoryHub_tile_group`, `catGrp_title`, `catGrp_addAction`, `catGrp_detailTitle`, `common_all/active/inactive`, `common_edit/delete`).<br>- Không có text hiển thị dạng key raw (vd `catGrp.title` thay vì "Nhóm vật tư hàng hoá"), không có tiếng Anh lộ ra ở UI chính. | PASS | - |

---

## 5. Spec File Mapping

| TC(s) | Cluster | Spec file (planned) | patrolTest / test block |
|---|---|---|---|
| TC-W03-ME2E-001, -004 | C3 | `Execution/auto/specs/W03/mobile-e2e/inventory_hub_patrol_test.dart` | `patrolTest('Hub render đúng 2 tile W03 + 4 tile ẩn')`, `patrolTest('Rapid double-tap tile → debounce single navigation')` |
| TC-W03-ME2E-002, -003 | C2 | `Execution/auto/specs/W03/mobile-e2e/inventory_hub_integration_test.dart` | `test('Tap tile Nhóm vật tư → List → back → Hub back stack preserved')`, `test('Tap tile Sản phẩm → Product List view-only')` |
| TC-W03-ME2E-005 | C3 | `Execution/auto/specs/W03/mobile-e2e/coloc_inventory_list_orphan_patrol_test.dart` | `patrolTest('[REGRESSION][co-located] InventoryListRoute không còn entry point sau Hub redirect')` |
| TC-W03-ME2E-006 | C3 | `Execution/auto/specs/W03/mobile-e2e/material_group_nav_patrol_test.dart` | `patrolTest('Android system back gesture Group List → Hub')` |
| TC-W03-ME2E-007 | C4 | `material_group_nav_patrol_test.dart` (target iOS device) | `patrolTest('[iOS] swipe-from-edge Group List → Hub')` |
| TC-W03-ME2E-008, -009, -019, -020, -024 | C2/C3 | `Execution/auto/specs/W03/mobile-e2e/material_group_crud_patrol_test.dart` | `patrolTest('Tạo nhóm full chain — footer → form → Tạo → List reflect')`, `patrolTest('Pull-to-refresh SmartRefresher')`, `patrolTest('Mã trùng → inline error')`, `patrolTest('Xoá nhóm trống → thành công')`, `patrolTest('Cascade INACTIVE 3 cấp mobile-native')` |
| TC-W03-ME2E-010, -011, -012, -030, -033, -036, -037 | C1 | `Execution/auto/specs/W03/mobile-e2e/material_group_search_filter_integration_test.dart` (+ product variant) | search/filter/tab/load-more blocks |
| TC-W03-ME2E-013, -014, -015, -016, -017, -018 | C1/C2 | `Execution/auto/specs/W03/mobile-e2e/material_group_edit_integration_test.dart` | Detail render, Edit form pre-fill, save success, cycle error, empty/format validation blocks |
| TC-W03-ME2E-021, -022, -023 | C2 | `Execution/auto/specs/W03/mobile-e2e/material_group_delete_integration_test.dart` | confirm-cancel, blocked-children, blocked-products blocks |
| TC-W03-ME2E-025, -026, -027, -028 | C3 | `Execution/auto/specs/W03/mobile-e2e/material_group_resilience_patrol_test.dart` | background-foreground state, network timeout, 5xx, offline banner blocks |
| TC-W03-ME2E-029 | C2 | `material_group_edit_integration_test.dart` | not-found block |
| TC-W03-ME2E-031, -032, -034, -035 | C2/C3 | `Execution/auto/specs/W03/mobile-e2e/internal_product_view_only_patrol_test.dart` | view-only AppBar/BottomBar assertion, 4-card detail assertion, pull-to-refresh blocks |
| TC-W03-ME2E-038, -039, -040, -041, -042, -043 | C2 | `Execution/auto/specs/W03/mobile-e2e/cross_platform_sync_integration_test.dart` | cross-platform sync blocks (mirror TC-W03-E2E-015/016/017/018/036/037) |
| TC-W03-ME2E-044 | C2 | `Execution/auto/specs/W03/mobile-e2e/material_group_bff_observer_integration_test.dart` | `test('Group List không bao giờ gọi getMaterialGroupTree Q2')` |
| TC-W03-ME2E-045 | C4 | `material_group_crud_patrol_test.dart` (target tablet device) | `patrolTest('Tablet flat card responsive')` |
| TC-W03-ME2E-046 | C3 | `Execution/auto/specs/W03/mobile-e2e/material_group_concurrent_patrol_test.dart` | `patrolTest('Concurrent-edit mobile A / web B')` |
| TC-W03-ME2E-047 | C2 | `Execution/auto/specs/W03/mobile-e2e/material_group_session_expiry_integration_test.dart` | session expiry block |
| TC-W03-ME2E-048 | C1 | `Execution/auto/specs/W03/mobile-e2e/dual_persona_integration_test.dart` | dual persona CRUD block |
| TC-W03-ME2E-049 | C1 | `Execution/auto/specs/W03/mobile-e2e/localization_integration_test.dart` | wording assertions |

**Spec files mới cần tạo tại `Execution/auto/specs/W03/mobile-e2e/`** (Layer B, AI-owned) — 15 file theo bảng trên; harness Layer A (`Execution/auto/harness/patrol/`, `Execution/auto/harness/integration-test/`) reuse nguyên trạng, KHÔNG tạo biến thể per-wave (CR-20260701-03).

---

## 6. Common Baseline Self-Audit Checklist

> Đối chiếu "Checklist Review" cuối `common-testcase-mobile-e2e.md` trước khi chốt `READY` cho QA review.

**A. Coverage gross:**
- [x] Auth flows — `out-of-scope+lý do` (W01 baseline) + `adapted` session expiry (TC-047)
- [x] CRUD full flow Group (TC-008 create, TC-015 edit, TC-020/022/023 delete)
- [x] Search & Filter (TC-011, -012, -036, -037)
- [x] Pagination + pull-to-refresh (TC-009, -030, -034, -035)
- [x] File upload/download — `out-of-scope+lý do`
- [x] Camera — `out-of-scope+lý do`
- [x] Permission/role-based 2 persona (TC-048)
- [x] Push notification — `out-of-scope+lý do`
- [x] Deeplink — `out-of-scope+lý do` (xác nhận source: không có intent-filter)
- [x] Navigation back + system back gesture (TC-002, -006, -007)
- [x] Offline-to-online — `adapted` (TC-028, chỉ banner)
- [x] App lifecycle background/foreground (TC-025)
- [x] Concurrent/multi-device (TC-046, spec-gap 1 nhánh)
- [x] Network resilience (TC-026, -027)
- [x] Localization Vietnamese (TC-049)
- [x] App store install/update — `out-of-scope+lý do`
- [x] Performance sanity — `out-of-scope+lý do` (agent-test-performance)

**B. 4 lớp checkpoint + Native lớp 5:**
- [x] Mọi TC có Entry UI checkpoint (Hub render / List render / Detail render / form mở)
- [x] Mọi TC có Critical action/result checkpoint (tap tile/nút/gesture)
- [x] Mọi TC có Route/feedback checkpoint (SnackBar/pop/push/dialog dismiss)
- [x] Mọi TC có Final observable end-state đa-layer (UI + BFF/DB nơi applicable)
- [x] TC native (pull-to-refresh, system back, background-foreground, offline toggle) có Native checkpoint riêng

**C. Platform matrix:**
- [x] TC-001, -008, -020, -024, -031, -032 declare Android+iOS
- [x] TC-007 (iOS-specific swipe-back) có spec iOS riêng — KHÔNG bị skip
- [x] TC-006 (Android-specific hardware back) có spec Android riêng
- [x] TC-045 (tablet) declare device matrix riêng

**D. Native interaction 10 nhánh Exception, Timeout & Native:**
- [x] Network timeout/offline: TC-026, TC-028
- [x] Service 5xx + retry: TC-027
- [x] Concurrent-edit conflict: TC-046 (`covered+spec-gap`)
- [x] Session expiry mid-flow: TC-047
- [x] Partial failure + rollback: `spec-gap` (không có mock-injection harness cho DB-layer failure trên mobile; cascade rollback atomicity đã cover phía web `agent-test-e2e` TC-W03-E2E-020)
- [x] Permission denied: `out-of-scope` (không có permission dialog trong scope catalog)
- [x] Deeplink edge cases: `out-of-scope+lý do`
- [x] Push notification edge cases: `out-of-scope+lý do`
- [x] Background-foreground state: TC-025
- [x] Offline-to-online sync: `adapted` TC-028

**E. Cross-platform parity:**
- [x] Journey applicable cả 2 platform: TC-001, -008, -020, -024, -031, -032 có Android+iOS
- [x] Wording tiếng Việt: TC-049
- [x] Cross-ref web E2E counterpart: TC-038..044 ghi rõ `Cross-ref: TC-W03-E2E-0NN`

**F. Common Baseline Coverage Map — J:**
- [x] Mọi nhóm common §1-§21 áp dụng được đã `covered`/`adapted`/`out-of-scope+lý do` — xem bảng §2 Test Environment & Data.

**Impacted production journeys (regression coverage — bắt buộc re-run Patrol live, không mirror PASS cũ vì đây là wave đầu tiên có mobile E2E cho EP-INVENTORY-CATALOG):**
- Home "Quản lý kho hàng" tile → `InventoryListRoute` orphaned → **TC-W03-ME2E-005 (regression, CRITICAL)**.
- Shared cross-domain widget (`StatusBadge`, `StartInfoRow`, `CustomScaffold`, `AppBarCustom`) dùng chung với receipt/delivery/stock existing screens — verify gián tiếp qua TC-013/032 (render đúng, không phá vỡ shared widget contract).

**Auto-miss list:** không còn `auto-miss` chưa phân loại sau Parity Audit (§2).

---

## 7. Changelog

| Date | Change | Author |
|---|---|---|
| 2026-07-02 | v1: Khởi tạo TC-W03-MOBILE-E2E automated artifact (TEST_PLANNING, full-gen mode). 49 TC bao phủ: Hub navigation (`FEAT-INV-MOBILE-MENU`, 7 TC), Material Group full CRUD (`FEAT-CAT-GRP-LIST/CREATE/DETAIL/EDIT/DELETE`, 23 TC bao gồm cascade INACTIVE mobile-native + 2 popup delete + exception/timeout/lifecycle), Internal Product view-only (`FEAT-CAT-PROD-LIST/DETAIL`, 7 TC), Cross-platform sync (7 TC mirror 6 manual TC-W03-MOBILE-E2E.md + 2 mobile-only TC housed ở manual TC-W03-E2E.md), Concurrent-edit + session expiry + dual persona + localization (4 TC), Responsive tablet (1 TC). Cluster: 12×C1, 20×C2, 15×C3, 2×C4. Common Baseline Coverage Map §1-§21 đầy đủ — deeplink/push/camera/file-picker/biometric đều `out-of-scope+lý do` xác nhận qua source inspection (AndroidManifest không có intent-filter App Links; `local_auth` absent khỏi pubspec; Product attachment tab không render trên mobile). Auto vs Manual Parity Audit: 6 manual TC + 2 TC mobile-only (housed ở file web) → 0 `auto-miss`. **CRITICAL finding**: TC-W03-ME2E-005 phát hiện co-located regression — `InventoryListRoute` (existing production "Danh sách kho" V1) mất entry point sau khi Home tile redirect sang `InventoryHubRoute` (Hub chỉ ship 2/6 tile W03) — cần escalate BA/mobile dev xác nhận có phải interim gap chấp nhận được hay cần fix trước GA. Field-validation coverage giữ representative tối thiểu (bỏ trống/format/dup) — exhaustive family coverage thuộc `agent-test-mobile-ui`. Runner: reuse Layer A frozen `Execution/auto/harness/patrol/` + `integration-test/` (CR-20260701-03) — 15 spec file mới tại `Execution/auto/specs/W03/mobile-e2e/`. Environment gap documented: `Flavor.dev.graphQLUrl` hard-code `localhost:45401` (dead code sau early-return) → cần `adb reverse` + host port-forward tới `192.168.110.191` cho remote-box mode (không phải code fix, chỉ harness bootstrap runtime). | agent-test-mobile-e2e (W03 TEST_PLANNING) |
| 2026-07-03 | v2: TEST_EXECUTION Run 1 (agent-test-mobile-e2e) — Environment Readiness Gate PASS thật (Android emulator Pixel6_API33_arm64 booted, patrol_cli 4.4.0 patched hoạt động, APK dev flavor build+install thành công qua adb reverse+socat proxy tới 192.168.110.191, smoke test PASS xác nhận cold-start). Viết 5 spec file thật (`inventory_hub_patrol_test.dart`, `material_group_crud_patrol_test.dart`, `internal_product_view_only_patrol_test.dart`, `material_group_resilience_patrol_test.dart`, `cross_cutting_patrol_test.dart`) + `_helpers.dart` shared (login/bootstrap/nav helpers) — chạy live nhiều lần, phát hiện + fix 3 harness gap thật (thiếu `bootstrapApp()`/`start()` invocation vì `patrol test` không tự chạy app thật — chỉ `_preTestMessage` placeholder; thiếu codegen `.g.dart` cho local path package `flutter_callkit_incoming`; đã set `key.properties` local debug keystore để build không throw). Sau khi harness thật chạy được, phát hiện **BUG-W03-150 (P1, OPEN)**: sau `login` mutation thành công (token hợp lệ), `getTenantInfo` query kế tiếp trả `ErrorResponse` — app đứng yên ở màn Đăng nhập, không bao giờ vào Home. Đối chứng `curl` thủ công cùng token+query ngay sau login → THÀNH CÔNG (loại trừ nguyên nhân backend/tenant data) → thu hẹp về client-side (nghi race condition `saveIdToken()` không `await` trước `resetClient()` trong `auth_repository.dart`). Tái hiện 2/2 lần Patrol live device độc lập. **Toàn bộ 49 TC chuyển READY → BLOCKED** (Bug ID = BUG-W03-150) — KHÔNG PASS/FAIL bằng code-inspection theo `MOBILE_E2E_INSPECTION_PASS` guard. Chi tiết đầy đủ: `Tracking/WAVE03/verify/BUG-W03-150.verify.md`, `Execution/test-reports/TR-W03-MOBILE-E2E.md`, lesson learned `Tracking/TEST-LESSONS-LEARNED.md` section `agent-test-mobile-e2e`. | agent-test-mobile-e2e (W03 TEST_EXECUTION Run 1) |
| 2026-07-03 | v3: TEST_EXECUTION Run 2 (agent-test-mobile-e2e) — verify BUG-W03-150 theo yêu cầu user ("đã fix"). Phát hiện data-integrity anomaly TRƯỚC: `Tracking/WAVE03/BUGS.md` bị mất row BUG-W03-148/149/150 do commit ngoài `7987304` ("update bug") — đã khôi phục nguyên văn cả 3 row (xem note `## 0a` trong BUGS.md). **BUG-W03-150 → VERIFIED**: chạy Patrol live 5 lần độc lập (`inventory_hub_patrol_test.dart` + 1 kế thừa `_run_reach_home_test.dart` + 2 diagnostic riêng), cả 5 đều `login` → `getTenantInfo` trả `GetTenantInfoApiResponse` (không còn `ErrorResponse`), Home render + dashboard fetch OK. Fix thực tế = uncommitted working-tree change tại `lib/flavors.dart` (dev flavor URL `localhost:454xx` → IP LAN trực tiếp `192.168.110.191:454xx`, bỏ qua double-hop `adb reverse`+`socat` proxy) — KHÔNG phải sửa `auth_repository.dart` như hypothesis race-condition ban đầu (code đó chưa đổi). Root cause reattribution: nghi ngờ hàng đầu là proxy `adb reverse`+`socat` không ổn định cho port BFF chính 45401 (chưa đối chứng 100% do hết thời gian). **NEW blocker phát hiện ngay sau đó** (KHÔNG phải bug — harness): mọi thao tác Patrol tự động (`$(finder).tap()`, `WidgetTester.tap()`, `$.native.tap()`) sau khi Home render đều hang vô thời hạn (>10-20 phút, 5/5 lần) — loại trừ app-level cause bằng `adb shell input tap` thủ công (phản hồi tức thì, điều hướng đúng Hub). Đã thử giảm tải hệ thống (dừng gradle daemon dư) — không cải thiện. Toàn bộ 49 TC giữ `BLOCKED` (Bug ID column đổi `BUG-W03-150` → `ENV-HANG-R2` marker, KHÔNG phải bug ID thật vì đây không phải product defect) — 0 TC PASS/FAIL được trong Run 2 vì blocker mới chặn ngay bước điều hướng đầu tiên sau login (Hub tap). Chi tiết đầy đủ: `Tracking/WAVE03/verify/BUG-W03-150.verify.md` §7+§12, `Execution/test-reports/TR-W03-MOBILE-E2E.md` Run 2, lesson learned `TL-W03-MOB-E2E-004` (`Tracking/TEST-LESSONS-LEARNED.md`). Evidence: `Execution/auto/evidence/W03/mobile-e2e/`. | agent-test-mobile-e2e (W03 TEST_EXECUTION Run 2) |
| 2026-07-03 | v4: TEST_EXECUTION Run 3 (agent-test-mobile-e2e) — **PRIORITY 1: sửa harness Patrol tap-hang `ENV-HANG-R2` (Run 2)** thành công. Root cause thật xác định qua `Future.timeout()` diagnostic wrapper + `ps aux`/`adb shell dumpsys cpuinfo`: `pumpAndSettle()` (Flutter) không bao giờ tự "settle" được trên màn Home có `getDashboardRealtime` polling định kỳ; và `Future.timeout()` KHÔNG huỷ được future gốc (Dart không có cancellation) → vòng lặp bơm-frame orphan tiếp tục chạy NỀN VÔ HẠN, kéo CPU host emulator qemu lên >1000% (quan sát `ps aux` — giảm về ~60% NGAY sau `adb shell am force-stop` app) → toàn bộ emulator (kể cả UIAutomator native automator không phụ thuộc Flutter) phản hồi cực chậm, biểu hiện y hệt "treo vô hạn". **Fix**: viết lại `_helpers.dart` loại bỏ HOÀN TOÀN `pumpAndSettle`/`pumpAndTrySettle` — thay bằng `pumpFrames()` (bơm N frame CỐ ĐỊNH, không lặp tới khi idle) + `tapBounded()` (`settlePolicy: SettlePolicy.noSettle` + `pumpFrames`) + `nativeBackBounded()` (native `$.native.pressBack()` cũng bọc timeout — xác nhận đây LÀ điểm treo thứ 2 sau khi fix pumpAndSettle) + `withTimeout()` (diagnostic breadcrumb `[TIMING]` cho mọi thao tác 1-phát). Áp dụng blanket fix (108 auto-replacement + review) cho 4 spec file còn lại (`material_group_crud`, `internal_product_view_only`, `material_group_resilience`, `cross_cutting`). Verify: 3 lần chạy Patrol live HOÀN TẤT (build+install+run+kết quả) KHÔNG TREO — `tap_fix_smoke_patrol_test.dart` x2 (~3-5 phút/lần, trước đó Run 2 treo >10-20 phút không kết thúc) + `inventory_hub_patrol_test.dart` x1 (TC-001..006, 81s). **2 harness defect khác phát hiện+fix cùng lúc**: (a) Android Test Orchestrator `FATAL EXCEPTION: IllegalArgumentException ... contains a path separator` khi patrolTest() title chứa "/" (vd "TC-013/014/015") — fix đổi "/" → "+" trong 3 file title; (b) missing import `material_group_models.dart` (2 file, `SearchMaterialGroupsRequest`/`CreateMaterialGroupRequest`/`UpdateMaterialGroupRequest` undefined) → compile error chặn build hoàn toàn. **PRIORITY 2: execute 49 TC** — chạy được `inventory_hub_patrol_test.dart` (TC-001..006, business logic PASS qua evidence logcat: Hub render/nav/back/debounce/hardware-back đều đúng) NHƯNG phát hiện **BUG-W03-152 (P2, MỚI)**: lỗi async không catch (CometChat SDK ngay sau login, 3/3 lần; +1 lỗi tương tự ngay sau xác nhận xoá nhóm trong `material_group_crud_patrol_test.dart`, 1/1 lần) trip `flutter_test` binding assertion `_pendingExceptionDetails` — JUnit verdict tổng thể = `failure` (case #1, Dart code vẫn tiếp tục chạy đúng) hoặc STALL hẳn (case #2, không còn tiến). TC-001..006 → `FAIL` (Bug ID `BUG-W03-152`, business logic verified đúng qua evidence nhưng verdict chính thức KHÔNG PASS theo `MOBILE_E2E_INSPECTION_PASS` guard). 43 TC còn lại (008-049) → giữ `BLOCKED` (Bug ID `BUG-W03-152`) — `material_group_crud_patrol_test.dart` chạy được qua create/detail/edit/cascade-validate/2 popup delete (Huỷ nhánh) nhưng STALL ngay sau tap "Xác nhận" xoá (TC-020, trigger #2 BUG-152) trước khi tôi kịp re-run lại hết 4 file còn lại trong ngân sách thời gian phiên này — KHÔNG PASS/FAIL giả cho các TC chưa có run hoàn tất. **PRIORITY 3: verify BUG-W03-150** — không phát hiện regression (đăng nhập thành công NHIỀU LẦN bổ sung trong Run 3, xác nhận direct-IP `192.168.110.191` ổn định, giữ nguyên VERIFIED). Chi tiết đầy đủ: `Execution/test-reports/TR-W03-MOBILE-E2E.md` Run 3, `Tracking/WAVE03/verify/BUG-W03-152.verify.md`, lesson learned `TL-W03-MOB-E2E-005/006/007`. | agent-test-mobile-e2e (W03 TEST_EXECUTION Run 3) |
| 2026-07-03 | v5: TEST_EXECUTION Run 4 (agent-test-mobile-e2e) — **verify BUG-W03-152 (FIX_DONE→VERIFIED)** + execute nốt 49 TC. Rebuild APK dev flavor từ source hiện tại (working-tree chứa fix `main_cubit.dart initialCometChat()`). Re-run Patrol live `inventory_hub_patrol_test.dart` 2×: xác nhận CometChat token-parse error nay `catch`+log (`ERROR │ ⛔ initialCometChat failed: ...`), KHÔNG còn re-throw → **BUG-W03-152 VERIFIED** (phạm vi hẹp, root cause CometChat rethrow fix đúng thiết kế). **Phát hiện BUG-W03-153 (P2, MỚI)**: chuỗi lỗi async/test-binding KHÁC (GraphQL `SaveTokenInput` schema-mismatch qua `saveFcmToken` fire-and-forget; STALL deterministic 2/2 lần tại mở Group Detail trong `material_group_crud_patrol_test.dart`; STALL sau `TimeoutException` CÓ CHỦ ĐÍCH từ helper trong `internal_product_view_only_patrol_test.dart`) tiếp tục trip cùng assertion `_pendingExceptionDetails` — root cause hypothesis rộng hơn: `lib/start.dart` global `FlutterError.onError`/`PlatformDispatcher.instance.onError` override không tương thích `flutter_test`/Patrol binding. Áp dụng workaround môi trường: pre-grant `android.permission.POST_NOTIFICATIONS` qua `adb shell pm grant` ngay sau APK install (giúp 1 run hoàn tất thay vì stall vô hạn — dù vẫn FAIL vì lý do khác). **Verdict cuối Run 4**: TC-001..006 → `FAIL` (Bug ID `BUG-W03-153`, verdict Patrol thật `"status":"failure"`, test HOÀN TẤT 30s, business logic đúng qua evidence nhưng verdict chính thức KHÔNG PASS theo `MOBILE_E2E_INSPECTION_PASS` guard). TC-008/013-015/017-024 (12 TC, `material_group_crud`) + TC-009/010-012/030-034/036/037 (9 TC, `internal_product_view_only`) → `BLOCKED` (Bug ID `BUG-W03-153`, test STALL, không đạt bất kỳ verdict nào — kill tay sau >8-13 phút không tiến, KHÔNG PASS/FAIL giả). TC-025/026/028/049 (`material_group_resilience`, 4 TC) + TC-039/040/042/046/048 (`cross_cutting`, 5 TC) — spec đã sẵn sàng nhưng CHƯA kịp chạy trong ngân sách phiên → `BLOCKED` (Bug ID `BUG-W03-153` governing blocker + chưa-chạy-lần-này). TC-007/016/027/029/035/038/041/043/044/045/047 (11 TC) — CHƯA có spec file → `BLOCKED` (spec-gap + `BUG-W03-153` governing blocker). **0 TC đạt PASS trong Run 4** — dù BUG-152 đã VERIFIED, BUG-153 mới tiếp tục chặn verdict PASS sạch cho toàn bộ 49 TC. Chi tiết đầy đủ: `Execution/test-reports/TR-W03-MOBILE-E2E.md` Run 4, `Tracking/WAVE03/verify/BUG-W03-152.verify.md` (Verdict Log VERIFIED), `Tracking/WAVE03/verify/BUG-W03-153.verify.md` (bug mới), lesson learned `TL-W03-MOB-E2E-008/009` (`Tracking/TEST-LESSONS-LEARNED.md`). | agent-test-mobile-e2e (W03 TEST_EXECUTION Run 4) |
| 2026-07-03 | v6: TEST_EXECUTION Run 5 (agent-test-mobile-e2e) — **verify BUG-W03-153 (FIX_DONE→VERIFIED)** + execute nốt 49 TC. Fix môi trường host bắt buộc trước run (KHÔNG phải code fix): Flutter tự chọn JDK bundled của Android Studio (`/Applications/Android Studio.app/Contents/jbr`, bị `com.apple.quarantine` từ Homebrew Cask → Gatekeeper kill -9 instant khi spawn ngoài app bundle) làm `JAVA_HOME` cho patrol_cli's gradle invocation → MỌI `patrol test` fail `Gradle build failed with code -9` trong ~40ms (không liên quan product/spec) → fix bằng `flutter config --jdk-dir=<JDK khác không quarantine>`. Rebuild APK từ source hiện tại (working-tree chứa fix `lib/start.dart`/`firebase_notification_manager.dart`/`main_cubit.dart` từ `agent-fix-garage-mobile`). Re-run Patrol live **7 lần độc lập qua 5 spec file khác nhau** (bao gồm CẢ 2 spec từng STALL deterministic Run 4: `material_group_crud_patrol_test.dart`, `internal_product_view_only_patrol_test.dart`) — **7/7 lần test HOÀN TẤT 10-14s, 0/7 STALL, 0/7 trip `_pendingExceptionDetails`** → **BUG-W03-153 VERIFIED** (chi tiết Acceptance Criteria đạt tại `verify/BUG-W03-153.verify.md` §2+§5). **Phát hiện 2 bug MỚI, KHÔNG thuộc phạm vi 153**: `BUG-W03-155` (P2 — `getCurrentUser.role="garage-owner"` không decode được bởi enum mobile `GARAGE_OWNER`/`CA`, fire 2/7 lần) và `BUG-W03-156` (P2 — `handleReceivePushNotification()`'s inline `requestPermission()` race với `init()`'s call đã guard bởi fix 153, fire 5/7 lần — dominant) — cả 2 xảy ra NGAY SAU login/Home bootstrap, TRƯỚC Hub navigation, chặn verdict PASS sạch cho gần như toàn bộ suite (cùng blast-radius pattern 152/153 nhưng root cause khác hẳn, KHÔNG PHẢI regression của fix 153). Sửa 2 spec-level bug (Layer B, được phép): `internal_product_view_only_patrol_test.dart` `find.text('Tất cả')` → `find.widgetWithText(Tab, 'Tất cả')` (loại ambiguity finder gây TimeoutException tại `autoFixA41` Run 4); `cross_cutting_patrol_test.dart` sửa thứ tự `getIt<InventoryCatalogRepository>()` gọi SAU `loginAs()` (trước đó gọi trước DI container init → `StateError`). **Verdict cuối Run 5**: 30 TC (`TC-001..006/008/009/011-015/017-026/028/031-034/036/037/049`) → `FAIL` (Bug ID `BUG-W03-156`, dominant). 5 TC (`TC-039/040/042/046/048`, `cross_cutting`) → `FAIL` (Bug ID `BUG-W03-155`, owner login deterministic hit bug này). 14 TC (`TC-007/010/016/027/029/030/035/038/041/043/044/045/047`) → `BLOCKED` (spec-gap: chưa có spec hoặc header comment claim coverage nhưng thân spec chưa assert). **0 TC đạt PASS trong Run 5** — dù BUG-152 VÀ BUG-153 nay đều VERIFIED, 2 bug MỚI (155/156) tiếp tục chặn verdict PASS sạch. Đây LÀ tiến bộ thật: mỗi lần fix 1 lớp bug foundation-level lại lộ ra lớp KẾ TIẾP (152→153→155/156), KHÔNG PHẢI vòng lặp vô ích — mỗi bug đều mới, cụ thể, có evidence rõ ràng, và test framework giờ hoạt động ĐÚNG THIẾT KẾ (báo lỗi trung thực, nhanh, không opaque crash). Chi tiết đầy đủ: `Execution/test-reports/TR-W03-MOBILE-E2E.md` Run 5, `Tracking/WAVE03/verify/BUG-W03-153.verify.md` (Verdict Log VERIFIED), `verify/BUG-W03-155.verify.md` + `verify/BUG-W03-156.verify.md` (2 bug mới), lesson learned mới (`Tracking/TEST-LESSONS-LEARNED.md`). | agent-test-mobile-e2e (W03 TEST_EXECUTION Run 5) |
| 2026-07-03 | v7: TEST_EXECUTION Run 6 (agent-test-mobile-e2e) — **verify BUG-W03-155/156 (FIX_DONE→VERIFIED)** + execute 49 TC tới business verdict thật — **LẦN ĐẦU TIÊN CÓ PASS THẬT trong lịch sử wave này**. Rebuild APK từ source hiện tại (chứa fix 155 `user_role.dart`'s `UserRole.fromJson()` tolerant parser + `profile_response.g.dart` regen, và fix 156 xoá inline `requestPermission()` trùng lặp khỏi `handleReceivePushNotification()`). Chạy `inventory_hub_patrol_test.dart` (TC-001..006) → **PASS SẠCH** (1 Total, 1 Successful, 0 Failed, 30s) — `grep` xác nhận 0 dấu vết `ArgumentError`/`not one of the supported values`/`FirebaseException...already running` trên toàn bộ logcat → **BUG-W03-155 VÀ BUG-W03-156 → VERIFIED chính thức**. Chạy `material_group_resilience_patrol_test.dart` (TC-025/026/028/049): lần 1 FAIL với `IndexError` tại bước `enterText` (0 `TextFormField` tìm thấy) — điều tra logcat xác nhận nguyên nhân là network-response `searchMaterialGroups` (dropdown "Thuộc nhóm") mất 1.3s bất thường (so với <320ms các lần khác), vượt budget 4-pump-frame cố định của spec — **retry lần 2 PASS SẠCH HOÀN TOÀN** (2m17s, cả 9 native step + toàn bộ TC-025/026/028/049) → xác nhận đúng là timing flake do network-latency variance, KHÔNG phải bug sản phẩm. **10 TC PASS**: TC-001..006 (Hub) + TC-025/026/028/049 (Resilience). Chạy `material_group_crud_patrol_test.dart` (TC-008/013-015/017-024): TC-008 (Create+List) qua được bình thường, nhưng ngay khi mở Detail (TC-013, `tapBounded[autoFixA6]`) FAIL với `ServerError: Unauthorized`. Điều tra qua GraphQL introspection TRỰC TIẾP (`{ __type(name: "MaterialGroup") { fields { name } } }` chạy live tới `192.168.110.191:45401`) xác nhận **BUG-W03-157 (P1, MỚI)**: `getMaterialGroup` query select `createdByName`/`updatedByName` — ĐÚNG theo contract đã document (`Architecture/api/agg-garage-graph-graphql.md` R20 v7.22/23, dated 2026-06-25) và khớp `FEAT-CAT-GRP-DETAIL` AC-3 — nhưng BFF **deployed** THIẾU 2 field này 100% deterministic (introspection trả về đúng 13 field, không có 2 field mới) → `GRAPHQL_VALIDATION_FAILED` → mobile's `GraphQLServiceBase._handleGraphQLResponse` mislabel thành generic `"Unauthorized"` (secondary observation, không phải root cause) → chặn hoàn toàn Detail/Edit/Delete/Cascade. Cùng pattern deployment-drift với `BUG-W03-154` (SaveTokenInput/agg-sso-graph) nhưng trên `agg-garage-graph` + 1 query khác — mobile ĐÚNG theo contract, KHÔNG cần sửa. Chạy `cross_cutting_patrol_test.dart` (TC-039/040/042/046/048): FAIL tại TC-048 (dual persona, mở Detail sớm trong bundled test) — xác nhận LẠI đúng `BUG-W03-157` (2/2 lần trong phiên, 2 file khác nhau). Chạy `internal_product_view_only_patrol_test.dart` (TC-009/011/012/031-037) **3 lần độc lập** — **CẢ 3 LẦN đều timeout deterministic tại đúng 1 điểm** (`autoFixA41` — tap Tab "Tất cả" ngay sau khi xem tab "Ngừng hoạt động" 0-kết-quả/empty-state) — KHÁC HẲN pattern flake của resilience test (đó là 1-fail-rồi-pass-sạch; đây là 3/3 y hệt) — filed **BUG-W03-158 (P2, MỚI, root cause CHƯA xác định 100% — cần re-audit xem là app hit-test issue trên empty-state hay giới hạn riêng của Patrol `tap()` khi target đổi trạng thái nhanh)**. **Verdict cuối Run 6**: **10 TC PASS** (`TC-001..006/025/026/028/049`) — breakthrough đầu tiên của wave; **17 TC FAIL** linked `BUG-W03-157` (`TC-008/013-015/017-024/039/040/042/046/048`); **9 TC FAIL** linked `BUG-W03-158` (`TC-009/011/012/031-034/036/037`); **13 TC BLOCKED** spec-gap không đổi (`TC-007/010/016/027/029/030/035/038/041/043/044/045/047`). Verdict: NO-GO nhưng tiến bộ đột phá — toàn bộ 5 bug foundation-level (150/152/153/155/156) đã VERIFIED hết sạch; 2 bug MỚI (157/158) nay hoàn toàn ở tầng business-logic (contract-deployment drift + 1 UI-interaction finding cần re-audit) — journey catalog đầu tiên (Hub navigation + native resilience) đã đạt PASS xanh thật qua Patrol live device, xác nhận toàn bộ chuỗi fix foundation trước đó hoạt động đúng. Chi tiết đầy đủ: `Execution/test-reports/TR-W03-MOBILE-E2E.md` Run 6, `Tracking/WAVE03/verify/BUG-W03-157.verify.md` (MỚI), `verify/BUG-W03-158.verify.md` (MỚI), lesson learned mới `TL-W03-MOB-E2E-013/014` (`Tracking/TEST-LESSONS-LEARNED.md`). | agent-test-mobile-e2e (W03 TEST_EXECUTION Run 6) |
