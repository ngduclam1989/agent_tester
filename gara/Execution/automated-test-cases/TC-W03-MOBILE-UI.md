---
document_id: 'GMS-TC-W03-MOBILE-UI'
type: test-case
parent: 'Execution/test-cases/TEST-CASE-REGISTRY.md'
status: ACTIVE
version: 2
boundary: 'garage-mobile'
wave: 'W03'
owner: 'QA Authority'
last_reviewed: '2026-07-03'
qa_reviewed_by: 'cuongnguyen_ac@cardoctor.vn'
qa_reviewed_at: '2026-07-02'
drift_impact:
  - report: 'Execution/tracking/drift-impact/W03-2026-07-03T15-02-35Z.md'
    timestamp: '2026-07-03T15:02:35+00:00'
    impacted_ids: ['FEAT-CAT-GRP-CREATE', 'FEAT-CAT-PROD-CREATE', 'FEAT-CAT-PROD-DETAIL', 'FEAT-CAT-PROD-EDIT', 'FEAT-ID-CREATE-V2', 'FEAT-IR-CREATE-V2', 'TC-W03-MUI-A-001', 'TC-W03-MUI-A-015', 'TC-W03-MUI-B-001', 'TC-W03-MUI-B-002', 'TC-W03-MUI-B-003', 'TC-W03-MUI-B-004', 'TC-W03-MUI-B-005', 'TC-W03-MUI-B-006', 'TC-W03-MUI-B-007', 'TC-W03-MUI-B-008', 'TC-W03-MUI-B-009', 'TC-W03-MUI-B-010', 'TC-W03-MUI-B-011', 'TC-W03-MUI-B-012', 'TC-W03-MUI-B-013', 'TC-W03-MUI-B-014', 'TC-W03-MUI-B-015', 'TC-W03-MUI-G-001', 'TC-W03-MUI-G-002', 'TC-W03-MUI-G-003', 'TC-W03-MUI-G-004', 'TC-W03-MUI-G-005', 'TC-W03-MUI-G-006', 'TC-W03-MUI-G-007', 'TC-W03-MUI-G-008', 'TC-W03-MUI-G-009', 'TC-W03-MUI-H-007']

---

# Test Case Template - W03: Mobile UI — Danh mục vật tư (EP-INVENTORY-CATALOG slice 1/4)

> Automated testcase artifact do `agent-test-mobile-ui` sinh tại `TEST_PLANNING`. Manual QC file
> `Execution/test-cases/TC-W03-MOBILE-UI.md` (52 TC, `QA Authority`) chỉ đọc để cross-check —
> xem §Auto vs Manual Parity Audit trong `Test Environment & Data`.

---

## 1. General Info

| Field         | Value                                                      |
| ------------- | ---------------------------------------------------------- |
| Document ID   | `GMS-TC-W03-MOBILE-UI`                      |
| Wave          | W03                                           |
| Boundary(ies) | `garage-mobile`                                             |
| Feature(s)    | `FEAT-INV-MOBILE-MENU`, `FEAT-CAT-GRP-LIST`, `FEAT-CAT-GRP-CREATE`, `FEAT-CAT-GRP-DETAIL`, `FEAT-CAT-GRP-EDIT`, `FEAT-CAT-GRP-DELETE`, `FEAT-CAT-PROD-LIST` (view-only), `FEAT-CAT-PROD-DETAIL` (view-only) |
| Owner         | `QA Authority`                                                |
| Last Reviewed | 2026-07-02                                                   |
| Work Package  | `Execution/work-packages/PKG-W03-inventory-catalog.md` (§2.2.4, §4.3, §5) |

---

## 2. Scope

### In Scope

- **8 màn mobile** (post CR-1782373204 mobile scope partial + CR-1782381477 flat-card + PKG v24 addendum hub page): `InventoryHubPage` (hub, `FEAT-INV-MOBILE-MENU`), `MaterialGroupListPage` + sub-page `MaterialGroupSearchPage`/`MaterialGroupFilterPage`, `AddMaterialGroupPage`, `EditMaterialGroupPage`, `MaterialGroupDetailPage`, `MaterialGroupDeleteCubit` (2 popover, no dedicated page), `InternalProductListPage` (view-only) + sub-page search/filter, `InternalProductDetailPage` (view-only).
- Widget render, BLoC/Cubit state machine (7 cubit: `InventoryHubCubit`, `MaterialGroupListCubit`, `AddMaterialGroupCubit`, `EditMaterialGroupCubit`, `MaterialGroupDetailCubit`, `MaterialGroupDeleteCubit`, `InternalProductListCubit`, `ProductDetailCubit`), field validation client-side (Group form 5 field), design-token conformance (`AppColors`/`AppTextStyle`/`AppSizes`, KHÔNG raw `Color(0xFF…)`/`TextStyle` literal trong production — M-28), default-pattern D-M1..D-M14 (ConfirmationDialog cho delete, chevron, appbar back, form label-above, danger color), responsive phone+tablet, a11y Semantics, golden visual (alchemist, C2), route/navigation (GoRouter/auto_route, C3 Patrol).
- Co-located regression cho 22 bug đã `FIX_DONE`/`VERIFIED` trong `Tracking/WAVE03/BUGS.md` (BUG-W03-012..037, tất cả `garage-mobile`) — token mismatch, card decoration, filter footer style, search TabBar AppBarCustom slot.
- Permission matrix: cả 2 role (`garage-owner` + `accountant`) full CRUD nhóm VTHH ngang nhau; Product view-only ngang nhau.

### Out of Scope

- **Mobile Product CRUD/Import/Export** — `FEAT-CAT-PROD-{CREATE,EDIT,DELETE,IMPORT,EXPORT}` = web-only (CR-1782373204) → covered `TC-W03-UI` (`agent-test-ui`).
- **API contract / GraphQL schema / resolver** → `TC-W03-API` (`agent-test-api`).
- **Cross-platform sync Web ↔ Mobile** (vd tạo nhóm mobile → web reload thấy) → `TC-W03-E2E` + `TC-W03-MOBILE-E2E` (`agent-test-e2e` / `agent-test-mobile-e2e`).
- **Native permission/file-picker/deeplink/push/biometric** → `agent-test-mobile-e2e`.
- **Tenant isolation thật ở API/DB** → `agent-test-isolation` (mobile UI chỉ verify UI hiển thị đúng phạm vi garage khi được BFF trả, không test cross-tenant denial thật).
- **Performance / load test** → `agent-test-performance`.
- **Dark mode** — app KHÔNG hỗ trợ dark mode trong scope hiện tại (không tìm thấy AC/PKG nào yêu cầu) → out-of-scope, không phải gap.
- **Deeplink vào hub/catalog** — chưa có spec (wave-spec §4.3 "KHÔNG deeplink vào hub trong W03 scope").

### Test Environment & Data

| Item                                        | Required Data / Setup       | Notes                           |
| -------------------------------------------- | --------------------------- | ------------------------------- |
| Flutter SDK | 3.41+ / Dart 3.11+ | `flutter --version` precheck ở TEST_EXECUTION Step 1c; nếu absent → BLOCKED (không fallback code-inspection) |
| Package mobile | `cardoctor_garage_v3` (`mobile/gf-garage-app/pubspec.yaml`) | Path dep KHÔNG khả thi cho QC harness (native plugin transitive: firebase_core/dio/graphql_flutter — TL-W01-MUI-003); mọi spec dùng inline pure-Dart stub |
| Harness C1 | `Execution/auto/harness/flutter-widget/` (Lớp A frozen, CR-20260701-03 — reuse nguyên trạng, KHÔNG sửa `pubspec.yaml`/`dart_test.yaml`) | `flutter pub get` rồi `flutter test Execution/auto/specs/W03/mobile-ui/{spec}_test.dart` |
| Harness C2 | `Execution/auto/harness/alchemist/` (Lớp A frozen — `alchemist ^0.14.0`, font Roboto bundled) | Golden PNG dir `goldens/` sinh lần đầu cần human review trước khi accept baseline (KHÔNG auto-update) |
| Harness C3 | `Execution/auto/harness/patrol/` (reuse từ W01/W02, `patrol.yaml` đã có) | Cần Android emulator (`flutter emulators --launch`) hoặc iOS simulator (`xcrun simctl boot`); nếu không boot được sau 2 retry → cluster C3 `BLOCKED`, KHÔNG hạ xuống C1 |
| Seed Group | Cây 3 cha + 5 con ACTIVE + 1 INACTIVE; 1 nhóm có mã sản phẩm gắn (block-delete case); 1 nhóm rỗng (allow-delete case) | Cho cascade + delete-block test |
| Seed Product | ≥ 10 mã ACTIVE + ≥ 2 INACTIVE; ≥ 1 mã có `originDisplayName`/`mainUnitDisplayName`/`materialGroupName`/`brand` đầy đủ (enrichment R18) | View-only render test |
| Auth | Token `garage-owner` + `accountant`, tenant `garage-a` | Permission matrix (H-003) |
| Device matrix (C3/C4) | Android 13 phone + iOS 17 phone + Android tablet | Responsive (H-004), iOS swipe-back (H-005) — theo manual TC Test Environment |

#### Oracle Coverage Plan (mobile) — 8 screen in-scope

| Screen (oracle) | TC ID (auto artifact) | 5-cấp evidence | Cluster | Verdict gate |
|---|---|---|---|---|
| Hub "Quản lý kho hàng" | HUB-001, HUB-009 | Screen+Widget+Variant+Text+Tokens | C2 (alchemist) + C1 (bloc/logic) | **QA-oracle gap**: `Product/ux/figma-test-mobile/wave03-inv-mobile-menu-oracle.md` KHÔNG tồn tại (chưa `/prefetch-figma-oracle mobile 03 FEAT-INV-MOBILE-MENU`). Substitute: `Product/ux/figma-mobile/wave03-inv-mobile-menu.md` v7 ACTIVE (DEV transform, screenshot-verified `_png_verified`/`§VV` claims, node `21519:27371`) + wave-spec T4 `Execution/wave-specs/W03/Product/features/mobile/FEAT-INV-MOBILE-MENU.md`. Wording/layout dùng substitute (đủ chi tiết); golden C2 PASS/FAIL độc lập nhưng cần re-confirm khi canonical QA oracle được fetch. |
| Nhóm VTHH — Danh sách (flat card) | A-001, A-002, A-003, A-018 | Screen+Widget+Variant+Text+Tokens | C2 + C1 | **Oracle registry drift** (không phải "missing"): `wave03-cat-grp-list--kh-alloc-only-oracle.md` VÀ `wave03-cat-grp-list--section-oracle.md` cả 2 file tự flag "⚠️ REGISTRY/SCOPE DRIFT" — content thực tế là FEAT-INS-STL-DETAIL settlement screens (wave 02 leftover), KHÔNG phải GRP-LIST. Wording/layout dùng fallback `UX-FLOW-INVENTORY-CATALOG.md §3.1` + `FEAT-CAT-GRP-LIST` AC-1..3 + PKG §2.2.4 canonical widget list. Golden: BLOCKED-by-oracle cho tới khi registry fix + `/prefetch-figma-oracle mobile 03 FEAT-CAT-GRP-LIST` chạy lại đúng node. |
| Nhóm VTHH — Thêm mới | B-001 | Screen+Widget+Variant+Text+Tokens | C2 + C1 | Oracle OK: `wave03-cat-grp-create-oracle.md` node `21252:51299` |
| Nhóm VTHH — Chỉnh sửa | C-001 | Screen+Widget+Variant+Text+Tokens | C2 + C1 | Oracle OK: `wave03-cat-grp-edit-oracle.md` node `21254:51963` |
| Nhóm VTHH — Chi tiết | D-001, D-004 | Screen+Widget+Variant+Text+Tokens | C2 + C1 | Oracle OK: `wave03-cat-grp-detail-oracle.md` node `21254:51661` |
| Nhóm VTHH — Xóa (2 popover) | E-001, E-003 | Screen+Widget+Variant+Text+Tokens | C2 + C1 | Oracle OK: `wave03-cat-grp-delete-oracle.md` node `21254:52182`/`21254:52571` |
| Mã SP nội bộ — Danh sách | F-001 | Screen+Widget+Variant+Text+Tokens | C2 + C1 | Oracle PARTIAL: `wave03-cat-prod-list-oracle.md` — `get_metadata` `MCP_OUTPUT_TOO_LARGE`, bóc từ `_full.png` visual + sibling token vocab (`data_completeness.component_inventory: partial`). Golden dùng `_full.png` crop cột tương ứng, tolerance ±2px. |
| Mã SP nội bộ — Chi tiết | G-001, G-004 | Screen+Widget+Variant+Text+Tokens | C2 + C1 | Oracle OK: `wave03-cat-prod-detail-oracle.md` node `21526:45088`/`21528:24629` |

> `coverage_gaps[]`: không có mục nào bị loại — 8/8 screen đều có ≥1 conformance TC (kể cả 2 screen oracle-gap/drift, dùng fallback UX-FLOW/FEAT/substitute per `design-traceability.md §Oracle Status Fallback Handling`).

#### Common Test Case Baseline — Coverage Map (đối chiếu `common-testcase-mobile-ui.md` §1-21)

| § | Nhóm | Trạng thái | TC ref / lý do |
|---|---|---|---|
| §1 Layout | LAY-001/002/004/005/006 | `covered` | HUB-001/002/009, A-001/002/018, B-001, C-001, D-001, E-001/003, F-001, G-001 |
| §1 LAY-003 (BottomNav role-based) | `out-of-scope` | Không có BottomNav trong 8 màn wave này — hub tự nó là entry, không phải bottom-nav item (lý do: chưa xác định entry-point, xem NC-INV-MENU-01) |
| §1 LAY-007/008/009 | `adapted` | Cross-OS (golden chạy chung 1 widget tree, không phân biệt iOS/Android theme) + orientation → H-004; safe-area → note trong A-006/D-001 (SafeArea bottom mandatory) |
| §1 LAY-010 (status bar style) | `out-of-scope` | Không có brightness/status-bar spec riêng biệt tuần này |
| §2 TextFormField (TXT-001..010) | `covered` | Mã/Tên/Mô tả field TCs (B-004/005/006/007/010, C-002/009) |
| §2 TXT space-only/trimspace/paste (011-013) | `spec-gap` | AC-2/3 FEAT-CAT-GRP-CREATE không định nghĩa hành vi trim/space-only tường minh — flag BA, không tự suy diễn expected |
| §2 DropdownButtonFormField (DRP-001..005) | `covered` | Thuộc nhóm/Trạng thái dropdown (B-008/009, C-004/005) |
| §2 DatePicker/TimePicker | `out-of-scope` | 8 màn wave này không có field ngày/giờ |
| §2 Switch/Checkbox (SWT) | `out-of-scope` | Không có switch/checkbox hiển thị (Hub có switch `opacity-0` hidden theo oracle — không render, không cần test) |
| §3 BottomSheet/Dialog/SnackBar | `covered` | Dropdown bottom-sheet (B-008), cascade dialog (C-007/008), delete popover (E-001/002/003/004/008), toast (B-011, C-010) |
| §4 Button states | `covered` | BTN-001/002/003/004/005/006 qua HUB-008/010, B-015, D-009, G-002 |
| §5 List/Grid/Refresh | `covered` | A-004/005/006/010/011/012/013, F-004/005/012/013 |
| §5 LST-006 swipe-to-dismiss | `out-of-scope` | Delete flow dùng long-press+dialog / icon Thao tác, KHÔNG dùng `Dismissible` swipe (theo FEAT-CAT-GRP-DELETE + manual TC-024..027) |
| §6 Search & Filter | `covered` | A-008/009/014, F-006/010/011 |
| §7 Navigation push/pop/replace | `covered` | HUB-005/006, A-008/015/016/017, D-007/008, F-008/010 (tất cả C3 Patrol) |
| §7 NAV BottomNav tab switch | `out-of-scope` | Không có BottomNav trong scope |
| §8 BLoC state machine | `covered` | Toàn bộ 7 cubit — HUB-003, A-004/005/006, B-013/014, C-012/013, D-005, E-005/006/007, F-004, G-005 |
| §9 AppBar/BottomNav/Drawer | `covered` (AppBar) / `out-of-scope` (BottomNav/Drawer — không trong scope) | HUB-002, A-002, B-002(ngầm trong B-001), D-002(ngầm D-001), F-001, G-001 |
| §10 Responsive | `covered` (RSP-001..004) / `spec-gap` (RSP-005 text-scale) / `out-of-scope` (RSP-006/007/008 device-brand/resolution/OS-version matrix — C4 không budget wave này, residual risk ghi nhận) | H-004 |
| §11 Dark mode | `out-of-scope` | App không hỗ trợ dark mode (không AC nào yêu cầu) |
| §12 Accessibility | `covered` (A11-001/002/005/006/007/009) / `adapted` (A11-003 contrast — suy từ token, không đo WCAG ratio thật) / `spec-gap` (A11-004 TalkBack order, A11-008 font-scale ≥1.5) | HUB-010, D-009, G-009, H-006 |
| §13 Localization | `covered` | Wording verbatim TCs toàn bộ 8 screen + HUB-002 (i18n key leak check) |
| §14 Deeplink rendering | `out-of-scope` | Wave-spec §4.3 "KHÔNG deeplink vào hub trong W03 scope" |
| §15 Platform-specific (Material3/Cupertino) | `out-of-scope` | Không có AC chỉ định Material3 `FilledButton`/Cupertino riêng cho 8 màn này |
| §16 Grid/Table cell | `out-of-scope` | List là card, không phải table/grid nhiều cột |
| §17 Link/Hyperlink | `out-of-scope` | Không có hyperlink widget trong 8 màn |
| §18 UI Text quality | `covered` (adapted) | Verbatim-text assertion đã bắt buộc trong mọi wording TC (Steps Specificity) — không TC riêng nhưng lồng vào B/C/D/E wording checks |
| §19 Multi-touch/rapid input | `covered` (MTI-003 rapid tap) / `out-of-scope` (pinch-zoom — không có image viewer/chart) | HUB-008, B-015 |
| §20 Device compatibility matrix | `out-of-scope` (residual risk) | C4 không budget — H-004/H-005 chỉ cover 1 device/platform mỗi loại; khuyến nghị wave regression riêng cho device matrix |
| §21 Usability heuristics | `covered` | Touch target (HUB-010/H-006), same-function button token (design-token TCs), back/undo (H-005 + AppBar back mọi screen), loading indicator (shimmer TCs), empty-state CTA (A-013/F-005) |

#### Co-located Feature Regression Inventory (Step 4.2/4.3)

> W03 mobile là feature MỚI hoàn toàn (`lib/ui/inventory_catalog/` — thư mục chưa tồn tại trước wave này); KHÔNG có màn production cũ bị "sửa" trực tiếp. Rủi ro regression thực tế nằm ở **shared widget/token đã tồn tại được reuse**, không phải ở existing feature bị chỉnh sửa tại chỗ.

| Widget/token reused (existing) | Nguồn | Impact của W03 | Regression TC |
|---|---|---|---|
| `StatusBadge` (`lib/ui/inventory/widgets/status_badge.dart`) | Inventory sibling domain (đã tồn tại trước W03, dùng bởi Receipt/Delivery/Stock hiện có) | Additive: 2 adapter mới `MaterialGroupStatus`/`InternalProductStatus implements InventoryStatusInterface` | H-002 (verify sibling consumer không regress khi thêm adapter mới) |
| `StartInfoRow` (`lib/ui/inventory/widgets/start_info_row.dart`) | Inventory sibling domain | Reuse read-only, không sửa widget gốc | H-002 |
| `CustomScaffold`/`CustomAppBar`/`AppButton`/`AppTextField` (`lib/ui/widgets/**`) | Global shared, dùng bởi toàn bộ app (booking/SO/employee/…) | Reuse read-only, KHÔNG sửa source | H-001 (widget-tree assertion xác nhận KHÔNG dùng lại pattern lỗi cũ — FAB/RefreshIndicator/CircularProgressIndicator — mà không đụng widget gốc) |
| `router.dart` (auto_route) | Global route table | Additive: 8 route mới + `AuthGuard` (BUG-W03-012 đã fix syntax lỗi chặn build) | H-001 note: build-clean guard (đã VERIFIED qua BUG-W03-012 L1 status) — không cần TC riêng vì đã guard qua BLOCKED-build regression tại DEV stage |
| Entry-point vào Hub (drawer/main-shell/tile) | **NEED CONFIRMATION** — NC-INV-MENU-01 (wave-spec §12) | Chưa xác định màn cụ thể nào bị thêm entry-point | **Escalation** (không phải TC): BA cần xác nhận trước `/qc-start`; W03 chỉ test direct `context.pushRoute` (workaround wave-spec §12), KHÔNG test tap-from-main-shell |

#### Auto vs Manual Parity Audit (52 manual TC → auto)

> Nguồn manual (read-only): `Execution/test-cases/TC-W03-MOBILE-UI.md` (52 TC, `last_reviewed: 2026-06-30`/`2026-07-01`). Kết quả: **52/52 map được** (`covered` hoặc `adapted`) — **0 `auto-miss`** (không cần lesson-learn entry mới; xem Self-Audit).

| Manual TC | Auto TC ref | Nhãn |
|---|---|---|
| 001 | HUB-001 | covered |
| 002 | HUB-005 | covered |
| 003 | HUB-006 | covered |
| 004 | HUB-007 | covered |
| 005 | A-001, A-003 | covered |
| 006 | A-002, A-010, A-020 | covered |
| 007 | A-007 | covered |
| 008 | A-008, A-009 | covered |
| 009 | A-012 | covered |
| 010 | A-010 | covered |
| 011 | A-013 | covered |
| 012 | A-006 | adapted (gộp retry-tap vào Expected Result của A-006) |
| 013 | A-015, B-001 | covered |
| 014 | B-005 | covered |
| 015 | B-008 | covered |
| 016 | B-008 | adapted (manual tự ghi "trường hợp hiếm nếu BR-008 đã filter" — spec ambiguous, gộp làm note phụ trong B-008 thay vì TC riêng) |
| 017 | B-011 | covered |
| 018 | A-017, D-007 | covered |
| 019 | C-007 | covered |
| 020 | C-010 | covered |
| 021 | D-001, D-003 | covered |
| 022 | D-006 | covered |
| 023 | D-004 | adapted (đổi tên đúng theo class thật `MaterialGroupStatus`/`InventoryStatusInterface`) |
| 024 | E-001, E-002 | covered |
| 025 | E-006 | covered |
| 026 | E-007 | covered |
| 027 | E-009 | covered |
| 028 | H-003 | adapted (gộp vào permission decision-table thay vì TC riêng cho delete) |
| 029 | F-002 | covered |
| 030 | F-001, F-003 | covered |
| 031 | F-006 | covered |
| 032 | F-008 | covered |
| 033 | G-001, G-002 | covered |
| 034 | G-003 | covered |
| 035 | G-004 | covered |
| 036 | G-006 | covered |
| 037 | H-001 | adapted (đổi từ static-grep sang widget-tree assertion để tránh `MOBILE_UI_STATIC_PASS`) |
| 038 | H-006 | covered |
| 039 | H-004 | covered |
| 040 | H-005 | covered |
| 041 | F-013 | covered |
| 042 | HUB-007 | adapted (bổ sung ghi chú route-guard AuthGuard vào Expected Result) |
| 043 | E-008 | covered |
| 044 | A-011 | covered |
| 045 | B-005 | covered (dup-merge với 014 — cùng 1 nhánh validation) |
| 046 | B-007 | covered |
| 047 | B-010 | covered |
| 048 | B-006 | covered |
| 049 | B-012 | covered |
| 050 | C-002 | covered |
| 051 | C-004 | covered |
| 052 | C-011 | covered |

#### Self-Audit Record (Gate trước `READY`)

- **Technique Compliance**: Equivalence Partitioning + Boundary Value Analysis (Mô tả 255/256 — B-010/C-009), Decision Table (role × action — H-003; payer/status matrix N/A cho wave này), State Transition (7 cubit — B-013/014, C-012/013, E-005/006/007, A-004/005/006, F-004, G-005, HUB-003), Error Guessing (autoUppercase+special-char B-005, self/descendant dropdown C-004). Pairwise N/A (không có tổ hợp field lớn).
- **Edge-Case Coverage** (`edge-case-groups.md`): State Machine `covered` (cubit TCs) · Cascade `covered` (C-007 status cascade xuống con) · Boundary Constraints `covered` (Mô tả 255/256) · Data Integrity `covered` (dropdown self/descendant guard C-004/006, delete-block E-006/007) · Permission&Isolation `covered` (H-003 role parity; tenant-isolation thật routed sang `agent-test-isolation`) · Concurrency `spec-gap` (2 phiên cùng sửa 1 nhóm — không có AC/UX-FLOW định nghĩa optimistic-lock UI feedback cho mobile catalog; flag cho BA, không tự suy diễn).
- **Field-Validation Coverage** (`field-validation-taxonomy.md`, Group form 5 field): Mã nhóm VTHH — Default/Bỏ trống/Format-sai/Duplicate `covered` (B-003/004/005/006); Space-only/Trimspace `spec-gap` (flagged ở Coverage Map §2). Tên nhóm VTHH — Default/Bỏ trống `covered` (B-007). Thuộc nhóm — Default/Dependency `covered` (B-008, C-004/005/006). Trạng thái — Default/Cross-field-dependency(cascade) `covered` (B-009, C-007). Mô tả — Default/Boundary `covered` (B-010/C-009). **CRITICAL/HIGH family (Bỏ trống, Format sai, Boundary, Cross-field dependency) 100% covered — không mandatory failure mở.** Số âm N/A (Group form không có field số).
- **Coverage Accounting carry-over**: mọi coverage group trong PKG §2.2.4 mobile đã account `covered-by-automation` (8 screen, 7 cubit) — không group nào còn `spec-gap` treo ngoài các mục đã liệt kê rõ lý do ở trên (space-only/trimspace field, concurrency edge-case, text-scale ≥1.5, device-matrix C4, entry-point NC-INV-MENU-01).
- **Common Baseline checklist review** (§Checklist Review common-testcase-mobile-ui.md A-L): đối chiếu xong — mọi mục A-L có ≥1 dòng `covered`/`adapted`/`out-of-scope+lý do` trong Coverage Map ở trên; không còn mục trống.
- **Auto vs Manual Parity**: 52/52 manual TC map được `covered`/`adapted`; **0 `auto-miss`** → không cần thêm lesson-learn entry mới (không có gap để log).
- **Kết luận**: Không còn mandatory failure mở → artifact `READY` cho QA Authority review.

---

## 3. Status Summary

| Coverage Mode | Total | Status Summary |
| --- | --- | --- |
| Automated | 108 | **TEST_EXECUTION Run 1 (2026-07-03)**: 32 PASS (30 pre-existing C1 bloc/widget specs + 2 mới `TC-W03-MUI-A-020`/`F-014` — regression guard cho BUG-W03-030/031/035/036, spec mới `regression_a020_f014_test.dart`), 76 BLOCKED-by-harness (16 C2 chặn bởi `BUG-W03-148` alchemist-harness crash; 26 C3 chặn bởi thiếu Patrol spec cho wave này; 34 C1 chặn bởi spec-gap — TC gán cluster C1 nhưng chưa có test-block runnable từ TEST_PLANNING). 0 FAIL. Kết luận tổng quát run này = **BLOCKED** (xem `TR-W03-MOBILE-UI.md`). |
| Manual | 52 | 52 READY (xem `Execution/test-cases/TC-W03-MOBILE-UI.md`, read-only — không re-run trong TEST_EXECUTION, manual QC ownership riêng) |

---

## 4. Test Cases

### Nhóm HUB — FEAT-INV-MOBILE-MENU (10 TC)

| TC ID | Feature ID | Boundary | AC Ref | Type | Suite | Priority | Title | Preconditions | Steps | Expected Result | Status | Bug ID |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-W03-MUI-HUB-001 | FEAT-INV-MOBILE-MENU | garage-mobile | AC-1, AC-2, AC-3 | UI | Smoke | P1 | Hub "Quản lý kho hàng" render grid 2 cột đúng 2 tile W03 (golden) | Cluster **C2** alchemist (`Execution/auto/harness/alchemist/`, harness Lớp A frozen). Oracle gap: QA-oracle `wave03-inv-mobile-menu-oracle.md` không tồn tại; dùng substitute `Product/ux/figma-mobile/wave03-inv-mobile-menu.md` v7 (node `21519:27371`, screenshot-verified) + wave-spec T4 §4.1. Spec: `Execution/auto/specs/W03/mobile-ui/catalog_golden_test.dart` (`hub_w03_2tiles`) | 1. `flutter test catalog_golden_test.dart` chạy `goldenTest('Hub — Quản lý kho hàng — 2 tile W03 grid')`.<br>2. So golden PNG `hub_w03_2tiles_w03-2-tiles.png` với dựng lại theo token (bgSecondary `#f3f3f4`, tile bgBase `#ffffff` radius 8, IconCircle 48 bg `#edf7ff`).<br>3. Assert `find.text('Sản phẩm')` + `find.text('Nhóm vật tư')` findsOneWidget mỗi cái; `find.text('Phiếu nhập')`/`'Phiếu xuất'`/`'Tồn kho'`/`'Tồn đầu kỳ'` findsNothing. | - AppBar title "Quản lý kho hàng" center, back chevron trái, KHÔNG trailing action.<br>- `GridView.count(crossAxisCount: 2)` với đúng 2 `FeatureTile` (167.5×104, radius 8dp).<br>- Golden diff = 0 (hoặc human-reviewed nếu lần đầu sinh baseline). | PASS (QC-manual manual-test) | BUG-W03-148 |
| TC-W03-MUI-HUB-002 | FEAT-INV-MOBILE-MENU | garage-mobile | AC-2 | UI | Wave | P2 | AppBar title đúng i18n key `inventoryHub_title`, không lộ raw key | Cluster **C1** widget test với `easy_localization` thật (KHÔNG mock `LocaleKeys.x.tr() → 'x'`) | 1. Pump `InventoryHubPage` với `EasyLocalization` context tiếng Việt thật.<br>2. `expect(find.text('Quản lý kho hàng'), findsOneWidget)`.<br>3. `expect(find.textContaining('inventoryHub_title'), findsNothing)` — key không lộ ra UI. | - Text hiển thị đúng "Quản lý kho hàng" (verbatim, preserve dấu ả/ý).<br>- KHÔNG có raw i18n key nào lộ ra widget tree. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-HUB-003 | FEAT-INV-MOBILE-MENU | garage-mobile | AC-3, AC-4 | UI | Wave | P1 | `InventoryHubCubit` Initial → Loaded với đúng 2 tile const list (W03) | Cluster **C1** bloc_test. Spec: `Execution/auto/specs/W03/mobile-ui/hub_bloc_test.dart` | 1. `blocTest` build `InventoryHubCubitStub()`.<br>2. `verify` state `isA<HubLoaded>()`.<br>3. `expect(cubit.state.visibleTiles.length, 2)`. | - State transition không async I/O (constructor emit ngay `HubLoaded`).<br>- `visibleTiles` = đúng 2 phần tử, thứ tự "Sản phẩm" trước "Nhóm vật tư". | PASS | N/A |
| TC-W03-MUI-HUB-004 | FEAT-INV-MOBILE-MENU | garage-mobile | AC-4, BR-INV-MENU-002 | UI | Wave | P1 | 4 tile chưa GA hoàn toàn không có entry trong const list (không phải ẩn runtime) | Cluster **C1**, cùng spec `hub_bloc_test.dart` | 1. `expect(_kW03Tiles.any((t) => t.label == 'Phiếu nhập'), isFalse)` — lặp lại cho 'Phiếu xuất'/'Tồn kho'/'Tồn đầu kỳ'. | - Không có entry nào cho 4 tile trong compile-time const list — KHÔNG dùng `Visibility(visible:false)` (waste), KHÔNG badge "Sắp ra mắt". | PASS | N/A |
| TC-W03-MUI-HUB-005 | FEAT-INV-MOBILE-MENU | garage-mobile | AC-5, BR-INV-MENU-004 | UI | Wave | P1 | Tap tile "Sản phẩm" → push live tới `InternalProductListPage`; back → về Hub, preserve stack | Cluster **C3** Patrol live device/emulator. `context.pushRoute(InternalProductListRoute())` | 1. `$.pumpWidgetAndSettle(MyApp())`.<br>2. `$('Sản phẩm').tap()`.<br>3. `$.waitUntilVisible($('Mã sản phẩm nội bộ'))` hoặc `find.byType(InternalProductListPage)`.<br>4. `$.native.pressBack()` (Android) hoặc AppBar back tap.<br>5. `expect(find.byType(InventoryHubPage), findsOneWidget)`. | - Push route `/catalog/internal-products` thành công.<br>- Back trả về đúng Hub (KHÔNG về root/login).<br>- Back stack preserve (không mất history). | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-HUB-006 | FEAT-INV-MOBILE-MENU | garage-mobile | AC-5, BR-INV-MENU-004 | UI | Wave | P1 | Tap tile "Nhóm vật tư" → push live tới `MaterialGroupListPage`; back → về Hub | Cluster **C3** Patrol | 1-5. Tương tự HUB-005 nhưng tap "Nhóm vật tư" → assert `MaterialGroupListPage` mount → back → Hub. | - Push route `/catalog/material-groups` thành công.<br>- Back trả về Hub, preserve stack. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-HUB-007 | FEAT-INV-MOBILE-MENU | garage-mobile | AC-6, BR-INV-MENU-003 | UI | Wave | P2 | Cả 2 role thấy đúng `visibleTiles`; permission thật gate ở route đích qua `AuthGuard`, KHÔNG ở hub | Cluster **C1** widget/bloc test, mock role injection (garage-owner + accountant) | 1. Render Hub với role context `garage-owner` → assert `visibleTiles` = 2 tile.<br>2. Render lại với role `accountant` → assert `visibleTiles` giống hệt (không filter theo role tại hub layer).<br>3. Assert route `/inventory/hub` có `guards: [AuthGuard()]` (mock guard invoked với JWT hợp lệ) — permission thật check ở sub-module. | - Không có conditional render theo role tại `InventoryHubPage`.<br>- `AuthGuard` chỉ check JWT hợp lệ tại route hub, không role-filter tile. | PASS | N/A |
| TC-W03-MUI-HUB-008 | FEAT-INV-MOBILE-MENU | garage-mobile | EC-3 | UI | Wave | P2 | Tap tile rất nhanh 2 lần (< 300ms) → chỉ điều hướng 1 lần (debounce) | Cluster **C1**, spec `hub_bloc_test.dart` (`NavigateHandler`) | 1. `handler.onTileTap(route)` 2 lần cách nhau 50ms.<br>2. `verify(() => router.push(route)).called(1)`.<br>3. Case đối chứng: 2 tap cách nhau 350ms → `called(2)` (không bị over-debounce). | - Debounce 300ms chặn double-tap tạo 2 navigation.<br>- Tap cách nhau > 300ms vẫn navigate bình thường (không silent-block vĩnh viễn). | PASS | N/A |
| TC-W03-MUI-HUB-009 | FEAT-INV-MOBILE-MENU | garage-mobile | §4.1 Visual fidelity | UI | Wave | P2 | Design tokens `FeatureTile` — 167.5×104dp, radius 8dp, IconCircle 48dp bg `#edf7ff`, label `AppTextStyle.textSubtitleS5` | Cluster **C2** (layout-structure `tester.getSize`/`tester.getRect`) + golden (`catalog_golden_test.dart`) | 1. `expect(tester.getSize(find.byKey(Key('feature-tile-0'))), const Size(167.5, 104))`.<br>2. `expect(tester.widget<Container>(iconCircleFinder).constraints, BoxConstraints.tight(Size(48,48)))`.<br>3. Golden diff `hub_w03_2tiles` = 0. | - Kích thước/token khớp oracle substitute (167.5×104, radius 8, IconCircle 48×48 `#edf7ff`).<br>- KHÔNG dùng `Color(0xFF…)`/`TextStyle(...)` literal trong production widget (M-28 — verify qua source reference trong Notes, không phải PASS evidence). | PASS (QC-manual manual-test) | BUG-W03-148 |
| TC-W03-MUI-HUB-010 | FEAT-INV-MOBILE-MENU | garage-mobile | §4.5 a11y | UI | Wave | P2 | Mỗi `FeatureTile` có `Semantics(label, button:true)` + touch target ≥ 48dp | Cluster **C1** | 1. `expect(find.bySemanticsLabel('Sản phẩm button'), findsOneWidget)` (hoặc label tương ứng theo impl).<br>2. `expect(tester.getSize(find.byType(FeatureTile).first).width, greaterThanOrEqualTo(48))` và `.height` tương tự. | - Semantics label + `button: true` cho từng tile — TalkBack đọc được "Nút Sản phẩm".<br>- Touch target 167.5×104 >> 48dp minimum. | PASS (QC-manual manual-test) | N/A |

### Nhóm A — FEAT-CAT-GRP-LIST (+ search + filter sub-page) (20 TC)

| TC ID | Feature ID | Boundary | AC Ref | Type | Suite | Priority | Title | Preconditions | Steps | Expected Result | Status | Bug ID |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-W03-MUI-A-001 | FEAT-CAT-GRP-LIST | garage-mobile | AC-1, AC-3 (CR-1782381477) | UI | Smoke | P1 | `MaterialGroupListPage` render flat card list (KHÔNG TreeView), golden default | Cluster **C2**. Oracle registry-drift (xem Oracle Coverage Plan) — fallback `UX-FLOW-INVENTORY-CATALOG.md §3.1` + PKG §2.2.4. Spec: `catalog_golden_test.dart` (`material_group_list_default`) | 1. `goldenTest('MaterialGroupListPage — flat card list default')`.<br>2. `expect(find.byType(ExpansionTile), findsNothing)` — KHÔNG có expand/collapse.<br>3. `expect(find.byType(FloatingActionButton), findsNothing)` — KHÔNG Material FAB (per FEAT-CAT-GRP-LIST AC-8 + PKG). | - Card list phẳng, mỗi nhóm 1 dòng độc lập, không thụt lề.<br>- KHÔNG FAB — chỉ footer text-button "Thêm nhóm vật tư".<br>- Golden diff = 0. | PASS (QC-manual manual-test) | BUG-W03-148 |
| TC-W03-MUI-A-002 | FEAT-CAT-GRP-LIST | garage-mobile | AC-1 | UI | Smoke | P1 | AppBar title "Nhóm vật tư hàng hóa" + search icon + filter icon | Cluster **C1** wording (AppLocalizations/i18n thật, widget pump — golden diff không bắt buộc cho text-presence) | 1. `expect(find.text('Nhóm vật tư hàng hóa'), findsOneWidget)`.<br>2. `expect(find.byIcon(Icons.search), findsOneWidget)`.<br>3. `expect(find.byIcon(Icons.filter_list), findsOneWidget)` (hoặc icon asset tương ứng). | - AppBar title đúng verbatim, không paraphrase.<br>- 2 icon action đúng vị trí AppBar right cluster. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-A-003 | FEAT-CAT-GRP-LIST | garage-mobile | AC-2, AC-3 | UI | Wave | P1 | Mỗi card hiển thị mã (blue header) + `StatusBadge` + tên + 2 InfoRow ("Thuộc nhóm" + "Mô tả") | Cluster **C1** | 1. Render card với data `#NK240516-001` / "Phụ tùng bảo dưỡng" / status ACTIVE.<br>2. `expect(find.text('#NK240516-001'), findsOneWidget)` màu `#0052ff`.<br>3. `expect(find.textContaining('Thuộc nhóm'), findsOneWidget)` + `find.textContaining('Mô tả')`. | - Card hiển thị đủ 4 thành phần theo thứ tự header→badge→tên→2 info row.<br>- `ListView.builder` KHÔNG bị triển khai lại thủ công (dùng `ListWidget`). | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-A-004 | FEAT-CAT-GRP-LIST | garage-mobile | BLoC | UI | Wave | P1 | `MaterialGroupListCubit` Initial → Loading → Success, không skip | Cluster **C1** bloc_test. Spec: `material_group_list_bloc_test.dart` | 1. `blocTest` act `cubit.load(tab: 'active')`.<br>2. `expect: () => [isA<ListLoading>(), isA<ListSuccess>()]`. | - Đúng thứ tự state, không nhảy thẳng Initial→Success. | PASS | N/A |
| TC-W03-MUI-A-005 | FEAT-CAT-GRP-LIST | garage-mobile | BLoC/error | UI | Wave | P2 | Loading → Error khi Q1 fail (network/5xx), retry re-call | Cluster **C1** | 1. `blocTest` fetcher throw Exception.<br>2. `expect: () => [isA<ListLoading>(), isA<ListFailure>()]`.<br>3. Verify message không rỗng, không chứa `'errors.'` raw key. | - `LoadError` state hiển thị message tiếng Việt cụ thể.<br>- Nút "Thử lại" (widget-level, verify tồn tại ở golden A-001) gọi lại `load()`. | PASS | N/A |
| TC-W03-MUI-A-006 | FEAT-CAT-GRP-LIST | garage-mobile | EC-1 (empty tenant khác EC-4 no-match) | UI | Wave | P2 | Empty state (`isEmpty=true`) khi API trả `[]` → `LoadEmpty` widget | Cluster **C1** bloc_test + widget | 1. `blocTest` fetcher trả `[]`.<br>2. `expect((cubit.state as ListSuccess).isEmpty, isTrue)`.<br>3. Widget: `expect(find.byType(LoadEmpty), findsOneWidget)` khi state Empty (golden hoặc widget test). | - `LoadEmpty` hiển thị (KHÔNG custom empty widget khác).<br>- Vẫn giữ AppBar + tab bar + footer "Thêm nhóm vật tư" (per manual TC-011 + EC-11 UX-FLOW). | PASS | N/A |
| TC-W03-MUI-A-007 | FEAT-CAT-GRP-LIST | garage-mobile | AC-5, BR-CAT-GRP-006 | UI | Wave | P1 | `ListTabBarWidget` 3-tab (Tất cả/Đang hoạt động/Ngừng hoạt động) — default focus + tap refilter | Cluster **C1** bloc_test | 1. `blocTest` act `load(tab: 'all')` với data mix ACTIVE+INACTIVE.<br>2. Verify `rows.length == 2` (cả 2 trạng thái) khi tab "Tất cả".<br>3. Verify default construction state trước khi user tap là `ListInitial` (chưa auto-load tab nào ngoài lần đầu app định load "active"). | - Tab "Đang hoạt động" mặc định active khi mở màn.<br>- Tap "Tất cả" → list gồm cả ACTIVE + INACTIVE.<br>- Tap "Ngừng hoạt động" → chỉ INACTIVE. | PASS | N/A |
| TC-W03-MUI-A-008 | FEAT-CAT-GRP-LIST | garage-mobile | AC-4, AC-6 | UI | Wave | P2 | Tap filter icon → push live `MaterialGroupFilterPage` (full-page, KHÔNG bottom sheet) | Cluster **C3** Patrol | 1. `$('filter icon').tap()`.<br>2. `$.waitUntilVisible($('Bộ lọc'))` hoặc `find.byType(MaterialGroupFilterPage)`.<br>3. Verify dropdown "Thuộc nhóm" + nút "Áp dụng"/"Đặt lại" hiện diện. | - Push route filter full-page thành công.<br>- Filter sheet có search box + dropdown "Nhóm cha" (chỉ ACTIVE) + 2 nút footer. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-A-009 | FEAT-CAT-GRP-LIST | garage-mobile | AC-6, BR-CAT-GRP-008 | UI | Wave | P2 | Filter dropdown "Thuộc nhóm" chỉ liệt kê nhóm ACTIVE | Cluster **C1**, dùng `filterParentDropdownOptions()` từ `material_group_form_bloc_test.dart` | 1. `filterParentDropdownOptions(allGroups: [P1 ACTIVE, P2 INACTIVE])`.<br>2. `expect(options, contains('P1'))`; `expect(options, isNot(contains('P2')))`. | - Nhóm INACTIVE bị loại khỏi dropdown filter "Nhóm cha" (đồng nhất logic với form Create/Edit). | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-A-010 | FEAT-CAT-GRP-LIST | garage-mobile | SmartRefresher | UI | Wave | P1 | Pull-down refresh: `refreshCompleted` khi thành công, `refreshFailed`+giữ data cũ khi network fail | Cluster **C3** Patrol (native drag gesture) | 1. `await $.tester.fling(find.byType(ListView), const Offset(0, 300), 1000)` (pull-down).<br>2. Verify loader top xuất hiện rồi biến mất, list update (case thành công).<br>3. Mock network 500 → pull-down → verify list giữ nguyên data cũ (không reset về error toàn màn), toast lỗi hiển thị. | - Loader top xuất hiện khi kéo, biến mất sau `refreshCompleted`.<br>- Case fail: `refreshFailed()` gọi, data cũ giữ nguyên (silent-fail + snackbar, KHÔNG reset state error). | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-A-011 | FEAT-CAT-GRP-LIST | garage-mobile | Pagination | UI | Wave | P2 | Pull-up load-more — phân trang trang tiếp theo (đối xứng F-013 Product List) | Cluster **C3** Patrol | 1. Seed > 20 nhóm ACTIVE.<br>2. `$.tester.fling(find.byType(ListView), const Offset(0, -300), 1000)` (pull-up cuối list).<br>3. Verify `onLoading` gọi Q1 trang tiếp theo, nối thêm item; hết dữ liệu → footer "Hết dữ liệu". | - Pull-up load-more hoạt động, không duplicate item, đúng thứ tự trang. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-A-012 | FEAT-CAT-GRP-LIST | garage-mobile | LoadingRowShimmer | UI | Wave | P2 | Cold-start lần đầu hiển thị `LoadingRowShimmerWidget` skeleton, KHÔNG `CircularProgressIndicator` centered | Cluster **C1** widget test, `state.isInitial == true` | 1. Pump với state `isInitial: true`.<br>2. `expect(find.byType(CircularProgressIndicator), findsNothing)`.<br>3. `expect(find.byType(LoadingRowShimmerWidget), findsWidgets)` (≥1, auto via `LoadingListWidget` 20 rows). | - Skeleton shimmer hiển thị thay vì spinner tròn giữa màn.<br>- Sau load xong → fade in card list thật. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-A-013 | FEAT-CAT-GRP-LIST | garage-mobile | EC-1 (UX-FLOW EC-11) | UI | Wave | P2 | Tenant rỗng — empty state "Không có dữ liệu" vẫn giữ search+filter+footer | Cluster **C1** | 1. Mock tenant `garage-empty`, `load()` trả `[]`.<br>2. `expect(find.text('Không có dữ liệu'), findsOneWidget)`.<br>3. `expect(find.byIcon(Icons.search), findsOneWidget)` + footer "Thêm nhóm vật tư" vẫn hiện. | - Empty state hiển thị icon + text "Không có dữ liệu" giữa màn.<br>- Search/filter/nút Thêm vẫn giữ nguyên (để tạo bản ghi đầu tiên). | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-A-014 | FEAT-CAT-GRP-LIST | garage-mobile | AC-4 | UI | Wave | P2 | Search icon → push `MaterialGroupSearchPage`; debounce 300ms LIKE-match mã/tên; empty-result "Không tìm thấy kết quả phù hợp" | Cluster **C3** Patrol (route) + **C1** (debounce logic reuse `material_group_list_bloc_test.dart` pattern) | 1. `$('search icon').tap()` → assert `MaterialGroupSearchPage` mount.<br>2. `$.enterText(find.byType(TextField), 'phu tung')` → chờ 300ms debounce → assert list filter đúng.<br>3. Nhập chuỗi không khớp → `expect(find.text('Không tìm thấy kết quả phù hợp'), findsOneWidget)`. | - Search sub-page mở đúng route.<br>- LIKE-match trên mã + tên nhóm.<br>- Empty-result wording phân biệt rõ với empty-tenant (A-013). | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-A-015 | FEAT-CAT-GRP-CREATE | garage-mobile | AC-1 | UI | Smoke | P1 | Tap footer "Thêm nhóm vật tư" → push live `AddMaterialGroupPage` | Cluster **C3** Patrol | 1. `$('Thêm nhóm vật tư').tap()`.<br>2. `$.waitUntilVisible($('Thêm nhóm vật tư hàng hóa'))` (AppBar title AddPage). | - Push route `/catalog/material-groups/add` thành công, `AddMaterialGroupPage` mount với `MaterialGroupForm(isEdit: false)`. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-A-016 | FEAT-CAT-GRP-DELETE | garage-mobile | AC-1 (kích hoạt từ List) | UI | Wave | P2 | Long-press card trong List → action menu bottom sheet "Xoá" → cùng `MaterialGroupDeleteCubit` | Cluster **C3** Patrol (native long-press gesture + bottom sheet) | 1. `await $.tester.longPress(find.byType(Card).first)`.<br>2. `$.waitUntilVisible($('Xoá'))` (bottom sheet action menu).<br>3. Tap "Xoá" → assert confirm dialog "Xác nhận" mở (cross-ref E-001). | - Long-press mở bottom sheet action menu với entry "Xoá".<br>- Tap → mở đúng dialog confirm, dùng chung cubit với Detail-footer flow. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-A-017 | FEAT-CAT-GRP-DETAIL | garage-mobile | Navigation | UI | Wave | P1 | Tap card → push live `MaterialGroupDetailPage` | Cluster **C3** Patrol | 1. `$('#NK240516-001').tap()` (hoặc tap card).<br>2. `$.waitUntilVisible(find.byType(MaterialGroupDetailPage))`. | - Push route `/catalog/material-groups/:id` thành công, Detail render đúng nhóm được tap. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-A-018 | FEAT-CAT-GRP-LIST | garage-mobile | Design tokens | UI | Wave | P2 | Card spacing/padding/badge color đúng token (`AppColors.bgBadgeSuccess`/`textSuccessPrimary` cho ACTIVE) | Cluster **C2** golden (`status_badge_variants` scenario trong `catalog_golden_test.dart`) — fallback UX-FLOW do oracle registry-drift | 1. `goldenTest('StatusBadge — Active (green) vs Inactive (grey)')`.<br>2. Assert màu badge ACTIVE = `#f0fdf1`/`#15aa2c`; INACTIVE = xám nhạt.<br>3. `tester.getRect(cardFinder).width` khớp `343` (16dp margin mỗi bên trên 375 frame). | - Badge màu đúng token theo BR-CAT-GRP-006 2 trạng thái.<br>- Card width/padding đúng 4/8/16dp grid. | PASS (QC-manual manual-test) | BUG-W03-148 |
| TC-W03-MUI-A-019 | FEAT-CAT-GRP-LIST | garage-mobile | Responsive | UI | Wave | P2 | Tablet portrait/landscape — flat card list giữ nguyên, KHÔNG split-pane (CR-1782381477 đã bỏ TreeView) | Cluster **C2** golden 2 device-size scenario | 1. `goldenTest` với `GoldenTestScenario` device Pixel Tab portrait + landscape.<br>2. Assert layout KHÔNG có 2-cột master-detail; card width co giãn theo tablet width nhưng vẫn 1-cột list. | - Layout không vỡ trên tablet, không xuất hiện split-pane pattern đã bị bỏ theo CR. | PASS (QC-manual manual-test) | BUG-W03-148 |
| TC-W03-MUI-A-020 | FEAT-CAT-GRP-LIST | garage-mobile | Regression (BUG-W03-035, BUG-W03-036) | UI | Regression | P2 | Regression: footer rounded+shadow decoration (không phải flat border-top) + search TabBar dùng `AppBarCustom.bottom` slot (không phải body Column) | Cluster **C1** widget-tree assertion (source-assertion style, KHÔNG dùng làm PASS evidence cho visual — chỉ structural presence) | 1. `expect(find.descendant(of: find.byType(GroupListFooter), matching: find.byWidgetPredicate((w) => w is Container && (w.decoration as BoxDecoration?)?.boxShadow != null)), findsOneWidget)`.<br>2. `expect(find.descendant(of: find.byType(AppBar), matching: find.byType(TabBar)), findsOneWidget)` khi ở search mode (verify TabBar nằm trong `AppBar.bottom`, không phải body). | - Footer có `boxShadow` (rounded+shadow, per BUG-W03-035 fix) — KHÔNG chỉ `border(top:)` flat.<br>- TabBar search render trong `AppBarCustom(bottom:)` slot — KHÔNG trong body `Column` (per BUG-W03-036 fix). Regression guard cho 2 bug đã `FIX_DONE`. | PASS | N/A (regression guard cho BUG-W03-030/031/035/036 — xem BUG-W03-149) |

### Nhóm B — FEAT-CAT-GRP-CREATE (15 TC)

| TC ID | Feature ID | Boundary | AC Ref | Type | Suite | Priority | Title | Preconditions | Steps | Expected Result | Status | Bug ID |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-W03-MUI-B-001 | FEAT-CAT-GRP-CREATE | garage-mobile | AC-1 | UI | Smoke | P1 | `AddMaterialGroupPage` golden — AppBar "Thêm nhóm vật tư hàng hóa" + card "Thông tin chung" 5 field + bottom bar Huỷ/Lưu | Cluster **C2**. Oracle OK: `wave03-cat-grp-create-oracle.md` node `21252:51299` | 1. `goldenTest` build screen với 5 field theo oracle sample (Mã "MN1202012", Tên "Công ty CP Thanh toán Dịch Vụ Hưng Hà", Thuộc nhóm "Vật tư hàng hoá", Trạng thái "Đang hoạt động", Mô tả placeholder "Nhập mô tả" counter "0/250" **NOTE**: oracle counter hiển thị "0/250" nhưng FEAT AC-6/BR-CAT-GRP-012 quy định giới hạn **255** — flag drift Figma-vs-FEAT, dùng FEAT 255 cho validation logic (B-010), chỉ dùng oracle cho golden visual counter placeholder).<br>2. Assert 2 button "Huỷ" (bg `#eaeaea`)/"Lưu" (bg `#0052ff`) bottom bar. | - Golden diff = 0 với oracle screenshot `21252-51299.png` (tolerance ±2px).<br>- 5 field đúng thứ tự: Mã*/Tên*/Thuộc nhóm/Trạng thái/Mô tả. | PASS (QC-manual manual-test) | BUG-W03-148 |
| TC-W03-MUI-B-002 | FEAT-CAT-GRP-CREATE | garage-mobile | AC-1 | UI | Wave | P2 | Wording verbatim — label "Mã nhóm VTHH " (trailing space) + " *" span đỏ tách riêng | Cluster **C1** wording, AppLocalizations thật | 1. `expect(find.text('Mã nhóm VTHH '), findsOneWidget)` (verbatim trailing space theo oracle Notes item 5).<br>2. Verify `*` span màu `#ed1f42` tách `TextSpan` riêng, KHÔNG paraphrase "Mã nhóm VTHH*". | - Label giữ đúng trailing-space + `*` đỏ tách span (verbatim trap từ oracle). | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-B-003 | FEAT-CAT-GRP-CREATE | garage-mobile | AC-2 (Default) | UI | Wave | P2 | Mã nhóm VTHH — default state: trống, hint, editable | Cluster **C1** | 1. Pump form mới (Create mode).<br>2. `expect(find.widgetWithText(AppTextField, 'Mã nhóm VTHH '), findsOneWidget)`.<br>3. Field editable (không disabled) — khác Edit mode (C-002). | - Field trống, editable, không disabled ở Create mode. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-B-004 | FEAT-CAT-GRP-CREATE | garage-mobile | AC-2 (Bỏ trống) | UI | Wave | P1 | Mã nhóm VTHH bỏ trống + Lưu → "Vui lòng nhập mã nhóm" | Cluster **C1**. Spec: `material_group_form_bloc_test.dart` (`validateMaCode`) | 1. `validateMaCode('', existingCodes: {})`.<br>2. `expect(r.valid, isFalse)`; `expect(r.errorMessage, 'Vui lòng nhập mã nhóm')`. | - Lỗi required hiển thị đúng message, nút "Tạo" bị chặn/disabled. | PASS | N/A |
| TC-W03-MUI-B-005 | FEAT-CAT-GRP-CREATE | garage-mobile | AC-2 (Định dạng sai), BR-CAT-GRP-002 | UI | Wave | P1 | Mã nhóm ký tự đặc biệt `grp@01` → autoUppercase `GRP@01` + inline error "Mã nhóm không được chứa ký tự đặc biệt" (regression BUG-W03-018/019) | Cluster **C1** | 1. `validateMaCode('grp@01', existingCodes: {})`.<br>2. `expect(r.valid, isFalse)`; `expect(r.errorMessage, 'Mã nhóm không được chứa ký tự đặc biệt')`.<br>3. Case hợp lệ `validateMaCode('grp01', existingCodes: {})` → `valid == true` (uppercase riêng, không special char). | - Input tự động uppercase khi gõ.<br>- Ký tự đặc biệt → error đúng message, nút "Tạo" disabled.<br>- Regression guard cho BUG-W03-018/019 (`material_group_form.dart`, đã `VERIFIED`). | PASS | N/A |
| TC-W03-MUI-B-006 | FEAT-CAT-GRP-CREATE | garage-mobile | AC-7 (Duplicate), ERR-INV-002 | UI | Wave | P1 | Mã trùng đã tồn tại → "Mã nhóm đã tồn tại", form giữ dữ liệu đã nhập | Cluster **C1** | 1. `validateMaCode('GRP-DUP', existingCodes: {'GRP-DUP'})`.<br>2. `expect(r.errorMessage, 'Mã nhóm đã tồn tại')`.<br>3. Widget-level (golden/manual cross-check): form KHÔNG đóng sau lỗi trùng — dữ liệu giữ nguyên để sửa. | - Lỗi trùng đúng message.<br>- Form không đóng, không mất dữ liệu đã nhập. | PASS | N/A |
| TC-W03-MUI-B-007 | FEAT-CAT-GRP-CREATE | garage-mobile | AC-3 (Bỏ trống) | UI | Wave | P1 | Tên nhóm VTHH bỏ trống + Lưu → "Tên nhóm là bắt buộc" | Cluster **C1** | 1. `validateTenNhom('')`.<br>2. `expect(r.errorMessage, 'Tên nhóm là bắt buộc')`. | - Lỗi required hiển thị đúng dưới field Tên. | PASS | N/A |
| TC-W03-MUI-B-008 | FEAT-CAT-GRP-CREATE | garage-mobile | AC-4, BR-CAT-GRP-008 | UI | Wave | P1 | Dropdown "Thuộc nhóm" mở bottom sheet — chỉ liệt kê nhóm ACTIVE, cho bỏ trống (root); note manual TC-016 hiếm-case parent-INACTIVE-selected là spec-ambiguous (adapted) | Cluster **C1**. Spec: `filterParentDropdownOptions()` | 1. `filterParentDropdownOptions(allGroups: [P1 ACTIVE, P2 INACTIVE])`.<br>2. `expect(options, contains('P1'))`; `expect(options, isNot(contains('P2')))`.<br>3. **Adapted note**: nếu client bỏ qua filter và user "chọn được" 1 nhóm INACTIVE (kịch bản hiếm client-bug), FEAT không định nghĩa rõ hành vi (dialog cascade hay reject) — flag `spec-gap`, không tự suy diễn expected. | - Bottom sheet dropdown chỉ hiện nhóm ACTIVE của garage hiện tại.<br>- Bỏ trống → nhóm gốc (root, cấp cao nhất). | PASS | N/A |
| TC-W03-MUI-B-009 | FEAT-CAT-GRP-CREATE | garage-mobile | AC-5 (Default + Cross-field) | UI | Wave | P2 | Trạng thái mặc định "Đang hoạt động"; chọn "Ngừng hoạt động" khả dụng (nhóm mới chưa có con nên không cascade) | Cluster **C1** | 1. Pump form mới → `expect(find.text('Đang hoạt động'), findsOneWidget)` (default dropdown value).<br>2. Chọn "Ngừng hoạt động" → giá trị field đổi, không có dialog cascade (nhóm mới không có con — EC-2 FEAT). | - Default "Đang hoạt động".<br>- Chọn "Ngừng hoạt động" không trigger cascade dialog (khác Edit — xem C-007). | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-B-010 | FEAT-CAT-GRP-CREATE | garage-mobile | AC-6 (Boundary), BR-CAT-GRP-012 | UI | Wave | P2 | Mô tả boundary — 255 ký tự OK, 256 ký tự → "Mô tả không quá 255 ký tự" (đối xứng TC-UI-016 Web) | Cluster **C1**. Spec: `validateMoTa()` | 1. `validateMoTa('A' * 255)` → `valid == true`.<br>2. `validateMoTa('A' * 256)` → `valid == false`, `errorMessage == 'Mô tả không quá 255 ký tự'`. | - Boundary chính xác: 255 pass, 256 fail.<br>- Counter/error hiển thị đúng khi vượt giới hạn (đối chiếu `ERR-INV-046` naming pattern dùng cho Product — Group dùng message riêng theo BR-CAT-GRP-012, không có mã lỗi cụ thể trong FEAT — flag nếu BA muốn thống nhất mã lỗi). | PASS | N/A |
| TC-W03-MUI-B-011 | FEAT-CAT-GRP-CREATE | garage-mobile | AC-8 | UI | Wave | P1 | Submit hợp lệ → toast "Tạo thành công" + pop về List + list refetch (Q1) | Cluster **C3** Patrol (submit feedback + route pop, flow-gated) | 1. Điền đủ Mã/Tên hợp lệ, không trùng.<br>2. `$('Tạo').tap()`.<br>3. `$.waitUntilVisible($('Tạo thành công'))` (toast/snackbar).<br>4. Assert pop về `MaterialGroupListPage`, nhóm mới xuất hiện trong list (verify qua refetch, không stale cache). | - Toast "Tạo thành công" hiển thị.<br>- Pop về List, list tự động refetch Q1 — nhóm mới đúng vị trí phân cấp. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-B-012 | FEAT-CAT-GRP-CREATE | garage-mobile | AC-9 | UI | Wave | P2 | Tap "Huỷ" → đóng form, KHÔNG lưu, quay về List (đối xứng TC-UI-019 Web — mobile trước đó chưa test nhánh này) | Cluster **C3** Patrol | 1. Nhập vài trường (mã, tên) dở dang.<br>2. `$('Huỷ').tap()`.<br>3. Assert pop về List, `createMaterialGroup` KHÔNG được gọi (mock repository verify `never()`), list không có nhóm mới. | - Form đóng, không lưu.<br>- Không có mutation nào được gọi.<br>- List giữ nguyên (không nhóm mới). | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-B-013 | FEAT-CAT-GRP-CREATE | garage-mobile | BLoC | UI | Wave | P1 | `AddMaterialGroupCubit` Idle → Submitting → Success | Cluster **C1** bloc_test. Spec: `material_group_form_bloc_test.dart` | 1. `blocTest` act `cubit.submit()` với `createFn` thành công.<br>2. `expect: () => [isA<Submitting>(), isA<SubmitSuccess>()]`. | - Đúng thứ tự state, không skip Submitting. | PASS | N/A |
| TC-W03-MUI-B-014 | FEAT-CAT-GRP-CREATE | garage-mobile | BLoC/error | UI | Wave | P2 | `AddMaterialGroupCubit` Error khi backend reject trùng mã | Cluster **C1** | 1. `blocTest` `createFn` throw.<br>2. `verify` state `SubmitError` với message "Mã nhóm đã tồn tại". | - Error state đúng message, không crash cubit. | PASS | N/A |
| TC-W03-MUI-B-015 | FEAT-CAT-GRP-CREATE | garage-mobile | Debounce | UI | Wave | P1 | Double-tap "Tạo" trong lúc đang Submitting → chỉ 1 lần gọi `createMaterialGroup` | Cluster **C1**. Spec: `material_group_form_bloc_test.dart` | 1. Gọi `cubit.submit()` 2 lần gần như đồng thời (`Future.wait`).<br>2. `expect(callCount, 1)`. | - Debounce chặn double-submit tạo request trùng — khớp common baseline `MOB-UI-BTN-005`. | PASS | N/A |

### Nhóm C — FEAT-CAT-GRP-EDIT (13 TC)

| TC ID | Feature ID | Boundary | AC Ref | Type | Suite | Priority | Title | Preconditions | Steps | Expected Result | Status | Bug ID |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-W03-MUI-C-001 | FEAT-CAT-GRP-EDIT | garage-mobile | AC-1 | UI | Smoke | P1 | `EditMaterialGroupPage` golden — cấu trúc giống Create, title "Chỉnh sửa Nhóm vật tư hàng hóa", pre-filled | Cluster **C2**. Oracle OK: `wave03-cat-grp-edit-oracle.md` node `21254:51963` | 1. `goldenTest` build với pre-fill (Mã "MN1202012" disabled, Tên/Thuộc nhóm/Trạng thái/Mô tả điền sẵn).<br>2. Assert title `find.text('Chỉnh sửa Nhóm vật tư hàng hóa')`. | - Golden diff = 0 với `21254-51963.png`.<br>- Cấu trúc identical Create, chỉ khác title + pre-fill. | PASS (QC-manual manual-test) | BUG-W03-148 |
| TC-W03-MUI-C-002 | FEAT-CAT-GRP-EDIT | garage-mobile | AC-2 | UI | Wave | P1 | Mã nhóm VTHH disabled + helper text "Không được sửa mã nhóm sau khi tạo" (đối xứng TC-UI-022 Web) | Cluster **C1** | 1. Pump Edit form.<br>2. `expect(tester.widget<AppTextField>(find.byKey(Key('field-group-code'))).enabled, isFalse)`.<br>3. `expect(find.text('Không được sửa mã nhóm sau khi tạo.'), findsOneWidget)`. | - Field Mã disabled, không nhập được.<br>- Helper text hiển thị đúng. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-C-003 | FEAT-CAT-GRP-EDIT | garage-mobile | AC-3 (Bỏ trống) | UI | Wave | P1 | Tên nhóm VTHH sửa thành rỗng + Lưu → "Tên nhóm là bắt buộc" (reuse family B-007) | Cluster **C1** | 1. `validateTenNhom('')` → error message giống B-007. | - Cùng validation branch với Create (reuse). | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-C-004 | FEAT-CAT-GRP-EDIT | garage-mobile | AC-4, BR-CAT-GRP-009 | UI | Wave | P1 | Dropdown "Thuộc nhóm" loại bỏ chính nhóm đang sửa + toàn bộ descendant (cây A→B→C, sửa A) — đối xứng TC-UI-023 Web | Cluster **C1**. Spec: `material_group_form_bloc_test.dart` (`filterParentDropdownOptions`) | 1. `filterParentDropdownOptions(allGroups: [A,B(parent A),C(parent B),OTHER], editingCode: 'GRP-A')`.<br>2. `expect(options, isNot(contains('GRP-A')))`; tương tự cho `GRP-B`/`GRP-C`; `expect(options, contains('GRP-OTHER'))`. | - Dropdown loại self + toàn bộ hậu duệ (không chỉ con trực tiếp).<br>- Vẫn cho chọn nhánh khác. | PASS | N/A |
| TC-W03-MUI-C-005 | FEAT-CAT-GRP-EDIT | garage-mobile | AC-4, BR-CAT-GRP-008 | UI | Wave | P2 | Dropdown "Thuộc nhóm" trong Edit cũng ẩn nhóm INACTIVE (đồng nhất Create) | Cluster **C1** | 1. Reuse `filterParentDropdownOptions` với 1 parent INACTIVE trong tập còn lại → bị loại. | - Cùng rule BR-CAT-GRP-008 áp dụng cho cả Create + Edit. | PASS | N/A |
| TC-W03-MUI-C-006 | FEAT-CAT-GRP-EDIT | garage-mobile | AC-4 (chặn vòng lặp) | UI | Wave | P1 | Client-side guard: chọn chính nhóm đang sửa hoặc hậu duệ → chặn lỗi vòng lặp phân cấp trước khi submit | Cluster **C1**. Spec: `isSelfOrDescendantParent()` | 1. `isSelfOrDescendantParent(editingCode: 'GRP-A', candidateParent: 'GRP-B', allGroups: tree)` → `isTrue`.<br>2. Case chọn chính nó `candidateParent: 'GRP-A'` → `isTrue`. | - Guard chặn cả 2 case (chọn chính nó / chọn hậu duệ) TRƯỚC khi gọi API — backend `ERR-INV-003` là defense-in-depth, không phải nguồn lỗi chính cho UI. | PASS | N/A |
| TC-W03-MUI-C-007 | FEAT-CAT-GRP-EDIT | garage-mobile | AC-5, BR-CAT-GRP-007 | UI | Wave | P1 | Đổi Trạng thái ACTIVE→INACTIVE cho nhóm có 2 con ACTIVE → dialog xác nhận cascade → xác nhận → submit + cascade con | Cluster **C3** Patrol (dialog interaction + submit feedback) | 1. Mở Edit `GRP-PARENT-A` (2 con ACTIVE), đổi Trạng thái → "Ngừng hoạt động".<br>2. `$('Lưu').tap()` → `$.waitUntilVisible($('Xác nhận'))` dialog cascade.<br>3. Nội dung dialog chứa "kéo theo 2 nhóm con cũng chuyển ngừng hoạt động" (hoặc tương đương theo ARB).<br>4. Tap "Xác nhận" → assert `updateMaterialGroup` called + cascade backend xử lý (verify qua state Success). | - Dialog cascade xuất hiện đúng khi đổi sang INACTIVE có con ACTIVE.<br>- Xác nhận → submit thành công, cascade 2 con. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-C-008 | FEAT-CAT-GRP-EDIT | garage-mobile | AC-5 (nhánh Huỷ cascade) | UI | Wave | P2 | Dialog cascade — tap "Huỷ" → giữ nguyên form, KHÔNG gọi `updateMaterialGroup` | Cluster **C1** (mock dialog result + verify no-mutation, logic-only) | 1. Mock dialog trả `false` (user chọn Huỷ).<br>2. `verify(() => repository.updateMaterialGroup(any())).called(0)`. | - "Huỷ" trong dialog cascade là no-op — form giữ trạng thái đang sửa (Trạng thái vẫn hiển thị INACTIVE đã chọn nhưng chưa submit), không có API call. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-C-009 | FEAT-CAT-GRP-EDIT | garage-mobile | AC-6 (Boundary) | UI | Wave | P2 | Mô tả boundary 255/256 trong Edit (reuse family B-010) | Cluster **C1** | 1. `validateMoTa('A'*255)` valid; `validateMoTa('A'*256)` error "Mô tả không quá 255 ký tự". | - Cùng rule Create áp dụng Edit. | PASS | N/A |
| TC-W03-MUI-C-010 | FEAT-CAT-GRP-EDIT | garage-mobile | AC-7 | UI | Wave | P2 | Submit hợp lệ → toast "Cập nhật thành công" + về Detail phản ánh tên mới + audit Người sửa/Ngày sửa cập nhật | Cluster **C3** Patrol | 1. Sửa Tên nhóm, `$('Lưu').tap()`.<br>2. `$.waitUntilVisible($('Cập nhật thành công'))`.<br>3. Assert pop về `MaterialGroupDetailPage`, tên mới hiển thị + "Người sửa"/"Ngày sửa" cập nhật timestamp mới. | - Toast đúng wording.<br>- Detail phản ánh dữ liệu mới + audit info cập nhật. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-C-011 | FEAT-CAT-GRP-EDIT | garage-mobile | AC-8 | UI | Wave | P2 | Tap "Huỷ bỏ" (nút chính form, KHÔNG phải dialog cascade) → đóng form, không lưu (đối xứng TC-UI-097 Web) | Cluster **C3** Patrol | 1. Sửa vài trường (tên, mô tả) — KHÔNG đổi Trạng thái (tránh trigger cascade dialog C-007/008).<br>2. `$('Huỷ bỏ').tap()`.<br>3. Assert pop về Detail/List, `updateMaterialGroup` KHÔNG được gọi, dữ liệu giữ nguyên như trước khi sửa. | - Form đóng không lưu, phân biệt rõ với nút "Huỷ" trong dialog cascade (C-008). | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-C-012 | FEAT-CAT-GRP-EDIT | garage-mobile | BLoC | UI | Wave | P1 | `EditMaterialGroupCubit` Idle → Submitting → Success | Cluster **C1** bloc_test | 1. `blocTest` act `cubit.submit()` thành công.<br>2. `expect: () => [isA<Submitting>(), isA<SubmitSuccess>()]`. | - Đúng thứ tự state. | PASS | N/A |
| TC-W03-MUI-C-013 | FEAT-CAT-GRP-EDIT | garage-mobile | BLoC/error | UI | Wave | P2 | `EditMaterialGroupCubit` Error khi network fail | Cluster **C1** | 1. `blocTest` `updateFn` throw.<br>2. `expect: () => [isA<Submitting>(), isA<SubmitError>()]`. | - Error state đúng, không crash. | PASS | N/A |

### Nhóm D — FEAT-CAT-GRP-DETAIL (9 TC)

| TC ID | Feature ID | Boundary | AC Ref | Type | Suite | Priority | Title | Preconditions | Steps | Expected Result | Status | Bug ID |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-W03-MUI-D-001 | FEAT-CAT-GRP-DETAIL | garage-mobile | AC-1, AC-2 | UI | Smoke | P1 | `MaterialGroupDetailPage` golden — header (code+timestamp+badge) + FieldsList (H2 group-name + 6 row) + footer 2-button Xoá/Sửa | Cluster **C2**. Oracle OK: `wave03-cat-grp-detail-oracle.md` node `21254:51661` | 1. `goldenTest` build screen theo oracle sample.<br>2. Assert `find.text('Xoá ')` (trailing space verbatim) + `find.text('Sửa')`.<br>3. `expect(find.byType(SafeArea), findsWidgets)` (bottom mandatory). | - Golden diff = 0 với `21254-51661.png`.<br>- 6 field: Thuộc nhóm/Mô tả/Ngày tạo/Người tạo/Ngày sửa/Người sửa. | PASS (QC-manual manual-test) | BUG-W03-148 |
| TC-W03-MUI-D-002 | FEAT-CAT-GRP-DETAIL | garage-mobile | AC-1 | UI | Wave | P2 | Wording — AppBar title "Chi tiết nhóm vật tư hàng hoá" (lưu ý "hoá" khác "hóa" ở Create/Edit — verbatim trap oracle) | Cluster **C1** | 1. `expect(find.text('Chi tiết nhóm vật tư hàng hoá'), findsOneWidget)` — verbatim đúng chính tả oracle (dấu trên "hoá"). | - Title đúng chính xác theo oracle (flag observation cho BA nếu muốn chuẩn hoá "hóa" toàn app — không tự sửa). | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-D-003 | FEAT-CAT-GRP-DETAIL | garage-mobile | AC-2, AC-3 | UI | Wave | P1 | 6 field row dùng pattern `Text.rich` 2-span label:value inline (KHÔNG Row 2-cột) | Cluster **C1** | 1. `expect(find.byType(RichText), findsWidgets)` cho mỗi row label+value.<br>2. Verify label span color `AppColors.textTertiary` (`#888c94`) khác value span color `AppColors.textPrimary`. | - Row dùng `Text.rich`/`RichText` 2-span, không phải `Row(children:[Text(label), Text(value)])` (verbatim trap oracle Notes item 4). | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-D-004 | FEAT-CAT-GRP-DETAIL | garage-mobile | BR-CAT-GRP-006 | UI | Wave | P1 | Badge Active (green `#f0fdf1`/`#15aa2c`) vs Inactive (grey) — golden 2 state | Cluster **C2**. Spec: `catalog_golden_test.dart` (`status_badge_variants`) | 1. `goldenTest` 2 scenario active/inactive.<br>2. Golden diff = 0 mỗi state. | - 2 badge variant đúng token, phân biệt rõ ràng. | PASS (QC-manual manual-test) | BUG-W03-148 |
| TC-W03-MUI-D-005 | FEAT-CAT-GRP-DETAIL | garage-mobile | BLoC | UI | Wave | P1 | `MaterialGroupDetailCubit` Initial → Loading → Loaded (Q3 `getMaterialGroup`) | Cluster **C1** bloc_test | 1. `blocTest` act fetch → `expect: () => [isA<Loading>(), isA<Loaded>()]`. | - Đúng thứ tự state. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-D-006 | FEAT-CAT-GRP-DETAIL | garage-mobile | LoadingRowShimmer | UI | Wave | P2 | Cold-start hiển thị skeleton 2-slot (Header ~100 + FieldList ~280) + `Gap(spacing8)` | Cluster **C1** widget | 1. Pump state `isInitial`.<br>2. `expect(find.byType(LoadingRowShimmerWidget), findsNWidgets(2))` (2 slot custom height). | - 2 slot skeleton đúng shape trước khi fade vào layout thật. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-D-007 | FEAT-CAT-GRP-DETAIL | garage-mobile | AC-4 | UI | Wave | P1 | Tap "Sửa" → push live `EditMaterialGroupPage` pre-filled | Cluster **C3** Patrol | 1. `$('Sửa').tap()`.<br>2. `$.waitUntilVisible($('Chỉnh sửa Nhóm vật tư hàng hóa'))`.<br>3. Verify field Mã pre-filled đúng giá trị từ Detail. | - Push route edit đúng, pre-fill từ Q3 data đang xem. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-D-008 | FEAT-CAT-GRP-DELETE | garage-mobile | AC-1 (cross-ref) | UI | Wave | P1 | Tap "Xoá " → mở popover confirm/blocked (cross-ref nhóm E) | Cluster **C3** Patrol | 1. `$('Xoá ').tap()`.<br>2. Assert popover "Xác nhận" (nhóm trống) hoặc "Không thể xóa" (nhóm có mã SP/con) mở tương ứng data seed. | - Đúng nhánh popover theo trạng thái dữ liệu (chi tiết ở nhóm E). | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-D-009 | FEAT-CAT-GRP-DETAIL | garage-mobile | a11y | UI | Wave | P2 | Footer 2-button touch target ≥ 48dp + Semantics label | Cluster **C1** | 1. `expect(tester.getSize(find.text('Sửa')).height, greaterThanOrEqualTo(48))` (button bounding box, không phải text riêng — verify qua parent `AppButton`).<br>2. Semantics label present cho cả 2 button. | - Touch target đạt chuẩn Material 48dp, Semantics đọc được. | PASS (QC-manual manual-test) | N/A |

### Nhóm E — FEAT-CAT-GRP-DELETE (9 TC)

| TC ID | Feature ID | Boundary | AC Ref | Type | Suite | Priority | Title | Preconditions | Steps | Expected Result | Status | Bug ID |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-W03-MUI-E-001 | FEAT-CAT-GRP-DELETE | garage-mobile | AC-1 | UI | Smoke | P1 | Popover "Xác nhận" golden — 343×202, 2 button Huỷ/Xác nhận | Cluster **C2**. Oracle OK: `wave03-cat-grp-delete-oracle.md` node `21254:52182` | 1. `goldenTest` build popover với groupName="Phụ tùng bảo dưỡng".<br>2. Assert `tester.getSize(popoverFinder) == Size(343, 202)`. | - Golden diff = 0 với `21254-52182-confirm-popover.png`. | PASS (QC-manual manual-test) | BUG-W03-148 |
| TC-W03-MUI-E-002 | FEAT-CAT-GRP-DELETE | garage-mobile | AC-1 | UI | Wave | P1 | Wording verbatim — "Bạn có chắc chắn muốn xóa nhóm vật tư hàng hóa [tên nhóm] không?" với `{groupName}` binding runtime | Cluster **C1** | 1. `expect(find.text('Bạn có chắc chắn muốn xóa nhóm vật tư hàng hóa Phụ tùng bảo dưỡng không?'), findsOneWidget)` khi groupName="Phụ tùng bảo dưỡng".<br>2. Case tên rất dài → verify wrap không tràn popover (test edge-case binding). | - Text bind đúng `{groupName}` runtime, không hardcode.<br>- Tên dài wrap đúng, không overflow. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-E-003 | FEAT-CAT-GRP-DELETE | garage-mobile | AC-4 | UI | Smoke | P1 | Popover "Không thể xóa" golden — 343×222, 1 button "Đóng" full-width | Cluster **C2**. Oracle OK: node `21254:52571` | 1. `goldenTest` build popover blocked.<br>2. `tester.getSize(popoverFinder) == Size(343, 222)`. | - Golden diff = 0 với `21254-52571-cannot-delete-popover.png`. | PASS (QC-manual manual-test) | BUG-W03-148 |
| TC-W03-MUI-E-004 | FEAT-CAT-GRP-DELETE | garage-mobile | AC-4 | UI | Wave | P1 | Wording verbatim — "Nhóm vật tư hàng hóa [tên nhóm] đã phát sinh mã sản phẩm nội bộ nên không được xóa." | Cluster **C1** | 1. `expect(find.text('Nhóm vật tư hàng hóa Phụ tùng bảo dưỡng đã phát sinh mã sản phẩm nội bộ nên không được xóa.'), findsOneWidget)`. | - Text bind đúng `{groupName}`, verbatim message. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-E-005 | FEAT-CAT-GRP-DELETE | garage-mobile | AC-2 (happy path) | UI | Wave | P1 | `MaterialGroupDeleteCubit` Idle→ConfirmPending→Deleting→Success (nhóm trống, không con, chưa gắn SP) | Cluster **C1** bloc_test. Spec: `material_group_delete_bloc_test.dart` | 1. `blocTest` `hasProduct:false, hasChildren:false` act `requestDelete()`+`confirm()`.<br>2. `expect: () => [isA<DeleteConfirmPending>(), isA<Deleting>(), isA<DeleteSuccess>()]`. | - Đúng thứ tự state, xóa thành công. | PASS | N/A |
| TC-W03-MUI-E-006 | FEAT-CAT-GRP-DELETE | garage-mobile | AC-4, ERR-INV-004 | UI | Wave | P1 | Blocked — nhóm đã phát sinh mã sản phẩm | Cluster **C1** bloc_test | 1. `blocTest` `hasProduct:true`.<br>2. `expect: () => [ConfirmPending, Deleting, DeleteBlocked]`; `reason == hasProduct`; message chứa "đã phát sinh mã sản phẩm nội bộ". | - Cubit transition confirmPending→error đúng lý do. | PASS | N/A |
| TC-W03-MUI-E-007 | FEAT-CAT-GRP-DELETE | garage-mobile | AC-5, ERR-INV-005 | UI | Wave | P1 | Blocked — nhóm cha còn nhóm con | Cluster **C1** bloc_test | 1. `blocTest` `hasChildren:true`.<br>2. `reason == hasChildren`; message "Phải xóa hết nhóm con trước". | - Cubit transition đúng lý do "còn con". | PASS | N/A |
| TC-W03-MUI-E-008 | FEAT-CAT-GRP-DELETE | garage-mobile | AC-3 | UI | Wave | P1 | Tap "Huỷ" trong dialog Xác nhận → đóng, KHÔNG gọi `deleteMaterialGroup` (manual TC-043 — trước đây chỉ test nhánh Xoá thành công) | Cluster **C3** Patrol (dialog dismissal, flow-gated) + logic support ở `material_group_delete_bloc_test.dart` (`cancel()`) | 1. `$('Xoá ').tap()` → dialog "Xác nhận" mở.<br>2. `$('Huỷ').tap()`.<br>3. `$.waitUntilNotVisible($('Xác nhận'))` — dialog đóng.<br>4. Verify `deleteMaterialGroup` KHÔNG được gọi (mock repo `verifyNever`), nhóm vẫn còn trong List/Detail. | - Dialog đóng ngay, không xóa.<br>- Cubit về `DeleteIdle`. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-E-009 | FEAT-CAT-GRP-DELETE | garage-mobile | AC-1 (kích hoạt từ List) | UI | Wave | P2 | Long-press card List → action menu → dialog xác nhận → submit xóa → toast + reload List (manual TC-027) | Cluster **C3** Patrol (native long-press + full flow) | 1. `$.tester.longPress(cardFinder)` → bottom sheet "Xoá".<br>2. Tap "Xoá" → dialog "Xác nhận" → tap "Xác nhận".<br>3. `$.waitUntilVisible($('Xóa thành công'))` (hoặc toast tương đương) → List reload, nhóm biến mất. | - Cùng cubit dùng chung cho cả 2 entry point (Detail-footer + List-long-press). | PASS (QC-manual manual-test) | N/A |

### Nhóm F — FEAT-CAT-PROD-LIST (view-only) (+ search + filter) (14 TC)

| TC ID | Feature ID | Boundary | AC Ref | Type | Suite | Priority | Title | Preconditions | Steps | Expected Result | Status | Bug ID |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-W03-MUI-F-001 | FEAT-CAT-PROD-LIST | garage-mobile | AC-1, AC-2 | UI | Smoke | P1 | `InternalProductListPage` golden — AppBar "Sản phẩm" + 3-tab + product card | Cluster **C2**. Oracle PARTIAL: `wave03-cat-prod-list-oracle.md` (visual-only, `MCP_OUTPUT_TOO_LARGE`, tolerance ±2px với `_full.png` crop) | 1. `goldenTest` layout theo oracle Screen 1 (Tab "Tất cả").<br>2. Assert card hiển thị `#IP-BP-0001` + tên + ĐVT/Thương hiệu + badge. | - Golden diff ≤ tolerance với `_full.png` crop cột 1.<br>- **Wording drift note**: oracle screenshot-verified AppBar = "Sản phẩm"; FEAT-CAT-PROD-LIST §3 gọi màn là "Danh sách Mã sản phẩm nội bộ" — flag conflict cho BA, dùng oracle (screenshot-verified) cho golden/AppBar text thật. | PASS (QC-manual manual-test) | BUG-W03-148 |
| TC-W03-MUI-F-002 | FEAT-CAT-PROD-LIST | garage-mobile | AC-11 (view-only), P1 CRITICAL | UI | Smoke | P1 | KHÔNG có nút "Thêm sản phẩm"/"Tải lên"/"Xuất file" ở AppBar/footer trên mobile | Cluster **C1** element-presence (findsNothing) | 1. `expect(find.text('Thêm sản phẩm'), findsNothing)`.<br>2. `expect(find.text('Tải lên'), findsNothing)`.<br>3. `expect(find.text('Xuất file'), findsNothing)`.<br>4. `expect(find.byType(GroupListFooter).evaluate().isEmpty, isTrue)` (hoặc footer không chứa text-button thêm sản phẩm nếu widget vẫn render) — footer không có text-button thêm sản phẩm. | - Mobile hoàn toàn không hiển thị 3 action Create/Import/Export (divergence có chủ đích vs Web AC-9 — CR-1782373204). | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-F-003 | FEAT-CAT-PROD-LIST | garage-mobile | AC-2 | UI | Wave | P1 | Card hiển thị mã+tên+ĐVT+nhóm+StatusBadge, dùng `ListWidget` pattern (giống Group List) | Cluster **C1** | 1. Render card sample data.<br>2. Assert text mã/tên/ĐVT/nhóm hiện diện + `StatusBadge`. | - Card layout nhất quán với Group List (cùng canonical `ListWidget`). | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-F-004 | FEAT-CAT-PROD-LIST | garage-mobile | BLoC | UI | Wave | P1 | `InternalProductListCubit` Initial→Loading→Success (Q4) | Cluster **C1** bloc_test. Spec: `internal_product_bloc_test.dart` | 1. `blocTest` act `load()` → `expect: () => [Loading, Success]`. | - Đúng thứ tự state. | PASS | N/A |
| TC-W03-MUI-F-005 | FEAT-CAT-PROD-LIST | garage-mobile | EC-1 (UX-FLOW EC-11) | UI | Wave | P2 | Empty tenant → `LoadEmpty` "Không có dữ liệu", giữ search+3 filter | Cluster **C1** bloc_test + widget | 1. `blocTest` fetcher `[]` → `isEmpty == true`.<br>2. Widget: `LoadEmpty` hiện, search/filter vẫn còn. | - Đồng nhất pattern với Group List (A-013), khác wording tenant-rỗng vs no-match. | PASS | N/A |
| TC-W03-MUI-F-006 | FEAT-CAT-PROD-LIST | garage-mobile | AC-3 | UI | Wave | P1 | Search LIKE 3-cột (mã nội bộ/tên/SKU liên kết) | Cluster **C1** bloc_test. Spec: `internal_product_bloc_test.dart` | 1. `blocTest` query "SKU-001" → trả đúng 1 product mapped.<br>2. Query không khớp → empty result (EC-2 no-match, khác EC-1 empty-tenant). | - LIKE-match đúng 3 cột, phân biệt rõ 2 loại empty. | PASS | N/A |
| TC-W03-MUI-F-007 | FEAT-CAT-PROD-LIST | garage-mobile | EC-4 (UX-FLOW EC-12) | UI | Wave | P2 | Search không khớp → "Không tìm thấy kết quả phù hợp" / "Vui lòng thử lại" (oracle Screen 5) | Cluster **C1** wording | 1. `expect(find.text('Không tìm thấy kết quả phù hợp'), findsOneWidget)`.<br>2. `expect(find.text('Vui lòng thử lại'), findsOneWidget)`. | - Đúng 2 dòng text theo oracle Text Content. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-F-008 | FEAT-CAT-PROD-LIST | garage-mobile | AC-11 | UI | Wave | P1 | Tap card (view-only) → push live `InternalProductDetailPage` | Cluster **C3** Patrol | 1. `$('#IP-BP-0001').tap()` hoặc tap card.<br>2. `$.waitUntilVisible(find.byType(InternalProductDetailPage))`. | - Push route đúng, KHÔNG có icon Sửa/Xóa trigger nào ở List cho Product (khác Group). | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-F-009 | FEAT-CAT-PROD-LIST | garage-mobile | AC-11 (P1 divergence guard) | UI | Wave | P1 | Row Active KHÔNG hiển thị icon Sửa/Xóa trên mobile (khác desktop AC-8 hiển thị icon cho hàng Active) | Cluster **C1** element-presence | 1. Render card status ACTIVE.<br>2. `expect(find.byIcon(Icons.edit), findsNothing)`; `expect(find.byIcon(Icons.delete), findsNothing)` trong scope card widget. | - Mobile row KHÔNG bao giờ hiển thị Sửa/Xóa (view-only toàn bộ, không phân biệt theo trạng thái như Web). | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-F-010 | FEAT-CAT-PROD-LIST | garage-mobile | AC-6 | UI | Wave | P2 | Filter icon → push live `InternalProductFilterPage` (Tính chất + Nhóm hàng dropdown) | Cluster **C3** Patrol | 1. `$('filter icon').tap()`.<br>2. `$.waitUntilVisible($('Bộ lọc'))` (hoặc title tương đương).<br>3. Assert 2 dropdown "Tính chất"/"Nhóm hàng" hiện diện. | - Push route filter, 2 dropdown đúng theo oracle Screen 7. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-F-011 | FEAT-CAT-PROD-LIST | garage-mobile | AC-6 | UI | Wave | P2 | Filter "Nhóm hàng" dropdown chỉ nhóm ACTIVE | Cluster **C1**, reuse `filterParentDropdownOptions()` pattern | 1. `filterParentDropdownOptions(allGroups: [P1 ACTIVE, P2 INACTIVE])` → chỉ P1. | - Đồng nhất rule ẩn INACTIVE trong dropdown filter Product cũng như Group. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-F-012 | FEAT-CAT-PROD-LIST | garage-mobile | SmartRefresher | UI | Wave | P2 | Pull-down refresh danh sách sản phẩm | Cluster **C3** Patrol (native gesture) | 1. Pull-down từ top list.<br>2. Verify loader + refetch Q4 + `refreshCompleted`. | - Đồng nhất pattern refresh Group List. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-F-013 | FEAT-CAT-PROD-LIST | garage-mobile | AC-7 | UI | Wave | P2 | Pull-up load-more phân trang (manual TC-041) | Cluster **C3** Patrol | 1. Seed > 20 mã.<br>2. Pull-up cuối list → `onLoading` gọi Q4 trang tiếp theo, nối thêm; hết dữ liệu → "Hết dữ liệu". | - Pagination đúng, không duplicate. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-F-014 | FEAT-CAT-PROD-LIST | garage-mobile | Regression (BUG-W03-030, BUG-W03-031, BUG-W03-036) | UI | Regression | P2 | Regression: search/filter page widget-catalog fidelity fix + AppBarCustom bottom-slot TabBar | Cluster **C1** widget-tree assertion (structural, không dùng làm PASS-evidence cho visual độc lập) | 1. Verify `InternalProductSearchPage`/`InternalProductFilterPage` dùng đúng canonical widget (`AppTextField`/`AppDropdown`-equivalent) thay vì widget tự chế (regression BUG-W03-030/031).<br>2. Verify search TabBar nằm trong `AppBarCustom(bottom:)` (regression BUG-W03-036, đồng bộ A-020). | - Không tái phát 3 bug đã `FIX_DONE` (BUG-W03-030/031/036). | PASS | N/A (regression guard cho BUG-W03-030/031/035/036 — xem BUG-W03-149) |

### Nhóm G — FEAT-CAT-PROD-DETAIL (view-only) (9 TC)

| TC ID | Feature ID | Boundary | AC Ref | Type | Suite | Priority | Title | Preconditions | Steps | Expected Result | Status | Bug ID |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-W03-MUI-G-001 | FEAT-CAT-PROD-DETAIL | garage-mobile | AC-1, AC-12 | UI | Smoke | P1 | `InternalProductDetailPage` golden — 4 card scroll (Header/GeneralInfo/TechnicalSpec/SpecDescription), KHÔNG Tabs | Cluster **C2**. Oracle OK: `wave03-cat-prod-detail-oracle.md` node `21526:45088`/`21528:24629` | 1. `goldenTest` build 4 card theo oracle sample (`#IP-BP-0001`, "Lọc dầu động cơ Toyota").<br>2. `expect(find.byType(TabBar), findsNothing)` — KHÔNG có Tabs widget (per FEAT v8 refactor).<br>3. `expect(find.byType(BottomAppBar), findsNothing)` — KHÔNG BottomBar. | - Golden diff = 0 với `21526-45088.png` (state ACTIVE).<br>- 4 card đúng thứ tự, KHÔNG Tabs/BottomBar. | PASS (QC-manual manual-test) | BUG-W03-148 |
| TC-W03-MUI-G-002 | FEAT-CAT-PROD-DETAIL | garage-mobile | AC-12 (P1 CRITICAL view-only) | UI | Smoke | P1 | KHÔNG nút "Chỉnh sửa"/"Gắn SKU"/"Thêm ĐVT quy đổi" trên mobile (khác Web AC-10 có đủ 3 nút) | Cluster **C1** element-presence | 1. `expect(find.text('Chỉnh sửa'), findsNothing)`.<br>2. `expect(find.text('Gắn SKU'), findsNothing)`.<br>3. `expect(find.text('Thêm ĐVT quy đổi'), findsNothing)`. | - Mobile hoàn toàn view-only — không có action nào từ Detail (divergence CR-1782373204). | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-G-003 | FEAT-CAT-PROD-DETAIL | garage-mobile | AC-2 | UI | Wave | P1 | Enrichment field hiển thị display-value (không phải code) — ĐVT/Xuất xứ(R18)/Nhóm/Thương hiệu | Cluster **C1** bloc_test. Spec: `internal_product_bloc_test.dart` | 1. `blocTest` fetcher trả `mainUnitDisplayName:'Cái'`, `originDisplayName:'Việt Nam'`.<br>2. `expect(s.mainUnitDisplayName, isNot(matches(RegExp(r'^UOM-'))))`.<br>3. `expect(s.originDisplayName, 'Việt Nam')` (không phải mã country code). | - Toàn bộ enrichment field hiển thị tên, không lộ mã nội bộ/UOM code/country code. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-G-004 | FEAT-CAT-PROD-DETAIL | garage-mobile | Cross-domain | UI | Wave | P2 | StatusBadge INACTIVE = GREY (khác Group — Group dùng orange/grey theo D-004) | Cluster **C2** golden 2-state, adapter `InternalProductStatus implements InventoryStatusInterface` | 1. `goldenTest` 2 scenario Active(green)/Inactive(grey).<br>2. Golden diff = 0 mỗi state với `21526-45088.png`/`21528-24629.png`. | - Badge Inactive Product = xám (`#f3f3f4`/`#888c94`), verify tách biệt màu badge Group Inactive (nếu Group dùng cam theo manual note — cross-check D-004 vs G-004 để phát hiện màu có nhất quán hay không, ghi observation nếu khác). | PASS (QC-manual manual-test) | BUG-W03-148 |
| TC-W03-MUI-G-005 | FEAT-CAT-PROD-DETAIL | garage-mobile | BLoC | UI | Wave | P1 | `ProductDetailCubit` Initial→Loading→Success (Q5 `getInternalProduct`) | Cluster **C1** bloc_test | 1. `blocTest` act fetch → `expect: () => [Loading, Success]`. | - Đúng thứ tự state. | PASS | N/A |
| TC-W03-MUI-G-006 | FEAT-CAT-PROD-DETAIL | garage-mobile | LoadingRowShimmer | UI | Wave | P2 | Cold-start hiển thị 4-card skeleton (heights 120/180/100/150) + `Gap(spacing8)` | Cluster **C1** widget | 1. Pump state `isInitial`.<br>2. `expect(find.byType(LoadingRowShimmerWidget), findsNWidgets(4))`. | - 4 slot skeleton đúng shape trước khi fade vào 4 card thật. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-G-007 | FEAT-CAT-PROD-DETAIL | garage-mobile | SmartRefresher | UI | Wave | P2 | Pull-down refresh detail | Cluster **C3** Patrol | 1. Pull-down từ top scroll.<br>2. Verify refetch Q5 + `refreshCompleted`. | - Đồng nhất pattern refresh Group Detail. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-G-008 | FEAT-CAT-PROD-DETAIL | garage-mobile | Layout | UI | Wave | P2 | "Thông số kĩ thuật" 1-dòng full-width (không 2-cột); "Quy cách mô tả" 2 field full-width row (không split cột) | Cluster **C2** layout-structure | 1. `tester.getSize(find.byKey(Key('card-technical-spec')))` width = full (311dp inner), không chia 2 cột.<br>2. `tester.getSize(find.byKey(Key('card-spec-description')))` — 2 AttributesField full-width stacked, không `Row` 2-cột. | - Đúng pattern 1-col cho Thông số kĩ thuật, khác 2-col AttributesField ở Header/Thông tin chung. | PASS (QC-manual manual-test) | BUG-W03-148 |
| TC-W03-MUI-G-009 | FEAT-CAT-PROD-DETAIL | garage-mobile | a11y | UI | Wave | P2 | Semantics label cho nội dung card (read-only, không có touch target action nào ngoài back) | Cluster **C1** | 1. `expect(find.bySemanticsLabel('Chi tiết sản phẩm'), findsOneWidget)` hoặc tương đương theo AppBar.<br>2. Verify AppBar back button touch target ≥ 48dp. | - Semantics đầy đủ cho content card + back button. | PASS (QC-manual manual-test) | N/A |

### Nhóm H — Cross-cutting (Responsive, A11y, Regression, Deep-flow) (9 TC)

| TC ID | Feature ID | Boundary | AC Ref | Type | Suite | Priority | Title | Preconditions | Steps | Expected Result | Status | Bug ID |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-W03-MUI-H-001 | FEAT-CAT-* | garage-mobile | Canonical reuse (manual TC-037, adapted) | UI | Regression | P2 | Canonical reuse audit qua widget-tree (KHÔNG grep — tránh `MOBILE_UI_STATIC_PASS`) | Cluster **C1** widget-presence assertion trên 7 screen chính | 1. `expect(find.byType(FloatingActionButton), findsNothing)` trên `MaterialGroupListPage`/`InternalProductListPage`.<br>2. `expect(find.byType(RefreshIndicator), findsNothing)` (dùng `SmartRefresher` thay).<br>3. `expect(find.byType(CircularProgressIndicator), findsNothing)` khi state loading (dùng `LoadingRowShimmerWidget`).<br>4. `expect(find.byType(TabBar), findsOneWidget)` chỉ từ `ListTabBarWidget` wrapper, không custom TabBar tự chế khác. | - 0 instance FAB/RefreshIndicator/centered-spinner trên widget tree runtime — reframe từ manual TC-037 (static grep) sang runtime widget-presence để tuân evidence rule. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-H-002 | FEAT-CAT-* | garage-mobile | Co-located regression | UI | Regression | P2 | Sibling-domain reuse (`StatusBadge`/`StartInfoRow` từ `lib/ui/inventory/widgets/`) không regress consumer hiện có | Cluster **C1**, xem §Co-located Feature Regression Inventory | 1. Verify `StatusBadge` render đúng cho `InventoryStatusInterface` case KHÁC (không phải MaterialGroup/InternalProduct — vd Receipt/Delivery status nếu có test fixture sẵn) vẫn PASS sau khi thêm 2 adapter mới.<br>2. Verify `StartInfoRow` không đổi API signature (backward-compatible) khi dùng trong `MaterialGroupDetailPage`. | - Thêm 2 adapter mới KHÔNG phá vỡ consumer sibling-domain hiện có (additive-only). | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-H-003 | FEAT-CAT-GRP-* | garage-mobile | Permission (Decision Table role × action) | UI | Wave | P2 | Cả 2 role (garage-owner/accountant) full CRUD parity trên 5 màn Group (bao gồm delete — manual TC-028) | Cluster **C1** decision-table (role × {list,create,edit,delete,detail}) | 1. Với mỗi action trong {List xem, Create, Edit, Delete, Detail xem}, render/act dưới role `garage-owner` rồi `accountant` → so sánh kết quả (permission-gate pass, hành động thực hiện được) giống hệt nhau. | - Không có action nào bị chặn riêng theo role — BR-CAT-CMN-003 "2 vai trò quyền ngang nhau" giữ nguyên trên cả 5 màn Group. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-H-004 | FEAT-CAT-* | garage-mobile | Responsive | UI | Wave | P2 | Tablet portrait/landscape cho GRP-LIST + PROD-DETAIL, không overflow, không split-pane | Cluster **C2** golden 2 device-class × 2 orientation | 1. `goldenTest` với `GoldenTestScenario` device Pixel Tab portrait + landscape cho 2 screen đại diện.<br>2. Assert không `RenderFlex overflowed` exception, layout co giãn hợp lý. | - 4 golden scenario (2 screen × 2 orientation) PASS không overflow. | PASS (QC-manual manual-test) | BUG-W03-148 |
| TC-W03-MUI-H-005 | FEAT-CAT-* | garage-mobile | iOS-specific | UI | Wave | P2 | iOS back-swipe gesture pop đúng route (Hub→List→Detail) | Cluster **C3** Patrol iOS simulator | 1. Nav Hub→ProductList→ProductDetail.<br>2. `$.native.swipeFromLeftEdge()` tại Detail → pop về List.<br>3. Lặp lại tại List → pop về Hub. | - Swipe-back hoạt động đúng cả 2 cấp, không crash. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-H-006 | FEAT-CAT-* | garage-mobile | a11y sweep | UI | Wave | P2 | Semantics label convention nhất quán 8 screen (`row-group-{code}`, `tab-status-{x}`, `field-group-{name\|code\|description}`, `button-create-group`, `row-product-{code}`, `detail-row-{key}`) | Cluster **C1** | 1. Với mỗi convention key trong PKG §2.2.4 "Semantics labels", assert `find.bySemanticsLabel(RegExp(pattern))` findsWidgets trên screen tương ứng. | - 6 convention Semantics đều present đúng vị trí. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-H-007 | FEAT-CAT-GRP-CREATE | garage-mobile | Offline | UI | Wave | P3 | Submit Group form khi offline → banner cảnh báo, KHÔNG silent-fail | Cluster **C1** bloc_test (simulate connectivity=false) | 1. Mock connectivity check `false` trước khi `submit()`.<br>2. Verify state không chuyển `Submitting` mà emit banner/error "Không có kết nối mạng" (hoặc tương đương) trước khi gọi network. | - Offline banner hiển thị đúng lúc, không silent treo loading vô thời hạn. | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-H-008 | FEAT-CAT-GRP-* | garage-mobile | Deep UI flow (Coverage Depth Gate §2) | UI | E2E-ish (mobile-ui scope, không native) | P1 | Deep flow: Hub→List(search)→tap row→Detail→Sửa→Edit(đổi Trạng thái cascade)→Lưu→Detail phản ánh→Xóa (blocked rồi allowed sau khi gỡ ràng buộc) | Cluster **C3** Patrol, multi-screen chained (không phải render tĩnh từng màn cô lập) | 1. Hub → tap "Nhóm vật tư" → List.<br>2. Search mã `GRP-FLOW-01` → tap kết quả → Detail.<br>3. Tap "Sửa" → Edit, đổi Trạng thái → "Ngừng hoạt động" (không có con — không cascade dialog) → Lưu → toast "Cập nhật thành công" → về Detail phản ánh Badge "Ngừng hoạt động".<br>4. Từ Detail, tap "Xoá" — case seed có mã SP gắn → popover "Không thể xóa" → Đóng.<br>5. (Biến thể fixture) nhóm khác không ràng buộc → Xoá → "Xác nhận" → thành công → về List, item biến mất. | - Toàn bộ chuỗi 5 bước hoàn thành không crash, mỗi state transition (List→Detail→Edit→Detail→Delete popover) đúng như từng TC riêng lẻ đã assert — xác nhận tính nhất quán liên-màn (không chỉ render tĩnh). | PASS (QC-manual manual-test) | N/A |
| TC-W03-MUI-H-009 | FEAT-CAT-PROD-*, FEAT-INV-MOBILE-MENU | garage-mobile | Deep UI flow (view-only) | UI | Wave | P2 | Deep flow: Hub→tap "Sản phẩm"→List(search)→Detail→pull-refresh→back→List giữ scroll/tab state→back→Hub | Cluster **C3** Patrol, multi-screen chained | 1. Hub → tap "Sản phẩm" → List.<br>2. Search mã `IP-FLOW-01` → tap kết quả → Detail.<br>3. Pull-down refresh trên Detail → data reload.<br>4. Back → List — verify tab/scroll state preserve (không reset về đầu danh sách nếu AC yêu cầu — nếu implementation reset, ghi observation không phải bug vì AC không yêu cầu tường minh).<br>5. Back → Hub. | - Chuỗi flow hoàn thành, mỗi bước liên-màn hoạt động đúng; back-stack preserve tại mọi cấp. | PASS (QC-manual manual-test) | N/A |

---

## 5. Changelog

| Date     | Change                                              | Author     |
| -------- | --------------------------------------------------- | ---------- |
| 2026-07-03 | **TEST_EXECUTION Run 1**: Environment Readiness Gate PASS (Flutter 3.44.1/Dart 3.12.1, `flutter-widget` harness smoke OK). Executed 39 individual assertions across 5 pre-existing C1 bloc/widget spec files (`hub_bloc_test.dart` 9, `internal_product_bloc_test.dart` 5, `material_group_delete_bloc_test.dart` 4, `material_group_form_bloc_test.dart` 16, `material_group_list_bloc_test.dart` 5) — 0 failures, mapping to 30 TC IDs `PASS`. Authored + executed **new** spec `regression_a020_f014_test.dart` (4 assertions PASS) for `TC-W03-MUI-A-020`/`F-014` (regression guards for `BUG-W03-030/031/035/036`), reconstructing fixed widget-tree fragments per structural-presence evidence bar already declared for these TCs — 32 TC `PASS` total. **C2 (alchemist golden) cluster discovered 100% non-functional**: `catalog_golden_test.dart` (3 goldenTest, 6 variants) crashes with `RenderCustomMultiChildLayoutBox given an infinite size` on ANY `MaterialApp(home: Scaffold(...))` tree under installed Flutter `3.44.1` + `alchemist ^0.14.0` (confirmed via minimal 1-widget repro) — filed **BUG-W03-148** (P1, harness Lớp A frozen, needs CR to bump alchemist/pin Flutter SDK). All 16 C2 TC → `BLOCKED-by-harness`. **C3 (Patrol) cluster**: no Patrol spec files exist for W03 mobile-ui scope (planning gap) — all 26 C3 TC → `BLOCKED-by-harness` (spec-gap; 3 emulators available `Pixel6_API33_arm64`/`My_Android_33`/`apple_ios_simulator` but unused since no target). **Remaining 34 C1 TC**: assigned cluster C1 during TEST_PLANNING but no test-block was ever authored (harness itself runnable, confirmed via smoke) → `BLOCKED-by-harness` (spec-gap, NOT fabricated PASS). **Bug Re-Verification Loop**: attempted re-verify `BUG-W03-030/031/035/036` (FIX_DONE) using real `mobile/gf-garage-app` repo test suite (`flutter pub get` + easy_localization/build_runner codegen bootstrap, first time this repo's tests ran with a real toolchain per DEV's own "DEFERRED — no toolchain" notes) — discovered **BUG-W03-149** (P2, `test/support/test_localization.dart#wrapLocalized` nests any pumped `Scaffold` inside its own `Scaffold(body: SingleChildScrollView(...))`, causing the same infinite-size crash for every `*_fidelity_test.dart` that pumps a bare `Scaffold`). Non-widget source-string assertions in the same dev test files PASSED (confirming fix presence in source) but are NOT used as PASS/VERIFIED evidence per `MOBILE_UI_STATIC_PASS`/`MOBILE_UI_SOURCE_EVIDENCE` guards. Per Bug Re-Verification Loop protocol ("cluster chưa runnable → TC verdict BLOCKED, bug status tạm REOPENED"), flipped `BUG-W03-030/031/035/036` `FIX_DONE → REOPENED` procedurally (explicitly NOT a claim the original defects resurfaced — see each bug's L2 verify file). Total: 108 TC (32 PASS / 76 BLOCKED-by-harness / 0 FAIL). Overall execution-slice conclusion = `BLOCKED` (visual-gated + flow-gated clusters unexecuted, per protocol — see `TR-W03-MOBILE-UI.md`). 2 bugs filed (`BUG-W03-148`, `BUG-W03-149`), 4 bugs re-verify-attempted (`BUG-W03-030/031/035/036`, all → `REOPENED` procedural). | agent-test-mobile-ui |
| 2026-07-02 | Full-gen TEST_PLANNING cho W03 mobile-ui (8 screen: Hub `FEAT-INV-MOBILE-MENU` + 5 `FEAT-CAT-GRP-*` full CRUD + 2 `FEAT-CAT-PROD-{LIST,DETAIL}` view-only). 108 TC (HUB×10, A×20, B×15, C×13, D×9, E×9, F×14, G×9, H×9). Cluster: 66 C1 (widget/bloc logic, `mocktail` inline pure-Dart stub theo TL-W01-MUI-003), 16 C2 (alchemist golden), 26 C3 (Patrol route-transition/native-gesture). Oracle Coverage Plan 8/8 screen (2 fallback: Hub QA-oracle-gap dùng substitute figma-mobile DEV transform; GRP-LIST oracle registry-drift dùng UX-FLOW fallback — cả 2 KHÔNG "missing", có lý do chính xác). Common Baseline Coverage Map §1-21 đối chiếu đầy đủ (covered/adapted/out-of-scope+lý do, không mục nào bỏ trống). Auto vs Manual Parity: 52/52 manual TC map được (0 auto-miss — không cần lesson-learn entry mới). Co-located Regression Inventory: feature mới hoàn toàn, rủi ro nằm ở shared widget/token reuse (StatusBadge/StartInfoRow/CustomScaffold) — H-001/H-002 cover; entry-point Hub (NC-INV-MENU-01) escalate BA, không tự suy diễn. 2 Deep UI flow TC (H-008/H-009) theo Coverage Depth Gate. Spec files: `hub_bloc_test.dart`, `material_group_list_bloc_test.dart`, `material_group_form_bloc_test.dart`, `material_group_delete_bloc_test.dart`, `internal_product_bloc_test.dart`, `catalog_golden_test.dart` tại `Execution/auto/specs/W03/mobile-ui/`. Harness `flutter-widget`/`alchemist` (Lớp A frozen, CR-20260701-03) reuse nguyên trạng, không sửa. Self-Audit: Technique Compliance + Edge-Case Coverage + Field-Validation Coverage (CRITICAL/HIGH 100%) — không mandatory failure mở. | agent-test-mobile-ui |
