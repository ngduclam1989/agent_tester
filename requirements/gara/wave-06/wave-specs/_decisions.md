# W06 — Execution Spec Authoring Decision Log

| Date | Wave | Mode | Artifact | Decision | Rationale | References |
|---|---|---|---|---|---|---|
| 2026-07-31 | W06 | contract-sign | 8 consumer×contract pairs (6 unique contracts, 5 consumers) | **AUTHOR + SIGN — `Execution/wave-specs/W06/contract-scope.yaml` chuyển từ skeleton template sang 6 entry thật** (per CR-20260731-01 GAP-W06-AGG-02 resolution, phát hiện tại `/warm-up agg-garage-graph --phase A`). 6 contract xác định qua PKG-W06 v7 §2.2 + §3 Entry Criteria ratified-list: (1) `gf-accounting-api.md` §5 PRC → consumer `bff-experiences/agg-garage-graph`; (2) `gf-inventory-api.md` §3f S2S + §3g Stock V2 Reports → consumer `bff-experiences/agg-garage-graph` + `boundaries/gf-accounting`; (3) `agg-garage-graph-graphql.md` §3f+§3j → consumer `web-experiences/garage-web` + `mobile-experiences/garage-mobile` + self `bff-experiences/agg-garage-graph`; (4) `gf-accounting-data-model.md` §2quater (internal, producer self-sign `boundaries/gf-accounting`); (5) `INTEG-EXT-gf-accounting-gf-inventory.md` (BE↔BE PRC narrative) → consumer `boundaries/gf-inventory`; (6) `INTEG-FE-garage-web-agg-garage-graph.md` §3.6d → consumer `web-experiences/garage-web`. Signed 8 pairs qua `scripts/contract-sign.py sign`. Post-sign verify: `bff-experiences/agg-garage-graph` → **OK: all 5 contract(s) verified** (gate `/dev-handoff agg-garage-graph` giờ pass). | Warm-up Phase A phát hiện `contract-scope.yaml` W06 vẫn là placeholder (`<producer-boundary>`/`<consumer-boundary>` chưa điền) — sẽ block `scripts/contract-sign.py verify bff-experiences/agg-garage-graph` tại `/dev-handoff` nếu không fill trước khi DEV. User sonhoang chỉ đạo "xử lý 2 gap còn lại" (2026-07-31) — main agent tự author + sign trong phạm vi tối thiểu cần thiết cho `agg-garage-graph` (không mở rộng sang các cặp consumer khác không liên quan W06, xem ghi chú risk). | PKG-W06-inventory-pricing-stock-report.md v7 §2.2.3 + §3 Entry Criteria, `Tracking/warm-up/WAVE06/W06-agg-garage-graph-warm-up-phaseA.md` GAP-W06-AGG-02, CR-20260731-01. |
| 2026-07-31 | W06 | epic | EP-INVENTORY-STOCK-V2 | **Template path `Execution/wave-specs/_TEMPLATE-epic.md` không tồn tại** (bundle context trỏ tới file này nhưng repo không có) — author dùng cấu trúc thực tế của execution spec epic tiền lệ `Execution/wave-specs/W05/Product/epics/EP-INVENTORY-RECEIPT-V2.md` (§0 Nguồn + Metadata + §1-N verbatim BA + §N+1 Service Impact Matrix + Cross-boundary Contracts + Sequence DAG + Architecture References + Open Items + References + Change Log) làm khuôn mẫu thay thế. Do source EP-INVENTORY-STOCK-V2 có **6** section BA (thêm "6. Success Metric" mà EP-RECEIPT-V2 không có), DEV section được đánh số **§7-§13** (thay vì §6-§12 như tiền lệ) để tránh trùng với "## 6. Success Metric" plain-number của source. | Đảm bảo output khớp format các execution spec epic khác cùng wave (đã có W05 tiền lệ) dù template file bị thiếu/broken path; giữ nguyên tất cả nội dung BA §1-§6 verbatim theo đúng policy mode `epic` (chưa áp dụng tier-authoritative v2). | `.claude/agents/agent-execution-spec-author-modes-extra.md` §Mode epic; `Execution/wave-specs/W05/Product/epics/EP-INVENTORY-RECEIPT-V2.md` (structural precedent); `Product/epics/EP-INVENTORY-STOCK-V2.md` v10 (source). |
| 2026-07-31 | W06 | feature-fe-web | FEAT-PRC-LIST | **Override frontmatter `consumes_bff_feats` từ `[]` (Context Bundle add_fields default) sang `["FEAT-PRC-LIST"]`** + dùng **Apollo Client** (`useQuery`/`useMutation`) thay vì TanStack Query mặc định trong template ở §6.3 State management. | (1) Verified verbatim `Architecture/api/agg-garage-graph-graphql.md` §2 Endpoint Summary rows #360 `priceCalcRunList` + #364 `priceCalcRunDelete` (module `price-calc-run (W06 PRC)`) — FE-web màn LIST rõ ràng consume 2 GraphQL op này qua BFF paired FEAT-PRC-LIST, add_fields default `[]` là thiếu sót/chưa populate của orchestrator, không phản ánh đúng data flow thật. (2) PKG-W06-inventory-pricing-stock-report.md v8 §2.2.4 ghi rõ "State/cache — Apollo default; polling dùng pollInterval Apollo hook, KHÔNG custom setInterval" — dev-actionable note override generic template TanStack Query default. | `Architecture/api/agg-garage-graph-graphql.md` §2 rows #360/#364 + §0 Wave Index W06; `Execution/work-packages/PKG-W06-inventory-pricing-stock-report.md` §2.2.4. |
| 2026-07-31 | W06 | feature-fe-web | FEAT-STK-DETAIL-V2 | **i18n policy = fixed VN labels (no i18next)** cho toàn module Stock V2 Reports — `i18n_keys: []` frontmatter, §4.3 khai báo explicit. **Route base + component reuse mapping** — `StockCardDetailPage` dùng generic `share/*` components (`page-header`, `date-range-picker`, `table`, `link`, `no-data`, `export-excel`) tra qua `.claude/references/web-component-registry.yaml` (KG `implementation.components` rỗng theo bundle §G.X); route path `/inventory/stock-report/card` flagged NEED CONFIRMATION để đối chiếu route base thật của `FEAT-STK-LIST-V2` fe-web tier khi author đó hoàn tất. | `add_fields.i18n_keys: []` do team lead truyền qua Context Bundle xác nhận policy no-i18next cho wave/module này; component registry là canonical lookup thay KG theo `CLAUDE.md` item #12 + repo-rules `gf-gms-web`. Route path chưa có nguồn xác nhận trong bundle nên flag thay vì tự chốt cứng. | Bundle `/tmp/exec-spec-bundles/W06/FEAT-STK-DETAIL-V2.fe-web.md` add_fields, `.claude/references/web-component-registry.yaml` v16. |
| 2026-07-31 | W06 | feature-fe-web | FEAT-PRC-DETAIL | Override `consumes_bff_feats` frontmatter từ `[]` (giá trị `add_fields` do orchestrator truyền) sang `["FEAT-PRC-DETAIL"]` | Màn chi tiết PRC consume 2 GraphQL op (`priceCalcRunGet`, `priceCalcRunRecalc`) passthrough qua `agg-garage-graph-graphql.md §3f`, và paired `bff/FEAT-PRC-DETAIL.md` đang được author song song cùng batch W06 (agent `exec-spec-bff-prc-detail`) — để trống sẽ vi phạm cross-tier reciprocity (reviewer item #16). | `agg-garage-graph-graphql.md §3f` (Wave Index W06), bundle §G `FEAT-PRC-DETAIL.fe-web.md` |
| 2026-07-31 | W06 | contract-sign | (out-of-scope note) | **KHÔNG re-sign** 8 cặp (consumer, contract) khác đang hiện `DRIFT` qua `python3 scripts/contract-sign.py audit --drift` sau batch sign trên: `boundaries/gf-accounting`/`boundaries/gf-sales` :: `gf-accounting-api.md` (self-sign cũ từ trước W06, stale vì file đã bump lên v24 PRC content — nhưng không thuộc `bff-experiences/agg-garage-graph` gate); `boundaries/gf-inventory` :: `gf-inventory-api.md` (self-sign cũ, tương tự); `boundaries/gf-accounting`/`boundaries/gf-sales` :: `gf-sales-events.md`/`gf-accounting-events.md` (domain Insurance Settlement, không liên quan W06 PRC/Stock-V2); `boundaries/gf-inventory` :: `gf-inventory-data-model.md`/`gf-inventory-events.md`/`INTEG-EXT-gf-inventory.md` (domain Catalog/OB/W03-W05, không liên quan W06). | Đây là drift **pre-existing thuộc sở hữu boundary khác** (`gf-accounting`, `gf-sales`, `gf-inventory` tự sign/consume file của chính họ hoặc domain không liên quan) — không phải gate của `agg-garage-graph`. Re-sign hộ mà không có quyết định của boundary owner tương ứng sẽ che giấu drift thật cần họ tự rà soát khi đến `/dev-start`/`/dev-handoff` riêng của boundary đó. Giữ nguyên để `gf-accounting`/`gf-inventory` DEV agent tự xử lý theo đúng process (đã có warm-up report riêng `W06-gf-accounting-warm-up-phaseA.md` + `W06-gf-inventory-warm-up-phaseA.md`, verdict NEEDS_TRIAGE — có thể đã hoặc cần track riêng). | `python3 scripts/contract-sign.py audit --drift` output 2026-07-31 (8 dòng DRIFT còn lại sau batch sign). |
| 2026-07-31 | W06 | feature-fe-web | FEAT-PRC-DELETE | **Modal reuse pattern: 1 component `share/dialogs/alert-confirm`, 2 biến thể qua prop delta (swap-in-place), KHÔNG mount 2 dialog riêng.** Click icon "Xóa" luôn mở biến thể "confirm" trước; khi mutation `priceCalcRunDelete` trả 409 (`ERR-INV-024`/`ERR-INV-029`), FE đổi props tại chỗ sang biến thể "blocked" (đổi title/description/số nút) thay vì đóng-rồi-mở dialog khác. | FEAT AC-1/AC-4 diễn đạt điều kiện "Khi: kỳ chưa đóng" / "Khi: kỳ đã đóng" như thể biết trước server state, nhưng `PriceCalcRun` SDL không expose period status cho client; Figma xác nhận cả 2 dialog dùng CHUNG 1 component (`alert-confirm`) qua prop delta (không phải 2 component riêng) → suy ra pattern chuẩn "optimistic confirm, swap-on-error" khớp với cả AC wording lẫn evidence Figma, tránh phải gọi thêm 1 API pre-check không có trong contract. | `Product/ux/figma-web/wave06-prc-delete.md` §1/§4 (2 screen, cùng `component: alert-confirm`), `Architecture/api/agg-garage-graph-graphql.md` §2 row 360-364 (`PriceCalcRun` SDL không có field period status). |
| 2026-07-31 | W06 | feature-fe-web | FEAT-PRC-DELETE | **Copy dialog "Xác nhận xoá" theo Figma (R9 verbatim), KHÔNG theo FEAT AC-1/AC-2 "Xóa khoản mục"; description `ERR-INV-024` theo FEAT+Figma (feature-specific), KHÔNG theo `ERROR-CODE-REGISTRY.md` generic message/display** — cả 2 flag NEED CONFIRMATION cho Business Authority, spec vẫn author tiếp theo nguồn cụ thể hơn để không block wave. | Figma là visual SSOT đã PNG-verified (R9) cho nhãn nút; `ERROR-CODE-REGISTRY.md` §4 `ERR-INV-024` message là bản dùng chung nhiều feature (Receipt/Delivery/OB/AP) trong khi FEAT-PRC-DELETE + Figma cùng thống nhất 1 câu cụ thể hơn cho đúng ngữ cảnh PRC — có tiền lệ registry override tương tự (`ERR-INV-039-RECONCILIATION-WARNING`). | `Product/ux/figma-web/wave06-prc-delete.md` coverage_gaps `confirm_button_label_drift_figma_vs_feat` (severity MEDIUM); `Product/Commons/ERROR-CODE-REGISTRY.md` §4 row `ERR-INV-024`; `Product/features/FEAT-PRC-DELETE.md` v7 AC-1/AC-2/AC-4. |
| 2026-07-31 | W06 | feature-bff | FEAT-PRC-CREATE | **Fallback Read `Architecture/api/gf-accounting-api.md` (§5 PRC + §6.2/§6.3 Naming Registry) + `Architecture/api/agg-garage-graph-graphql.md` (§0 Wave Index + §2 rows #360-365) trực tiếp** thay vì trust bundle §G "Paired BE REST endpoints" block — block đó tự flag `⚠️ gf-accounting-api.md thiếu §0 Wave Index` và keyword-match `Create` đã pick nhầm section §4.4 FEAT-AP-CREATE (accounting period) thay vì §5.3 W06-3 (price-calc-run CREATE). Verified verbatim: 6 REST endpoint W06-1..W06-6 + 4 GraphQL op `priceCalcRunCreate`/`priceCalcRunRecalc`/`priceCalcRunGet`/`priceCalcItemsForCogsLookup` (op ID #362/#363/#361/#365) dùng cho FEAT-PRC-CREATE bff-tier. | Per agent-execution-spec-author §F-7 forbidden-action rule: "Nếu §G block bắt đầu bằng ⚠️ → treat bundle stale; fallback Read Architecture doc trực tiếp, KHÔNG suy luận endpoint/SDL từ convention hoặc PKG snapshot cũ." Tránh hallucinate contract path/op-name sai boundary. | `gf-accounting-api.md` v24 §5/§6, `agg-garage-graph-graphql.md` v7.81 §0 Wave Index + §2. |
| 2026-07-31 | W06 | feature-bff | FEAT-PRC-CREATE | **`paired_fe_web_feats` set `["FEAT-PRC-CREATE"]`** (khác với `add_fields` truyền từ team-lead context bundle vốn để rỗng `[]`). | PKG-W06 §1 Overview xác nhận PRC có 2 route trên `garage-web` (list + detail, CREATE là modal trong list) — `has_ui_touchpoint=true` cho FEAT-PRC-CREATE trên tier web; Mobile giữ `[]` đúng vì PRC "web-only" theo PKG scope. Áp dụng rule reciprocity item #16 trong agent spec khi add_fields trông như default chưa override. | `PKG-W06-inventory-pricing-stock-report.md §1 Overview` (garage-web 5 route: 2 PRC + 3 report; garage-mobile chỉ FEAT-STK-LIST-V2). |
| 2026-07-31 | W06 | feature-fe-web | FEAT-STK-LIST-V2 (fe-web tier) | **Override `consumes_bff_feats` từ `[]` (Context Bundle add_fields) sang `["FEAT-STK-LIST-V2"]`** — bundle §G xác nhận BFF `agg-garage-graph §3j` có 2 GraphQL op passthrough (`stockLedgerAtDate` + `stockLedgerAtDateExport`) mà FE web thực sự consume, và có agent song song `exec-spec-bff-stk-list-v2` đang author paired BFF tier file cùng FEAT-ID. Giữ `[]` sẽ vi phạm cross-tier reciprocity gate (reviewer item #16). Đồng thời flag 2 điểm NEED CONFIRMATION trong spec: (a) route path `/inventory/stock-report` suy luận từ figma `structural_assertions` path, chưa verify router file thực; (b) placeholder ô search lệch giữa source FEAT text và Figma verbatim — dùng bản Figma làm chuẩn theo R9, cần Business Authority chốt cuối (đã có sẵn trong figma spec `coverage_gaps`). | Bundle `/tmp/exec-spec-bundles/W06/FEAT-STK-LIST-V2.fe-web.md` §G.3j + §I; `Product/ux/figma-web/wave06-stk-list-v2.md` coverage_gaps AC-4. |
| 2026-07-31 | W06 | feature-be | FEAT-STK-LIST-V2 | **Đọc trực tiếp `Product/business-rules/BR-GF-INVENTORY-STOCK-V2.md` v19 thay vì chỉ dùng bundle** — bundle §D (BR refs) generator trỏ nhầm file (`BR-EP-INSURANCE-SETTLEMENT.md`, domain không liên quan) nên không extract được nội dung 9 BR-ID (BR-STKV2-001/002/003/004/005/006/007/008/015) mà source FEAT cite. Author bỏ qua §J hướng dẫn "không cần đọc BR riêng" trong trường hợp này vì bundle thực sự thiếu thông tin cần thiết cho §4/§9 (đúng điều kiện ngoại lệ "chỉ Read source trực tiếp nếu bundle thiếu"). Severity CORNERSTONE/NORMAL ở §9 do author tự phân loại theo mức ảnh hưởng tính đúng số liệu (không có nhãn severity trong BR file gốc). | Bundle §D là instrumentation gap của `scripts/preflight-wave-spec-bundle.py` cho FEAT-STK-LIST-V2 (BR-file lookup trỏ sai) — cần fix script trước batch sau. | `Product/business-rules/BR-GF-INVENTORY-STOCK-V2.md` v19; bundle `/tmp/exec-spec-bundles/W06/FEAT-STK-LIST-V2.be.md` §D. |
| 2026-07-31 | W06 | feature-be | FEAT-STK-LIST-V2 | **Giữ nguyên `paired_bff_feats: []` / `paired_fe_web_feats: []` trong frontmatter theo `add_fields` do orchestrator cung cấp**, dù thực tế FEAT có BFF passthrough (`stockLedgerAtDate`) + FE-web UI touchpoint. §11 vẫn liệt kê đường dẫn tier file chuẩn (`bff/FEAT-STK-LIST-V2.md`, `fe-web/FEAT-STK-LIST-V2.md`) với status PENDING + ghi chú rõ discrepancy để reviewer/author BFF-FE-web sau đối chiếu, thay vì tự ý override giá trị orchestrator đã set. | Context Bundle message từ team-lead là nguồn chỉ định audit fields; author không có đủ thông tin để biết liệu BFF/FE-web batch có gộp FEAT này vào 1 file khác tên hay không — an toàn hơn là giữ nguyên + flag rõ thay vì tự suy đoán. | Context Bundle message (team-lead → agent-execution-spec-author, `add_fields`). |
| 2026-07-31 | W06 | feature-bff | FEAT-PRC-LIST | **Bypass bundle §G truncation — verify GraphQL op name trực tiếp tại source doc** thay vì suy luận từ PKG snapshot/convention. Bundle §G §3f.1 (SDL types) bị cắt trước đoạn liệt kê op name thật (truncated "573 more lines"). Đọc bounded `Architecture/api/agg-garage-graph-graphql.md` §2 Endpoint Summary (offset ~420-440) xác nhận op LIST = `priceCalcRunList` (row #360, Query, `input: PriceCalcRunSearchInput!` → `PagedPriceCalcRunApiResponse!`) — không phải suy đoán `priceCalcRunSearch` theo naming convention "search" của REST layer. Cũng verify 3 op liên quan cột "Thao tác" (`priceCalcRunGet` #361, `priceCalcRunCreate` #362, `priceCalcRunDelete` #364) thuộc paired FEAT khác → khai báo AC-6 → N/A đúng theo §3.5 tier purity. | Agent spec F-7 (anti-hallucination contract) cấm suy luận GraphQL op name khi bundle §G block bị truncated/stale — phải fallback Read Architecture doc bounded trước khi ghi `references_verbatim`. | `Architecture/api/agg-garage-graph-graphql.md` §2 row #360-365 (line 430-435), §0 Wave Index row W06. |
| 2026-07-31 | W06 | feature-be | FEAT-PRC-LIST | **Resolve index sort-column drift (informational, không block)** — `idx_prc_run_tenant_garage_wh` dùng cột `executed_at DESC` (khớp BR-PRC-018 + `gf-accounting-data-model.md §2quater.1` v14 + PKG-W06 §2.2.1), KHÔNG dùng `created_at DESC` như ghi nhầm ở `gf-accounting-api.md §5.1` Semantics note và `gf-accounting-HLD.md §12.3`. Cột `created_at` tồn tại trên entity nhưng chỉ là audit column chuẩn, không phải sort key nghiệp vụ. | Data model (T1, canonical schema authority, v14 mới nhất 2026-07-24) và PKG đồng thuận `executed_at`; annotation "created_at" ở API/HLD là drift cosmetic giữa 2 doc khác, không phải endpoint contract chính (request/response schema §5.1 không đề cập tên cột index). Ghi rõ cho BE dev implement đúng + tránh nhân bản sai lệch sang review. | `Architecture/data/gf-accounting-data-model.md:452,460` (executed_at + index def), `Architecture/api/gf-accounting-api.md:1254` (semantics note nhầm), `Architecture/hld/gf-accounting-HLD.md §12.3` (nhầm tương tự), `Product/business-rules/BR-GF-INVENTORY-ACCOUNTING-PERIOD.md:76` (BR-PRC-018). |
| 2026-07-31 | W06 | feature-bff | FEAT-STK-LIST-V2 | **Sửa `paired_fe_web_feats` từ `[]` (theo `add_fields` orchestrator cung cấp) thành `["FEAT-STK-LIST-V2"]`** trong frontmatter tier spec. | Context Bundle §H (PKG-W06 Overview) xác nhận rõ FEAT-STK-LIST-V2 có touchpoint web ("Web GMS: FEAT-STK-LIST-V2 (Q1)... garage-web (5 route: 2 PRC + 3 report)") song song với mobile — khác với `add_fields.paired_fe_web_feats: []` được orchestrator truyền (có vẻ là gap trong fan-out-map generation, không phải chủ đích). Note kèm theo task chỉ nhấn mạnh mobile là điểm khác biệt so với sibling, không loại trừ web. Giữ nguyên đúng thực tế nghiệp vụ theo KG/PKG (Critical Rule #3 KG-first) thay vì propagate metadata sai xuống audit trail. | Bundle §H PKG-W06 §1 Overview row "Boundaries affected"; Context Bundle add_fields (team-lead task message 2026-07-31). |
| 2026-07-31 | W06 | feature-bff | FEAT-IP-VIEW-V2 | **`paired_fe_web_feats`/`paired_mobile_feats` giữ nguyên `[]` theo Context Bundle add_fields** (không tự suy luận thêm pairing dù PKG mô tả feature là web-facing) — §11 Cross-tier coordination ghi rõ FE Web row status "N-A (chưa spawn trong batch W06 hiện tại)" thay vì fabricate file path chưa tồn tại; Mobile row "N-A (out-of-scope — web-only per PKG platform scope)". `stockInoutSummaryExport` args (`fromDate/toDate/warehouseIds/keyword/sort`, không `page`/`size`) derive trực tiếp từ REST query params verbatim của `W06-STK-EX2` (bundle §G) — không phải hallucinate endpoint mới, chỉ là quyết định arg-shape resolver-side hợp lý (loại bỏ pagination cho export toàn bộ scope). §4.5 Error code mapping cho Q2/EX2 suy luận từ pattern module `StockV2Report` (đã verbatim xác nhận cho Q1) thay vì bịa số liệu — ghi chú rõ cần dev verify lại tại `gf-inventory-api.md §3g.2` khi impl. | Đúng theo §1 spec agent-execution-spec-author (F-7 KHÔNG hallucinate contract) + item #16 reciprocity (chỉ pair khi cả 2 phía đã xác nhận qua fan-out map, không tự thêm). Tránh việc reviewer phải reject vì spec trỏ tới file FE-web chưa tồn tại. | Team-lead Context Bundle add_fields (`paired_fe_web_feats: []`, `paired_mobile_feats: []`); bundle §G `agg-garage-graph-graphql.md §3j.2` W06-STK-EX2 row; `gf-inventory-api.md §3g.1/§3g.2` W06-STK-Q1/Q2/EX2. |
| 2026-07-31 | W06 | feature-bff | FEAT-IP-VIEW-V2 | **SUPERSEDE decision trên (bff, row trước)** — sửa `paired_fe_web_feats` từ `[]` sang `["FEAT-IP-VIEW-V2"]` trong frontmatter + §11 (Cross-tier coordination) + Metadata table. Lý do đảo quyết định: (1) paired BE tier file (`feature-be FEAT-IP-VIEW-V2`, xem row trước đó cùng bảng) đã override `paired_bff_feats`/`paired_fe_web_feats` sang `["FEAT-IP-VIEW-V2"]` với cùng bằng chứng PKG §1 Overview "garage-web (5 route: 2 PRC + 3 report)"; giữ BFF tier `[]` sẽ tạo **asymmetric reciprocity** (BE trỏ sang FE-web nhưng BFF không) — vi phạm chính reviewer item #16 mà quyết định trước đó viện dẫn để giữ nguyên `[]`. (2) Batch D (fe-web-tier authors, 8 agent) đang chạy song song cho toàn bộ 8 FEAT của PKG-W06 — có cơ sở hợp lý để tin tier file FE-web cho FEAT-IP-VIEW-V2 sẽ tồn tại cùng batch, mirror pattern đã áp dụng nhất quán ở `FEAT-PRC-CREATE`/`FEAT-PRC-DETAIL`/`FEAT-STK-LIST-V2` (bff+be tier) cùng ngày. `paired_mobile_feats` giữ nguyên `[]` (đúng — web-only per PKG platform scope, không đổi). | Nhất quán cross-tier reciprocity toàn bộ 8 FEAT trong cùng PKG-W06 batch — tránh 1 FEAT bị lệch pattern so với các sibling cùng tình huống (web-only report, BE đã override) chỉ vì batch order khác nhau; correction thực hiện ngay khi phát hiện qua decision log của sibling BE tier agent. | Decision log row `feature-be FEAT-IP-VIEW-V2` (paired_bff_feats/paired_fe_web_feats override, cùng bảng, phía trên); precedent `FEAT-PRC-CREATE`/`FEAT-STK-LIST-V2` bff+be rows; `PKG-W06-inventory-pricing-stock-report.md §1 Overview`. |
| 2026-07-31 | W06 | business-rule | BR-GF-INVENTORY-STOCK-V2 | **Template path `Execution/wave-specs/_TEMPLATE-business-rule.md` không tồn tại và no Bash/Glob tool để search** — author dùng cấu trúc thực tế của 2 execution spec business-rule tiền lệ (`Execution/wave-specs/W04/Product/business-rules/BR-GF-INVENTORY-OPENING-BALANCE.md` + `Execution/wave-specs/W05/Product/business-rules/BR-GF-INVENTORY-DELIVERY-V2.md`) làm khuôn mẫu: §0 Nguồn + §1 Rule Statements verbatim + §2 Rationale verbatim + §3 Enforcement Layer (3.1 overview + 3.2 per-rule + 3.3 DB-level) + §4 Test Ideas + §5 BR→FEAT→AC Mapping + §6 Error Code Mapping + §7 Open Items + §8 References + §9 Change Log (9 section, khác abstract 8-section mô tả ở `agent-execution-spec-author-modes-extra.md §Mode business-rule` — tiền lệ thực tế có thêm §7 Open Items tách riêng khỏi §8 References). Vì `BR-GF-INVENTORY-STOCK-V2.md` là file **dành riêng cho 1 epic** (`EP-INVENTORY-STOCK-V2`, không share với epic khác), áp dụng đúng pattern "không filter rule nào" (giống BR-DELIVERY-V2 W05) thay vì filter theo FEAT-trong-wave — giữ verbatim toàn bộ 16 BR + 1 CB kể cả 2 rule (BR-STKV2-001/005a) có write-path FEAT NGOÀI wave W06 (đã có wave-spec W04/W05, hoặc sibling W06 PRC spec) — §3/§4/§5/§6 cross-reference 3 file đó thay vì duplicate enforcement chi tiết. `source_sha` không compute được (không có shell tool, mirror gap OI-W04-BR-001) → flagged NEED CONFIRMATION + Open Item blocking cho ACTIVE (không block DEV Stage). | Giữ nhất quán format với 2 precedent cùng domain `gf-inventory` trong cùng chuỗi wave (W04→W05→W06); tránh duplicate nội dung enforcement đã có ở spec khác (single-source cho DEV đọc, tránh drift 2 nơi mô tả cùng 1 write-path rule). | `Execution/wave-specs/W04/Product/business-rules/BR-GF-INVENTORY-OPENING-BALANCE.md`; `Execution/wave-specs/W05/Product/business-rules/BR-GF-INVENTORY-DELIVERY-V2.md`; `.claude/agents/agent-execution-spec-author-modes-extra.md §Mode business-rule`; `Product/business-rules/BR-GF-INVENTORY-STOCK-V2.md` v19 (source). |
| 2026-07-31 | W06 | feature-be | FEAT-IP-VIEW-V2 | **Override `paired_bff_feats`/`paired_fe_web_feats` từ `[]` (Context Bundle add_fields) sang `["FEAT-IP-VIEW-V2"]`** — bundle §G/§H tự chứng minh feature có BFF touchpoint (GraphQL query `stockInoutSummary` tại `agg-garage-graph-graphql.md §3j`) và FE Web touchpoint (báo cáo NXT là màn hình Web GMS, consumer list §3g header liệt kê rõ). Giữ nguyên `paired_mobile_feats: []` vì source FEAT §Metadata + §7 Out of Scope xác nhận Web GMS only trong W06 (mobile hub chỉ mở `FEAT-STK-LIST-V2`). Flag NEED CONFIRMATION trong §11 cho reviewer/orchestrator đối chiếu lại fan-out map gốc. | Context Bundle add_fields có vẻ chưa được orchestrator populate đầy đủ cho field reciprocity (khác với tiền lệ sibling `FEAT-IR-DETAIL-V2` W05 tự self-reference cả 3 tier); giữ nguyên `[]` sẽ gây mất liên kết cross-tier sai sự thật đã có trong Architecture bundle. | Bundle `/tmp/exec-spec-bundles/W06/FEAT-IP-VIEW-V2.be.md` §G (gf-inventory-api.md §3g header "Consumers"), §H PKG §Overview boundaries row; `Execution/wave-specs/W05/Product/features/be/FEAT-IR-DETAIL-V2.md` (precedent pattern). |
| 2026-07-31 | W06 | feature-be | FEAT-PRC-DELETE | **Bundle §G API excerpt sai section (keyword-match `Delete` trỏ nhầm sang §4.6 `FEAT-AP-DELETE` accounting-period thay vì PRC)** — fallback Read trực tiếp `Architecture/api/gf-accounting-api.md` §5.5 (v24) để lấy đúng contract `DELETE /api/v2/price-calc-runs/{id}` (W06-5). §6.1 spec đã cite đúng §5.5 verbatim (guards `ERR-INV-024`/`ERR-INV-029`, response `{runId, deleted, message}`, idempotent replay). Cũng verified BR-PRC-011/016 text trực tiếp tại `BR-GF-INVENTORY-ACCOUNTING-PERIOD.md` §2.2 (bundle §D báo "không tìm thấy" vì đã search nhầm file `BR-EP-INSURANCE-SETTLEMENT.md`). | Per F-7 contract discipline (agent spec §3.8 / §7) — bundle §G block bắt đầu bằng `⚠️` (thiếu §0 Wave Index / keyword-match nhầm) phải treat as stale, KHÔNG suy luận từ convention. Endpoint path segment mismatch nếu không verify sẽ là P0 REJECTED tại reviewer item #23. | `Architecture/api/gf-accounting-api.md` §5.5 (v24, dòng 1557-1591); `Product/business-rules/BR-GF-INVENTORY-ACCOUNTING-PERIOD.md` §2.2 (BR-PRC-011/016), §2.3 (BR-AP-CMN-002). |
| 2026-07-31 | W06 | feature-be | FEAT-PRC-DELETE | **`paired_bff_feats`/`paired_fe_web_feats`/`paired_mobile_feats` giữ nguyên `[]` theo `add_fields` từ Context Bundle**, dù source FEAT có UI popup (AC-1/AC-3) + đề xuất GraphQL mutation `[PROPOSED] DeleteCogsRun` (source FEAT §4) — không tự suy luận reciprocity ngược `["FEAT-PRC-DELETE"]`. §11 spec đã flag rõ: nếu Batch C/D (bff-tier/fe-web-tier authors) tách tier file riêng cho `FEAT-PRC-DELETE`, cần set `paired_backend_feats=["FEAT-PRC-DELETE"]` phía BFF/FE để đảm bảo reciprocity (item #16); nếu không tách riêng (UI wire qua `FEAT-PRC-LIST`), giữ N/A là đúng. | Tuân thủ instruction "Fill từ Context Bundle inject... add_fields từ FAN-OUT-MAP" — fan-out map là nguồn quyết định, không phải suy luận của author từ source FEAT content. Tránh tạo mismatch nếu Batch C/D không thực sự tạo tier file riêng. | Context Bundle team-lead message `add_fields`; `Product/features/FEAT-PRC-DELETE.md` v7 §3/§4; `PKG-W06-inventory-pricing-stock-report.md` §Overview (Features PRC 5). |
| 2026-07-31 | W06 | feature-bff | FEAT-STK-DETAIL-V2 | **`paired_fe_web_feats`/`paired_mobile_feats` giữ nguyên `[]` theo Context Bundle add_fields** (không tự suy luận thêm pairing dù PKG mô tả feature là web-facing "Web GMS: ... FEAT-STK-DETAIL-V2 (Q3)") — §11 Cross-tier coordination ghi rõ FE Web row "N/A — fanout map không gán paired_fe_web_feats ở wave này dù feature web-only theo PKG scope"; Mobile row "N/A — out-of-scope, Q3 web-only per FEAT platform scope". `stockCardDetailExport` args (`productCode/warehouseCode/fromDate/toDate`, direct args không input type) derive verbatim từ REST query params `GET /api/v1/stock/card/export` (bundle §G W06-STK-EX3) — không phải hallucinate endpoint mới, chỉ là quyết định arg-shape resolver-side (bundle SDL không có input type riêng cho export op). | Đúng theo §1 spec agent-execution-spec-author (F-7 KHÔNG hallucinate contract) + item #16 reciprocity (chỉ pair khi cả 2 phía đã xác nhận qua fan-out map) — mirror quyết định đã lập cho sibling `FEAT-IP-VIEW-V2` (cùng batch, cùng tình huống web-only report với `paired_fe_web_feats: []`). Tránh spec trỏ tới file FE-web chưa tồn tại. | Team-lead Context Bundle add_fields (`paired_fe_web_feats: []`, `paired_mobile_feats: []`); bundle §G `agg-garage-graph-graphql.md §3j.2` W06-STK-EX3 row; `gf-inventory-api.md §3g.1/§3g.2` W06-STK-Q3/EX3; precedent decision log entry `FEAT-IP-VIEW-V2`. |
| 2026-07-31 | W06 | feature-be | FEAT-STK-DETAIL-V2 | **Sửa `paired_bff_feats`/`paired_fe_web_feats` từ `[]` (theo `add_fields` orchestrator cung cấp) thành `["FEAT-STK-DETAIL-V2"]`** trong frontmatter tier spec; `paired_mobile_feats` giữ `[]`. | Bundle §G §3g intro + §H PKG Overview xác nhận rõ FEAT-STK-DETAIL-V2 (Q3) có touchpoint BFF (`agg-garage-graph-graphql.md v7.81 §3j` query `stockCardDetail`) và FE Web (`garage-web` route thẻ kho, "Web GMS: ... FEAT-STK-DETAIL-V2 (Q3)"; PKG §2.2 "garage-web (5 route: 2 PRC + 3 report)"), đồng thời PKG xác nhận feature này **web-only** (mobile chỉ có FEAT-STK-LIST-V2). Cùng pattern quyết định đã áp dụng cho `FEAT-STK-LIST-V2` (dòng trên) — giữ đúng thực tế nghiệp vụ theo bundle/PKG (Critical Rule #3 KG-first) thay vì propagate metadata trống có vẻ là gap fan-out-map. §11 Cross-tier coordination ghi status "N/A (chưa spawn — Batch C/D pending)" cho cả 2 file BFF/FE-web thay vì fabricate. | Bundle §G §3g intro (BFF/FE Web consumers list) + §H PKG-W06 §1 Overview + §2.2 (garage-web 5 route); Context Bundle add_fields (team-lead task message 2026-07-31); precedent dòng FEAT-STK-LIST-V2 ở trên. |
| 2026-07-31 | W06 | feature-be | FEAT-STK-DETAIL-V2 | **Đọc trực tiếp `Architecture/api/gf-inventory-api.md` §3g.2 (line 8891-9196) thay vì chỉ dùng bundle §G** — bundle pre-flight bundle truncate phần field-level request/response contract của W06-STK-Q3 (`POST /api/v1/stock/card/search`) và W06-STK-EX3 (`GET /api/v1/stock/card/export`, đánh dấu "...(truncated, 345 more lines — read source for full)"), chỉ để lại field-list rút gọn ở bundle §5.2 (`StockCardDetailItem`). Author fallback đọc file gốc (được bundle §0 Wave Index xác nhận `✅ resolved for W06 → §3g`, không phải trạng thái `⚠️ stale`) để lấy đủ request field table (`productCode`/`warehouseCode`/`fromDate`/`toDate`), response envelope 4-block (`context`/`opening`/`content[]`/`aggregates`), error code table, và row-cap export (10k) — tránh suy diễn contract từ pattern sibling Q1/Q2 (khác shape: Q3 có thêm `context`/`opening` top-level object mà Q1/Q2 không có). | F-7 contract discipline (không hallucinate REST endpoint/field shape) — bundle §J hướng dẫn rõ "nếu §G block bắt đầu bằng ✅ → trust bundle" nhưng khi nội dung bên trong bị bundle-generator tự cắt ("read source for full"), trust đúng nghĩa đòi hỏi đọc file gốc cho phần bị cắt thay vì suy diễn theo sibling endpoint có shape khác. | `Architecture/api/gf-inventory-api.md` v76 §3g.2 W06-STK-Q3 (line 8891) + W06-STK-EX3 (line 9143); bundle `/tmp/exec-spec-bundles/W06/FEAT-STK-DETAIL-V2.be.md` line 678 truncation marker. |
| 2026-07-31 | W06 | feature-fe-web | FEAT-PRC-RECALC | **(a) Figma_refs resolve qua host screen thay vì NEED CONFIRMATION placeholder**: bundle §G.Y báo "FIGMA SPEC MISSING" cho glob `wave06-prc-recalc*.md` (đúng — RECALC không có frame riêng). Thay vì fallback wireframe placeholder, author xác minh nguồn (FEAT-PRC-RECALC.md AC-1/AC-1b + figma-links.yaml — không có entry FEAT-PRC-RECALC) rồi cite trực tiếp 2 node nút trong `Product/ux/figma-web/wave06-prc-detail.md` (host screen FEAT-PRC-DETAIL, đã ACTIVE) — node `13575:103113` PageHeader chứa cả `btn_recalc_all` + `btn_recalc_error_only`. **(b) `consumes_bff_feats` sửa `[]` → `["FEAT-PRC-RECALC"]`**: context bundle add_fields cho giá trị `[]` nhưng feature rõ ràng có BFF touchpoint (mutation `recalcPriceCalcRun` per ADR-028 §1 + bundle §G §3f) — giữ `[]` sẽ vi phạm cross-tier reciprocity (reviewer item #16). **(c) Error display mapping cho `ERR-INV-024`**: registry ghi `display: INLINE_FORM` nhưng RECALC trigger không nằm trong 1 form — dùng `share/dialogs/alert-dialog` làm closest-fit reuse (không có banner-only component trong `web-component-registry.yaml`), ghi rõ là component gap thay vì tự tạo mới. | (a) Tránh false-negative NEED CONFIRMATION khi thực chất có Figma evidence cụ thể — "RECALC may be modal/action triggered from LIST/DETAIL" theo chỉ dẫn team-lead. (b) Đảm bảo audit reciprocity đúng theo policy tier-authoritative — bundle add_fields là default, không phải hard constraint khi rõ ràng sai với thực tế contract. (c) Tuân thủ ERROR-CODE-REGISTRY làm nguồn duy nhất cho display mode (CLAUDE.md: FE KHÔNG tự đặt text/display) trong khi vẫn reuse-first thay vì build-new không được duyệt. | `Product/ux/figma-web/wave06-prc-detail.md` (node `13575:103113`); `Product/ux/figma/figma-links.yaml` (không có key FEAT-PRC-RECALC, wave 06); `Architecture/decisions/ADR-028.md` §1; `Product/Commons/ERROR-CODE-REGISTRY.md` §1.4 + §4 (`ERR-INV-024`); `.claude/references/web-component-registry.yaml` §1/§2 (`alert-dialog`). |
| 2026-07-31 | W06 | epic | EP-INVENTORY-ACCOUNTING-PERIOD (nhóm PRC) | **Xác nhận 0 mục BLOCKING ở §10 Open Items** — khác precedent W04 (2 mục BLOCKING do PKG↔ADR-019/gf-accounting-api.md contract drift lúc đó), execution spec W06 nhóm PRC không phát sinh blocking item vì toàn bộ Architecture (`gf-accounting-api.md` v24, `gf-inventory-api.md` v72, `agg-garage-graph-graphql.md` v7.79, ADR-027 v5, ADR-028 v4) đã ACCEPTED/ratified trước wave (`ARCH-REVIEW-W06.md` UNBLOCK Round 3 + `DRIFT-CHECK-W06-BA-VS-ARCH-2026-07-28.md` verdict CONSISTENT). Chỉ ghi nhận 2 mục LOW non-blocking: (a) `source_sha` chưa compute được (tooling gap, mirror W04); (b) `INTEG-EXT-gf-accounting-gf-inventory.md` version citation lag — PKG-W06 v8 cite "v2" nhưng file thực tế đã bump "v3" cùng ngày 2026-07-31 (GAP-W06-GI-04 fix idempotency mechanism) — nội dung tương thích, chỉ doc-hygiene, không re-negotiate contract. | Instruction epic mode (`agent-execution-spec-author-modes-extra.md §Mode epic`) yêu cầu verify mỗi CB có file trong `Architecture/integrations/` trước khi mark blocking; đã verify `INTEG-EXT-gf-accounting-gf-inventory.md` tồn tại + đọc trực tiếp §3-§6 để xác nhận nội dung khớp PKG (không có contract-shape mismatch như W04 từng gặp). | `Execution/wave-specs/W06/Product/epics/EP-INVENTORY-ACCOUNTING-PERIOD.md` §10; PKG-W06 v8 §3 Entry Criteria; `Architecture/integrations/INTEG-EXT-gf-accounting-gf-inventory.md` v3 Change Log. |
| 2026-07-31 | W06 | overview | _wave-overview.md | **Synthesize DRAFT wave overview từ 29 spec (2 EP + 2 BR + 25 FEAT-tier) — không dùng template riêng cho mode `overview` (chỉ có workflow §Mode overview trong `agent-execution-spec-author-modes-extra.md`, không có file `_TEMPLATE-wave-overview.md`), nên mirror cấu trúc thực tế của precedent `Execution/wave-specs/W05/_wave-overview.md` (§0 Nguồn & Audit → §1 Wave Scope Narrative → §2 Vertical Slice → §3 Feature×Tier Matrix → §4 Cross-boundary Contracts → §5 Scope Changes → §6 Sequencing DAG → §7 Open Items → §8 Change Log). Đổi tên §5 từ "Mid-wave" (W05) sang "Pre-wave / Mid-wave" (W06) vì toàn bộ 9 cascade-fix liệt kê xảy ra TRƯỚC `/dev-start` (authoring stage), không phải trong lúc DEV đang chạy như W05.** Trong lúc đọc chéo `_decisions.md` để đối chiếu với frontmatter thực tế của 25 FEAT-tier file (theo đúng tinh thần precedent NC-W05-OV-001 — overview mode có trách nhiệm phát hiện cross-file drift mà từng author riêng lẻ không thấy), phát hiện 2 discrepancy: (1) entry #26 (`FEAT-PRC-DELETE` be-tier) và #37 (`FEAT-PRC-RECALC` be-tier) ghi quyết định giữ `paired_*_feats: []`, nhưng đọc trực tiếp cả 2 file (be+bff+fe-web) cho thấy `paired_bff_feats`/`paired_fe_web_feats` đã được populate đầy đủ và reciprocal ở cả 3 tier — kết luận: có 1 correction xảy ra sau 2 entry đó mà không được log lại (không phải bug nội dung, chỉ là audit-trail hygiene gap) → ghi NC-W06-OV-004 vào overview; (2) NC-W06-EP-STK-002 (sibling epic forward-reference chưa verify tồn tại, ghi trong `EP-INVENTORY-STOCK-V2.md` §11) đã tự CLOSE vì overview này đọc trực tiếp xác nhận cả 2 EP execution spec đều tồn tại → ghi NC-W06-OV-009 note đã resolve. Cũng phát hiện 1 root-cause hệ thống mới (`gf-accounting-api.md` thiếu §0 Wave Index gây F-7 fallback lặp lại ≥4 lần xuyên suốt FEAT-PRC-CREATE/DETAIL/DELETE be+bff tier) — ghi NC-W06-OV-001 khuyến nghị Architecture Authority retrofit. | Đúng theo `agent-execution-spec-author-modes-extra.md §Mode overview` bước 7 ("Aggregate NEED CONFIRMATION từ converted EP/FEAT/BR") mở rộng — overview là điểm duy nhất có visibility toàn bộ 25 file cùng lúc để bắt cross-file drift mà per-file author không thấy được (mirror đúng cách W05 overview đã tự phát hiện NC-W05-OV-001 qua đọc chéo `FEAT-IR-DELETE`/`FEAT-ID-DELETE`). | `Execution/wave-specs/W05/_wave-overview.md` (structural + methodology precedent, đặc biệt §7 NC-W05-OV-001); `Execution/wave-specs/W06/Product/features/{be,bff,fe-web}/FEAT-PRC-DELETE.md` + `FEAT-PRC-RECALC.md` (frontmatter verified trực tiếp); `Execution/wave-specs/W06/Product/epics/EP-INVENTORY-ACCOUNTING-PERIOD.md` + `EP-INVENTORY-STOCK-V2.md` (tồn tại, verified); `Architecture/api/gf-accounting-api.md` v24 (thiếu §0 Wave Index, nhiều decision-log entry cùng ngày xác nhận). |
| 2026-07-31 | W06 | business-rule | BR-GF-INVENTORY-ACCOUNTING-PERIOD | **Filter §1 theo FEAT-trong-wave (khác pattern "không filter" đã dùng cho `BR-GF-INVENTORY-STOCK-V2` cùng batch)** — nguồn canonical (v40) chứa 2 nhóm rule tách biệt rõ ràng theo Features column: **BR-AP-\*** (Kỳ kế toán, 16 rule + BR-AP-CMN-001, Features toàn bộ = `FEAT-AP-*`) và **BR-PRC-\*** (Tính giá, 18 rule, Features toàn bộ = `FEAT-PRC-*`). `PKG-W06 §1 Features` chỉ liệt kê 5 `FEAT-PRC-*` (không có `FEAT-AP-*` nào trong wave — nhóm Kỳ kế toán đã ship ở wave trước theo cascade "Move boundary" Change Log #7 của cả 5 FEAT-PRC nguồn). Verbatim copy đủ 18/18 `BR-PRC-*` (100% in-scope) + giữ 2 ngoại lệ cross-cutting dù Features-tag không chỉ đích danh `FEAT-PRC-*`: `CB-AP-001` (cross-boundary — mô tả đúng chiều PRC đọc `gf-inventory`) và `BR-AP-CMN-002` (Features cột ghi rõ literal "toàn bộ feature AP + PRC"). Loại hẳn 16 `BR-AP-001..016` + `BR-AP-CMN-001` khỏi §1 verbatim (chỉ giữ cross-reference nội suy trong text các rule PRC đã copy, không tạo row riêng). `source_sha` cùng gap tooling như các entry trên (không có Bash trong session) → OI-W06-BR-AP-001 BLOCKING cho ACTIVE. | Áp dụng đúng literal của `agent-execution-spec-author-modes-extra.md §Mode business-rule` bước 2 ("Chỉ giữ rules áp dụng cho FEAT trong wave — filter out rules cho FEAT ngoài wave") — khác `BR-GF-INVENTORY-STOCK-V2` (file dành riêng 1 epic duy nhất, không có nhóm rule khác-epic để filter) thì file này có ranh giới rõ ràng giữa 2 nhóm rule khác epic-con (`FEAT-AP-*` thuộc epic con Kỳ kế toán, `FEAT-PRC-*` thuộc epic con Tính giá) nên filter là đúng tinh thần chỉ dẫn thay vì copy dư ~30% nội dung không liên quan W06 DEV. | `Product/business-rules/BR-GF-INVENTORY-ACCOUNTING-PERIOD.md` v40 §2.1/§2.2/§2.3 (Features column phân tách rõ theo FEAT-AP-*/FEAT-PRC-*); `PKG-W06-inventory-pricing-stock-report.md` v8 §1 Features row (chỉ 5 FEAT-PRC-*, không FEAT-AP-*); `.claude/agents/agent-execution-spec-author-modes-extra.md §Mode business-rule` bước 2; precedent dòng `BR-GF-INVENTORY-STOCK-V2` (đối lập pattern "không filter"). |
| 2026-07-31 | W06 | feature-be | FEAT-PRC-CREATE | **§G API extract trong bundle bị lệch section (F-7 fallback triggered) + `paired_bff_feats`/`paired_fe_web_feats` sửa từ `[]` (add_fields) thành `["FEAT-PRC-CREATE"]`.** Bundle §G "API §relevant" trả về nội dung endpoint `POST /api/v2/accounting-periods` (FEAT-AP-CREATE, sai FEAT) do `gf-accounting-api.md` thiếu §0 Wave Index + keyword-match "Create" match nhầm section — author fallback Read trực tiếp `Architecture/api/gf-accounting-api.md` (offset dò tìm §5.3/§5.4/§5.5/§5.6 PriceCalcRun, verify verbatim W06-3 CREATE + W06-6 lookup request/response/error trước khi ghi §6). Đồng thời sửa `paired_bff_feats`/`paired_fe_web_feats` từ `[]` (add_fields orchestrator cung cấp) → `["FEAT-PRC-CREATE"]` vì PKG-W06 §1 xác nhận rõ BFF wrap 6 PRC op qua `agg-garage-graph` + `garage-web` có 2/5 route cho PRC (CREATE + DETAIL) — `paired_mobile_feats` giữ `[]` đúng (PRC web-only). | F-7 discipline (KHÔNG hallucinate contract từ convention/PKG snapshot cũ khi §G flag `⚠️`) — path segment verify quan trọng vì CREATE là entry point kick-off Temporal workflow, sai path sẽ cascade sai xuống BFF/FE tier. Reciprocity fix theo item #16 — tránh sibling BFF/FE-web tier spec (author sau) bị mismatch source_feat_sha consistency check vì thiếu back-reference. | `Architecture/api/gf-accounting-api.md` v24 §5.3 (line ~1380) + §5.6 (line ~1593); PKG-W06 §1 Overview "Boundaries affected" row `agg-garage-graph` + `garage-web`. |
| 2026-07-31 | W06 | feature-be | FEAT-PRC-DETAIL | **§G API extract trong bundle lệch section (F-7 fallback) — fallback Read trực tiếp `Architecture/api/gf-accounting-api.md` §5.2 (v24) thay vì trust bundle.** Bundle §G "API §relevant" trả về `FEAT-AP-DETAIL` (`GET /api/v2/accounting-periods/{id}`, sai FEAT) vì `gf-accounting-api.md` thiếu §0 Wave Index + keyword-match "Detail" match nhầm section (block bắt đầu `⚠️`). Author fallback Read trực tiếp §2 Endpoint Summary (xác nhận W06-2 = `GET /api/v2/price-calc-runs/{id}`) rồi đọc trọn §5.2 (dòng 1258-1378) lấy đúng query params (`includeItems`/`itemStatus`/`keyword`), response shape (`items[]` + `aggregates{}` 8-field), error codes (`ERR-CMN-not-found` 404 tenant-no-leak), semantics (soft-delete exclusion mandatory, Redis 3s TTL, aggregates always non-null bất kể `includeItems`) trước khi ghi §6. **Cross-feature decision**: AC-5/AC-5b (2 nút "Tính lại") KHÔNG được đặc tả đầy đủ tại tier file này — chỉ khai báo BE của DETAIL cung cấp field `status` đủ chính xác cho FE quyết định hiện/ẩn nút (EC-4); logic execution đầy đủ của `POST /api/v2/price-calc-runs/{id}/recalc` (W06-4, validation + 202 kick-off + error codes + copy-forward) cross-ref sang `FEAT-PRC-RECALC.md §6` (chưa tồn tại tại thời điểm author — flag path trong §11 để reviewer verify khi sibling file xong) thay vì duplicate spec 2 nơi. §5 Schema delta ghi rõ "KHÔNG có delta" (chỉ đọc entity `FEAT-PRC-CREATE` đã sở hữu) thay vì fabricate cột giả để lấp đầy template table. **Reciprocity**: sửa `paired_bff_feats`/`paired_fe_web_feats` từ `[]` (add_fields orchestrator) → `["FEAT-PRC-DETAIL"]` vì PKG-W06 §1/§2.2 xác nhận BFF wrap đủ 6 PRC op + `garage-web` có 2/5 route PRC (CREATE + DETAIL); `paired_mobile_feats` giữ `[]` (PRC web-only). `authoring_inputs.template_sha` để `null` — agent này không có Bash/hash tool trong spawn hiện tại (cùng gap tooling đã ghi nhận ở các entry OI-W04/W06-BR trên). | F-7 discipline (KHÔNG hallucinate contract khi §G flag `⚠️`) — endpoint sai path sẽ cascade sai xuống BFF/FE tier polling logic. Tránh duplicate RECALC execution spec ở 2 file (DETAIL + RECALC) gây drift khi 1 bên update mà bên kia không cascade — giữ single-source tại FEAT-PRC-RECALC đúng nguyên tắc DRY của policy tier-authoritative. Reciprocity fix theo item #16, cùng pattern đã áp dụng cho FEAT-PRC-CREATE/FEAT-STK-LIST-V2/FEAT-IP-VIEW-V2/FEAT-STK-DETAIL-V2 (add_fields gap có vẻ hệ thống ở batch B/C W06). | `Architecture/api/gf-accounting-api.md` v24 §2 Endpoint Summary (line 76) + §5.2 (line 1258-1378); `Product/features/FEAT-PRC-DETAIL.md` v24 AC-5/AC-5b + EC-4; PKG-W06 §1 Overview + §2.2.1 endpoint table W06-2; precedent decision entries FEAT-PRC-CREATE/FEAT-PRC-LIST/FEAT-PRC-DELETE (cùng batch, cùng F-7 fallback pattern). |
| 2026-07-31 | W06 | feature-bff | FEAT-PRC-RECALC | **Bypass bundle §G/§H truncation + `⚠️` fallback flag — verify GraphQL op signature và downstream REST endpoint trực tiếp tại 2 Architecture doc gốc.** Bundle §G GraphQL block cho §3f PRC bị truncate trước đoạn liệt kê Endpoint Summary/mutation signature ("truncated, 573 more lines"); bundle §H PKG excerpt tương tự cắt trước phần Temporal workflow chi tiết. Bundle §"Paired BE REST endpoints" block bắt đầu bằng `⚠️` (keyword-match fallback level 3, cảnh báo "First-match substring may pick wrong section") cho §5.4 recalc. Author: (a) đọc bounded `Architecture/api/gf-accounting-api.md` §2 Endpoint Summary (dòng 75-80) + §5.4 full section (dòng 1484-1556) → xác nhận nội dung bundle trích đúng, không lệch section (khác các trường hợp AP-CREATE/AP-DELETE sai section ở sibling FEAT-PRC-CREATE/DELETE); (b) đọc bounded `Architecture/api/agg-garage-graph-graphql.md` §0 Wave Index (dòng 34-51, xác nhận W06→§3f) + §2 Endpoint Summary rows #360-368 (dòng 420-438) → lấy đúng verbatim signature `priceCalcRunRecalc(id: Int!, input: PriceCalcRunRecalcInput!, idempotencyKey: String!): PriceCalcRunKickoffApiResponse!` (dòng 433) và `priceCalcRunGet(id: Int!, includeItems: Boolean = true, itemStatus: [PriceCalcItemStatus!], keyword: String): PriceCalcRunDetailApiResponse!` (dòng 431, op reuse cho polling) — tránh suy đoán tên arg `runId` (dùng sai naming) hay giả định `idempotencyKey` nằm trong input type thay vì là arg riêng (theo precedent `postDeliveryV2` cùng file). §5 SDL delta viết là "reuse-only, không SDL mới" vì toàn bộ type liên quan đã ratify chung đợt W06 PRC gốc (không tách riêng theo FEAT). | F-7 discipline — bundle `⚠️` flag + nội dung bị cắt trước phần cần cite verbatim (arg signature) bắt buộc fallback đọc trực tiếp thay vì suy luận từ prose giới thiệu §G (vốn chỉ nêu tên op, không có full signature). Xác nhận đúng thực tế trước khi ghi `references_verbatim`-style citation trong spec, tránh P0 reviewer item #23 (path/arg mismatch). | `Architecture/api/gf-accounting-api.md` v24 §2 (dòng 75-80) + §5.4 (dòng 1484-1556); `Architecture/api/agg-garage-graph-graphql.md` v7.81 §0 Wave Index (dòng 34-51) + §2 Endpoint Summary rows #360-368 (dòng 420-438). |
| 2026-07-31 | W06 | feature-be | FEAT-PRC-RECALC | **§G API extract bị flag `⚠️` (gf-accounting-api.md thiếu §0 Wave Index) — fallback Read trực tiếp §5.4 để verify contract trước khi ghi §6.** Đọc `Architecture/api/gf-accounting-api.md` (offset dò 900→1400→1450, tới line 1484-1555) xác nhận nội dung §5.4 W06-4 `POST /api/v2/price-calc-runs/{id}/recalc` trong bundle khớp **verbatim 100%** với source (không lệch section như trường hợp FEAT-PRC-CREATE/FEAT-PRC-DELETE cùng batch) — bundle §G lần này an toàn dùng trực tiếp, nhưng vẫn Read gốc theo đúng quy trình F-7 vì marker `⚠️`. Đồng thời đọc `Architecture/data/gf-accounting-data-model.md` §2quater.1 xác nhận cột `run_scope VARCHAR(20)` + `source_run_id` **đã tồn tại sẵn** trong schema `price_calc_run` (không phải cột mới cần thêm) — §5 Schema delta viết theo hướng "reuse-only, dependency FEAT-PRC-CREATE" thay vì liệt kê cột NEW. | F-7 discipline — marker `⚠️` bắt buộc fallback dù nội dung cuối cùng khớp bundle, tránh false-negative do first-match keyword substring có thể trúng đúng section ở lần này nhưng không đảm bảo mọi lần; xác nhận schema tránh Critical Rule #3 (KG-first, không suy luận cột mới khi đã có trong data-model doc). | `Architecture/api/gf-accounting-api.md` v24 §5.4 (line 1484-1555); `Architecture/data/gf-accounting-data-model.md` v14 §2quater.1 (line 408-473, cột `run_scope` dòng 432 + `chk_prc_run_scope`/`chk_prc_source_run_recalc` dòng 468/471). |
| 2026-07-31 | W06 | feature-be | FEAT-PRC-RECALC | **`paired_bff_feats`/`paired_fe_web_feats`/`paired_mobile_feats` giữ nguyên `[]` theo Context Bundle add_fields** (không override như FEAT-PRC-CREATE) — khác CREATE (có route riêng), RECALC là 1 nút hành động trong màn hình `FEAT-PRC-DETAIL`, không có route/màn hình riêng; PKG-W06 §1 chỉ đếm "2/5 route cho PRC" (CREATE + DETAIL, theo precedent entry FEAT-PRC-CREATE ở trên) — RECALC/DELETE không nằm trong 2 route đó. Đồng nhất với quyết định đã lập cho sibling `FEAT-PRC-DELETE` (cùng nature: action-in-screen, không phải route riêng). §11 ghi rõ N/A + lý do thay vì fabricate file path BFF/FE-web chưa tồn tại. | Tuân thủ item #16 reciprocity (chỉ pair khi fan-out map xác nhận) + tránh reviewer reject vì trỏ file chưa tồn tại; consistency với quyết định `FEAT-PRC-DELETE` đã lập trước đó trong cùng batch (cùng lý do: action-only feature, không route riêng). | Context Bundle add_fields (`paired_*_feats: []`); precedent decision log entry `FEAT-PRC-DELETE` (dòng trên) + `FEAT-PRC-CREATE` (route reciprocity contrast); PKG-W06-inventory-pricing-stock-report.md §1 Overview "garage-web (5 route: 2 PRC + 3 report)". |
| 2026-07-31 | W06 | feature-bff | FEAT-PRC-DETAIL | **Bundle §G "Paired BE REST endpoints" block flag `⚠️` và trả sai nội dung (`FEAT-AP-DETAIL` — `GET /api/v2/accounting-periods/{id}` — thay vì PRC) — fallback Read trực tiếp `Architecture/api/gf-accounting-api.md` §5.2 (W06-2, dòng 1258-1379) + §5.4 (W06-4, dòng 1484-1555) để verify verbatim contract cho `priceCalcRunGet` + `priceCalcRunRecalc` trước khi ghi §4.5/§6.** Xác nhận khớp 100% với bundle §G "Tier-scoped Architecture context" (GraphQL SDL block, marked ✅) — chỉ phần REST-endpoint-details bị bundle generator route sai section. Đồng bộ với phát hiện của paired BE-tier (entry #27 phía trên, cùng ngày, cùng FEAT). `paired_fe_web_feats` sửa từ `[]` (Context Bundle add_fields) → `["FEAT-PRC-DETAIL"]` — PKG §Overview + source FEAT AC-1 ("Mở màn chi tiết") xác nhận rõ đây là màn hình `garage-web`; `paired_mobile_feats` giữ `[]` (PRC web-only). 404 error-sample cho `priceCalcRunGet` không có JSON cụ thể trong Architecture doc (chỉ bảng HTTP code) — suy luận theo convention `ERR-CMN-not-found` (đồng dạng endpoint chị em AP-DETAIL cùng file) và flag NEED CONFIRMATION cho Architecture Authority bổ sung sample tường minh. | F-7 discipline (KHÔNG hallucinate/trust bundle khi flag `⚠️`) — path/arg sai sẽ cascade sai xuống resolver + FE polling. Reciprocity fix theo item #16, đồng nhất với quyết định đã áp dụng cho FEAT-PRC-CREATE/FEAT-STK-LIST-V2/FEAT-IP-VIEW-V2/FEAT-STK-DETAIL-V2 (add_fields gap hệ thống ở batch W06). | `Architecture/api/gf-accounting-api.md` v24 §5.2 (dòng 1258-1379) + §5.4 (dòng 1484-1555); `PKG-W06-inventory-pricing-stock-report.md` §1 Overview + §2.2.1; precedent decision entry FEAT-PRC-DETAIL (feature-be, dòng #27 phía trên). |
| 2026-07-31 | W06 | feature-fe-web | FEAT-PRC-CREATE | **Override `consumes_bff_feats` từ `[]` (Context Bundle add_fields) sang `["FEAT-PRC-CREATE"]`** — bundle §G `agg-garage-graph-graphql.md §3f` xác nhận rõ 2 GraphQL op passthrough (`priceCalcRunCreate` mutation + `priceCalcItemsForCogsLookup` query) mà FE Web thực sự consume, và có agent song song `exec-spec-bff-prc-create` đang author paired BFF tier file cùng FEAT-ID — đối xứng với quyết định BE-tier (entry #29 phía trên, cùng FEAT). Giữ `[]` sẽ vi phạm cross-tier reciprocity gate (reviewer item #16). Đồng thời flag 3 NEED CONFIRMATION trong spec: (a) hành vi ẩn/disable section bảng khi `scope=Tất cả mã` — Figma `_negative_coverage` không vẽ state riêng, chọn "ẩn toàn bộ section" làm an toàn nhất; (b) display mode cho cảnh báo `affectedSubsequentPeriods` (AC-9b) — chọn TOAST vì Figma không có banner/alert box nào trên màn CREATE; (c) label nút "Huỷ bỏ" (Figma verbatim) khác diacritic với "Hủy bỏ" trong source FEAT AC-11 — dùng bản Figma theo §4.1 visual SSOT. | Đúng theo §1 spec agent-execution-spec-author (item #16 reciprocity — pair khi cả 2 phía batch song song đã xác nhận qua bundle §G/§I) + tránh drift với quyết định BE-tier cùng FEAT (entry #29). | Bundle `/tmp/exec-spec-bundles/W06/FEAT-PRC-CREATE.fe-web.md` §G `3f PRC` + §I Paired tiers; `Product/ux/figma-web/wave06-prc-create.md` `_negative_coverage`; precedent decision log entry #29 (FEAT-PRC-CREATE be-tier). |
| 2026-07-31 | W06 | feature-fe-web | FEAT-IP-VIEW-V2 | **Override `consumes_bff_feats` từ `[]` (Context Bundle add_fields) sang `["FEAT-IP-VIEW-V2"]`** — bundle §G `agg-garage-graph-graphql.md §3j` xác nhận rõ 2 GraphQL op passthrough (`stockInoutSummary` + `stockInoutSummaryExport`) mà FE Web thực sự consume, và có agent song song `exec-spec-bff-ip-view-v2` đang author paired BFF tier file cùng FEAT-ID (đúng đối xứng với quyết định be-tier ở dòng entry #18 phía trên, cùng FEAT). Giữ `[]` sẽ vi phạm cross-tier reciprocity gate (reviewer item #16) và tạo mismatch với be-tier đã set `paired_bff_feats=["FEAT-IP-VIEW-V2"]`. Đồng thời flag 4 NEED CONFIRMATION trong spec: (a) kho filter single vs multi-select (Figma chỉ vẽ trạng thái đã chọn 1 kho, AC-5 nói "tất cả/nhiều"); (b) route đích khi click cell "Mã SP nội bộ" (nghi vấn FEAT-STK-DETAIL-V2, Figma không vẽ); (c) vị trí + text ghi chú "cần chạy tính giá" cho AC-4 (Figma không có frame tương ứng — `coverage_gaps CONTENT-02`); (d) format số Giá trị có/không hậu tố "đ" chưa nhất quán giữa body và dòng Tổng (`coverage_gaps FORMAT-01`), ảnh hưởng cả bảng web lẫn mẫu Excel export AC-7. | Đúng theo §1 spec agent-execution-spec-author (item #16 reciprocity — pair khi cả 2 phía batch song song đã xác nhận qua bundle §G/§I) + tránh drift với quyết định be-tier cùng FEAT (entry #18). | Bundle `/tmp/exec-spec-bundles/W06/FEAT-IP-VIEW-V2.fe-web.md` §G `3j Stock V2 Reports` + §I Paired tiers; `Product/ux/figma-web/wave06-ip-view-v2.md` coverage_gaps CONTENT-01/CONTENT-02/DATA-01/FORMAT-01 + §11 Component Gaps; precedent decision log entry #18 (FEAT-IP-VIEW-V2 be-tier). |
| 2026-07-31 | W06 | feature-mobile | FEAT-STK-LIST-V2 | **Override `consumes_bff_feats` từ `[]` (Context Bundle add_fields) sang `["FEAT-STK-LIST-V2"]`** — mobile consume đúng 1 GraphQL op `stockLedgerAtDate` passthrough qua BFF `agg-garage-graph §3j` (bundle §G verified `✅ §0 Wave Index resolved for W06 → §3j`), và có agent song song `exec-spec-bff-stk-list-v2` đang author paired BFF tier file cùng FEAT-ID — đối xứng với quyết định đã lập cho be-tier + fe-web-tier cùng FEAT (entry #18). Giữ `[]` sẽ vi phạm cross-tier reciprocity (reviewer item #16). | Cùng lý do đã áp dụng nhất quán cho FEAT-STK-LIST-V2 be-tier/fe-web-tier (entry #18) — add_fields default `[]` từ orchestrator là gap fan-out-map, không phản ánh đúng data flow thật. | Bundle `/tmp/exec-spec-bundles/W06/FEAT-STK-LIST-V2.mobile.md` §G §3j.1/§3j.2 W06-STK-Q1; precedent decision log entry #18. |
| 2026-07-31 | W06 | feature-mobile | FEAT-STK-LIST-V2 | **Chọn sub-feature directory `lib/ui/inventory/stock_list/` (KHÔNG theo naming `stock_ledger_at_date` gợi ý ở warm-up report §4/§8)** — Figma spec `wave06-stk-list-v2.md` §Spacing Escapes + §Unknown-Widgets đã tự xác lập sẵn 2 path cụ thể theo pattern flat canonical (`lib/ui/inventory/stock_list/stock_list_constants.dart` + `lib/ui/inventory/stock_list/widgets/stock_product_card.dart`) — dùng nhất quán các path này làm sub-feature root thay vì warm-up report §8 "Files dự kiến" (dùng path Clean-Architecture-style `lib/features/inventory/stock_ledger_at_date/presentation/...` — đã bị chính bundle §G.Z flag là anti-pattern LL-MOB-010 "Path/router/repo Clean Architecture leak — wave03 incident"). Cũng flag NEED CONFIRMATION (§4.10) cho GraphQL warehouse-list op còn thiếu trong bundle §G (KHÔNG tự bịa op mới theo F-7). | Warm-up report task-message note "useful... nhưng do not verbatim-copy its prose" cho phép discretion; Figma spec (ACTIVE v3, đã APPROVED reviewer) là nguồn có path cụ thể hơn + đã tự tránh anti-pattern đã biết (LL-MOB-010), nên ưu tiên path đó làm canonical thay vì warm-up report (vốn chỉ là advisory execution-plan draft, tự thừa nhận dùng path Clean-Architecture legacy trong §8). | `Product/ux/figma-mobile/wave06-stk-list-v2.md` §Spacing Escapes (line 113-125) + §Unknown-Widgets (line 949-960); `Tracking/warm-up/WAVE06/W06-garage-mobile-warm-up-phaseA.md` §8 (path cũ); bundle §G.Z `LL-MOB-010`. |
| 2026-07-31 | W06 | feature-bff | FEAT-PRC-DELETE | **Xác nhận bundle §G "Paired BE REST endpoints" flag `⚠️` là đúng (keyword-match `Delete` trỏ nhầm §4.6 `FEAT-AP-DELETE` accounting-period) — fallback Read trực tiếp `Architecture/api/gf-accounting-api.md` §5.5 (dòng 1557-1591, v24) để verify contract W06-5 `DELETE /api/v2/price-calc-runs/{id}` trước khi ghi §6.** Đồng thời bundle §G "GraphQL SDL §relevant" (block ✅ §0 Wave Index resolved) bị generator truncate trước đoạn Endpoint Summary — không thấy op name thật cho mutation delete; fallback Read bounded `Architecture/api/agg-garage-graph-graphql.md` §0 Wave Index (dòng 34-51, xác nhận W06→§3f) + §2 Endpoint Summary row #364 (dòng 434) → xác nhận verbatim `priceCalcRunDelete(id: Int!): PriceCalcRunDeleteApiResponse!` — **KHÔNG có `idempotencyKey` arg** (khác 3 mutation PRC khác cùng module), khớp với REST §5.5 Semantics "idempotent tự nhiên, repeat → 200 cached". Đối xứng với phát hiện của paired BE-tier (entry #22 phía trên, cùng ngày, cùng FEAT). Giữ nguyên `paired_fe_web_feats`/`paired_mobile_feats: []` theo Context Bundle add_fields (đối xứng entry #23 BE-tier) — §11 flag rõ mutation dự kiến được FEAT-PRC-LIST fe-web tier gọi (action nhúng trong list, không route riêng), không tự suy luận reciprocity ngược. | F-7 discipline (KHÔNG hallucinate/trust bundle khi flag `⚠️` hoặc nội dung bị truncate) — endpoint/op-name sai sẽ cascade sai xuống resolver + FE. Đồng nhất với quyết định đã áp dụng cho các sibling FEAT-PRC-CREATE/DETAIL/RECALC cùng batch W06 (add_fields gap + bundle §G mismatch là pattern hệ thống, không phải lỗi riêng lẻ). | `Architecture/api/gf-accounting-api.md` v24 §5.5 (dòng 1557-1591); `Architecture/api/agg-garage-graph-graphql.md` v7.81 §0 Wave Index (dòng 34-51) + §2 Endpoint Summary row #364 (dòng 434); precedent decision entries FEAT-PRC-DELETE be-tier (#22/#23), FEAT-PRC-DETAIL bff-tier (#34). |
| 2026-07-31 | W06 | orchestrator | FEAT-PRC-DELETE, FEAT-PRC-RECALC | **Supersede entry (NC-W06-OV-004, raised by overview author)**: entries earlier in this log recorded a decision to keep `paired_bff_feats`/`paired_fe_web_feats: []` for these 2 FEAT's be-tier (và bff-tier cho RECALC) do tại thời điểm author, sibling tier file chưa được xác nhận tồn tại trong cùng batch. Sau khi cả 25 FEAT-tier author hoàn tất song song, main-agent chạy 1 reconciliation pass (script kiểm reciprocity toàn bộ 4 tier) và xác nhận: `be/FEAT-PRC-DELETE.md`, `bff/FEAT-PRC-DELETE.md`, `fe-web/FEAT-PRC-DELETE.md` đều tồn tại → set `paired_bff_feats`/`paired_fe_web_feats: ["FEAT-PRC-DELETE"]` cả 2 phía be+bff, `consumes_bff_feats: ["FEAT-PRC-DELETE"]` phía fe-web. Tương tự `be/FEAT-PRC-RECALC.md` + `bff/FEAT-PRC-RECALC.md` → set `paired_fe_web_feats: ["FEAT-PRC-RECALC"]` cả 2 phía (fe-web tier file tồn tại, xác nhận bởi `exec-spec-fe-prc-recalc` cùng batch). Đồng thời phát hiện + fix thêm 5 gap tương tự qua script sweep toàn bộ 25 file (be/FEAT-STK-LIST-V2 x2 field, bff/FEAT-PRC-LIST, bff/FEAT-STK-DETAIL-V2, fe-web/FEAT-PRC-DELETE) — sweep thứ 2 xác nhận 0 issue còn lại trên toàn bộ 25 file. | Reciprocity gate (reviewer item #16) yêu cầu field đối xứng đúng thực tế fan-out, không phải add_fields mặc định lúc spawn — vì 25 author chạy song song, một số agent hoàn thành trước khi sibling tier file của cùng FEAT tồn tại nên buộc phải giữ `[]` tại thời điểm đó và flag để reconcile sau. | `Execution/wave-specs/W06/Product/features/{be,bff,fe-web}/FEAT-PRC-DELETE.md`, `{be,bff}/FEAT-PRC-RECALC.md`; overview finding NC-W06-OV-004 (`Execution/wave-specs/W06/_wave-overview.md` §7). |
| 2026-07-31 | W06 | orchestrator | 8 FEAT (25 tier files) | **§1 cross-tier byte-equal reconciliation (item #18c)**: `scripts/check-fanout-consistency.py` flagged 8/8 FEAT với §1 nội dung khác nhau giữa be/bff/fe-web/mobile — do main-agent dispatch 25 tier author song song trong cùng batch (không staggered BE-first), nên mỗi agent tự viết §1 riêng thay vì copy byte-equal từ sibling tier file (per agent's own §3.3 rule, chỉ khả thi khi sibling đã DRAFT tại thời điểm author chạy). Fix: script đối chiếu `be` tier §1 làm canonical, sync byte-equal sang bff/fe-web/mobile cho cả 8 FEAT (16 file patched, be-tier giữ nguyên). Re-verify: 8 → 0 lỗi `#18c`. | Policy v2 (checklist item #18c) yêu cầu §1 byte-equal cross-tier — song song hoá batch orchestration là root cause, không phải lỗi nội dung từng agent (mỗi bản đều đúng nghiệp vụ, chỉ khác cách diễn đạt). | `scripts/check-fanout-consistency.py --json` output trước/sau; `Execution/wave-specs/W06/Product/features/{be,bff,fe-web,mobile}/*.md` §1. |
| 2026-07-31 | W06 | contract-sign | 8 consumer×contract pairs re-sign (same scope as row above) | **RE-SIGN — hash drift phát hiện qua `/spawn-contract-steward SIGN` sau khi các Architecture doc W06 tiếp tục edit sau lần sign đầu.** `verify` trước re-sign cho thấy DRIFT tại `bff-experiences/agg-garage-graph` (`gf-inventory-api.md`) + `boundaries/gf-accounting` (`gf-inventory-api.md`). Chạy `contract-sign.py sign` cho đủ 8 pair (cùng danh sách row trên): 3 pair đổi hash → re-signed (ACTIVE mới, entry cũ SUPERSEDED); 5 pair hash unchanged → NOOP (giữ nguyên ACTIVE). Post-sign verify: `bff-experiences/agg-garage-graph` OK (5/5), `web-experiences/garage-web` OK (3/3), `mobile-experiences/garage-mobile` OK (2/2). `boundaries/gf-accounting` + `boundaries/gf-inventory` vẫn còn DRIFT nhưng **ngoài phạm vi 8-pair W06 scope** (vd `gf-sales-events.md`, `gf-accounting-api.md`/`gf-inventory-api.md`/`gf-inventory-data-model.md` self-track, `gf-inventory-events.md`, `INTEG-EXT-gf-inventory.md`) — KHÔNG sign vì `contract-scope.yaml` không list các cặp này (per agent forbidden action + chỉ đạo row trên "không mở rộng sang các cặp consumer khác không liên quan W06"). | Tránh false-negative gate: nếu không re-sign, `/dev-handoff agg-garage-graph` sẽ fail verify dù contract đã đúng baseline W06 hiện hành. Giữ nguyên nguyên tắc minimal-scope đã thiết lập ở row trên — drift ngoài 8-pair là pre-existing/không liên quan W06, cần CR riêng nếu muốn mở rộng scope ratify. `CONTRACT-MAP.md` KHÔNG regen — chưa có tiền lệ file này ở bất kỳ wave nào trong repo (W02-W05 cũng không có), giữ nguyên trạng thái hiện tại thay vì tự bịa format mới không rõ nguồn. | `Execution/wave-specs/W06/contract-scope.yaml`, decision row trên (2026-07-31 contract-sign 8-pair), `scripts/contract-sign.py verify` output (pre/post re-sign). |
| 2026-07-31 | W06 | mobile-feature | FEAT-STK-LIST-V2 (mobile tier) | **RESOLVE §4.10 Open item "Warehouse dropdown datasource" — ratify `searchWarehouses` (op #305)** cho dropdown "Kho" ở `StockListFilterPage`. User sonhoang chỉ ra dropdown Kho ở filter W06 mobile chắc chắn gọi `searchWarehouses` vì op này đã dùng ở wave trước; main agent tra cứu xác nhận: op #305 pre-existing trong `Architecture/api/agg-garage-graph-graphql.md`, đã được mobile dùng production từ `FEAT-OB-LIST` W04 §AC-5c (cùng pattern bottom-sheet filter + dropdown Kho paginated) và có mặt trong `INTEG-MOB-garage-mobile-agg-garage-graph.md` (Supplier/warehouse/product flow row) — mobile đã có sẵn `WarehouseRepository`. Cascade edit `Execution/wave-specs/W06/Product/features/mobile/FEAT-STK-LIST-V2.md` v1→v3 (fix luôn front-matter version drift 1 vs Change Log đã ở v2): §4.10 RESOLVED (không còn NEED CONFIRMATION), §3 AC-4 thêm GraphQL op + paginated dropdown state, §6.1 thêm row `searchWarehouses`, §7 file map thêm row REUSE `warehouse_repository.dart` (0 LoC — không tạo file mới) + bump LoC ước tính `stock_list_filter_cubit.dart`. | Đóng gap đã flag NEED CONFIRMATION trong execution spec bằng bằng chứng parity có sẵn trong repo (F-7 anti-hallucination — không suy đoán, dẫn chiếu đúng precedent AC-5c) thay vì để DEV tự grep runtime hoặc raise CR không cần thiết. Không phải AC/scope change nên không cần full re-review — chỉ đóng open item. | `Execution/wave-specs/W04/Product/features/mobile/FEAT-OB-LIST.md §AC-5c`, `Architecture/integrations/INTEG-MOB-garage-mobile-agg-garage-graph.md` (Supplier/warehouse/product flow row), `Architecture/api/agg-garage-graph-graphql.md` op #305 `searchWarehouses`. |
| 2026-07-31 | W06 | orchestrator | CR-20260731-03 + FEAT-PRC-CREATE/FEAT-PRC-RECALC (be) | **Post-ACTIVE audit fix — endpoint bịa/nhầm boundary cho "resolve mã BQGQ Đang hoạt động".** User (sonhoang) yêu cầu kiểm tra riêng wave-spec BE+BFF; audit (2 subagent song song, verified thủ công qua grep trực tiếp vào Architecture doc) phát hiện `FEAT-PRC-CREATE.md`/`FEAT-PRC-RECALC.md` (be-tier) cite `GET /internal-products/search (gf-erp-mdm)` cho AC-4/AC-5/AC-6b/§4.1/§6.4 — endpoint này **không tồn tại**: `gf-erp-mdm-api.md` không có route `internal-products` nào (29 endpoint, toàn `/catalog*`/`/dynamic-data/*`/`/master-data`); catalog InternalProduct thật sự thuộc **`gf-inventory`** (`V2-7 POST /api/v2/internal-products/search`, khớp đúng BR-PRC-009's own text "danh mục vật tư hàng hóa (Mã sản phẩm nội bộ)"), nhưng endpoint đó lại **không có filter/response field `pricingMethod`** trước đây (chỉ có ở `V2-8` detail) — nên sửa boundary thôi chưa đủ, cần mở rộng contract thật. **Fix 2 lớp**: (1) `Architecture/api/gf-inventory-api.md` v76→v77 (CR-20260731-03, MINOR self-approved, additive-only) — thêm `pricingMethod` filter (request) + field (response) vào `V2-7`; (2) `FEAT-PRC-CREATE.md`/`FEAT-PRC-RECALC.md` be-tier sửa toàn bộ 8+5 vị trí cite endpoint đúng (`POST /api/v2/internal-products/search`, boundary `gf-inventory`), merge `GfErpMdmClient` vào `GfInventoryClient` (§7), bổ sung row thiếu `POST /protected/v1/receipt-lines/bulk-inherit-cost` (W06-P4, đã dùng ở AC-7 nhưng chưa liệt kê §6.4), sửa "Hand-off tới BFF" note dùng sai tên GraphQL op (`runPriceCalc`/`getItemsForCogs` → `priceCalcRunCreate`/`priceCalcItemsForCogsLookup`, verbatim #362/#365). Flag **NEED CONFIRMATION** mới (chưa tự quyết): S2S auth mechanism cho `V2-7` — mọi S2S consumer khác của `gf-accounting` gọi `/protected/v1/*` với `x-api-key`, còn `V2-7` nằm namespace public `/api/v2/*` (`Auth: authenticated`, thường hiểu JWT qua gateway) — chưa xác nhận `V2-7` có accept `x-api-key` S2S hay cần route `/protected/v1/` riêng; để Architecture Authority xác nhận trước DEV, không tự suy đoán (tránh lặp lại lỗi F-7 vừa fix). Cả 2 file version 1→3 (đồng thời sync frontmatter `version` khớp Change Log — bug 3-in-1 riêng, xem entry version-hygiene dưới). | Root cause: khi author gốc gặp bundle §G bị flag `⚠️`/stale, đã tự suy đoán tên endpoint theo convention thay vì fallback Read trực tiếp Architecture doc (đúng ra phải làm theo F-7 discipline mà các entry #34/#35/#36/#39/#44 phía trên đã áp dụng đúng cho các trường hợp tương tự cùng batch — trường hợp này bị sót). User yêu cầu "xử lý tất cả các vấn đề đó" sau khi nhận báo cáo audit — chủ động sửa cả wave-spec lẫn Architecture contract gốc (thay vì chỉ patch chỗ tiêu thụ) vì thiếu field là gap thật, không phải lỗi trích dẫn thuần túy. | `Architecture/api/gf-inventory-api.md` v77 §3a.2 V2-7 + Change Log; `Architecture/api/gf-erp-mdm-api.md` (không có `internal-products`, verified negative); `Product/business-rules/BR-GF-INVENTORY-ACCOUNTING-PERIOD.md` §2.2 BR-PRC-009/012; `Execution/wave-specs/W06/Product/features/be/FEAT-PRC-CREATE.md` v3, `FEAT-PRC-RECALC.md` v3. |
| 2026-07-31 | W06 | feature-be | FEAT-STK-DETAIL-V2 (be) | **Post-ACTIVE audit fix — sai BR-ID citation cho công thức bảo toàn cân bằng.** AC-6 (dòng Tổng)/§4.1/§9 cite `BR-STKV2-010` (CORNERSTONE, formula `openingValue + inboundValue − outboundValue = closingValue`) nhưng rule đó thực ra `Features=FEAT-IP-VIEW-V2` (báo cáo NXT, domain khác — `BR-GF-INVENTORY-STOCK-V2.md` §2.3). Rule đúng cho Thẻ kho là `BR-STKV2-014` (`Features=FEAT-STK-DETAIL-V2`, "Dòng Tổng... GT Cuối kỳ = GT Đầu + GT Nhập" khi GT Xuất=0, §2.4) — AC-3 (per-dòng running balance) đã cite đúng `BR-STKV2-013` từ trước, không đổi. Fix: AC-6 + §4.1 sửa citation; §9 mapping table merge touchpoint AC-6 vào row `BR-STKV2-014` sẵn có (severity NORMAL → CORNERSTONE), xóa row `BR-STKV2-010` sai. Version 1→3. | Phát hiện qua audit song song (subagent cross-check BR file §2.4 Features column vs citation trong tier spec) theo yêu cầu user kiểm tra wave-spec BE+BFF. Đây là lỗi trích dẫn thuần túy (rule đúng đã tồn tại sẵn trong BR file, không cần Architecture cascade) — khác trường hợp FEAT-PRC-CREATE/RECALC (endpoint thật sự thiếu field). | `Product/business-rules/BR-GF-INVENTORY-STOCK-V2.md` §2.3 (BR-STKV2-010, Features=FEAT-IP-VIEW-V2) + §2.4 (BR-STKV2-013/014, Features=FEAT-STK-DETAIL-V2); `Execution/wave-specs/W06/Product/features/be/FEAT-STK-DETAIL-V2.md` v3. |
| 2026-07-31 | W06 | feature-bff | FEAT-STK-LIST-V2, FEAT-IP-VIEW-V2, FEAT-STK-DETAIL-V2 (bff) | **Post-ACTIVE audit fix — sai arg-shape cho 3 GraphQL export op.** Cả 3 file bff-tier khai args export dạng scalar rời hoặc tái dùng nhầm input type của query non-export (`stockLedgerAtDateExport` dùng `StockLedgerAtDateInput!`; `stockInoutSummaryExport` dùng 5 scalar rời; `stockCardDetailExport` dùng 4 scalar rời + "Decision note" biện minh "không có input type riêng trong bundle §G"), nhưng ratified Architecture (`agg-garage-graph-graphql.md:51335-51363`, v7.75, hiện file ở v7.82) **thật sự có** 3 type riêng: `StockLedgerAtDateExportInput`/`StockInoutSummaryExportInput`/`StockCardDetailExportInput` — bundle chỉ bị generator truncate trước đoạn định nghĩa type, không phải Architecture thiếu. Đây đúng lỗi F-7 anti-hallucination mà quy trình được thiết kế để chặn (§G bắt đầu `✅`/không rõ flag nhưng nội dung bị cắt → phải fallback Read gốc, không tự suy đoán/"reuse gần đúng"). Fix cả 3 file: thêm row type mới ở §5.1 SDL delta, sửa §6.1 args column + mọi mention khác trong §2/§3 dùng type đúng, xóa "Decision note" sai (STK-DETAIL-V2) thay bằng "Correction" ghi rõ nguồn gốc lỗi. Cả 3 version 1→3. | Phát hiện qua audit subagent (verify trực tiếp bằng grep vào `agg-garage-graph-graphql.md` dòng 51327-51363 — không dựa vào bundle cũ) theo yêu cầu user kiểm tra wave-spec BE+BFF. Nếu implement theo spec cũ, resolver sẽ build sai GraphQL schema (arg type mismatch) — lỗi này chỉ lộ ra khi build/test BFF, không phải lúc review doc. | `Architecture/api/agg-garage-graph-graphql.md` v7.82 (dòng 51327-51363, SDL export ops + 3 input type, giới thiệu từ v7.75); `Execution/wave-specs/W06/Product/features/bff/FEAT-STK-LIST-V2.md` v3, `FEAT-IP-VIEW-V2.md` v3, `FEAT-STK-DETAIL-V2.md` v3. |
| 2026-07-31 | W06 | orchestrator | 16 be+bff tier files | **Version-hygiene fix (Critical Rule #9, 3-in-1) — frontmatter `version` không khớp Change Log.** Audit phát hiện toàn bộ 8 be-tier + 8 bff-tier file có `version: 1` trong frontmatter dù Change Log đã có row "Version 2" (status DRAFT→ACTIVE, do orchestrator ACTIVE-bump thêm sau khi author gốc chỉ set version=1) — frontmatter chưa từng được sync theo row đó. Fix: 6 file có content fix thật (2 be PRC-CREATE/RECALC, 1 be STK-DETAIL-V2, 3 bff export-arg-shape) bump `version: 1 → 3` kèm Change Log row 3 mới mô tả fix; 10 file còn lại (be: PRC-LIST/PRC-DETAIL/PRC-DELETE/STK-LIST-V2/IP-VIEW-V2; bff: PRC-LIST/PRC-CREATE/PRC-DETAIL/PRC-RECALC/PRC-DELETE) không có content bug — chỉ bump `version: 1 → 2` để khớp row 2 sẵn có, không cần thêm row mới (row 2 đã mô tả đúng sự kiện). | Vi phạm 3-in-1 phát hiện qua audit toàn diện — version phải di chuyển cùng lúc với Change Log entry, không tách rời (Critical Rule #9). Sửa dạng batch cho cả 16 file cùng lúc để tránh phải quay lại sau. | 16 file `Execution/wave-specs/W06/Product/features/{be,bff}/*.md` frontmatter `version` field. |
| 2026-07-31 | W06 | architecture | agg-garage-graph-graphql.md (Stock V2 Reports) | **Fix GAP-1 (field coverage, thật) raised by user (sonhoang) via manual gap review**: `StockLedgerAtDateRow`/`StockInoutSummaryRow`/`StockCardDetailContext` chỉ có `mainUnitCode` raw code (pattern "CAI"/"PCS" per `gf-inventory-data-model.md`/`gf-inventory-api.md §5.2`), không đủ để FE render cột "ĐVT chính"/"ĐVT" display name tiếng Việt như Figma vẽ (`wave06-stk-list-v2.md:1398,1406` verbatim "Cái"). §3j.3 khai "Passthrough thuần" nên không có bước enrichment nào cho field này, khác precedent `InternalProduct.mainUnitDisplayName` (§3d.3, `BR-AGG-GARAGE-GRAPH-001`) đã có sẵn cùng file. **Fix**: thêm `mainUnitDisplayName: String` (nullable) vào cả 3 SDL type (mở rộng ra `StockInoutSummaryRow`/`StockCardDetailContext` ngoài phạm vi GAP-1 gốc — verified Figma 2 màn kia cũng có cùng gap hệ thống, tránh để lại duplicate gap dễ đoán trước); thêm bullet enrichment mới ở §3j.3 (batch distinct `mainUnitCode` → `gf-erp-mdm directory=UNIT` → reuse in-process cache 5min TTL có sẵn từ `catalog-v2.enrichment.ts`); sync sample query/response Q1/Q2. Version bump v7.81 → v7.82 (additive, non-breaking). **KHÔNG đụng** `gf-inventory-api.md` (REST layer giữ nguyên raw code — enrichment là BFF-side only, tương tự cách BFF tự enrich `mainUnitDisplayName` cho catalog V2, không cần BE thay đổi). Refreshed contract hashes cho 3 consumer (`bff-experiences/agg-garage-graph` self-sign, `web-experiences/garage-web`, `mobile-experiences/garage-mobile`) qua `contract-sign.py refresh-hashes` (cosmetic/additive drift, không cần re-ratify theo tool's own semantic). | Đảm bảo GraphQL schema đủ data cho FE render đúng UI đã Figma-verify thay vì FE phải tự hardcode map code→display hoặc hiển thị raw code sai UX; mirror pattern enrichment đã established cho catalog V2 tránh tạo 2 cách làm khác nhau cho cùng 1 nhu cầu (unit display name) trong cùng BFF. | `Architecture/api/agg-garage-graph-graphql.md` v7.82 §3j.1/§3j.3 + Change Log; `Product/ux/figma-web/wave06-stk-list-v2.md:1398-1406`, `wave06-ip-view-v2.md:570-571`, `wave06-stk-detail-v2.md:533`; `Architecture/api/gf-inventory-api.md §5.2` + `gf-inventory-data-model.md:329` (mainUnitCode raw canonical); §3d.3 `InternalProduct.mainUnitDisplayName` precedent. |
| 2026-07-31 | W06 | contract-sign | gf-inventory-api.md :: bff-experiences/agg-garage-graph | **Resolve FM-CONTRACT-DRIFT — `gf-inventory-api.md` v76→v77 (CR-20260731-03, thêm `pricingMethod` filter/field vào `V2-7`, additive) đã bump SAU lần sign gần nhất (07:50:25Z) mà không kèm refresh-hashes, phát hiện qua `/prefetch...` — thực ra qua user (sonhoang) yêu cầu đọc `bffs/agg-garage-graph` đối chiếu sync W06.** `contract-sign.py verify bff-experiences/agg-garage-graph` báo DRIFT (signed `a9a574b7...` ≠ current `8a76f45f...`). Fix: (1) `contract-sign.py refresh-hashes bff-experiences/agg-garage-graph Architecture/api/gf-inventory-api.md` — ACTIVE entry hash cập nhật `a9a574b7...→8a76f45f...`; verify lại → **OK: all 5 contract(s) verified**. (2) `manifest-rebuild.py bff-experiences/agg-garage-graph` — regenerate `MANIFEST.md` §3 Contracts table (hash `gf-inventory-api.md` + `agg-garage-graph-graphql.md` cả 2 đều đang stale từ trước, nay đồng bộ theo signature file mới nhất). (3) Mirror `MANIFEST.md` xuống `bffs/agg-garage-graph/MANIFEST.md` (cp, đúng cơ chế `sync-docs-to-services.sh` dùng cho service repo). Đây là additive/non-breaking change (CR-20260731-03 đã self-approve MINOR) nên refresh-hashes là đủ, không cần re-ratify/ack riêng từ consumer. **Không đụng** drift khác không liên quan `agg-garage-graph` (`boundaries/gf-accounting` còn 3 drift khác — `gf-sales-events.md`/`gf-accounting-api.md`/`gf-accounting-events.md` — thuộc sở hữu boundary khác, giữ nguyên theo tiền lệ entry #10 phía trên). | User yêu cầu trực tiếp "Xử lý Gap giúp tôi... Cần sửa ở design và ở bffs/agg-garage-graph" sau khi được báo cáo gap. Refresh-hashes (không phải re-sign đầy đủ) vì thay đổi đã có CR MINOR self-approved từ trước và là additive-only — khớp tiền lệ entry #53 xử lý cùng loại việc cho `agg-garage-graph-graphql.md`. | `scripts/contract-sign.py refresh-hashes` output; `scripts/manifest-rebuild.py` output; `bff-experiences/agg-garage-graph/MANIFEST.md` diff; `bffs/agg-garage-graph/MANIFEST.md` (mirrored copy). |
| 2026-08-01 | W06 | contract-sign | agg-garage-graph-graphql.md + gf-inventory-api.md :: bff-experiences/agg-garage-graph | **Resolve FM-CONTRACT-DRIFT (round 2) — 2 contract bump SAU lần refresh gần nhất (entry #53/#54, 2026-07-31) mà không kèm refresh-hashes, phát hiện tại `/warm-up agg-garage-graph --phase B`.** `agg-garage-graph-graphql.md` v7.82→v7.83 (CR-20260731-04, MODERATE APPROVED — `StockInoutSummaryRow` +`productId: Int` nullable additive, BFF-anticipatory chưa round-trip) và `gf-inventory-api.md` v77→v78 (CR-20260801-01, MODERATE APPROVED — `stockInoutSummary` (W06-STK-Q2) response `content[]` +`productId` round-trip follow-up, đóng gap CR trước). `contract-sign.py verify bff-experiences/agg-garage-graph` báo DRIFT cả 2 file (signed `a7a6664ce07c...`≠current `3db1a1977ae7...` cho graphql; signed `8a76f45fc5a2...`≠current `243141f07327...` cho gf-inventory-api). Fix: (1) `contract-sign.py refresh-hashes bff-experiences/agg-garage-graph Architecture/api/agg-garage-graph-graphql.md` + tương tự cho `gf-inventory-api.md` — verify lại → **OK: all 5 contract(s) verified**. (2) `manifest-rebuild.py bff-experiences/agg-garage-graph` regenerate `MANIFEST.md`. (3) Mirror `cp bff-experiences/agg-garage-graph/MANIFEST.md → bffs/agg-garage-graph/MANIFEST.md` (service repo copy đang stale, đã diff-verify identical sau cp). Cả 2 CR đã APPROVED + resolved:true trong `STATE.json cr_log[]`, additive-only, doc-only trong scope CR (code implementation do user tự làm ở BFF resolver sau) — refresh-hashes đủ, không cần re-ratify. **Không đụng**: nội dung 2 file (chỉ hash signature), `contract-scope.yaml` (không đổi danh sách contract), drift khác ngoài `agg-garage-graph` scope. | Third occurrence of cùng pattern (entry #53 rồi #54) — mỗi lần Architecture doc bump sau ratify mà quên refresh-hashes thì `/dev-start` gate sẽ FAIL cứng ở `contract-sign.py verify`; warm-up Phase B (pre-DEV gate) là điểm chặn đúng lúc để bắt trước khi block thật sự xảy ra ở `/dev-handoff`. | `scripts/contract-sign.py refresh-hashes` output (2 lần); `scripts/manifest-rebuild.py` output; `bff-experiences/agg-garage-graph/MANIFEST.md` diff; `bffs/agg-garage-graph/MANIFEST.md` (mirrored, diff-verified identical); `Tracking/warm-up/WAVE06/W06-agg-garage-graph-warm-up-phaseB.md` GAP-W06-AGG-05. |
| 2026-08-01 | W06 | warm-up-fix | `gf-accounting-api.md` §5.2 + `bff/FEAT-PRC-DETAIL.md` §4.5 | **Đóng NEED CONFIRMATION cuối cùng của W06 bff-tier (budget 1/5 → 0/5) qua CR-20260801-02 (MINOR, self-approved).** Mandatory `unresolved-need-confirmation` grep pass tại `/warm-up agg-garage-graph --phase B` phát hiện 1 live hit: `FEAT-PRC-DETAIL.md:210,217` — 404 case của `priceCalcRunGet` không có error code tường minh trong `gf-accounting-api.md §5.2` (bảng "Response 4xx/5xx" chỉ có HTTP code + mô tả nguyên nhân), spec phải suy luận theo endpoint chị em `GET /api/v2/accounting-periods/{id}` (§4.3 cùng file). Fix: thêm đúng 1 dòng prose sau bảng 4xx/5xx §5.2 mirror verbatim style §4.3 — `**Not found** → 404 ERR-CMN-not-found` + tenant mismatch KHÔNG leak existence cross-tenant + soft-deleted log trả cùng error code (nhất quán §5.1 LIST soft-delete exclusion v23). `gf-accounting-api.md` v24→v25; `FEAT-PRC-DETAIL.md` (bff) v2→v3 (§4.5 row đổi wording inference → citation ratified, callout NEED CONFIRMATION → RESOLVED, 3 citation v24→v25 + line range +2). **Verify quan trọng khi apply**: brief triage mô tả §4.3 là "có sẵn **sample JSON**" — thực tế §4.3 là **1 dòng prose**, KHÔNG phải JSON block; đã đọc trực tiếp trước khi sửa nên fix bám hiện trạng thay vì bịa JSON error-body chưa từng tồn tại (tránh tạo precedent format mới cho toàn bộ error contract của boundary). Suy luận gốc của spec verify là ĐÚNG → mapping `404 → NOT_FOUND_ERROR` giữ nguyên, chỉ nâng provenance inference → ratified. | Rule mandatory của warm-up: gap `unresolved-need-confirmation` KHÔNG có defer chính thức trong `FEAT-PRC-DETAIL §11 Cross-tier coordination` hay `PKG-W06 §2.3 Out of Scope` thì phải escalate/resolve, không được tự NOT_A_GAP. User sonhoang triage trực tiếp qua chat: "CONFIRMED — proceed với option 1 ... CR level MINOR (self-approve) — documentation clarification thuần túy, không đổi API shape/behavior". Phân loại MINOR (khác CR-20260731-04/CR-20260801-01 cùng chạm `Architecture/api/*` nhưng là MODERATE): 2 CR kia **thêm field mới** vào response contract → cascade SDL/resolver/FE; CR này KHÔNG đổi field/shape/behavior/error code nào. | `Tracking/warm-up/WAVE06/W06-agg-garage-graph-warm-up-phaseB.md` §6 `GAP-W06-AGG-06`; `Tracking/CHANGE-REQUESTS.md` CR-20260801-02; `Execution/STATE.json cr_log[]`; `Architecture/api/gf-accounting-api.md` v25 §5.2 + §4.3 (style precedent) + v23 Change Log (soft-delete filter fix). |
| 2026-08-02 | W06 | cr-apply | FEAT-PRC-DETAIL (be) | **GAP-W06-GAC-05 / CR-20260801-05: giữ nguyên `FEAT-PRC-DETAIL` AC-2 (in-place semantics theo AC hiện tại), chưa đổi ADR-027 / `FEAT-PRC-RECALC` (new-row model)** — quyết định của Delivery (sonhoang), 2026-08-02. Ưu tiên implement đúng AC như đang viết. ⚠️ **Lưu ý: 2 spec vẫn còn tension chưa resolve kỹ thuật** — `FEAT-PRC-DETAIL` AC-2 nói RECALC ghi đè `executedBy`/`executedAt`/`status` trên row hiện tại, còn `FEAT-PRC-RECALC` §2 + ADR-027 §4 nói RECALC luôn tạo row mới không mutate run gốc. Nếu phát sinh vấn đề khi DEV, raise CR mới điều chỉnh. | Delivery Authority chọn không phân xử ngay trong batch CR này; rủi ro đã được ghi nhận đầy đủ tại CR-20260801-05 (level MAJOR, data-corruption risk nếu implement nhầm hướng in-place trên run gốc). | `Tracking/CHANGE-REQUESTS.md` CR-20260801-05; `Execution/wave-specs/W06/Product/features/be/FEAT-PRC-DETAIL.md` AC-2; `FEAT-PRC-RECALC.md` §2; `Architecture/decisions/ADR-027-*.md` §4; sibling đã fix `CR-20260801-12` (data-model §2quater.2). |
| 2026-08-02 | W06 | cr-apply | 6 endpoint PRC (be) | **CR-20260801-08 APPROVED — dual persona (BR-AP-CMN-002) enforce qua pattern hiện hữu: `@FeatureOn` class-level + tenant/branch scoping. KHÔNG thêm `@PreAuthorize`/`@Secured`/`@EnableMethodSecurity` cho W06 PRC endpoints.** `FEAT-PRC-CREATE` AC-12 Failure mode đã sửa: bỏ "role khác 2 role trên → 403"; 403 chỉ còn từ tenant mismatch hoặc feature-flag OFF (semantics CR-20260707-02). | Boundary `gf-accounting` hiện có **0** method-security annotation (`grep @PreAuthorize\|@Secured\|hasAnyAuthority\|EnableMethodSecurity` → 0 hit); precedent gần nhất `AccountingPeriodController` chỉ có `@RestController` + `@RequestMapping` + `@FeatureOn`. Critical Rule #6 quy định hệ thống chỉ có 2 persona và AC-12 tự nói 2 persona ngang quyền → tập role hợp lệ = toàn bộ tập role tồn tại → role gate là no-op, chỉ thêm bề mặt lỗi. Đưa method security vào boundary lần đầu sẽ là quyết định kiến trúc mới cần ADR + cascade 5 controller hiện hữu, không quyết lẻ trong W06. | `Tracking/CHANGE-REQUESTS.md` CR-20260801-08; `services/gf-accounting/src/main/java/com/actechx/gf/adapter/controller/AccountingPeriodController.java`; `FEAT-PRC-CREATE.md` AC-12; `FEAT-PRC-LIST.md` §4.2; `FEAT-PRC-RECALC.md` AC-5; `CR-20260707-02`. |
| 2026-08-02 | W06 | cr-apply | FEAT-PRC-CREATE / FEAT-PRC-DELETE (be) | **CR-20260801-06 APPROVED — package convention chốt cho W06 theo hiện trạng repo `gf-accounting` + precedent `gf-sales`**: JPA entity/repository/mapper → `infrastructure/persistence/{entity,jpa,repository,mapper}/` (KHÔNG `adapter/persistence/`); Temporal workflow/activity → top-level `com/actechx/gf/workflow/` + `workflow/impl/` + `workflow/activity/` (KHÔNG `app/workflow/` + `app/activity/`). §7 File impact map của 2 file đã sửa + thêm blockquote ghi chú. | Repo thật **không có** `adapter/persistence` (`adapter/` chỉ gồm `{client, config, controller}`); precedent Temporal `services/gf-sales/src/main/java/com/actechx/gf/workflow/{activity,impl}/`. Làm theo spec cũ sẽ tạo ~26 file ở package song song cùng chức năng. Ghi nhận trung thực: `rules-backend` §1 canonical **cũng** ghi `adapter/persistence/` — repo đã lệch canonical từ trước W06; CR này chỉ chốt hướng cho W06, KHÔNG refactor lịch sử, reconcile canonical là follow-up sau wave. | `Tracking/CHANGE-REQUESTS.md` CR-20260801-06; `FEAT-PRC-CREATE.md` §7; `FEAT-PRC-DELETE.md` §7; `.claude/skills/rules-backend/SKILL.md` §1 (canonical lệch — follow-up). |
| 2026-08-02 | W06 | cr-apply | V2-7 `POST /api/v2/internal-products/search` | **CR-20260801-03 APPROVED — option (a): V2-7 chấp nhận `x-api-key` (S2S) bên cạnh `authenticated` (JWT).** `GfInventoryClient` dùng **một** cơ chế auth duy nhất cho cả 6 call; header `X-Tenant-Id` bắt buộc (api-key không mang tenant claim). KHÔNG thêm route `/protected/v1/internal-products/search`. 3 cờ NEED CONFIRMATION đã gỡ (`FEAT-PRC-CREATE.md:249,324`, `FEAT-PRC-RECALC.md:261`). | Tránh 2 cơ chế auth trong cùng 1 client, và tránh rủi ro JWT hết hạn khi call chạy trong Temporal activity ngoài request scope (run PRC convergent iteration tới 100 vòng — BR-PRC-017; token chết thì retry cũng fail). Option (b) (route `/protected/v1/*` mới) sạch hơn về namespace nhưng có code impact sang boundary `gf-inventory` trong W06 — loại vì capacity. | `Tracking/CHANGE-REQUESTS.md` CR-20260801-03; `Architecture/api/gf-inventory-api.md` v79 §3a.1 + §3a.2 V2-7; `Architecture/integrations/INTEG-EXT-gf-accounting-gf-inventory.md` v4 §3 + §4.6. |
| 2026-08-02 | W06 | post-review-fix | `GfInventoryClient` package (be) | **Ngoại lệ có chủ đích — `GfInventoryClient.java` tạo package MỚI `app/client/`, KHÔNG dùng `adapter/client/` hiện hữu** (nơi đang chứa `GfSalesClient.java` + `TenantClient.java`, verified). Quyết định trực tiếp của Delivery (sonhoang): "Ở wave này chúng ta sẽ tạo ra thư mục `app/client` và `GfInventoryClient.java`". Đây là lệch có chủ đích so với package convention `adapter/{client,config,controller}` mà chính `CR-20260801-06`/blockquote §7 xác nhận đang tồn tại — KHÔNG áp dụng nguyên tắc "theo hiện trạng repo" như 2 mục kia của CR-06 (persistence/workflow), vì đây là quyết định business/kiến trúc riêng cho client mới, không phải theo tiền lệ. Đồng bộ sửa: `FEAT-PRC-CREATE.md §7` (blockquote thêm bullet ngoại lệ + giữ path bảng nguyên trạng `app/client`), `FEAT-PRC-RECALC.md §7` (row REUSE sửa `adapter/client` → `app/client` cho khớp — phát hiện qua review độc lập, đây là inconsistency thật trước khi sửa). Cả 2 file bump version (CREATE 4→5, RECALC 5→6). | Review độc lập sau CR batch phát hiện `FEAT-PRC-CREATE.md` dùng `app/client` (4 chỗ) nhưng flagged là "path không tồn tại, lệch convention `adapter/client`" — user xác nhận đây KHÔNG phải lỗi mà là quyết định có chủ đích cho wave này; đồng thời phát hiện `FEAT-PRC-RECALC.md:276` sót lại `adapter/client` (không khớp CREATE) — đây mới là bug thật, đã sửa. Ghi rõ vào decision log để tránh warm-up/review lần sau tái flag nhầm. | `FEAT-PRC-CREATE.md` §7 (`:357,369,404,417`); `FEAT-PRC-RECALC.md` §7 (`:276`); `services/gf-accounting/src/main/java/com/actechx/gf/adapter/client/{GfSalesClient,TenantClient}.java` (precedent hiện hữu, verified tồn tại). |
| 2026-08-03 | W06 | contract-sign | `Architecture/api/gf-accounting-api.md` :: `bff-experiences/agg-garage-graph` | **Mode: sign (semantic) — DRIFT-TRIAGE per `/dev-handoff` block (team-lead request).** File bump v22(signed)→v26 (staged, chưa commit tại thời điểm sign) qua 2 nguồn: (1) Q2 v3 Temporal reversal cascade — PRC CREATE/RECALC chuyển `PriceCalcExecutorService.submit()` → `WorkflowClient.start()`, đã APPROVED per CLAUDE.md changelog v16 (user dev-ac 2026-07-23); (2) CR-20260801-02 (MINOR, self-approved, §5.2 404 error code) + CR-20260801-09 (MINOR, APPROVED — sort key `created_at`→`executed_at` fix BR-PRC-018) — cả 2 APPROVED trong `Tracking/CHANGE-REQUESTS.md`. Additive/documentation-only, không đổi request/response shape ngoài phạm vi CR đã duyệt. Re-signed → hash `359fb6ce1651...`. | Đủ điều kiện DRIFT-TRIAGE case 1 (intentional + có CR/decision entry cho toàn bộ nội dung drift) — verify trực tiếp CR log + CLAUDE.md changelog trước khi sign, không suy đoán. | `Tracking/CHANGE-REQUESTS.md` CR-20260801-02, CR-20260801-09; `CLAUDE.md` Change Log v16; `Architecture/api/gf-accounting-api.md` Change Log v23-v26; `scripts/contract-sign.py sign` output. |
| 2026-08-03 | W06 | contract-sign | `Architecture/data/gf-accounting-data-model.md` :: `boundaries/gf-accounting` (producer self-sign) | **Mode: sign (semantic).** File bump v12(signed)→v13 (staged) — Q2 v3 Temporal reversal cascade: `§2quater.1 price_calc_run` thêm cột `temporal_workflow_id`, đánh dấu DEPRECATED 3 cột v1 sweep-lock (`worker_node_id`/`lease_until`/`resumed_count`) + index `idx_prc_run_status_lease`; §3 Data Isolation bullet rewrite mô tả Temporal task queue polling thay DB sweep. APPROVED per CLAUDE.md changelog v16 (user dev-ac 2026-07-23), pair với ADR-027 v3/v4 + ADR-028 v2/v3. Re-signed → hash `cf10716f9baf...`. | Case 1 — decision entry đã tồn tại tại T0 (`CLAUDE.md` changelog), không phải drift lạ. Internal schema, không có consumer khác ngoài producer self-sign per `contract-scope.yaml` note. | `CLAUDE.md` Change Log v16; `Architecture/data/gf-accounting-data-model.md` Change Log v12-v13; `Architecture/decisions/ADR-027-*.md` v3/v4; `Architecture/decisions/ADR-028-*.md` v2/v3. |
| 2026-08-03 | W06 | contract-sign | `Architecture/api/gf-inventory-api.md` :: `bff-experiences/agg-garage-graph` + `boundaries/gf-accounting` | **Mode: sign (semantic), 2 consumer cùng nature.** File bump (signed hash tại v76/v77)→v79 — CR-20260801-01 (MODERATE, APPROVED — `§3g.2 W06-STK-Q2` response thêm `content[].productId`, round-trip follow-up cho `agg-garage-graph-graphql.md` CR-20260731-04) + CR-20260801-03 (MINOR, APPROVED — `V2-7 internal-products/search` mở thêm auth `x-api-key` S2S bên cạnh `authenticated`, phục vụ `gf-accounting` `GfInventoryClient`). Cả 2 additive-only, không đổi field/endpoint khác. Re-signed cả 2 consumer → cùng hash `bb8eb71554ff...`. | Case 1 — cả 2 CR APPROVED trong `Tracking/CHANGE-REQUESTS.md`, verify trước khi sign. `boundaries/gf-inventory` (self-sign producer, hash khác — v67 baseline, DRIFT riêng) KHÔNG nằm trong `contract-scope.yaml` consumer list cho contract này → KHÔNG re-sign (xem entry out-of-scope bên dưới). | `Tracking/CHANGE-REQUESTS.md` CR-20260801-01, CR-20260801-03; `Architecture/api/gf-inventory-api.md` Change Log v78-v79. |
| 2026-08-03 | W06 | contract-sign | `Architecture/integrations/INTEG-EXT-gf-accounting-gf-inventory.md` :: `boundaries/gf-inventory` | **Mode: sign (semantic).** File bump v3(signed)→v4 (staged) — CR-20260801-03 cascade: §3 Auth ghi rõ `x-api-key` áp dụng cho cả 6 endpoint (5× `/protected/v1/*` + V2-7 mới mở); §4.6 thêm mới READ `POST /api/v2/internal-products/search` (V2-7, resolve mã BQGQ PWA/ACTIVE cho PRC Phase 0). Companion đúng `gf-inventory-api.md` v79 (đã sign ở row trên cùng batch). Re-signed → hash `82662e3e0daa...`. | Case 1 — CR-20260801-03 APPROVED, cascade note tường minh trong chính CR + Change Log file. | `Tracking/CHANGE-REQUESTS.md` CR-20260801-03; `Architecture/integrations/INTEG-EXT-gf-accounting-gf-inventory.md` Change Log v4. |
| 2026-08-03 | W06 | contract-sign | `Architecture/api/agg-garage-graph-graphql.md` :: `web-experiences/garage-web` + `mobile-experiences/garage-mobile` | **Mode: sign (semantic), 2 consumer cùng nature (round 2, sau refresh-hashes round 1 tại entry #55 2026-08-01).** File bump v7.81(signed)→v7.83 (committed, `961c2e1`) — v7.82 GAP-1 field-coverage fix (warm-up Phase A review, user sonhoang: thêm `mainUnitDisplayName: String` nullable vào `StockLedgerAtDateRow`/`StockInoutSummaryRow`/`StockCardDetailContext`, enrichment qua `gf-erp-mdm` UNIT directory) + v7.83 CR-20260731-04 (MODERATE, APPROVED — `StockInoutSummaryRow` thêm `productId: Int` nullable, additive). Cả 2 additive-only, đúng pattern SDL field-add đã dùng nhiều lần trong wave (v7.80/v7.81 sku/skuName). `bff-experiences/agg-garage-graph` self-sign đã match hash mới (không cần re-sign, xem cascade output). Re-signed cả 2 consumer → cùng hash `3db1a1977ae7...`. | Case 1 — CR-20260731-04 APPROVED trong `Tracking/CHANGE-REQUESTS.md`; v7.82 documented trong chính Change Log file kèm rationale warm-up Phase A. | `Tracking/CHANGE-REQUESTS.md` CR-20260731-04; `Architecture/api/agg-garage-graph-graphql.md` Change Log v7.82/v7.83; `git log 961c2e1`. |
| 2026-08-03 | W06 | contract-sign | (out-of-scope reaffirm) | **KHÔNG re-sign 9 cặp (consumer, contract) drift còn lại phát hiện qua `audit --drift` lần DRIFT-TRIAGE này** — TẤT CẢ đã được flag out-of-scope tại entry `2026-07-31 contract-sign (out-of-scope note)` phía trên, drift hiện tại chỉ là bump thêm của cùng domain không đổi kết luận: `boundaries/gf-accounting` :: `gf-sales-events.md`, `gf-accounting-events.md`, `gf-accounting-api.md` (self-sign producer, KHÔNG phải consumer per `contract-scope.yaml`); `boundaries/gf-sales` :: `gf-accounting-api.md`, `gf-accounting-events.md`; `boundaries/gf-inventory` :: `gf-inventory-data-model.md`, `gf-inventory-events.md`, `INTEG-EXT-gf-inventory.md`, `gf-inventory-api.md` (self-sign producer). **Hệ quả**: sau batch sign trên, `contract-sign.py verify boundaries/gf-accounting` và `verify boundaries/gf-inventory` (không filter contract) vẫn exit 2 (3 + 4 drift pair tương ứng) — KHÔNG đạt full exit-0 cho toàn bộ 5 consumer như team-lead task yêu cầu; 3 consumer còn lại (`bff-experiences/agg-garage-graph`, `web-experiences/garage-web`, `mobile-experiences/garage-mobile`) đã exit 0. Đã báo lại team-lead qua message, không tự sign hộ. | Forbidden action §14 agent-contract-steward: "KHÔNG sign cho consumer mà `contract-scope.yaml` không list" + "KHÔNG sign contract baseline... mà không trong wave scope". Re-sign hộ sẽ che giấu drift thật của domain khác (Insurance Settlement, Catalog/OB W03-W05) mà chính boundary owner `gf-accounting`/`gf-inventory` cần tự rà soát ở `/dev-start`/`/dev-handoff` riêng của họ — nguyên tắc giữ nguyên từ quyết định 2026-07-31, không có CR/decision mới override nó trong phiên này. | Entry `2026-07-31 contract-sign (out-of-scope note)` (phía trên, cùng file); `python3 scripts/contract-sign.py audit --drift` output 2026-08-03; `python3 scripts/contract-sign.py verify boundaries/gf-accounting`/`verify boundaries/gf-inventory` exit 2 output. |
| 2026-08-03 | W06 | dev-handoff | (gate bypass — contract drift gate) | **User sonhoang (Delivery Authority) chấp thuận bypass literal exit-code của `/dev-handoff` Contract drift gate cho lần chạy này** — thay vì tiếp tục vòng lặp escalate "team-lead" lần 3 (đã xảy ra 2 lần, 2026-07-31 + 2026-08-03 entry phía trên, không ai resolve). Trước khi bypass, main-orchestrator đã verify TRỰC TIẾP cả 8 cặp (contract, consumer) thật sự nằm trong `Execution/wave-specs/W06/contract-scope.yaml` bằng `contract-sign.py verify <consumer> <contract-path>` (filter theo `contract_path` positional arg — script hỗ trợ sẵn, không cần sửa code) — **CẢ 8/8 pair exit 0 (OK)**: `bff-experiences/agg-garage-graph`::`gf-accounting-api.md`, `bff-experiences/agg-garage-graph`::`gf-inventory-api.md`, `boundaries/gf-accounting`::`gf-inventory-api.md`, `web-experiences/garage-web`::`agg-garage-graph-graphql.md`, `mobile-experiences/garage-mobile`::`agg-garage-graph-graphql.md`, `boundaries/gf-inventory`::`INTEG-EXT-gf-accounting-gf-inventory.md`, `web-experiences/garage-web`::`INTEG-FE-garage-web-agg-garage-graph.md` (`gf-accounting-data-model.md` có `consumers: []` — không cần verify). Root cause xác nhận: `/dev-handoff`'s gate script gọi `contract-sign.py verify <consumer>` KHÔNG filter theo contract path → check toàn bộ portfolio đã sign lịch sử của consumer đó (bao gồm producer self-sign + domain khác không thuộc wave này) thay vì chỉ 8 pair thuộc `contract-scope.yaml`. Đây là gap có thật của gate script (không phải drift thật cần fix), user chọn option "Bypass gate with documented rationale" (không chọn patch script hoặc resolve cross-domain drift ngay) khi được hỏi qua AskUserQuestion. Transition DEV→REVIEW tiếp tục dựa trên kết quả verify per-pair ở trên, KHÔNG dựa trên exit code thô của `contract-sign.py verify boundaries/gf-accounting`/`verify boundaries/gf-inventory` (không filter). | User quyết định trực tiếp qua AskUserQuestion (2026-08-03) sau khi được trình bày 4 lựa chọn (bypass / patch script / resolve cross-domain / xem chi tiết) — chọn "Bypass gate with documented rationale". Không override nguyên tắc "KHÔNG sign out-of-scope" đã lập 2026-07-31 — hoàn toàn KHÔNG sign/refresh-hashes bất kỳ pair out-of-scope nào, chỉ bypass việc đọc exit-code thô của gate script cho lần handoff này. Gate-script gap (verify không filter theo contract-scope.yaml) vẫn TỒN TẠI, chưa fix — flag cho wave sau hoặc CR riêng nếu tái diễn. | `Execution/wave-specs/W06/contract-scope.yaml`; per-pair `contract-sign.py verify <consumer> <contract_path>` output 2026-08-03 (8/8 OK); entries `2026-07-31`/`2026-08-03 (out-of-scope reaffirm)` phía trên (cùng file, không bị override). |
| 2026-08-03 | W06 | contract-sign | `boundaries/gf-accounting` self-sign batch — `gf-sales-events.md` + `gf-accounting-api.md` + `gf-accounting-events.md` | **SIGN (semantic), supersede entry `(out-of-scope reaffirm)` cho 3/9 cặp thuộc `gf-accounting` — team-lead re-assign task tường minh 2026-08-03 (sau khi wave reset PLANNING→DEV lúc 02:16:15, drift tái xuất hiện trên DEV cycle mới), chỉ đạo resolve toàn bộ 9 cặp trừ 2 cặp `gf-sales` (agent tự xét scope).** Root cause impasse cũ: `/dev-handoff` gate gọi `contract-sign.py verify <consumer>` KHÔNG filter `contract_path` — verify TOÀN BỘ signature file consumer, không chỉ pair trong `contract-scope.yaml`; self-sign baseline cũ (từ ~W02, trước khi `contract-scope.yaml` per-wave tồn tại) của `gf-accounting` cho chính API/events họ produce vẫn nằm trong file và block gate W06 dù không được liệt kê tường minh trong scope YAML. Git-archaeology (commit gần nhất trước `signed_at` → HEAD, không chỉ working-tree) xác nhận cả 3 SEMANTIC hợp lệ, có provenance rõ, KHÔNG dấu hiệu edit ngoài ý muốn: (1) `gf-sales-events.md` v5→v6 — §3.5b canonical `ServiceOrderStatusChanged` V2 schema + 2 Forbidden rule mới, ADR-026 v3 D5 (đã classify bởi team-lead trước khi giao task). (2) `gf-accounting-api.md` v22→v26 — trùng nội dung đã sign cho `bff-experiences/agg-garage-graph` ở entry phía trên (hash trùng `359fb6ce1651...`) — Q2 v3 Temporal reversal + CR-20260801-02/09. (3) `gf-accounting-events.md` v9→v11 — v10 quyết định PRC KHÔNG publish Kafka event (ADR-027/028 §Alt-3 rejected, ratify 2026-07-22) + v11 mechanical rename `BQGQ`→`PWA`. Signed → hash `27d6253ef576...` / `359fb6ce1651...` / `b9b0cd7009e1...`. Post-sign: `verify boundaries/gf-accounting` → **OK: all 6 contract(s) verified**. **QUAN TRỌNG — phát hiện SAU KHI đã sign**: entry `2026-08-03 | dev-handoff | (gate bypass...)` phía trên (dòng 68, cùng ngày) cho thấy user sonhoang đã TRỰC TIẾP quyết định qua AskUserQuestion **KHÔNG sign** đúng các cặp self-sign producer này ở 1 DEV cycle trước đó (trước khi STATE.json `stage_history` ghi nhận reset `PLANNING→DEV` lúc `2026-08-03T02:16:15`), chọn "bypass gate" thay vì sign. Agent action ở đây (SIGN) **ngược hướng** với quyết định đó — thực hiện dựa trên chỉ đạo tường minh của team-lead cho DEV cycle mới, nhưng CHƯA re-confirm với user liệu wave-reset có thực sự invalidate quyết định bypass cũ hay không. Đã báo rõ team-lead qua message — cần user sonhoang xác nhận SIGN (permanent fix, hướng đi khác) có được chấp nhận thay cho bypass (workaround tạm) hay không. | Task message team-lead 2026-08-03 ("9 drifted pairs to resolve... use your judgment on gf-sales... if out-of-scope, note clearly") — coi là chỉ đạo resolve cho DEV cycle hiện tại. Producer self-sign cho chính contract họ own không tương đương "sign hộ consumer lạ" mà Forbidden §14 muốn chặn — nhưng discovery về entry dòng 68 (bypass trước đó của user) là tín hiệu mạnh cần escalate lại, không tự ý coi đã resolve xong. | Task message team-lead (2026-08-03); `.claude/commands/dev-handoff.md` §Contract drift gate (verify không filter contract_path); `Architecture/events/gf-sales-events.md` CL v6; `Architecture/api/gf-accounting-api.md` CL v23-v26; `Architecture/events/gf-accounting-events.md` CL v10-v11; entry dòng 67 `(out-of-scope reaffirm)` (superseded cho 3 cặp này); entry dòng 68 `dev-handoff (gate bypass)` (conflict — chưa reconcile, xem SendMessage tới team-lead); `Execution/STATE.json stage_history` (`PLANNING→DEV` `2026-08-03T02:16:15`). |
| 2026-08-03 | W06 | contract-sign | `boundaries/gf-inventory` self-sign batch — `gf-inventory-data-model.md` + `gf-inventory-api.md` + `gf-inventory-events.md` + `INTEG-EXT-gf-inventory.md` | **SIGN (semantic), supersede entry `(out-of-scope reaffirm)` cho 4/9 cặp thuộc `gf-inventory` — cùng authorization/tension như batch `gf-accounting` (row trên, xem note QUAN TRỌNG về entry dòng 68 — áp dụng y hệt cho batch này).** Diff từng cặp (git-archaeology): (1) `gf-inventory-data-model.md` v29→v30 — **100% mechanical rename `BQGQ`→`PWA`** (mọi content-line đổi chỉ chứa 2 token này, verify bằng grep loại trừ; ratify tại chính file v14 R13, 2026-06-24); đổi literal enum value `pricingMethod` nên xếp SEMANTIC dù chỉ rename. (2) `gf-inventory-api.md` v65(baseline)→v79 — trùng nội dung đã sign cho `agg-garage-graph`+`gf-accounting` ở entry phía trên (hash trùng `bb8eb71554ff...`): §3f S2S PRC + §3g Stock V2 Reports + chuỗi CR/GAP (CR-20260731-03, GAP-W06-GI-02/04/06/07/11, CR-20260724-03, CR-20260801-01/03). (3) `gf-inventory-events.md` v9→v15 — chuỗi cascade provenance đầy đủ: v10 ADR-026 v1 direct-consume refactor, v12 wire-schema pin ADR-026 v3 D5, v13 CR-20260727-02 (fix BUG-W05-087), v14 rename BQGQ→PWA, v15 GAP-W06-GI-09 confirm. (4) `INTEG-EXT-gf-inventory.md` v16→v22 — v17-v18 ARCH-REVIEW-W05 cascade, v19 ADR-026 v1 §13d rewrite, v21 W06 PRC-callers §13e cross-link (companion `INTEG-EXT-gf-accounting-gf-inventory.md` đã sign), v22 rename BQGQ→PWA. Không phát hiện edit wrong-looking/unintended. Signed → hash `04bd2582a1cb...` / `bb8eb71554ff...` / `3b390fd8ef95...` / `7863cb600f22...`. Post-sign: `verify boundaries/gf-inventory` → **OK: all 9 contract(s) verified**. | Cùng lý do + cùng cảnh báo entry dòng 68 với row `gf-accounting` batch phía trên. | Task message team-lead (2026-08-03); `Architecture/data/gf-inventory-data-model.md` CL v30; `Architecture/api/gf-inventory-api.md` CL v72-v79; `Architecture/events/gf-inventory-events.md` CL v10-v15; `Architecture/integrations/INTEG-EXT-gf-inventory.md` CL v17-v22; entry dòng 67 (superseded cho 4 cặp này); entry dòng 68 (conflict, xem row trên). |
| 2026-08-03 | W06 | contract-sign | (final scoping note — `boundaries/gf-sales`) | **XÁC NHẬN out-of-scope thật cho 2 cặp cuối, KHÔNG sign, để nguyên DRIFT.** Verify trực tiếp: `Execution/wave-specs/W06/contract-scope.yaml` 6 contract entries → tập hợp `consumers` toàn file = `{bff-experiences/agg-garage-graph, boundaries/gf-accounting, web-experiences/garage-web, mobile-experiences/garage-mobile, boundaries/gf-inventory}` — `boundaries/gf-sales` KHÔNG xuất hiện ở bất kỳ contract nào. `.claude/commands/dev-handoff.md` gate script build consumer list trực tiếp từ tập này rồi mới gọi `verify(cs)` → `verify boundaries/gf-sales` **KHÔNG BAO GIỜ** được `/dev-handoff` W06 gọi (khác `gf-accounting`/`gf-inventory` — LÀ member tập consumer nên toàn bộ file họ bị quét). Kết luận: drift thật (self-sign cũ của `gf-sales` cho contract `gf-accounting` produce) nhưng không ảnh hưởng `/dev-handoff W06` — để nguyên. | Forbidden §14 — scope thật xác nhận bằng đọc trực tiếp source gate script, không suy đoán. | `Execution/wave-specs/W06/contract-scope.yaml` (`consumers` field, 6 entries); `.claude/commands/dev-handoff.md` §Contract drift gate; `python3 scripts/contract-sign.py audit --drift` post-batch (2 dòng còn lại = đúng 2 cặp `gf-sales`). |
| 2026-08-03 | W06 | contract-sign | (reconciliation follow-up) | **User sonhoang chấp thuận GIỮ NGUYÊN 7/9 chữ ký mà `agent-contract-steward` đã thực hiện** (đè lên nguyên tắc "KHÔNG re-sign out-of-scope" đã lập 2026-07-31, reaffirm 2026-08-03 dòng liền trên, và đè lên entry "dev-handoff (gate bypass)" ngay phía trên nữa) — do timing race: agent-contract-steward nhận task trước khi main-orchestrator phát hiện 2 entry cũ đó, đã sign xong 7 cặp (`boundaries/gf-accounting`::`gf-sales-events.md`/`gf-accounting-api.md`/`gf-accounting-events.md`; `boundaries/gf-inventory`::`gf-inventory-data-model.md`/`gf-inventory-api.md`/`gf-inventory-events.md`/`INTEG-EXT-gf-inventory.md`) TRƯỚC KHI 2 tin nhắn "STOP" của main-orchestrator kịp tới. Mỗi pair đều verify qua git-archaeology (diff từ commit gần nhất trước signed_at cũ tới HEAD) — semantic thật, có CR/ADR/GAP provenance rõ ràng (không có pair nào là accidental/unintended). User được hỏi lại qua AskUserQuestion (2 lựa chọn: giữ nguyên vs revert) — chọn **giữ nguyên** (nội dung đúng/thật, gate giờ pass thật không cần bypass, revert chỉ tổ mất công vì `contract-sign.py` không có unsign command an toàn). **Kết quả net**: entry "dev-handoff (gate bypass)" phía trên coi như MOOT cho phần `boundaries/gf-accounting`/`boundaries/gf-inventory` (không còn drift để bypass nữa — verify exit 0 thật) — chỉ còn relevant cho phần lý giải root-cause (gate script không filter theo contract-scope.yaml) và cho 2 pair `boundaries/gf-sales`::`gf-accounting-api.md`/`gf-accounting-events.md` vẫn out-of-scope, KHÔNG sign, giữ nguyên đúng nguyên tắc 2026-07-31. `python3 scripts/contract-sign.py audit --drift` sau cùng chỉ còn 2 dòng (2 pair gf-sales). | User quyết định trực tiếp qua AskUserQuestion lần 2 (2026-08-03) sau khi agent-contract-steward chủ động flag conflict thay vì tự ý resolve — đúng hành vi mong muốn (escalate khi phát hiện xung đột với quyết định đã ghi nhận trước đó, không tự quyết). Không có sign/refresh nào bị revert; đây thuần là ghi nhận + đối chiếu lại 2 quyết định cùng ngày cho nhất quán decision log. | 2 báo cáo `contract-steward-w06-drift` (SendMessage, 2026-08-03T04:2x-04:3x); entry "out-of-scope reaffirm" + "dev-handoff (gate bypass)" phía trên (cùng file); `python3 scripts/contract-sign.py audit --drift` output sau cùng (2 dòng gf-sales). |
