---
type: business-rules
artifact_kind: business-rules
status: ACTIVE
version: 41
tier: T1
owner_authority: Business Authority
boundary: "gf-inventory"
last_reviewed: "2026-07-16"
supersedes: "none"
---

# Business Rules — gf-inventory Delivery V2 (Phiếu xuất kho)

> Tập business rules V2 cho `EP-INVENTORY-DELIVERY-V2`. File **mới** (không thay thế `BR-GF-INVENTORY.md` §2.2 BR-ID cũ — baseline giữ nguyên). Dùng chung mô hình tồn/giá/vòng đời phiếu V2 với `BR-GF-INVENTORY-RECEIPT-V2`.

---

## §1 Cross-boundary Rules

| # | Rule | Hướng | Boundary liên quan | Cơ chế |
|---|---|---|---|---|
| CB-IDV2-001 | Phiếu xuất kho có thể liên kết phiếu dịch vụ (SO) của `gf-sales` — không bắt buộc. Khi liên kết, đối soát SL/sản phẩm giữa phiếu xuất và SO. | Outbound sync | `gf-sales` | REST |
| CB-IDV2-002 | Ngày chứng từ phiếu rơi vào kỳ kế toán đã đóng → chặn thêm/sửa/xóa/ghi sổ. | Tham chiếu | (kỳ kế toán, cùng boundary) | Kiểm tra trạng thái kỳ |
| CB-IDV2-003 | **Auto-create phiếu Xuất từ event SO**: `gf-sales` phát event **`ServiceOrderStatusChanged`** (topic **`AC-NONPROD-DEV-SERVICE-ORDER-EVENTS`**, MessageStep `DELIVERED`; xem `Architecture/events/gf-sales-events.md` §3.5) khi SO chuyển trạng thái phù hợp theo loại — SO **SERVICE** → `"Đang thực hiện"` lần đầu; SO **RETAIL** → `"Đã xác nhận"` lần đầu (canonical FEAT-SO-DETAIL AC-17). `gf-inventory-worker` consume event, start `DeliveryFulfillmentWorkflow` (Temporal) → auto-create phiếu Xuất **"Nháp"** theo BR-IDV2-032 (loại phiếu theo SO: SERVICE → `DELIVERY_REPAIR`, RETAIL → `DELIVERY_SALE`; nguồn xuất fix "Nền tảng"; chỉ trigger khi SO có active parts với `source=INVENTORY` và flag `Inventory:InventoryV2` bật). **Idempotency**: 1 SO chỉ sinh phiếu 1 lần — dedup qua inbox table (ADR-004); partial delivery / lần chuyển trạng thái thứ 2+ **không** phát lại → user tạo tay. **Workflow**: giữ `DeliveryFulfillmentWorkflow` (Temporal, gf-inventory-worker) — logic bên trong cập nhật theo V2 spec. **Note contract**: `ServiceOrderStatusChanged` hiện là raw DTO có embedded headers (chưa full `KafkaMessageWrapper` — xem `gf-sales-events.md` §3.5 note). | Inbound event | `gf-sales` | Kafka event (outbox → inbox dedup) + Temporal workflow |

---

## §2 Rules Registry

### 2.1 Vòng đời & ghi sổ (BR-IDV2-001 .. BR-IDV2-009)

| BR ID | Rule | Category | Features |
|---|---|---|---|
| BR-IDV2-001 | Phiếu xuất kho khởi tạo ở trạng thái **"Nháp"**. Số phiếu tự sinh (PX-xxxxx), unique theo garage. | Status Init | FEAT-ID-CREATE-V2 |
| BR-IDV2-002 | Vòng đời: **Nháp → Ghi sổ kho → Bỏ ghi sổ kho** (về Nháp). Không có "Đã hủy" — chỉ Xóa. | Status Transition | FEAT-ID-DETAIL-V2 |
| BR-IDV2-003 | **Ghi sổ kho**: trừ tồn theo **SL quy đổi (ĐVT chính)** cho từng (mã + kho + garage) tại ngày xuất → **gọi quy tắc tính lại sổ tồn (BR-STKV2-005a)**. | Stock Impact | FEAT-ID-DETAIL-V2 |
| BR-IDV2-004 | **Trước khi ghi sổ, kiểm tra tồn khả dụng**: không cho ghi sổ nếu làm **tồn (mã + kho + garage) < 0 tại bất kỳ thời điểm nào** từ ngày chứng từ trở đi (chặn tồn âm point-in-time). Dòng vượt tồn hiển thị cảnh báo **"Không đủ tồn"** → mã lỗi **`ERR-INV-037`** (ghi sổ làm âm → **`ERR-INV-036`**). *(Phiếu xuất có ngày **trước "Tồn đến ngày" của OB** → tồn tại thời điểm đó = 0 nên **tự bị chặn** theo quy tắc này; không cần rule chặn riêng như phiếu nhập — xem BR-IRV2-030.)* | Core Rule | FEAT-ID-CREATE-V2, FEAT-ID-DETAIL-V2, FEAT-ID-EDIT-V2, FEAT-ID-DELETE |
| BR-IDV2-005 | **Bỏ ghi sổ kho**: cộng tồn lại, đưa phiếu về **"Nháp"** → **gọi quy tắc tính lại sổ tồn (BR-STKV2-005a)**. | Stock Impact | FEAT-ID-DETAIL-V2 |
| BR-IDV2-006 | Sửa/xóa phiếu **đã Ghi sổ kho** (đổi sản phẩm/SL/ngày/kho/xóa dòng) → **gọi quy tắc tính lại sổ tồn (BR-STKV2-005a)** — engine tính lại từ (bảng OB + phiếu detail) cho (mã+kho+gara) từ ngày chứng từ bị ảnh hưởng trở đi, kể cả khi lùi ngày. Phiếu **Nháp** sửa/xóa tự do — không gọi engine. | Recompute | FEAT-ID-EDIT-V2, FEAT-ID-DELETE |
| BR-IDV2-007 | Phiếu có ngày chứng từ thuộc **kỳ kế toán đã đóng** → chặn thêm/sửa/xóa/ghi sổ/bỏ ghi sổ → mã lỗi **`ERR-INV-024`**. Kỳ **chưa khóa** → cho sửa/xóa (kể cả phiếu đã Ghi sổ kho — không cần đưa về Nháp). | Lock | FEAT-ID-CREATE-V2, FEAT-ID-EDIT-V2, FEAT-ID-DELETE, FEAT-ID-DETAIL-V2 |
| BR-IDV2-008 | **Giá vốn xuất = 0** cho tới khi chạy **BQGQ cuối kỳ**; sau khi chạy → cập nhật giá vốn thực vào toàn bộ phiếu xuất trong kỳ (xem PRC). Trên phiếu, đơn giá vốn / tiền vốn / tổng giá trị hiển thị **0** trước khi chạy BQGQ. | Valuation | FEAT-ID-CREATE-V2, FEAT-ID-DETAIL-V2 |
| BR-IDV2-009 | **Đối soát phiếu dịch vụ (SO)**: đối soát SL/sản phẩm giữa phiếu Xuất và SO liên kết — **CHỈ trigger tại thời điểm user chuyển phiếu sang trạng thái Ghi sổ** (Nháp → Ghi sổ), **bất kể entry point**: (a) form Sửa — user đổi dropdown Trạng thái từ Nháp → Ghi sổ + click Lưu; (b) form Chi tiết — user click nút "Ghi sổ kho"; (c) mobile màn Danh sách — user tap icon "Ghi sổ" inline per-row. **KHÔNG trigger tại các thao tác khác**: Save form Tạo (bất kể có SO hay không), Save form Sửa giữ trạng thái Nháp, Bỏ ghi sổ (Ghi sổ → Nháp). Đối xứng V1 baseline `BR-ID-DTL-006` — V2 narrow lại về V1 semantic (không broaden CREATE + EDIT như spec cũ). **Flow BE**: sau khi user xác nhận popup Ghi sổ chung → BE gọi `gf-sales` `/protected/v1/product/so-summary` lấy SO snapshot → so sánh SL quy đổi/mã nội bộ phiếu vs SO. **Case lệch (`ERR-INV-039`)** → hiển thị **popup cảnh báo verbatim**: title "Số lượng phụ tùng trong phiếu xuất kho và phiếu dịch vụ #{mã_phiếu} chưa trùng khớp. Vui lòng cập nhật chính xác số lượng phụ tùng." + content list dòng lệch (VD: "Phụ tùng A thừa 5 cái chưa khớp phiếu xuất kho") + **2 button [Đóng] + [Vẫn Ghi sổ]**. Click **[Đóng]** → hủy Ghi sổ, phiếu giữ trạng thái cũ (Nháp). Click **[Vẫn Ghi sổ]** → commit Ghi sổ (warning-only semantic, non-blocking). **Case DEGRADED (`ERR-CMN-007-DEGRADED`, `gf-sales` unreachable/timeout, fail-OPEN)** → hiển thị popup verbatim: "Hệ thống chưa đối soát được vì mất kết nối phòng dịch vụ. Bạn vẫn muốn Ghi sổ hay đợi thử lại?" + **2 button [Đóng] + [Vẫn Ghi sổ]**. **Case match / phiếu không có SO** → commit Ghi sổ trực tiếp không hiển thị popup thứ 2. **Popup 2 rời** (không merge), **scope popup xác nhận Ghi sổ chung khác nhau theo entry point**: **(a) Nút "Ghi sổ kho"** ở form Chi tiết web + mobile Chi tiết + mobile List inline (`FEAT-ID-DETAIL-V2` AC-5 + `FEAT-ID-LIST-V2` mobile) → **CÓ popup xác nhận Ghi sổ chung** "Bạn có chắc chắn muốn thực hiện Ghi sổ kho phiếu này hay không?" trước → user Xác nhận → BE check → nếu lệch/DEGRADED → popup 2 hiện. Lý do: nút Ghi sổ là primary action, cần confirm tránh nhầm. **(b) Form Sửa** dropdown Trạng thái Nháp→Ghi sổ + Lưu (`FEAT-ID-EDIT-V2` AC-4b) → **KHÔNG popup xác nhận Ghi sổ chung**, đi thẳng BE check → nếu lệch/DEGRADED → popup 2 hiện. Lý do: user đã explicit chọn dropdown + click Lưu, hành động rõ ràng không cần confirm lại. Popup cảnh báo lệch + popup DEGRADED wording verbatim IDENTICAL cho cả 2 entry point. Wording verbatim ở đây là canonical — DEV/FE/QA đọc dùng verbatim. **UI Figma pending** (BA sẽ vẽ mới sau — placeholder link đăng ký ở `Product/ux/figma/figma-links.yaml`). **Banner cũ trên màn Chi tiết (AC-2b cũ) BỎ HOÀN TOÀN** — thay bằng popup logic ở AC-5. | Reconciliation | FEAT-ID-EDIT-V2 (AC-4b Save form Sửa chuyển Ghi sổ), FEAT-ID-DETAIL-V2 (AC-5 click nút Ghi sổ kho), FEAT-ID-LIST-V2 (mobile inline Ghi sổ per-row) |

### 2.2 Thông tin phiếu & phân loại (BR-IDV2-010 .. BR-IDV2-014)

| BR ID | Rule | Category | Features |
|---|---|---|---|
| BR-IDV2-010 | Hai trường phân loại riêng: **Nguồn xuất** (Mua ngoài / Nền tảng — kế thừa V1) và **Loại phiếu** (Xuất bán / Xuất trả hàng mua / Xuất sửa chữa / Xuất khác). **Mã enum backend (Loại phiếu)** — lock cho API/DB/event, KHÔNG thay đổi sau ratify:<br>• `DELIVERY_SALE` = Xuất bán<br>• `DELIVERY_PURCHASE_RETURN` = Xuất trả hàng mua<br>• `DELIVERY_REPAIR` = Xuất sửa chữa<br>• `DELIVERY_OTHER` = Xuất khác<br>Label tiếng Việt chỉ dùng cho hiển thị UI; mọi filter/API request/response/Kafka event/log dùng mã enum. | Enum | FEAT-ID-CREATE-V2, FEAT-ID-EDIT-V2, FEAT-ID-LIST-V2 |
| BR-IDV2-011 | **Mã đơn hàng / phiếu dịch vụ (SO) không bắt buộc**. Nếu chọn → kế thừa dữ liệu; không chọn → nhập sản phẩm thủ công. | Validation | FEAT-ID-CREATE-V2, FEAT-ID-EDIT-V2 |
| BR-IDV2-012 | Trường bắt buộc header: **Loại phiếu**, **Đối tượng**, **Kho xuất**, **Số phiếu** (tự sinh), **Ngày xuất kho**, **Trạng thái**. | Validation | FEAT-ID-CREATE-V2, FEAT-ID-EDIT-V2 |
| BR-IDV2-013 | **Kho xuất** chọn ở header → đổ xuống dòng; kho từng dòng vẫn cho chọn lại. Tồn trừ theo **kho của dòng**. | Validation | FEAT-ID-CREATE-V2, FEAT-ID-EDIT-V2 |
| BR-IDV2-014 | **Tồn khả dụng** hiển thị trên từng dòng = tồn hiện tại của (mã + kho + garage) tại thời điểm lập phiếu. | Display | FEAT-ID-CREATE-V2, FEAT-ID-EDIT-V2 |

### 2.3 Dòng chi tiết & quy đổi (BR-IDV2-015 .. BR-IDV2-018)

| BR ID | Rule | Category | Features |
|---|---|---|---|
| BR-IDV2-015 | Chọn **SKU** (không bắt buộc) → đổ Tên phụ tùng + Mã nội bộ + Tên nội bộ + ĐVT chính. Chọn thẳng **Mã nội bộ** → đổ Tên nội bộ + ĐVT chính. | Auto-fill | FEAT-ID-CREATE-V2, FEAT-ID-EDIT-V2 |
| BR-IDV2-016 | Mỗi dòng xuất theo **ĐVT xuất**; **SL quy đổi = SL xuất × tỷ lệ quy đổi** (ĐVT xuất → ĐVT chính). **Tồn trừ theo SL quy đổi (ĐVT chính)**. | Calculation | FEAT-ID-CREATE-V2, FEAT-ID-EDIT-V2 |
| BR-IDV2-017 | Mỗi dòng lưu: SKU (tùy chọn), mã nội bộ, ĐVT xuất, SL xuất, SL quy đổi, ĐVT chính, đơn giá vốn (=0 đến khi BQGQ), **tiền vốn = Đơn giá vốn × SL quy đổi** (theo ĐVT chính; =0 đến khi BQGQ), kho, ghi chú. | Data Shape | FEAT-ID-CREATE-V2 |
| BR-IDV2-018 | Tab chi tiết: thanh trên chỉ có nút **"Thêm phụ tùng"**; **xóa dòng = icon ở cột "Thao tác"** trên từng dòng (cả Tạo và Sửa). Không có nút "Xóa dòng" hàng loạt. | Form | FEAT-ID-CREATE-V2, FEAT-ID-EDIT-V2 |

### 2.4 In / Export / Audit / Phân quyền / Ẩn-hiện nút (BR-IDV2-019 .. BR-IDV2-034)

| BR ID | Rule | Category | Features |
|---|---|---|---|
| BR-IDV2-019 | **In phiếu** xuất PDF 1 phiếu theo **Mẫu 02-VT** (TT 99/2025/TT-BTC). Cột "Mã số" = **mã sản phẩm nội bộ** (không phải SKU). Khối chữ ký **5 vai**: Người lập biểu / Người nhận hàng / Thủ kho / Kế toán trưởng (Hoặc bộ phận có nhu cầu nhập) / **Giám đốc** — 4 vai đầu hint "(Ký, họ tên)", **Giám đốc** hint "(Ký, họ tên, **đóng dấu**)" (đại diện pháp lý). Template oracle: `Product/ux/assets/phieu-xuat-kho-02-vt.html`. | Output | FEAT-ID-PRINT |
| BR-IDV2-020 | **Xuất excel** xuất danh sách theo bộ lọc hiện tại — dump đúng các cột đang hiển thị ra `.xlsx` (bám mẫu chuẩn `Product/ux/assets/Danh sách phiếu xuất kho.xlsx`). **Cap 1.000 phiếu/lần xuất**: hệ thống đếm số phiếu match bộ lọc hiện tại **trước khi sinh file**; nếu > 1.000 phiếu → chặn xuất + báo mã lỗi **`ERR-INV-045`** ("Kết quả vượt 1.000 mục — vui lòng áp dụng bộ lọc để thu hẹp phạm vi rồi xuất lại"); ≤ 1.000 phiếu → xuất bình thường. Cap tính theo **PHIẾU** (business unit), không theo total rows Excel (mỗi phiếu có thể nested nhiều dòng phụ tùng). | Output | FEAT-ID-EXPORT |
| BR-IDV2-021 | Danh sách luôn phạm vi theo garage (tenant isolation). Tìm kiếm LIKE case-insensitive theo **Số phiếu xuất / Phiếu dịch vụ / Diễn giải** — match kiểu **OR** giữa 3 field (v26 thêm Diễn giải; bỏ Người tạo — v24); lọc theo **5 bộ lọc**: Loại phiếu / Đối tượng / **Người phụ trách** (mới v24) / Trạng thái / Ngày xuất. **Filter Đối tượng**: dropdown **DISTINCT `(object_type, object_id)` từ bảng phiếu xuất** của garage (không lấy full 3 danh mục NCC/KH/NV) — **dedup theo mã (`object_id`)**, KHÔNG theo tên; hiển thị tên hiện thời từ danh mục master + suffix `{name} ({NCC\|KH\|NV} · {code})`; cascade với Loại phiếu (Xuất bán → KH; Xuất trả hàng mua → NCC; Xuất sửa chữa → NV). **Filter Người phụ trách** (v25): dropdown **DISTINCT `staff_id` từ bảng phiếu xuất** của garage (không lấy full danh mục nhân sự) — dedup **theo mã `staff_id`**; hiển thị tên hiện thời từ danh mục master (nhân sự đã ngừng vẫn hiện tên — cover phiếu cũ do nhân sự đã nghỉ tạo); sort alphabet. Search + filter combine AND; nút "Reset tất cả filter". Sắp xếp mặc định: Ngày xuất DESC. Hiển thị Tiền vốn. **Mobile-only pattern (v39 add — GAP-02 pre-DEV W05, đối xứng BR-IRV2-021 v41)**: cột "Thao tác" trên mobile hiển thị icon **Post/Unpost inline per-row** — Nháp + kỳ mở → icon "Ghi sổ kho"; Ghi sổ + kỳ mở → icon "Bỏ ghi sổ kho"; tap → popup xác nhận verbatim khớp FEAT-ID-DETAIL-V2 AC-5/AC-6 → mutation `postDeliveryV2` / `unpostDeliveryV2` → badge lật + sổ tồn cập nhật. Kỳ đóng → ẩn cả 2 icon. **KHÔNG có icon Sửa / Xóa / In trên mobile List** (4 op web-only). Khác web: web List row-action giữ Sửa/Xóa/In, Post/Unpost thực hiện trong màn chi tiết. | Tenant Isolation / Search / Mobile Row-action | FEAT-ID-LIST-V2 · FEAT-ID-DETAIL-V2 |
| BR-IDV2-022 | Phiếu hiển thị thông tin audit: Ngày tạo / Người tạo / Ngày sửa / Người sửa. | Audit | FEAT-ID-DETAIL-V2 |
| BR-IDV2-023 | 2 vai trò — chủ garage và kế toán — **quyền ngang nhau** trên toàn bộ phiếu xuất kho. | Permission | (toàn bộ feature) |
| BR-IDV2-024 | **Ẩn/hiện nút theo trạng thái + kỳ**: phiếu **"Nháp"** (kỳ chưa khóa) hiện **Sửa / Xóa / Ghi sổ kho**; phiếu **"Ghi sổ kho"** (kỳ chưa khóa) hiện **Sửa / Xóa / Bỏ ghi sổ kho**. **Kỳ đã khóa → ẩn các nút thao tác (Sửa/Xóa/Ghi sổ/Bỏ ghi sổ)**. **In phiếu / Xuất excel** luôn khả dụng. Nút **"Thêm mới"** không ẩn — chặn khi **Lưu** form Thêm nếu ngày chứng từ thuộc kỳ đã khóa (BR-IDV2-007, `ERR-INV-024`). | UI Behavior | FEAT-ID-DETAIL-V2, FEAT-ID-LIST-V2 |
| BR-IDV2-025 | **Nguồn dữ liệu trường form**: **Đối tượng** (bắt buộc) lọc theo Loại phiếu. **Xuất bán → Khách hàng** (dropdown mở ra list KH trực tiếp, KHÔNG có radio); **Xuất trả hàng mua → Nhà cung cấp** (dropdown mở ra list NCC trực tiếp, KHÔNG có radio); **Xuất sửa chữa → Nhân viên** (dropdown mở ra list NV trực tiếp, KHÔNG có radio); **Xuất khác → 2 dropdown song song trên form**: Loại phiếu = "Xuất khác" thì section "Thông tin chung" hiện **2 dropdown độc lập cạnh nhau**: (a) **"Loại đối tượng"*** (bắt buộc, single-select **NCC** / **KH** / **NV**); (b) **"Đối tượng"*** (bắt buộc, list load theo Loại đối tượng đã chọn). **Widget "Loại đối tượng" chỉ là UI filter** (không phải business field độc lập lưu riêng ở DB) — FE derive `objectType` từ widget này, gửi BE cùng payload `{objectType, objectId}`; **backend contract giữ nguyên** (data model `object_type + object_id` + enum `DeliveryObjectType SUPPLIER|CUSTOMER|EMPLOYEE` + REST/GraphQL/Kafka field không đổi). **Default trước khi chọn Loại đối tượng**: dropdown "Đối tượng" disabled + placeholder "Chọn loại đối tượng trước". Chọn Loại đối tượng → dropdown "Đối tượng" enable + list re-fetch từ bảng tương ứng (NCC/KH từ `gf-customer`, NV từ `gf-hrms`). Đổi Loại đối tượng → clear selection "Đối tượng" + reload list từ bảng mới. Chọn item → field "Đối tượng" hiện tên item; lưu phiếu ghi cả `(object_type, object_id)`. **Form Sửa phiếu Xuất khác cũ**: dropdown "Loại đối tượng" **auto set loại tương ứng `object_type` đã lưu** + dropdown "Đối tượng" load bảng đó với selected value; user có thể đổi Loại đối tượng để chọn Đối tượng loại khác (cascade như Tạo). Pattern 2-dropdown **CHỈ áp cho "Xuất khác"** — 3 loại còn lại (Xuất bán / Xuất trả hàng mua / Xuất sửa chữa) form chỉ hiện 1 field "Đối tượng" (Loại đối tượng derive fix theo Loại phiếu, không hiển thị widget). Lý do 3 loại lưu ở 3 bảng khác nhau (NCC + KH ở `gf-customer`, NV ở `gf-hrms`) → chọn loại trước để backend query 1 bảng. **Đối tượng dropdown filter cứng theo status** (áp cho mọi Loại phiếu): **NCC** chỉ show "Đang hoạt động"; **Khách hàng** chỉ show "Đang hoạt động"; **Nhân viên** chỉ show "Đang làm việc" (không hiện NCC/KH "Ngừng hoạt động" / NV "Ngừng làm việc" / "Nghỉ việc"). **Edge case phiếu cũ**: phiếu đã có Đối tượng nay đã ngừng → form Sửa vẫn hiện selected value (không mất reference), nhưng KHÔNG cho chọn lại đối tượng đã ngừng khác. **Người phụ trách** mặc định = nhân viên đang đăng nhập, chọn lại từ **danh sách nhân sự có trạng thái "Đang làm việc"** (filter cứng ở cả form Tạo + Sửa — không hiển thị nhân sự đã "Ngừng làm việc" / "Nghỉ việc"). **Trường hợp phiếu đã có Người phụ trách bị đổi status sau ghi sổ**: dropdown khi mở lại form Sửa vẫn cho hiện selected value (tên nhân sự đó, kể cả đã ngừng) để không mất reference — nhưng KHÔNG cho chọn lại nhân sự đã ngừng khác. **Người giao hàng** nhập tay tự do. **Nguồn xuất** (Mua ngoài/Nền tảng) **kế thừa tự động từ SO** khi chọn SO (không chọn tay). **ĐVT xuất** chọn trong ĐVT chính + các ĐVT quy đổi của mã. **SL xuất > 0** (cho số lẻ), ≤ tồn khả dụng. | Validation / Data Source | FEAT-ID-CREATE-V2, FEAT-ID-EDIT-V2 |
| BR-IDV2-026 | **Tệp đính kèm**: tối đa **5 tệp**, mỗi tệp **≤ 30 MB** (mã lỗi `ERR-CMN-004`), định dạng **PDF, JPG, PNG** (mã lỗi `ERR-CMN-005`) — theo chuẩn upload file toàn platform. Không bắt buộc. | Validation | FEAT-ID-CREATE-V2, FEAT-ID-EDIT-V2 |
| BR-IDV2-027 | Dropdown **"Mã SP nội bộ"** (dòng chi tiết) có mục cuối **"+ Tạo mới mã nội bộ"** → mở **modal inline** không rời form phiếu. Modal **reuse toàn bộ component form `FEAT-CAT-PROD-CREATE`** (full 4 tab: Thông tin chung / ĐVT quy đổi / Mã SKU / Đính kèm file) — cùng validation, cùng logic; chỉ khác container (modal vs full-page). Lưu thành công → mã mới (**"Đang hoạt động"**) tự chọn vào dòng phiếu; đóng modal, quay về form phiếu (giữ nguyên dữ liệu đã nhập). Hủy modal → không tạo, quay về form phiếu (giữ nguyên). **Không có cảnh báo rời trang** (vì không rời trang). **Nếu dòng có SKU chưa mapping mã nội bộ** → modal mở ra **gắn sẵn SKU đó vào tab "Mã SKU"**; **Mã + Tên sản phẩm nội bộ đều nhập tay**. **SKU đã mapping mã khác** → không gắn sẵn (modal trống; 1 SKU chỉ thuộc 1 mã — BR-CAT-PROD-013). Xem `FEAT-CAT-PROD-CREATE` AC-1b. | Form / Modal | FEAT-ID-CREATE-V2, FEAT-ID-EDIT-V2 |
| BR-IDV2-028 | **Phiếu nguồn "Nền tảng"** (bên bán `gf-sales` đẩy từ **phiếu dịch vụ / SO**) khởi tạo trạng thái **"Nháp"** — dòng có thể **chỉ có mã SKU + tên SKU, chưa có mã nội bộ**; ở **Nháp chưa tác động tồn**. Khi chuyển **"Ghi sổ kho"**: **bắt buộc mọi dòng phải có mã nội bộ**; nếu còn dòng thiếu (chỉ có SKU) → **chặn ghi sổ**, yêu cầu tạo/gắn mã nội bộ → mã lỗi **`ERR-INV-011`**. | Stock Impact / Validation | FEAT-ID-CREATE-V2, FEAT-ID-DETAIL-V2 |
| BR-IDV2-029 | Dropdown **"ĐVT xuất"** (dòng chi tiết, khi đã chọn mã nội bộ) có mục cuối **"+ Thêm ĐVT quy đổi"** → mở **modal inline** (ĐVT quy đổi từ master + Tỷ lệ > 0, số thập phân) **không rời form phiếu**; validate tỷ lệ > 0, **không trùng ĐVT chính / ĐVT quy đổi đã có** (BR-CAT-PROD-011). Lưu → thêm ĐVT vào mã nội bộ + **tự chọn vào dòng**. (Tái dùng `FEAT-CAT-PROD-CREATE` AC-11.) | Form | FEAT-ID-CREATE-V2, FEAT-ID-EDIT-V2 |
| BR-IDV2-030 | **Kế thừa từ phiếu Nhập mua (loại "Xuất trả hàng mua")**: khi **Loại phiếu = "Xuất trả hàng mua"** → thay trường "Mã đơn hàng (SO)" bằng trường **"Số phiếu nhập"** (dropdown phiếu **Nhập mua** đã ghi sổ của garage) — **KHÔNG bắt buộc**. **Nếu chọn số phiếu nhập** → hệ thống **kế thừa**: (a) **Đối tượng** = Nhà cung cấp của phiếu Nhập mua; (b) **Kho xuất** = kho của phiếu Nhập mua; (c) **toàn bộ dòng chi tiết** (SKU, mã nội bộ, tên, ĐVT, SL, SL quy đổi, kho, **đơn giá/giá vốn kế thừa từ phiếu Nhập mua gốc**) — đổ vào Thông tin chung + tab Chi tiết. **Các dòng kế thừa được SỬA**: đổi **SL**, **đơn giá**, hoặc **xóa dòng** (hỗ trợ trả một phần). **Nếu KHÔNG chọn** → nhập sản phẩm + **SL & đơn giá thủ công** như thường. Giá vốn (kế thừa hoặc nhập tay) là **tiền vốn của phiếu Xuất trả hàng mua** — khoản **giảm-trừ phía nhập** trong BQGQ, **KHÔNG** theo đơn giá BQ kỳ này (BR-PRC-001/005). | Inheritance / Form | FEAT-ID-CREATE-V2, FEAT-ID-EDIT-V2 |
| BR-IDV2-031 | **Chặn SL trả vượt số đã mua (loại "Xuất trả hàng mua")**: khi **có chọn "Số phiếu nhập"**, **SL xuất trả mỗi dòng (SL quy đổi) ≤ SL đã nhập** của dòng tương ứng trên phiếu Nhập mua nguồn; vượt → **chặn lưu / ghi sổ** → mã lỗi **`ERR-INV-040`**. **Không chọn** phiếu nhập → **không áp** (không có "đã nhập" để đối chiếu). (Đối xứng **BR-IRV2-032**.) | Validation | FEAT-ID-CREATE-V2, FEAT-ID-EDIT-V2 |
| BR-IDV2-032 | **Auto-create phiếu Xuất từ SO**: SO (`gf-sales`) chuyển trạng thái **lần đầu tiên** theo loại — SO **"Dịch vụ xe"** (`SERVICE`) → `"Đang thực hiện"`; SO **"Bán lẻ"** (`RETAIL`) → `"Đã xác nhận"` (canonical FEAT-SO-DETAIL AC-17) — VÀ SO có phụ tùng nguồn INVENTORY VÀ flag `Inventory:InventoryV2` bật → hệ thống auto-create 1 phiếu Xuất trạng thái **"Nháp"**. **Loại phiếu tạo** theo loại SO: `SERVICE` → `DELIVERY_REPAIR` (Xuất sửa chữa); `RETAIL` → `DELIVERY_SALE` (Xuất bán). **Nguồn xuất**: fix **"Nền tảng"** (phiếu do hệ thống tạo). **Kho xuất**: kho đầu tiên tìm được của garage (1 garage 1 kho). **Đối tượng** theo Loại phiếu: `DELIVERY_REPAIR` → Nhân viên (user chuyển SO trạng thái); `DELIVERY_SALE` → Khách hàng của SO. **Mã đơn hàng (SO)**: liên kết ngược SO nguồn (cho đối soát BR-IDV2-008). **Detail dòng lúc auto-create — 3 nhóm state**: (a) **Kế thừa từ SO** (có giá trị): SKU · Tên phụ tùng theo SKU · **ĐVT xuất + SL xuất** (kế thừa nguyên, không convert). (b) **Fix cứng** (có giá trị): Kho = kho đầu tiên garage · **Đơn giá vốn = 0 · Tiền vốn = 0** (KHÔNG kế thừa từ SO — nhất quán V2 §2A / BR-IDV2-017; chờ **BQGQ cuối kỳ** update giá vốn thực — BR-PRC-017). (c) **Để TRỐNG (hiển thị "—")** vì phụ thuộc Mã nội bộ chưa được chọn: **Mã SP nội bộ · Tên SP nội bộ · ĐVT chính · SL quy đổi · Tồn khả dụng**. User chọn Mã nội bộ trong form Sửa Nháp → auto-đổ Tên SP nội bộ + ĐVT chính (AC-6) · SL quy đổi = SL xuất × tỷ lệ quy đổi (AC-7) · Tồn khả dụng theo (mã+kho+garage). Người phụ trách = user chuyển SO trạng thái; Người giao hàng để trống. **1 SO chỉ sinh phiếu auto-create 1 lần duy nhất**; partial delivery / lần chuyển trạng thái thứ 2+ → user tạo tay. User được sửa Detail tự do trước khi ghi sổ; ghi sổ vẫn tuân BR-IDV2-028 (bắt buộc Mã nội bộ) + BR-IDV2-008 (chặn tồn âm) + BR-IDV2-007 (lock kỳ). Xem thêm CB-IDV2-003 (trigger event). Đối xứng **BR-IRV2-033** phía Nhập. | Auto-create | FEAT-ID-CREATE-V2 |
| BR-IDV2-033 | **Post-Save navigation & concurrent edit** (đối xứng **BR-IRV2-034**): (a) Sau khi user nhấn **Lưu** form Tạo/Sửa phiếu Xuất thành công → hệ thống đóng form, **chuyển sang màn Chi tiết phiếu vừa tạo/sửa** + hiển thị **toast xanh 3 giây** với nội dung "Lưu thành công". (b) **Concurrent edit — last-write-wins**: KHÔNG có optimistic-lock ở V2. Nếu 2 user cùng mở form Sửa 1 phiếu, người **Lưu sau đè lên** người trước — hệ thống KHÔNG hiển thị cảnh báo, KHÔNG track version. Trade-off chấp nhận cho V2; phiếu Ghi sổ đã đóng thì không cho Sửa (BR-IDV2-024) → cửa sổ xung đột chỉ ở trạng thái Nháp. | UI Behavior / Concurrency | FEAT-ID-CREATE-V2, FEAT-ID-EDIT-V2 |
| BR-IDV2-034 | **Chặn xóa phiếu Xuất bán khi có phiếu Nhập hàng bán bị trả lại tham chiếu (reference integrity)**: khi thao tác xóa phiếu Xuất có **Loại = `DELIVERY_SALE`** (Xuất bán), nếu tồn tại ≥ 1 phiếu Nhập kho **loại `RECEIPT_SALE_RETURN`** (Nhập hàng bán bị trả lại) có `source_delivery_id = {phiếu này}` — **bất kể trạng thái phiếu con** (Nháp / Ghi sổ kho — V2 không có "Đã hủy" theo BR-IDV2-002) → **chặn xóa** → mã lỗi **`ERR-INV-049`**. Popup hiển thị **danh sách tất cả** mã phiếu con để user điều hướng xóa từng phiếu trước khi xóa phiếu gốc. Message template: `"Không thể xóa phiếu — đã có {N} phiếu trả tham chiếu: {danh sách mã}. Vui lòng xóa các phiếu trả trước"`. **Chỉ áp cho `DELIVERY_SALE`** — các loại phiếu Xuất khác (`DELIVERY_PURCHASE_RETURN`, `DELIVERY_REPAIR`, `DELIVERY_OTHER`) không có logic phiếu con trả nên không áp. Lý do: phiếu con giữ FK `source_delivery_id` + kế thừa đơn giá/giá vốn từ phiếu gốc (BR-IRV2-031) → xóa gốc làm orphan reference + BQGQ tính sai (BR-PRC-001/005). Đối xứng **BR-IRV2-035** phía Nhập mua. | Reference Integrity | FEAT-ID-DELETE |
## §3 Change Log

| Date | Version | Author | Description |
|---|---|---|---|
| 2026-06-16 | 17 | Business Authority | Fix (quyết định BA cuối): kỳ đã khóa → ẩn nút Sửa (đảo lại "Sửa vẫn hiện"); làm rõ Thêm mới chặn khi Lưu (BR-IDV2-007). Guard Lưu = phòng vệ. |
| 2026-06-16 | 16 | Business Authority | Đăng ký mã lỗi: đổi bare UPPERCASE_SNAKE → ERR-INV-NNN (ERROR-CODE-REGISTRY §4) cho nhóm Xuất kho V2. |
| 2026-06-16 | 15 | Business Authority | Fix (quyết định BA — ý f): kỳ đã khóa → **KHÔNG ẩn nút "Sửa"** (vẫn mở form được, **chặn khi Lưu** — FEAT-ID-EDIT-V2 AC-2); chỉ ẩn Xóa/Ghi sổ/Bỏ ghi sổ (BR-IDV2-024). Đồng bộ FEAT-ID-DETAIL-V2 AC-4 + EC-2. |
| 2026-06-16 | 14 | Business Authority | Fix (quyết định BA): tệp đính kèm tuân platform ≤10MB + PDF/JPG/PNG (ERR-CMN-004/005), bỏ ≤30MB/5-định-dạng |
| 2026-06-03 | 1 | Business Authority | Khởi tạo BR-GF-INVENTORY-DELIVERY-V2 (file mới) — 25 rule: vòng đời Nháp/Ghi sổ/Bỏ ghi sổ, trừ tồn theo SL quy đổi, check tồn khả dụng (chặn tồn âm point-in-time), giá vốn xuất=0 đến khi BQGQ, đối soát SO (cảnh báo), lock kỳ, xóa phiếu ghi sổ khi kỳ chưa khóa, ẩn/hiện nút, BR-IDV2-025 nguồn trường form (Đối tượng theo loại phiếu, người phụ trách default user, nguồn xuất kế thừa SO, ĐVT từ mã, SL>0), in phiếu Mẫu 02-VT (mã số = mã nội bộ) + xuất excel danh sách kèm chi tiết phụ tùng. |
| 2026-06-03 | 2 | Business Authority | Gắn **mã lỗi** (UPPERCASE_SNAKE): INSUFFICIENT_STOCK, NEGATIVE_STOCK_NOT_ALLOWED, ACCOUNTING_PERIOD_CLOSED; cảnh báo SERVICE_ORDER_MISMATCH (warning, không chặn). |
| 2026-06-10 | 3 | Business Authority | Gỡ nhắc **"Import dòng"** khỏi BR-IDV2-018 (V1 vốn không có chức năng này; tab chi tiết chỉ Thêm phụ tùng + xóa per-dòng). |
| 2026-06-10 | 4 | Business Authority | Thêm **BR-IDV2-027**: dropdown "Mã SP nội bộ" có "+ Tạo mới mã nội bộ" → điều hướng `FEAT-CAT-PROD-CREATE` (cảnh báo rời trang nếu chưa lưu). |
| 2026-06-10 | 5 | Business Authority | BR-IDV2-027: bổ sung điều kiện — nếu dòng **đã có SKU** → pre-fill mã + tên SKU vào form Tạo mã nội bộ (FEAT-CAT-PROD-CREATE AC-1b). |
| 2026-06-10 | 6 | Business Authority | **Sửa BR-IDV2-027**: chỉ **gắn sẵn SKU vào tab Mã SKU**, **Mã + Tên nội bộ đều nhập tay**; SKU **đã mapping mã khác** → không gắn sẵn. |
| 2026-06-10 | 7 | Business Authority | Thêm **BR-IDV2-028**: phiếu Nền tảng (**bán `gf-sales` đẩy từ SO/phiếu dịch vụ**) khởi tạo **Nháp** (chưa tác động tồn, dòng có thể chỉ có SKU); **Ghi sổ kho bắt buộc mọi dòng có mã nội bộ** → thiếu thì chặn (`INTERNAL_PRODUCT_REQUIRED`). Đối xứng BR-IRV2-028 nhưng nguồn = bán/SO (không phải mua). |
| 2026-06-10 | 8 | Business Authority | Thêm **BR-IDV2-029**: "+ Thêm ĐVT quy đổi" trong dropdown ĐVT xuất → modal inline thêm ĐVT quy đổi cho mã nội bộ (validate tỷ lệ>0, không trùng) + tự chọn vào dòng. |
| 2026-06-15 | 9 | Business Authority | Theo quyết định BA: **KHÔNG** thêm rule chặn riêng cho phiếu xuất trước OB — xuất trước "Tồn đến ngày" OB tồn = 0 nên tự bị chặn bởi **tồn âm / quá số dư** (BR-IDV2-004); chỉ phiếu **nhập** cần rule riêng (BR-IRV2-030, vì nhập không gây tồn âm). Bổ sung ghi chú vào BR-IDV2-004; sửa heading §2.4 đếm đúng (..029). |
| 2026-06-15 | 10 | Business Authority | Đổi nhãn cột **"giá vốn" → "tiền vốn"** ở BR-IDV2-008 (cột hiển thị), BR-IDV2-017 (data shape), BR-IDV2-021 (LIST) — đồng bộ "Tiền vốn". Giữ "đơn giá vốn" và khái niệm "giá vốn xuất". |
| 2026-06-15 | 11 | Business Authority | Đổi thuật ngữ tiếng Anh sang **"sổ tồn"** (BR-IDV2-006). |
| 2026-06-15 | 12 | Business Authority | Rà lỗ hổng (Nhóm C-7): BR-IDV2-017 ghi rõ **công thức "tiền vốn = Đơn giá vốn × SL quy đổi"** (theo ĐVT chính) — trước đây chỉ liệt kê trường, thiếu công thức. |
| 2026-06-16 | 13 | Business Authority | Fix: bổ sung FEAT-ID-DELETE vào mapping BR-IDV2-004 (cột Features) — đồng bộ với FEAT-ID-DELETE §5 viện dẫn BR-IDV2-004 (chặn tồn âm point-in-time). |
| 2026-06-16 | 18 | Business Authority | Gỡ con trỏ intro tới `Plan/INVENTORY-V2-RULES.md` §7.1 (note file sắp xóa) → đổi sang `BR-GF-INVENTORY-RECEIPT-V2`. |
| 2026-06-16 | 19 | Business Authority | Thêm **BR-IDV2-030** (cơ chế kế thừa phía xuất, đối xứng BR-IRV2-031): loại "Xuất trả hàng mua" → trường **"Số phiếu nhập"** (thay "Mã đơn hàng SO"); chọn phiếu Nhập mua → kế thừa **Đối tượng** (NCC) + **Kho xuất** + **toàn bộ Detail** (kèm **đơn giá/giá vốn kế thừa từ phiếu Nhập mua gốc**); dòng kế thừa **sửa được** (SL/đơn giá/xóa dòng — trả một phần); **KHÔNG bắt buộc chọn — không chọn → nhập SL & đơn giá tay**. Giá vốn = khoản giảm-trừ phía nhập trong BQGQ (BR-PRC-001/005). Heading §2.4 → ..030. |
| 2026-06-16 | 20 | Business Authority | Thêm **BR-IDV2-031**: khi có chọn "Số phiếu nhập" → **SL xuất trả ≤ SL đã nhập** của dòng tương ứng; vượt → chặn lưu/ghi sổ → `ERR-INV-040`. Không chọn → không áp. Đối xứng BR-IRV2-032. Heading §2.4 → ..031. |
| 2026-07-02 | 22 | Business Authority | **BR-IDV2-003 + BR-IDV2-005**: thêm ref **"gọi quy tắc tính lại sổ tồn (BR-STKV2-005a)"** — centralize cascade logic, tất cả write-path gọi chung 1 engine. |
| 2026-06-29 | 21 | Business Authority | **Bump BR-IDV2-026 dung lượng tệp đính kèm 10 MB → 30 MB** (BA chốt rà soát toàn Inventory V2: kế toán cần upload PDF chứng từ + ảnh chất lượng cao). Đồng bộ với W03 Catalog (BR-CAT-PROD-015 v16) + W05 Receipt V2 (BR-IRV2-026 v25) — toàn Inventory V2 nhất quán 30 MB. Định dạng PDF/JPG/PNG + cap 5 tệp + mã lỗi `ERR-CMN-004/005` giữ nguyên. Đồng bộ FEAT-ID-CREATE-V2 + FEAT-ID-EDIT-V2 + ERROR-CODE-REGISTRY (registry follow-up CR dual-owner BA + Architect). |
| 2026-07-13 | 23 | Business Authority (BA in-session review W05 chuẩn bị) | **BR-IDV2-010 thêm mã enum backend cho Loại phiếu** (Option A prefix `DELIVERY_*`, đối xứng với `RECEIPT_*` — BR-IRV2-009 v28). Lock 4 mã: `DELIVERY_SALE` / `DELIVERY_PURCHASE_RETURN` / `DELIVERY_REPAIR` / `DELIVERY_OTHER`. Rationale: (a) khớp epic `EP-INVENTORY-DELIVERY-V2`, dễ trace; (b) unique namespace tránh đụng enum khác (VD `RepairStatus`); (c) đối xứng cặp trả: `DELIVERY_PURCHASE_RETURN` ↔ `RECEIPT_PURCHASE`, `DELIVERY_SALE` ↔ `RECEIPT_SALE_RETURN`. Label tiếng Việt chỉ dùng UI; mọi filter/API/DB/event/log dùng mã enum. |
| 2026-07-13 | 24 | Business Authority (BA in-session review W05 chuẩn bị) | **BR-IDV2-021 rà spec danh sách** (cascade FEAT-ID-LIST-V2 v4, W05 pre-DEV — apply pattern song song BR-IRV2-021 v27). (a) **Search LIKE bỏ "Người tạo"** — chỉ giữ Số phiếu xuất + Phiếu dịch vụ; Người tạo tách sang thông tin audit không phục vụ tìm nhanh. (b) **Filter 4→5** — thêm **"Người phụ trách"** (dropdown nhân sự). (c) **Filter Đối tượng** đổi nguồn: từ full 3 danh mục (NCC + KH + NV) → **DISTINCT `(object_type, object_id)` từ bảng phiếu xuất** — dedup **theo mã** (`object_id`) không theo tên; hiển thị tên hiện thời từ danh mục master + suffix loại + mã; cascade với Loại phiếu (Xuất bán → KH; Xuất trả hàng mua → NCC; Xuất sửa chữa → NV). Lý do: chỉ show Đối tượng đã có phiếu → chọn = có kết quả; single-boundary (không cross gf-customer/gf-hrms cho dropdown filter). (d) Ghi rõ: search + filter combine **AND**; nút "Reset tất cả filter"; sort mặc định **Ngày xuất DESC**. BA quyết KHÔNG thêm filter Kho + KHÔNG thêm filter Nguồn xuất (giữ nguyên baseline — Nguồn xuất kế thừa V1 không xuất hiện làm filter trong V2 doc). |
| 2026-07-13 | 25 | Business Authority (BA in-session review W05 chuẩn bị) | **BR-IDV2-025 filter Người phụ trách theo trạng thái "Đang làm việc"** (form Tạo + Sửa phiếu Xuất — apply song song BR-IRV2-025 v29). Danh sách nhân sự trong dropdown "Người phụ trách" **chỉ show nhân sự có status = "Đang làm việc"** — filter cứng tại nguồn (không hiện Ngừng làm việc / Nghỉ việc). **Edge case phiếu cũ**: nhân sự đã bị đổi status sau khi tạo phiếu → dropdown khi mở form Sửa vẫn hiện selected value (tên đó) để không mất reference, nhưng KHÔNG cho chọn lại nhân sự đã ngừng khác. Cascade FEAT-ID-CREATE-V2 v19 + FEAT-ID-EDIT-V2 (reference BR canonical). |
| 2026-07-13 | 26 | Business Authority (BA in-session review W05 chuẩn bị) | **BR-IDV2-025 filter Đối tượng theo status** (form Tạo + Sửa phiếu Xuất — apply song song BR-IRV2-025 v30 + cùng pattern Người phụ trách). Dropdown "Đối tượng" (cả 3 loại NCC/KH/NV theo Loại phiếu) **filter cứng theo status**: NCC chỉ "Đang hoạt động"; KH chỉ "Đang hoạt động"; NV chỉ "Đang làm việc". **Edge case phiếu cũ**: đối tượng đã ngừng → form Sửa vẫn hiện selected value, nhưng không cho chọn lại đối tượng đã ngừng khác. Cascade FEAT-ID-CREATE-V2 v20 + FEAT-ID-EDIT-V2 (reference BR canonical). |
| 2026-07-13 | 27 | Business Authority (BA in-session review W05 chuẩn bị) | **BR-IDV2-021 filter Người phụ trách đổi sang DISTINCT từ bảng phiếu** (Option B — apply cùng pattern Đối tượng + đồng nhất với BR-IRV2-021 v28, cascade FEAT-ID-LIST-V2 v5). Trước v24: "dropdown nhân sự". Sau v25: **DISTINCT `staff_id` từ bảng phiếu xuất** — không lấy full danh mục nhân sự; dedup theo mã `staff_id`; hiển thị tên hiện thời từ danh mục master; **nhân sự đã ngừng vẫn hiện tên** (cover phiếu cũ do nhân sự đã nghỉ tạo). Lý do BA chọn B: (a) chỉ show Người phụ trách đã có phiếu → chọn = có kết quả; (b) cover cả nhân sự đã nghỉ; (c) single-boundary consistent với Đối tượng filter. Khác nhau với form Tạo/Sửa (BR-IDV2-025 chỉ "Đang làm việc"): form tạo mới phiếu → chỉ chọn active; filter LIST → history bao gồm cả nhân sự đã nghỉ. |
| 2026-07-13 | 28 | Business Authority (BA in-session review W05 chuẩn bị) | **BR-IDV2-027 đổi từ điều hướng → modal inline** (Option A — modal reuse full component `FEAT-CAT-PROD-CREATE`, đồng bộ với BR-IRV2-027 v32). Trước: click "+ Tạo mới mã nội bộ" → điều hướng sang màn Tạo mã nội bộ (cảnh báo rời trang). Sau: **mở modal inline không rời form phiếu**; modal chứa full 4 tab (Thông tin chung / ĐVT quy đổi / Mã SKU / Đính kèm file) reuse component `FEAT-CAT-PROD-CREATE` (cùng validation/logic, khác container). Lưu thành công → mã mới tự chọn vào dòng, đóng modal, quay về form phiếu giữ nguyên dữ liệu. Hủy modal → không tạo. **Bỏ cảnh báo rời trang**. Pre-fill SKU khi dòng có SKU chưa mapping vẫn áp (AC-1b). Consistent với pattern BR-IDV2-029 "+ Thêm ĐVT quy đổi" đã là modal inline. Category cột đổi Form/Navigation → Form/Modal. Cascade FEAT-ID-CREATE-V2 AC-5b + FEAT-CAT-PROD-CREATE AC-1b. |
| 2026-07-13 | 29 | Business Authority (BA in-session review W05 chuẩn bị) | **BR-IDV2-025 thêm rule radio-inside-dropdown cho "Xuất khác"** (Option A — chỉ áp cho Xuất khác, đồng bộ BR-IRV2-025 v33). Trước: "Xuất khác → NCC/KH/NV" (không mô tả cơ chế). Sau: 1 trường "Đối tượng" duy nhất trên form; mở dropdown → popup hiện **radio group "Loại đối tượng" ở top** (3 lựa chọn NCC/KH/NV single-select) + search + list. Default trước khi tích radio: list rỗng + placeholder "Chọn loại đối tượng trước". Tích radio → list re-fetch từ bảng tương ứng (`gf-customer` supplier/customer, `gf-hrms` employee). Đổi radio → clear selection + reload list. Form Sửa: radio auto tick theo `object_type` đã lưu + cho đổi. **CHỈ áp cho "Xuất khác"** (Xuất bán/Xuất trả mua/Xuất sửa chữa fix 1 loại, dropdown mở ra list trực tiếp không radio). Lý do: 3 loại đối tượng lưu ở 3 bảng khác nhau, không merge được 1 dropdown → chọn loại trước để backend query 1 bảng tối ưu. Cascade FEAT-ID-CREATE-V2 row Đối tượng + FEAT-ID-EDIT-V2 §5 ref BR (auto). |
| 2026-07-13 | 30 | Business Authority | **Bổ sung BR-IDV2-032 auto-create phiếu Xuất từ SO + CB-IDV2-003 trigger event** (BA chốt in-session review W05 chuẩn bị, đối xứng BR-IRV2-033 phía Nhập). (a) BR-IDV2-032 mô tả rule nghiệp vụ auto-create: SO SERVICE → "Đang thực hiện" lần đầu (canonical FEAT-SO-DETAIL AC-17); SO RETAIL → "Đã xác nhận" lần đầu → phiếu Nháp; loại phiếu theo SO (`DELIVERY_REPAIR` / `DELIVERY_SALE`); nguồn xuất fix "Nền tảng"; kho = kho đầu tiên garage; điều kiện có phụ tùng nguồn INVENTORY + flag `Inventory:InventoryV2` bật; 1 SO 1 lần; partial delivery sau tạo tay. (b) CB-IDV2-003 mô tả cross-boundary event từ `gf-sales` qua Kafka outbox → inbox dedup (ADR-004) + workflow `DeliveryFulfillmentWorkflow` (Temporal — giữ tên V1, logic bên trong cập nhật V2). Đóng gap V1→V2 (V1 fix nguồn "Nền tảng" + trạng thái "Chờ duyệt"). Heading §2.4 → ..032. Đồng bộ FEAT-ID-CREATE-V2 (AC-15 + BR-IDV2-032 ref §5) + EP-INVENTORY-DELIVERY-V2 + UX-FLOW-INVENTORY-DELIVERY-V2 (cascade cùng session). Follow-up: cascade FEAT-SO-DETAIL AC-17 + BR-GF-SALES BR-CROSS-004 + EP-INVENTORY-DELIVERY V1 (V1 boundary) — deferred, chờ owner gf-sales confirm. V1 EP line 76 note stale (nói "Hoàn thành" — canonical là AC-17 theo Q0 chốt). |
| 2026-07-13 | 31 | Business Authority | **Clarify field-mapping BR-IDV2-032 (Q7-Q11 BA chốt)** — rule "Data kế thừa từ SO": tách rõ **ĐVT xuất + SL xuất kế thừa nguyên** (không convert — Q11=A); **KHÔNG kế thừa Mã nội bộ** (Q7=B — same pattern Nhập PO); **KHÔNG kế thừa Đơn giá vốn từ SO** (Q8=A đổi) — Đơn giá vốn = 0, Tiền vốn = 0 (nhất quán V2 §2A / BR-IDV2-017, chờ BQGQ update). Đối tượng `DELIVERY_REPAIR` = user chuyển SO trạng thái (Q9=A giữ nguyên); kho = kho đầu tiên garage (Q10=A giữ nguyên). Cascade FEAT-ID-CREATE-V2 v24 + UX-FLOW-INVENTORY-DELIVERY-V2 v12. |
| 2026-07-13 | 32 | Business Authority | **Restructure BR-IDV2-032 "Data kế thừa" thành 3 nhóm state field-level** (BA chốt sau khi rà tiếp): (a) Kế thừa từ SO có giá trị: SKU / Tên phụ tùng theo SKU / ĐVT xuất / SL xuất; (b) Fix cứng có giá trị: Kho / Đơn giá vốn = 0 / Tiền vốn = 0; (c) **Để TRỐNG "—"** vì phụ thuộc Mã nội bộ chưa chọn: **Mã SP nội bộ / Tên SP nội bộ / ĐVT chính / SL quy đổi / Tồn khả dụng**. Lý do rewrite: wording v31 chỉ nói "Mã nội bộ để trống" — user hỏi "SL quy đổi lúc đẩy sang để gì" → cần state rõ 5 field derived đều trống cho tới khi user chọn Mã nội bộ trong form Sửa Nháp. Cascade FEAT-ID-CREATE-V2 v25 + UX-FLOW EC-8 v13. |
| 2026-07-14 | 33 | Business Authority | **Sync BR-IDV2-019 với HTML oracle Mẫu 02-VT** (BA-review 2026-07-14 F-NEW-1 + F-NEW-2): "Người lập **phiếu**" → "Người lập **biểu**" (chuẩn TT 99/2025/TT-BTC); nêu rõ 5 vai ký — **4 vai đầu hint "(Ký, họ tên)"** + **Giám đốc hint "(Ký, họ tên, đóng dấu)"** (đại diện pháp lý, cần dấu công ty); cite template oracle `Product/ux/assets/phieu-xuat-kho-02-vt.html` làm reference cho DEV render `PrintDeliveryV2`. |
| 2026-07-15 | 37 | Business Authority | **Thêm BR-IDV2-034 — reference-integrity chặn xóa Xuất bán khi có Nhập hàng bán bị trả lại tham chiếu** (BA-review W05 prep, gap phát hiện trong session cross-check, đối xứng BR-IRV2-035). Áp cho `DELIVERY_SALE` only; check `RECEIPT_SALE_RETURN.source_delivery_id`; áp bất kể trạng thái phiếu con (V2 không có "Đã hủy" theo BR-IDV2-002). Dialog liệt kê tất cả mã phiếu con. Mã lỗi `ERR-INV-049` (mới). Heading §2.4 → ..034 (thêm 1 rule). Cascade BR-GF-INVENTORY-RECEIPT-V2 v40 (thêm BR-IRV2-035) + FEAT-ID-DELETE v3 (thêm AC-6 + BR ref + EC-4) + ERROR-CODE-REGISTRY v23 (thêm ERR-INV-049). |
| 2026-07-14 | 34 | Business Authority | **Thêm BR-IDV2-033 Post-Save navigation & concurrent edit** (BA-review 2026-07-14 C2.4 + C2.5 unblock, đối xứng BR-IRV2-034): (a) Standard nav "Chuyển Chi tiết + toast success 3s". (b) Concurrent edit last-write-wins — không optimistic-lock. Heading §2.4 → ..033 (thêm 1 rule). Cascade FEAT-ID-CREATE-V2 §5 + FEAT-ID-EDIT-V2 §5 cite. |
| 2026-07-14 | 35 | Business Authority | **BR-IDV2-021 thêm "Diễn giải" vào search LIKE** (BA chốt in-session, đối xứng BR-IRV2-021 v38): search giờ LIKE case-insensitive trên **3 field** (Số phiếu xuất / Phiếu dịch vụ / **Diễn giải**) — match kiểu OR. Lý do: user thường ghi context nghiệp vụ (VD "Xuất sửa xe cho KH-045 lô hàng 07/2026") vào Diễn giải. Cascade FEAT-ID-LIST-V2 v6. |
| 2026-07-14 | 36 | Business Authority | **BR-IDV2-026 mở rộng attachment whitelist: +DOC, +XLSX** (Figma-crosscheck W05 SYS-2 P0 unblock, đối xứng BR-IRV2-026 v39). Whitelist mới: PDF/JPG/PNG/DOC/XLSX. Cascade ERR-CMN-005 v22 + FEAT-ID-CREATE-V2 + FEAT-ID-EDIT-V2 AC-13/AC-5b. |
| 2026-07-15 | 38 | Business Authority | **BR-IDV2-009 mở rộng**: (a) cột "Features" thêm `FEAT-ID-EDIT-V2` (BE re-check reconciliation khi sửa phiếu POSTED — PKG-W05 W05-D4); (b) rule note thêm wording verbatim UI banner ("Đối soát với đơn dịch vụ có sai lệch" / "Không thể đối soát với đơn dịch vụ" case DEGRADED) + canonical FEAT AC ref (`FEAT-ID-DETAIL-V2` AC-2b). Cascade GAP #3 pre-DEV W05: FEAT-ID-DETAIL-V2 v10 (thêm AC-2b) + FEAT-ID-EDIT-V2 v14 (thêm AC-4b) + FEAT-ID-CREATE-V2 v29 (mở rộng AC-12). Vị trí/màu/icon chi tiết chờ Figma. |
| 2026-07-16 | 39 | Business Authority | **BR-IDV2-021 extend mobile-only Post/Unpost inline row-action** (GAP-02 pre-DEV W05 rà soát, đối xứng BR-IRV2-021 v41 — cover PKG-W05 §2.2.4 mobile scope). Mobile List cột "Thao tác" hiển thị icon Post/Unpost inline per-row (khác web). Nháp+kỳ mở → "Ghi sổ kho"; Ghi sổ+kỳ mở → "Bỏ ghi sổ kho"; kỳ đóng → ẩn cả 2. Popup verbatim khớp FEAT-ID-DETAIL-V2 AC-5/AC-6. Không có icon Sửa/Xóa/In trên mobile List (web-only). Category cột đổi Tenant Isolation / Search → Tenant Isolation / Search / Mobile Row-action. Features cột thêm FEAT-ID-DETAIL-V2. Cascade FEAT-ID-DETAIL-V2 v12 (AC-4 mobile carve-out). |
| 2026-07-16 | 41 | Business Authority (in-session BA fix — Figma FEAT-ID-CREATE-V2 W05 canonical) | **BR-IDV2-025 flip radio-inside-dropdown → 2 dropdown song song trên form** (Option B, đối xứng BR-IRV2-025 v34 upcoming — align với Figma FEAT-ID-CREATE-V2 W05 canonical). Trước v29: 1 field "Đối tượng" duy nhất, popup có radio "Loại đối tượng" ẩn ở top. Sau: **2 dropdown độc lập cạnh nhau trong section "Thông tin chung"** — "Loại đối tượng"* (NCC/KH/NV) + "Đối tượng"* (list load theo Loại đối tượng). Widget "Loại đối tượng" là **UI filter** (không phải business field độc lập lưu riêng ở DB), FE derive `objectType` gửi BE trong cùng payload. **Backend contract giữ nguyên 100%** — data model `object_type + object_id` + enum `DeliveryObjectType SUPPLIER\|CUSTOMER\|EMPLOYEE` + REST/GraphQL/Kafka field không đổi (0 architecture change). Lý do flip: Figma FEAT-ID-CREATE-V2 W05 vẽ 2 dropdown song song, user in-session 2026-07-16 confirm giữ Figma làm canonical UX. Cascade FEAT-ID-CREATE-V2 + FEAT-ID-EDIT-V2 row "Đối tượng" wording + 4 dòng prose comment `gf-inventory-api.md` §3c/§3d field-table `objectType` (text-only, không phải contract change). |
| 2026-07-16 | 40 | Business Authority | **BR-IDV2-009 rewrite — narrow V1 semantic (popup logic thay banner) + scope popup xác nhận Ghi sổ chung khác theo entry point + CB-IDV2-003 SO SERVICE/RETAIL trigger**. (a) BR-IDV2-009 từ "1 dòng bare" v38 chuyển thành spec đầy đủ: 3 entry point popup đối soát ((a) form Sửa dropdown, (b) form Chi tiết button, (c) mobile List inline), CHỈ trigger tại state transition Nháp → Ghi sổ (KHÔNG trigger Save form Tạo / Save Nháp giữ Nháp / Bỏ ghi sổ) — đối xứng V1 baseline `BR-ID-DTL-006`, không broaden CREATE+EDIT như spec cũ. Flow BE: popup confirm chung → BE call `gf-sales /protected/v1/product/so-summary` → case lệch (`ERR-INV-039`) popup verbatim với title "Số lượng phụ tùng trong phiếu xuất kho và phiếu dịch vụ #{mã_phiếu} chưa trùng khớp..." + `[Đóng]`+`[Vẫn Ghi sổ]`; case DEGRADED (`ERR-CMN-007-DEGRADED` fail-OPEN) popup verbatim "Hệ thống chưa đối soát được vì mất kết nối phòng dịch vụ..."; case match / không SO commit trực tiếp. **Scope popup xác nhận Ghi sổ chung khác nhau theo entry point**: (a) nút "Ghi sổ kho" ở Detail web+mobile+mobile List inline CÓ popup xác nhận (primary action cần confirm); (b) form Sửa dropdown Nháp→Ghi sổ+Lưu KHÔNG popup xác nhận (user đã explicit chọn dropdown + Lưu). Popup lệch/DEGRADED wording IDENTICAL cả 2 entry. **Banner cũ AC-2b BỎ HOÀN TOÀN** — thay bằng popup logic. (b) CB-IDV2-003 update: SO SERVICE → `"Đang thực hiện"` lần đầu; SO RETAIL → `"Đã xác nhận"` lần đầu (canonical FEAT-SO-DETAIL AC-17). Loại phiếu theo SO: SERVICE → `DELIVERY_REPAIR`, RETAIL → `DELIVERY_SALE`; nguồn xuất fix "Nền tảng"; chỉ trigger khi SO có phụ tùng nguồn INVENTORY + flag `Inventory:InventoryV2` bật. Idempotency: 1 SO chỉ sinh phiếu 1 lần (inbox dedup ADR-004); partial delivery/lần chuyển state 2+ không phát lại → user tạo tay. Cascade: FEAT-ID-DETAIL-V2 v13 (AC-2b deprecated + AC-5 rewrite popup) + FEAT-ID-EDIT-V2 v15 (AC-4b rewrite popup verbatim, không delegate về AC-2b) + FEAT-ID-CREATE-V2 v30 (AC-12 BỎ HOÀN TOÀN — không trigger đối soát tại Save form Tạo). |