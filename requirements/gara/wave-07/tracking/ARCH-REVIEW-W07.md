# ARCH-REVIEW W07 — 2026-08-07

> Scope note: label "W07" is informal (epic `EP-PARTNER-LINK`, authored via `/arch-design` outside standard wave-planning flow — see `Tracking/arch-design-partner-link-answers-1.md`). Review covers exactly the **14 files** changed by commit `eec8ee9` ("add architecture for wave 07"). Diffs verified via `git diff eec8ee9^..eec8ee9 -- <file>` to exclude unrelated W06 (Inventory V2 slice 4/4) changes co-resident on the same branch.

## Summary

| Files reviewed | P0 | P1 | P2 | Ready for SA ratify |
|---|---|---|---|---|
| 14 | 0 | 0 | 2 | **true** |

## Gate Verdict — wave-level (applies uniformly unless noted per-file below)

| Gate | Verdict | Evidence / Note |
|---|---|---|
| G1 Frontmatter compliance | PASS | All 14 files have `type/artifact_kind/tier/owner_authority/boundary/status/version/last_reviewed`. `boundary` matches file path for every touched file except pre-existing baseline fields NOT touched by this commit (`gf-system-events.md:8 boundary: tenant-system`, `gf-sales-events.md:8 boundary: sales`) — drift predates W07, not introduced by this diff, not scored. `INTEG-EXT-driver-plus.md:8 boundary: "gf-system, gf-sales"` — minor format deviation from documented convention `"cross-boundary (b1, b2)"` — **P2** (see Findings). ADR-029 `boundary: global` and ADR-030 `boundary: gf-system` both correct per G1 example rule. |
| G2 Versioning 3-in-1 | PASS | Verified all 14 files: version bump + `last_reviewed=2026-08-05` + Change Log row, simultaneously, for every Modified file (`gf-sales-api.md` v10→v11, `gf-system-api.md` v2→v3, `gf-sales-data-model.md` v1.6→v1.7, `gf-system-data-model.md` v2→v3, `gf-sales-events.md` v6→v7, `gf-system-events.md` v3→v4, `gf-sales-HLD.md` v7→v8, `gf-system-HLD.md` v3→v4, `agg-garage-graph-graphql.md` v7.83→v7.84, `INTEG-FE-garage-web-agg-garage-graph.md` v23→v24, `INTEG-MOB-garage-mobile-agg-garage-graph.md` v9→v10). New files (`ADR-029`, `ADR-030`, `INTEG-EXT-driver-plus.md`) at `version: 1` — N/A for 3-in-1 (new-file rule is G1, not G2). |
| G3 12 Critical Rules | PASS | Boundary isolation: Driver+ adapter kept inside owning boundary (`gf-system` partner-link, `gf-sales` booking), explicitly rejects `gf-erp-agent` hop (ADR-029 Alternatives). JPA ADR-009: `gf-system-data-model.md §2bis` + `gf-sales-data-model.md §2ter` — scalar columns only, "Không FK vật lý (ADR-009)" stated explicitly for both new tables. Outbox/inbox: `KafkaMessageWrapper`, topic `AC-DEV-PARTNER-LINK-EVENTS` (matches `AC-DEV-{DOMAIN}-EVENTS`), `MessageGroup=PARTNER_LINK` + step filter mandated + inbox dedupe by `messageId`, documented consistently in HLD/events/INTEG-EXT. Tenant isolation: REST endpoints require `X-Tenant-Id`; event payload `data.tenantId` must match `headers.OriginTenantId`, mismatch → "fail có kiểm soát, KHÔNG side-effect im lặng" (`gf-system-events.md §3.10`). Dual persona: grepped all 14 files' added lines for `kỹ thuật viên\|thợ\|lễ tân\|manager\|technician\|receptionist` — only 1 false-positive hit (`gf-hrms` "advisor/technician" as an *employee record field label for printing*, pre-existing baseline row in gf-sales-HLD §4.2, not a new UI actor). Every new API/UI surface explicitly scoped to `garage-owner` + `accountant` "ngang quyền" (`BR-DPL-CMN-004`). Projection-not-master: N/A this wave (no gf-sales customer/vehicle projection touched). |
| G4 Per-boundary toggle | PASS | `gf-system` not a ddl-auto boundary (Flyway) → correctly proposes `V7__create_tenant_profile.sql` + `V8__create_partner_link_request.sql`, both additive, explicit "KHÔNG rewrite V1..V6" citation (Gotcha #9). `gf-sales` uses a pre-existing documented hybrid (`ddl-auto=update` for entity-driven columns + Flyway for index/table, established since v1 of the data-model doc and reused by the May/June Insurance Settlement CR) — §2ter follows the exact same pre-established pattern, not a new deviation. No Temporal usage introduced/touched in either boundary (both are Temporal-eligible already for other flows; W07 doesn't touch Temporal). |
| G5 Contract-first completeness | PASS | **`gf-system-api.md`**: §2 Endpoint Summary 14 rows (8 baseline + 6 W07) vs §3 (8) + §3bis (6) = 14 — coverage match. All 6 new `partner-links*` endpoints verified with full 6-block bodies (Headers/Params/Request/Response 2xx/Response 4xx/Semantics) + fenced JSON examples. **`gf-sales-api.md`**: 0 new REST endpoints (Driver+ integration is Kafka-only per ADR-029) — N/A for Step1/2, correctly documented ("KHÔNG thêm endpoint REST"). **`agg-garage-graph-graphql.md` §3k (GraphQL Step 4)**: §2 tổng rows 372-377 (6 ops) match §3k.2 module-local table (6 ops) match §3k.6 detail blocks (`grep -c '^#### (Query\|Mutation)'` = 6). All 6 blocks have Transport/Auth/Idempotency + Downstream table + Request (GraphQL doc + variables, fenced) + Response 200 (fenced) + Error response shape (fenced) + Error codes table — spot-verified for all 6 (not "SDL + prose" only). Registry sync: rows present in §2 tổng (not only module-local) — no P1. §0 Wave Index row for W07 correctly points to `§3k Partner Link` — cascading rule satisfied, no drift flag needed. |
| G6 Coverage | PASS | 3 in-scope FEATs (`FEAT-SYS-DRIVERPLUS-LINK`, `FEAT-BOOK-DRIVERPLUS-INBOUND`, `FEAT-BOOK-DRIVERPLUS-OUTBOUND`) all map to the 5 `affected_boundaries` declared in `Tracking/arch-design-partner-link-answers-1.md` Q4 (`gf-system` primary, `gf-sales`, `agg-garage-graph`, `garage-web`, `garage-mobile`) — every boundary has ≥1 touched artifact in this commit. |
| G7 ADR numbering & cross-ref | PASS | `ls Architecture/decisions/ADR-*.md \| sort -V \| tail -1` = `ADR-030` before this commit's predecessor was `ADR-028` — ADR-029/ADR-030 are the next 2 consecutive numbers, no gap/collision. Both ADRs have all 7 required headings (Status/Context/Decision/Alternatives considered/Consequences/References/Change Log) and cross-reference ADR-004 (outbox), ADR-009 (JPA), ADR-013 (deprecation — explicitly reasoned as "not applicable, delta additive"), ADR-006 (Flyway), ADR-005. ADR-030 cross-refs ADR-029 and vice versa. |
| G8 Integration contracts | PASS | `garage-web` ∈ affected_boundaries → `INTEG-FE-garage-web-agg-garage-graph.md §3.9` has full UI Action → GraphQL Op → REST downstream mapping table (20 rows). `garage-mobile` → `INTEG-MOB-garage-mobile-agg-garage-graph.md §3.6` has the equivalent mapping (14 rows, explicitly reasoned "scope mobile = FULL 6/6 ops", different from prior waves' narrow/view-only pattern, with rationale). BFF `agg-garage-graph` has GraphQL operations defined in `agg-garage-graph-graphql.md §3k`. |
| G9 KG consistency | **UNVERIFIED** | New entities (`partner_link_request`, `tenant_profile`) and 9 new events introduced this wave, but `Execution/knowledge-graphs/gf-system.knowledge-graph.yaml` / `gf-sales.knowledge-graph.yaml` are **not** in the 14 changed files, and no `agent-arch-author` return-JSON artifact was persisted to `Tracking/` for this run (only the pre-flight answers doc exists). Cannot mechanically confirm `needs_kg_update: ["gf-system","gf-sales"]` was flagged. This matches the repo's established pattern (KG syncs happen at DEV/source-audit time, not DESIGN time — confirmed via `git log` on both KG files), so this is *not* scored as a P1 fabrication/omission, but flagged as an open item: **recommend Leader confirm with the author's actual return JSON that `needs_kg_update` includes both boundaries before `/dev-start`.** |
| G10 Ask-First Discipline | PASS | **T1** (boundary ownership): every new capability in both HLDs cites ADR-029/ADR-030 and/or KG/SERVICE-BOUNDARY-MATRIX precedent (`gf-sales` `BookingDriverPlusConsumer` production precedent explicitly cited as evidence for the "no `gf-erp-agent` hop" decision). **T2** (persona): no non-dual-persona actor introduced (see G3). **T3** (external integration): `INTEG-EXT-driver-plus.md` exists, 16/16 template sections filled, N/A sections explained rather than left blank (§3.2 webhook, §6.3 circuit breaker, §9 pricing). **T4** (markers): grepped all added lines across all 14 files for `[NEEDS-CONFIRM]`, `<TODO>`, `???`, `TBD` — 2 hits, both pre-existing baseline rows unrelated to W07 content (`gf-sales-HLD.md` §3 Key Design Decisions "TBD ADR (deprecation policy)" / "TBD ADR (projection sync policy)" — open items that predate this wave, reflowed by the section renumbering diff, not new W07 prose). **T5** (breaking change): ADR-029 explicitly enumerates the Driver+ booking delta as additive-only (D1-D12 table) and states "ADR-013 deprecation path — không áp dụng" with reasoning; `BOOKING.CANCEL.RESPONSE` is a net-new step (additive), not a rename. **T6** (naming ambiguity): `gf-system-api.md §5.2` has an explicit "Naming note (T6 resolved)" block reasoning through the `reject` vs Product-suggested `decline` naming choice. **Schema consistency**: cannot verify the raw author return JSON (see G9), but artifact-level evidence (Naming Registry present for both API files, Performance & Scale present for both HLDs, all endpoints/operations 6-block-complete) is internally consistent with a `COMPLETE` status. |
| G11 Naming Registry | PASS | Both `gf-system-api.md §5` and `gf-sales-api.md §5` have the 6-column table (Concept/BE/BFF/FE/Mobile/Cite). Spot-checked fields (`requestCode`, `partnerAccountName`, `businessName`, `driverPlusServiceType`, `cancelSource`) all present with matching names across API/registry, `gf-system-events.md` payload, `gf-system-data-model.md §2bis` column names (snake_case equivalents), and `agg-garage-graph-graphql.md §3k` SDL — no drift found. Enums (`PartnerLinkStatus`, `PartnerCode`, `PartnerLinkAction`, `CancelSource`, `DriverPlusServiceType`, `DriverPlusBookingStatus`) all enumerate full values, no `...`/`TBD`. BFF file references `See gf-system-api.md §5` instead of duplicating the registry — no P2 cleanup needed. |
| G12 Performance & Scale | PASS | `gf-system-HLD.md §7` and `gf-sales-HLD.md §7` both new, both cover all 6/6 items (expected load, pagination strategy incl. the deliberate no-pagination deviation with a 500-row defensive cap, index list, cache strategy, N+1 avoidance, tenant fairness/rate-limit). All new indexes on `partner_link_request`/`tenant_profile`/`booking` (W07-added) are `(tenant_id, ...)`-prefixed. Pre-existing non-prefixed `booking` indexes are explicitly flagged as known technical debt out of W07 scope (not silently ignored). |
| G13 Field provenance | PASS | Spot-checked 5 fields across DTO/event/entity: `partnerAccountName` (cite `FEAT AC-9`, verified exists), `driverPlusServiceType` (cite `INBOUND AC-2/AC-3`, verified), `cancelSource` (cite `BR-BOOK-023`, verified in `BR-GF-SALES.md`), `syncedAt` (cite `FEAT AC-21(a)`, verified), `truncated` (cite explicitly labeled "Design guard" rather than a false Product citation — transparent, not fabricated). No unresolvable/fabricated fields found. |

## Findings

### Architecture/events/gf-system-events.md
| Gate | Severity | Rule | Note |
|---|---|---|---|
| G5/G7-adjacent (doc structure) | P2 | Section numbering collision | `gf-system-events.md:391` (`### 3.10 Employee*Changed`, pre-existing baseline) and `gf-system-events.md:432` (`### 3.10 PartnerLink*`, new W07) both use heading number **3.10** — should have been 3.11, cascading §3.11/§3.12/§3.13 → §3.12/§3.13/§3.14. Every other touched artifact (`gf-system-HLD.md §9`, `gf-system-api.md §6`, `gf-system-data-model.md`, `INTEG-EXT-driver-plus.md §5/§15`) cites "§3.10–§3.13 Partner Link" — ambiguous given the collision (DEV/reviewer reading the events doc's own TOC will find 2 different `### 3.10` sections). Does not affect contract completeness (all 4 W07 sub-sections — inbound 3-variant + 3 outbound schemas — are individually complete and correctly ordered/linked via `xem §3.1x` prose refs), purely a cross-reference clarity defect. Recommend Leader open a lightweight follow-up (not blocking) to renumber `PartnerLink*`/`PartnerLinkRequestResponse`/`PartnerLinkProfileSync`/`PartnerLinkStatusChanged` to §3.11–§3.14 and cascade the ~5 citing files. |

### Architecture/integrations/INTEG-EXT-driver-plus.md
| Gate | Severity | Rule | Note |
|---|---|---|---|
| G1 | P2 | Frontmatter `boundary` format | `INTEG-EXT-driver-plus.md:8` uses `boundary: "gf-system, gf-sales"` for a 2-boundary cross-boundary integration contract. G1 rule specifies the convention `boundary: "cross-boundary (b1, b2)"` for this case. Content is otherwise fully correct (both boundaries clearly documented throughout §1/§4/§15); this is a pure format-convention nit with no functional impact. |

## Pre-existing / out-of-scope observations (not scored — predate commit `eec8ee9`, not touched by this diff)

- `gf-system-events.md:8` `boundary: tenant-system` and `gf-sales-events.md:8` `boundary: sales` don't match their file-path-implied boundary (`gf-system`/`gf-sales`) — this predates W07 (verified via `git show eec8ee9^:...`).
- `gf-system-HLD.md` frontmatter `depends_on` still references `../events/tenant-system-events.md` (actual file: `gf-system-events.md`); `gf-sales-HLD.md` references `../events/sales-events.md` (actual file: `gf-sales-events.md`). Both dangling references predate this commit (the W07 diff only *added* 2 new `depends_on` lines for ADR-029/030, it didn't touch these pre-existing lines).
- `Execution/SERVICE-BOUNDARY-MATRIX.md` row 2 (`gf-system`) "Modules" column doesn't yet list "Partner Link" — matrix file wasn't in this commit's scope; likely expected to be updated separately (consistent with how prior waves have handled this file).

These are flagged for Leader awareness/backlog only; they do not affect `ready_for_sa_ratify` for this review.

## Return Schema

```json
{
  "wave": "W07",
  "files_reviewed": 14,
  "p0_count": 0,
  "p1_count": 0,
  "p2_count": 2,
  "g5_body_incomplete_endpoints": 0,
  "g11_naming_violations": 0,
  "g12_perf_gaps": 0,
  "g13_fabricated_fields": [],
  "findings": [
    {"file": "Architecture/events/gf-system-events.md", "gate": "G5/doc-structure", "severity": "P2", "rule": "section numbering collision", "note": "§3.10 used twice (line 391 baseline Employee*Changed vs line 432 new PartnerLink*) — cascades ambiguity into §3.11-§3.13 cross-refs cited by 5 other files"},
    {"file": "Architecture/integrations/INTEG-EXT-driver-plus.md", "gate": "G1", "severity": "P2", "rule": "frontmatter boundary format", "note": "boundary: \"gf-system, gf-sales\" should follow documented convention boundary: \"cross-boundary (gf-system, gf-sales)\""}
  ],
  "report_file": "Tracking/ARCH-REVIEW-W07.md",
  "ready_for_sa_ratify": true
}
```
