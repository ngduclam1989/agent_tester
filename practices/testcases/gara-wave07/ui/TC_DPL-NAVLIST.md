# Test Cases — Wave 7 — Nhóm DPL (Driver Plus Link) — M1 PL-NAV-LIST

> File con của rollup [`TC_DPL.md`](TC_DPL.md) — xem file đó để biết Thông tin chung / Test Data / Traceability Matrix / Ambiguities & Q&A / Bảng tổng hợp Risk Level & Priority cho toàn bộ module DPL.

## 7. Bảng Test Cases chi tiết

**Nhóm Validate: N/A** — M1 không có ô nhập liệu tự do; lựa chọn filter là tổ hợp checkbox rời rạc, đã phủ đủ qua Decision Table ở nhóm Function (TC_003-007).

| TC ID | Module | Risk Level | Test Title | Pre-Condition | Test Steps | Expected Result | Priority | Test Data |
|---|---|---|---|---|---|---|---|---|
| **NHÓM FUNCTION — M1 PL-NAV-LIST** | | | | | | | | |
| GARA_DPL_TC_001 | M1 | High | Kiểm tra mở màn "Liên kết" thành công với layout master-detail và auto-chọn item đầu (Web) | Đăng nhập garage-owner (owner_test_20260817@gara.test); đang ở Trang chủ GMS; tenant 5001 có sẵn 4 record LKD-2026-001(PENDING)/002(LINKED)/003(REJECTED)/004(UNLINKED) | 1. Bấm menu "Liên kết" trên top-navigation | 1. Điều hướng vào tab "Driver Plus" (active mặc định)<br>2. Layout master-detail hiển thị: panel trái = danh sách, panel phải = form chi tiết<br>3. Item đầu tiên (LKD-2026-002, mới nhất theo ngày gửi trong phạm vi filter default) được auto-chọn, panel phải hiển thị đúng chi tiết item đó | Critical | tenant 5001 |
| GARA_DPL_TC_002 | M1 | High | Kiểm tra hiển thị đủ 5 thành phần mỗi item trong danh sách thành công với dữ liệu đã có record | Đăng nhập garage-owner; đang ở màn "Liên kết" tab "Driver Plus"; tenant 5001 có sẵn LKD-2026-002 (LINKED, tên D+ "Trần Thị Hoa", SĐT 0912345678, ngày gửi 05/08/2026) | 1. Quan sát item LKD-2026-002 trong panel trái | 1. Hiển thị đủ: mã "LKD-2026-002", badge "Đã liên kết" (xanh lá), tên "Trần Thị Hoa", SĐT "0912345678", ngày gửi "05/08/2026" | High | LKD-2026-002 |
| GARA_DPL_TC_003 | M1 | High | Kiểm tra hiển thị đúng default filter thành công khi mở màn "Liên kết" lần đầu | Đăng nhập garage-owner; đang ở Trang chủ GMS, vừa đăng nhập, chưa từng thao tác filter trong phiên; tenant 5001 có sẵn 4 record đủ 4 trạng thái | 1. Mở menu "Liên kết" | 1. Dropdown "Trạng thái liên kết" chỉ tick sẵn "Chờ liên kết" + "Đã liên kết"<br>2. "Từ chối" + "Đã hủy liên kết" KHÔNG tick<br>3. Panel trái chỉ hiện LKD-2026-001 (PENDING) + LKD-2026-002 (LINKED) | Critical | tenant 5001 |
| GARA_DPL_TC_004 | M1 | High | Kiểm tra tick thêm checkbox "Từ chối" thành công hiển thị bổ sung đúng item | Đăng nhập garage-owner; đang ở màn "Liên kết", filter đang ở default (2 tick); tenant 5001 có sẵn LKD-2026-003 (REJECTED) | 1. Mở dropdown filter<br>2. Tick thêm checkbox "Từ chối" | 1. Panel trái hiện thêm LKD-2026-003<br>2. Danh sách hiện đủ 3 item (001, 002, 003) | High | tenant 5001 |
| GARA_DPL_TC_005 | M1 | High | Kiểm tra untick checkbox "Đã liên kết" thành công ẩn đúng item | Đăng nhập garage-owner; đang ở màn "Liên kết", filter đang ở default (2 tick); tenant 5001 có sẵn LKD-2026-002 (LINKED) | 1. Mở dropdown filter<br>2. Untick checkbox "Đã liên kết" | 1. LKD-2026-002 biến mất khỏi panel trái<br>2. Danh sách chỉ còn LKD-2026-001 (PENDING) | High | tenant 5001 |
| GARA_DPL_TC_006 | M1 | Medium | Kiểm tra tick cả 4 checkbox trạng thái thành công hiển thị đủ 4 record | Đăng nhập garage-owner; đang ở màn "Liên kết", filter đang ở default; tenant 5001 có sẵn đủ 4 record 4 trạng thái | 1. Mở dropdown filter<br>2. Tick đủ 4 checkbox (Chờ liên kết, Đã liên kết, Từ chối, Đã hủy liên kết) | 1. Panel trái hiển thị đủ 4 item LKD-2026-001..004 | Medium | tenant 5001 |
| GARA_DPL_TC_007 | M1 | High | Kiểm tra hiển thị empty state do filter loại hết record đang có thành công khi untick toàn bộ checkbox | Đăng nhập garage-owner; đang ở màn "Liên kết", filter đang ở default; tenant 5001 có sẵn 4 record | 1. Mở dropdown filter<br>2. Untick lần lượt cả 4 checkbox | 1. Panel trái hiển thị empty state text "Không có yêu cầu nào khớp bộ lọc." (`ERR-DPL-009`)<br>2. Panel phải rỗng vì không còn item nào khớp | High | tenant 5001 |
| GARA_DPL_TC_008 | M1 | High | Kiểm tra sort danh sách theo ngày gửi mới nhất trước thành công không có phân trang | Đăng nhập garage-owner; đang ở màn "Liên kết"; tenant 9001 được seed sẵn 3 record: LKD-A (requestedAt 01/08/2026), LKD-B (requestedAt 10/08/2026), LKD-C (requestedAt 05/08/2026), tất cả PENDING | 1. Mở dropdown filter, tick "Chờ liên kết"<br>2. Quan sát thứ tự panel trái | 1. Thứ tự hiển thị: LKD-B (10/08) → LKD-C (05/08) → LKD-A (01/08)<br>2. Không có thanh phân trang xuất hiện dù có nhiều record | High | tenant 9001 |
| GARA_DPL_TC_009 | M1 | High | Kiểm tra hiển thị empty state chưa từng có yêu cầu nào thành công với tenant mới | Đăng nhập garage-owner (owner_test_20260817@gara.test) của tenant 7003 "Garage Mới"; đang ở Trang chủ GMS; tenant 7003 chưa từng có record partner_link_request nào | 1. Mở menu "Liên kết" | 1. Panel trái hiển thị tiêu đề "Danh sách yêu cầu liên kết" + text "Chưa có yêu cầu liên kết nào từ Driver Plus." (`ERR-DPL-008`)<br>2. Panel phải rỗng, không hiển thị form | High | tenant 7003 |
| GARA_DPL_TC_010 | M1 | High | Kiểm tra mở tab "Liên kết" thành công với danh sách dạng card và đếm số lượng (Mobile) | Đăng nhập garage-owner trên app Garage Mobile; đang ở màn Tổng quan; tenant 5001 có sẵn LKD-2026-001(PENDING)+LKD-2026-002(LINKED) khớp filter default | 1. Bấm tab "Liên kết" trên bottom-navigation | 1. Vào tab con "Driver Plus" (active mặc định)<br>2. Hiển thị danh sách dạng card xếp dọc (không master-detail)<br>3. Tiêu đề "Danh sách yêu cầu liên kết" kèm đếm "2 yêu cầu" | Critical | tenant 5001 |
| GARA_DPL_TC_011 | M1 | High | Kiểm tra hiển thị đủ field trên card thành công với record trạng thái Chờ liên kết (Mobile) | Đăng nhập garage-owner trên Mobile; đang ở màn "Liên kết" (tab Driver Plus, route tương đương `/partner-links`); tenant 5001 có sẵn LKD-2026-001 (PENDING, chưa xử lý) | 1. Quan sát card LKD-2026-001 | 1. Card hiển thị: mã LKD-2026-001, badge "Chờ liên kết" (cam), tên D+, SĐT, ngày gửi<br>2. Field "Người thực hiện" và "Lý do" hiển thị trống (chưa xử lý) | High | LKD-2026-001 |
| GARA_DPL_TC_012 | M1 | High | Kiểm tra hiển thị đủ field trên card thành công với record đã xử lý có Người thực hiện và Lý do (Mobile) | Đăng nhập garage-owner trên Mobile; đang ở màn "Liên kết" (tab Driver Plus, route tương đương `/partner-links`), filter tick "Từ chối"; tenant 5001 có sẵn LKD-2026-003 (REJECTED, Người thực hiện "Đăng Vinh (Chủ garage)", Lý do "SĐT không đúng") | 1. Quan sát card LKD-2026-003 | 1. Card hiển thị bổ sung "Người thực hiện: Đăng Vinh (Chủ garage)" + "Lý do: SĐT không đúng" | High | LKD-2026-003 |
| GARA_DPL_TC_013 | M1 | High | Kiểm tra hiển thị đúng nút hành động trên card thành công với record trạng thái Chờ liên kết (Mobile) | Đăng nhập garage-owner trên Mobile; đang ở màn "Liên kết" (tab Driver Plus, route tương đương `/partner-links`); tenant 5001 có sẵn LKD-2026-001 (PENDING) | 1. Quan sát card LKD-2026-001 | 1. Card hiển thị 2 nút ngay trên card: "Từ chối" + "Duyệt" | Critical | LKD-2026-001 |
| GARA_DPL_TC_014 | M1 | High | Kiểm tra hiển thị đúng nút hành động trên card thành công với record trạng thái Đã liên kết (Mobile) | Đăng nhập garage-owner trên Mobile; đang ở màn "Liên kết" (tab Driver Plus, route tương đương `/partner-links`); tenant 5001 có sẵn LKD-2026-002 (LINKED) | 1. Quan sát card LKD-2026-002 | 1. Card hiển thị 2 nút ngay trên card: "Đồng bộ lại" + "Hủy liên kết" | Critical | LKD-2026-002 |
| GARA_DPL_TC_015 | M1 | Medium | Kiểm tra card không hiển thị nút hành động nào thành công với record trạng thái Từ chối (Mobile) | Đăng nhập garage-owner trên Mobile; đang ở màn "Liên kết" (tab Driver Plus, route tương đương `/partner-links`), filter tick "Từ chối"; tenant 5001 có sẵn LKD-2026-003 (REJECTED) | 1. Quan sát card LKD-2026-003 | 1. Card không hiển thị nút hành động nào (terminal state) | Medium | LKD-2026-003 |
| GARA_DPL_TC_016 | M1 | Medium | Kiểm tra card không hiển thị nút hành động nào thành công với record trạng thái Đã hủy liên kết (Mobile) | Đăng nhập garage-owner trên Mobile; đang ở màn "Liên kết" (tab Driver Plus, route tương đương `/partner-links`), filter tick "Đã hủy liên kết"; tenant 5001 có sẵn LKD-2026-004 (UNLINKED) | 1. Quan sát card LKD-2026-004 | 1. Card không hiển thị nút hành động nào (terminal state) | Medium | LKD-2026-004 |
| GARA_DPL_TC_017 | M1 | High | Kiểm tra mở màn Bộ lọc full-screen thành công với default filter giống Web (Mobile) | Đăng nhập garage-owner trên Mobile của tenant 5001; đang ở màn "Liên kết" (tab Driver Plus, route tương đương `/partner-links`); chưa từng thao tác filter trong phiên | 1. Bấm icon phễu (bộ lọc) | 1. Mở màn full-screen riêng (điều hướng bằng nút back), tiêu đề "Bộ lọc"<br>2. Section "Trạng thái liên kết" chỉ tick sẵn "Chờ liên kết" + "Đã liên kết" (kèm chấm màu tương ứng) | High | tenant 5001 |
| GARA_DPL_TC_018 | M1 | High | Kiểm tra áp dụng lựa chọn filter thành công sau khi bấm back tại màn Bộ lọc (Mobile) | Đăng nhập garage-owner trên Mobile; đang ở màn "Bộ lọc"; tenant 5001 có sẵn LKD-2026-003 (REJECTED) | 1. Tick thêm checkbox "Từ chối"<br>2. Bấm nút back | 1. Quay lại danh sách dạng card<br>2. Danh sách hiện thêm LKD-2026-003 đúng theo filter vừa chọn | High | tenant 5001 |
| GARA_DPL_TC_019 | M1 | High | Kiểm tra hiển thị empty state 0 yêu cầu thành công với tenant chưa từng có dữ liệu (Mobile) | Đăng nhập garage-owner trên Mobile của tenant 7003 "Garage Mới"; đang ở Trang chủ; tenant 7003 chưa từng có record nào | 1. Bấm tab "Liên kết" | 1. Tiêu đề đếm "0 yêu cầu"<br>2. Minh họa hộp trống + text "Chưa có yêu cầu liên kết nào từ Driver Plus." (đúng verbatim AC-7) | High | tenant 7003 |
| GARA_DPL_TC_020 | M1 | High | Kiểm tra hiển thị empty state không khớp bộ lọc thành công khác text với case 0 yêu cầu (Mobile) | Đăng nhập garage-owner trên Mobile; đang ở màn "Liên kết" (tab Driver Plus, route tương đương `/partner-links`); tenant 5001 có sẵn 4 record | 1. Vào màn Bộ lọc, untick cả 4 checkbox<br>2. Bấm back | 1. Text hiển thị "Không có yêu cầu nào khớp bộ lọc." — khác text TC_019, KHÔNG dùng chung "Chưa có yêu cầu liên kết nào từ Driver Plus." | High | tenant 5001 |
| GARA_DPL_TC_021 | M1 | Medium | Kiểm tra ellipsis và xem full text thành công với tên tài khoản D+ dài (Mobile) | Đăng nhập garage-owner trên Mobile; đang ở màn "Liên kết" (tab Driver Plus, route tương đương `/partner-links`); tenant 5001 có sẵn LKD-2026-005 với tên D+ dài "Nguyễn Thị Kim Ngọc Bích Phương Trần" (vượt độ rộng card) | 1. Quan sát card LKD-2026-005<br>2. Tap vào phần tên bị cắt | 1. Tên hiển thị dạng ellipsis (...) trên card<br>2. Tap vào → hiện đầy đủ tên "Nguyễn Thị Kim Ngọc Bích Phương Trần" | Medium | LKD-2026-005 |
| GARA_DPL_TC_022 | M1 | Medium | Kiểm tra ellipsis và xem full text thành công với lý do dài (Mobile) | Đăng nhập garage-owner trên Mobile; đang ở màn "Liên kết" (tab Driver Plus, route tương đương `/partner-links`), filter tick "Từ chối"; tenant 5001 có sẵn LKD-2026-006 (REJECTED) với Lý do dài 300 ký tự | 1. Quan sát card LKD-2026-006<br>2. Tap vào phần Lý do bị cắt | 1. Lý do hiển thị dạng ellipsis trên card<br>2. Tap vào → hiện đầy đủ nội dung Lý do 300 ký tự | Medium | LKD-2026-006 |
| GARA_DPL_TC_023 | M1 | Medium | Kiểm tra giữ nguyên lựa chọn filter thành công khi điều hướng đi nơi khác rồi quay lại trong phiên (Web) | Đăng nhập garage-owner; đang ở màn "Liên kết", đã tick thêm "Từ chối" vào filter; tenant 5001 có sẵn LKD-2026-003 (REJECTED) | 1. Bấm menu "Tổng quan" điều hướng đi<br>2. Quay lại menu "Liên kết" | 1. Filter vẫn giữ nguyên trạng thái tick "Chờ liên kết"+"Đã liên kết"+"Từ chối"<br>2. LKD-2026-003 vẫn hiển thị trong danh sách | Medium | tenant 5001 |
| GARA_DPL_TC_024 | M1 | Medium | Kiểm tra filter reset về default thành công sau khi tải lại trang [ASSUMPTION Q5] (Web) | Đăng nhập garage-owner; đang ở màn "Liên kết", đã tick thêm "Từ chối" vào filter; tenant 5001 có sẵn 4 record | 1. Nhấn F5 (reload trang) | 1. Sau khi trang tải lại, dropdown filter về lại default (chỉ tick "Chờ liên kết"+"Đã liên kết")<br>2. LKD-2026-003 (REJECTED) không còn hiển thị | Medium | tenant 5001 |
| GARA_DPL_TC_025 | M1 | Medium | Kiểm tra giữ nguyên lựa chọn filter thành công khi quay lại màn Bộ lọc trong phiên (Mobile) | Đăng nhập garage-owner trên Mobile; đang ở màn "Liên kết" (tab Driver Plus, route tương đương `/partner-links`), đã vào Bộ lọc tick thêm "Từ chối" rồi back; tenant 5001 có sẵn 4 record | 1. Bấm icon phễu mở lại màn Bộ lọc | 1. Checkbox "Từ chối" vẫn đang ở trạng thái tick (giữ nguyên lựa chọn trước đó trong phiên) | Medium | tenant 5001 |
| GARA_DPL_TC_026 | M1 | High | Kiểm tra hiển thị lỗi tải danh sách và tải lại thành công với dữ liệu BE trả lỗi 503 (Web) | Đăng nhập garage-owner; đang ở Trang chủ GMS; BE được giả lập trả lỗi 503 (`ERR-DPL-007`) khi gọi `listPartnerLinkRequests` lần đầu, lần gọi lại (retry) trả về thành công | 1. Mở menu "Liên kết" (BE lỗi lần 1)<br>2. Bấm nút "Tải lại" trên banner | 1. Panel trái hiển thị banner lỗi + nút "Tải lại", không hiển thị bảng dữ liệu giả<br>2. Sau khi bấm "Tải lại", gọi lại API thành công → danh sách hiển thị bình thường | High | tenant 5001 |
| GARA_DPL_TC_027 | M1 | Medium | Kiểm tra hiển thị thông báo lỗi tải danh sách thành công với dữ liệu BE trả lỗi 503 [ASSUMPTION Q4] (Mobile) | Đăng nhập garage-owner trên Mobile; đang ở Trang chủ; BE được giả lập trả lỗi 503 (`ERR-DPL-007`) khi gọi `listPartnerLinkRequests` | 1. Bấm tab "Liên kết" | 1. Hiển thị thông báo lỗi tải dữ liệu + hành động thử lại (không hiển thị "0 yêu cầu" — phân biệt rõ với empty state TC_019) | Medium | tenant 5001 |
| **NHÓM UI & BEHAVIOR — M1 PL-NAV-LIST** | | | | | | | | |
| GARA_DPL_TC_028 | M1 | Medium | Kiểm tra hiển thị đúng tiêu đề panel trái với text verbatim (Web) | Đăng nhập garage-owner; đang ở màn "Liên kết"; tenant 5001 có sẵn dữ liệu | 1. Quan sát đầu panel trái | 1. Tiêu đề hiển thị đúng verbatim "Danh sách yêu cầu liên kết" | Medium | N/A |
| GARA_DPL_TC_029 | M1 | Medium | Kiểm tra hiển thị đúng label filter và tên 4 trạng thái với text verbatim (Web) | Đăng nhập garage-owner của tenant 5001; đang ở màn "Liên kết"; mở dropdown filter | 1. Quan sát dropdown filter | 1. Label "Trạng thái liên kết"<br>2. Đúng 4 tên checkbox verbatim: "Chờ liên kết", "Đã liên kết", "Từ chối", "Đã hủy liên kết" | Medium | N/A |
| GARA_DPL_TC_030 | M1 | Medium | Kiểm tra hiển thị đúng 4 màu badge trạng thái với dữ liệu đủ 4 trạng thái (Web) | Đăng nhập garage-owner; đang ở màn "Liên kết", filter tick đủ 4; tenant 5001 có sẵn 4 record LKD-2026-001..004 | 1. Quan sát badge của từng item | 1. PENDING → badge màu cam<br>2. LINKED → badge màu xanh lá<br>3. REJECTED → badge màu đỏ<br>4. UNLINKED → badge màu đỏ đậm | Medium | tenant 5001 |
| GARA_DPL_TC_031 | M1 | Medium | Kiểm tra không hiển thị ô tìm kiếm thành công tại panel trái (Web) | Đăng nhập garage-owner của tenant 5001; đang ở màn "Liên kết" | 1. Quan sát toàn bộ panel trái | 1. Không tồn tại bất kỳ input tìm kiếm nào trên UI | Medium | N/A |
| GARA_DPL_TC_032 | M1 | Medium | Kiểm tra không hiển thị thanh phân trang thành công dù danh sách nhiều record (Web) | Đăng nhập garage-owner; đang ở màn "Liên kết"; tenant 9001 có sẵn 501 record (giả lập cap phòng vệ) | 1. Quan sát cuối panel trái | 1. Không tồn tại thanh phân trang / control chuyển trang nào | Medium | tenant 9001 |
| GARA_DPL_TC_033 | M1 | Low | Kiểm tra tab order đúng thứ tự thành công từ menu đến item danh sách (Web) | Đăng nhập garage-owner; đang ở màn "Liên kết"; tenant 5001 có sẵn dữ liệu | 1. Bấm Tab liên tục từ đầu trang | 1. Thứ tự focus: menu "Liên kết" → tab "Driver Plus" → dropdown filter → item đầu tiên trong danh sách | Low | N/A |
| GARA_DPL_TC_034 | M1 | Low | Kiểm tra Shift+Tab di chuyển ngược đúng thứ tự thành công từ item danh sách về menu (Web) | Đăng nhập garage-owner; đang ở màn "Liên kết", focus tại item đầu tiên trong danh sách; tenant 5001 có sẵn dữ liệu | 1. Bấm Shift+Tab liên tục | 1. Thứ tự focus ngược lại đúng: item danh sách → dropdown filter → tab "Driver Plus" → menu "Liên kết" | Low | N/A |
| GARA_DPL_TC_035 | M1 | Low | Kiểm tra hiển thị trạng thái hover thành công khi di chuột qua item danh sách (Web) | Đăng nhập garage-owner; đang ở màn "Liên kết"; tenant 5001 có sẵn LKD-2026-001 | 1. Di chuột qua item LKD-2026-001 (không click) | 1. Item đổi style hover (nền/cursor pointer) rõ ràng | Low | N/A |
| GARA_DPL_TC_036 | M1 | Low | Kiểm tra layout không vỡ thành công khi resize viewport 1920x1080 (Web) | Đăng nhập garage-owner; đang ở màn "Liên kết"; tenant 5001 có sẵn dữ liệu | 1. Resize trình duyệt về 1920x1080 | 1. Layout master-detail hiển thị đúng tỷ lệ, không chồng chéo/tràn nội dung | Low | N/A |
| GARA_DPL_TC_037 | M1 | Low | Kiểm tra layout không vỡ thành công khi zoom in/out trình duyệt (Web) | Đăng nhập garage-owner; đang ở màn "Liên kết"; viewport 1920x1080; tenant 5001 có sẵn dữ liệu | 1. Zoom in (Ctrl +) tới 150%<br>2. Zoom out (Ctrl -) tới 67% | 1. Cả 2 mức zoom: layout không vỡ, nội dung vẫn đọc được, không bị cắt | Low | N/A |
| GARA_DPL_TC_038 | M1 | Low | Kiểm tra điều hướng đúng thành công khi bấm Browser Back tại màn Liên kết (Web) | Đăng nhập garage-owner; vừa điều hướng từ màn "Tổng quan" sang màn "Liên kết"; tenant 5001 có sẵn dữ liệu | 1. Bấm nút Back của trình duyệt | 1. Điều hướng quay lại đúng màn "Tổng quan" trước đó | Low | N/A |
| GARA_DPL_TC_039 | M1 | Low | Kiểm tra icon bộ lọc đúng vị trí và mở đúng màn khi tap thành công (Mobile) | Đăng nhập garage-owner của tenant 5001 trên Mobile; đang ở màn "Liên kết" (tab Driver Plus, route tương đương `/partner-links`) | 1. Quan sát vị trí icon phễu<br>2. Tap vào icon | 1. Icon phễu nằm ở đầu danh sách, cạnh tiêu đề đếm số lượng<br>2. Tap vào → mở đúng màn "Bộ lọc" full-screen | Low | N/A |
| GARA_DPL_TC_040 | M1 | Low | Kiểm tra trạng thái active của tab bottom-nav hiển thị đúng thành công khi đang ở màn Liên kết (Mobile) | Đăng nhập garage-owner của tenant 5001 trên Mobile; đang ở màn "Liên kết" (tab Driver Plus, route tương đương `/partner-links`) | 1. Quan sát bottom-navigation | 1. Tab "Liên kết" hiển thị đúng style active (khác các tab còn lại: Tổng quan/Danh mục/Tài khoản) | Low | N/A |
| **NHÓM PHÂN QUYỀN — M1 PL-NAV-LIST** | | | | | | | | |
| GARA_DPL_TC_041 | M1 | Critical | Kiểm tra truy cập màn Liên kết thành công với tài khoản garage-owner | Đăng nhập garage-owner (owner_test_20260817@gara.test); đang ở Trang chủ GMS; tenant 5001 có sẵn dữ liệu | 1. Bấm menu "Liên kết" | 1. Truy cập thành công, xem đủ danh sách + filter + form chi tiết | Critical | owner_test_20260817@gara.test |
| GARA_DPL_TC_042 | M1 | Critical | Kiểm tra truy cập màn Liên kết thành công với tài khoản kế toán giống hệt garage-owner | Đăng nhập accountant (acct_test_20260817@gara.test); đang ở Trang chủ GMS; tenant 5001 có sẵn dữ liệu | 1. Bấm menu "Liên kết" | 1. Truy cập thành công, xem đủ danh sách + filter + form chi tiết — không khác biệt so với garage-owner (GARA_DPL_TC_041) | Critical | acct_test_20260817@gara.test |
| GARA_DPL_TC_043 | M1 | High | Kiểm tra chặn truy cập màn Liên kết thành công với tài khoản không đủ quyền | Đăng nhập technician (tech_test_20260817@gara.test — role không phải garage-owner/accountant); đang ở Trang chủ GMS | 1. Quan sát top-navigation (Web) hoặc bottom-nav (Mobile)<br>2. Thử truy cập trực tiếp route `/partner-links` | 1. Menu/tab "Liên kết" không hiển thị cho role này<br>2. Truy cập route trực tiếp bị chặn (redirect hoặc trang lỗi phân quyền) | High | tech_test_20260817@gara.test |
| **NHÓM CÁC PHẦN ẢNH HƯỞNG CHỨC NĂNG LIÊN QUAN — M1 PL-NAV-LIST** | | | | | | | | |
| GARA_DPL_TC_044 | M1 | High | Kiểm tra cô lập dữ liệu chéo tenant thành công tại danh sách Liên kết | Đăng nhập garage-owner của tenant 5001; đang ở màn "Liên kết"; tenant 5001 có 2 record, tenant 6002 có 3 record khác | 1. Quan sát danh sách | 1. Chỉ hiển thị 2 record thuộc tenant 5001, không lẫn 3 record của tenant 6002 | High | tenant 5001 / 6002 |
| GARA_DPL_TC_045 | M1 | High | Kiểm tra danh sách phản ánh đúng trạng thái thành công sau khi Duyệt kèm cascade auto-reject | Đăng nhập garage-owner; đang ở màn "Liên kết"; tenant 5001 có sẵn LKD-2026-001(PENDING) vừa Duyệt xong (qua M3), đồng thời có LKD-2026-007(PENDING) khác cùng tenant tại thời điểm Duyệt | 1. Quan sát lại danh sách panel trái sau khi modal Duyệt đóng | 1. LKD-2026-001 hiển thị badge "Đã liên kết" (xanh lá)<br>2. LKD-2026-007 tự động chuyển badge "Từ chối" (đỏ) do cascade AC-16, không cần thao tác riêng | High | LKD-2026-001, LKD-2026-007 |
| GARA_DPL_TC_046 | M1 | High | Kiểm tra danh sách phản ánh đúng trạng thái thành công sau khi Từ chối không kèm cascade | Đăng nhập garage-owner; đang ở màn "Liên kết"; tenant 5001 có sẵn LKD-2026-003(PENDING) vừa Từ chối xong (qua M4), có LKD-2026-008(PENDING) khác cùng tenant | 1. Quan sát lại danh sách sau khi modal Từ chối đóng | 1. LKD-2026-003 hiển thị badge "Từ chối" (đỏ)<br>2. LKD-2026-008 vẫn giữ nguyên badge "Chờ liên kết" (không bị cascade — Từ chối cá nhân không kích rule single-active-link) | High | LKD-2026-003, LKD-2026-008 |
| GARA_DPL_TC_047 | M1 | High | Kiểm tra danh sách phản ánh đúng trạng thái thành công sau khi Hủy liên kết | Đăng nhập garage-owner; đang ở màn "Liên kết"; tenant 5001 có sẵn LKD-2026-002(LINKED) vừa Hủy liên kết xong (qua M6) | 1. Quan sát lại danh sách sau khi modal Hủy đóng | 1. LKD-2026-002 hiển thị badge "Đã hủy liên kết" (đỏ đậm) | High | LKD-2026-002 |
| GARA_DPL_TC_048 | M1 | High | Kiểm tra ẩn menu Liên kết thành công khi feature-flag tắt (Web) | Đăng nhập garage-owner của tenant 5099; đang ở Trang chủ GMS; tenant 5099 có `PartnerLink:DriverPlus=off` | 1. Quan sát top-navigation | 1. Menu "Liên kết" không hiển thị trên top-navigation | High | tenant 5099 |
| GARA_DPL_TC_049 | M1 | High | Kiểm tra ẩn tab Liên kết thành công khi feature-flag tắt (Mobile) | Đăng nhập garage-owner trên Mobile của tenant 5099; đang ở Trang chủ; tenant 5099 có `PartnerLink:DriverPlus=off` | 1. Quan sát bottom-navigation | 1. Tab "Liên kết" không hiển thị trên bottom-navigation | High | tenant 5099 |
| GARA_DPL_TC_050 | M1 | High | Kiểm tra danh sách cập nhật ngầm đúng trạng thái thành công sau sự kiện D+ tự hủy khi tải lại trang | Đăng nhập garage-owner; đang ở màn "Liên kết"; tenant 5001 có sẵn LKD-2026-001 (PENDING); trong lúc user đang xem, D+ gửi inbound event `PARTNER_LINK.REQUEST.WITHDRAW` hủy LKD-2026-001 | 1. Không thao tác gì, quan sát danh sách hiện tại (không có toast xuất hiện)<br>2. Nhấn F5 tải lại trang | 1. Trước F5: KHÔNG có toast nào xuất hiện, badge LKD-2026-001 vẫn hiển thị "Chờ liên kết" trên UI cũ<br>2. Sau F5: badge LKD-2026-001 cập nhật đúng thành "Đã hủy liên kết" | High | LKD-2026-001 |
