# Test Cases — Wave 7 — Nhóm DPL (Driver Plus Link) — M8 PL-PERM-ERROR

> File con của rollup [`TC_DPL.md`](TC_DPL.md) — xem file đó để biết Thông tin chung / Test Data / Traceability Matrix / Ambiguities & Q&A / Bảng tổng hợp Risk Level & Priority cho toàn bộ module DPL.
> Phạm vi: các khía cạnh cross-cutting chưa được phủ ở M1-M7 — outbound push fail không hiển thị UI (AC-32), hành vi đầy đủ kill-switch `PartnerLink:DriverPlus` (AC-43), và ma trận phân quyền tổng hợp không có ngoại lệ (AC-26). Race condition (AC-27/31) và lỗi hệ thống chung (AC-29) đã được kiểm thử đầy đủ theo từng modal riêng ở các file con `TC_DPL-APPROVE.md`, `TC_DPL-REJECT.md`, `TC_DPL-RESYNC.md`, `TC_DPL-CANCEL.md` (nhóm Function của mỗi file) — không lặp lại ở đây. Người thực hiện bị vô hiệu hóa (AC-30) đã kiểm thử ở `TC_DPL-DETAIL.md`.

## 7. Bảng Test Cases chi tiết

**Nhóm Validate: N/A** — Module này không có ô nhập liệu, tập trung vào hành vi hệ thống/cấu hình flag.

| TC ID | Module | Risk Level | Test Title | Pre-Condition | Test Steps | Expected Result | Priority | Test Data |
|---|---|---|---|---|---|---|---|---|
| **NHÓM FUNCTION — M8 PL-PERM-ERROR** | | | | | | | | |
| GARA_DPL_TC_196 | M8 | High | Kiểm tra Duyệt liên kết thành công không hiển thị badge cảnh báo dù outbound push sang D+ thất bại ngầm | Đăng nhập garage-owner của tenant 5001; đang ở màn "Liên kết"; tenant 5001 có sẵn LKD-2026-001 (PENDING); hạ tầng outbound push sang Driver Plus được giả lập lỗi (timeout/5xx) ngay sau khi Duyệt commit thành công ở `gf-system` | 1. Duyệt thành công LKD-2026-001 (cuộn hết, tick, xác nhận) | 1. Toast "Đã liên kết thành công với tài khoản Driver Plus..." vẫn hiển thị bình thường<br>2. LKD-2026-001 chuyển "Đã liên kết" trên UI<br>3. KHÔNG hiển thị bất kỳ badge "Đang đồng bộ"/"Đồng bộ lỗi", cảnh báo, hay nút thử lại nào trên Web | High | LKD-2026-001 |
| GARA_DPL_TC_197 | M8 | High | Kiểm tra chặn thao tác thành công khi cố truy cập trực tiếp route Liên kết trong lúc flag đang tắt | Đăng nhập garage-owner của tenant 5099 (`PartnerLink:DriverPlus=off`); đang ở Trang chủ GMS | 1. Truy cập trực tiếp URL route `/partner-links` (gõ URL, không qua menu) | 1. Bị chặn — điều hướng về trang lỗi phân quyền/trang chủ, không hiển thị nội dung màn "Liên kết" | High | tenant 5099 |
| GARA_DPL_TC_198 | M8 | High | Kiểm tra không xuất hiện record mới thành công khi Driver Plus gửi yêu cầu liên kết trong lúc flag đang tắt | Đăng nhập garage-owner của tenant 5099; đang ở Trang chủ GMS; tenant 5099 có `PartnerLink:DriverPlus=off`, có sẵn 2 record trước đó; D+ gửi 1 yêu cầu liên kết mới trong lúc flag đang tắt | 1. Đăng nhập garage-owner của tenant 5099<br>2. Bật lại flag `PartnerLink:DriverPlus=on`<br>3. Mở menu "Liên kết" | 1. Vẫn chỉ có đúng 2 record cũ, KHÔNG có record mới nào được tạo từ yêu cầu D+ gửi trong lúc flag tắt (request đó đã bị từ chối tại adapter gate, không lưu lại) | High | tenant 5099 |
| GARA_DPL_TC_199 | M8 | High | Kiểm tra dữ liệu và audit hiện hữu giữ nguyên thành công sau khi tắt rồi bật lại flag | Đăng nhập garage-owner của tenant 5099; đang ở Trang chủ GMS; tenant 5099 có `PartnerLink:DriverPlus=off`, có sẵn 3 record trước khi tắt flag: LKD-9001(LINKED), LKD-9002(REJECTED), LKD-9003(UNLINKED) | 1. Bật lại flag `PartnerLink:DriverPlus=on`<br>2. Đăng nhập garage-owner của tenant 5099, mở menu "Liên kết" | 1. Đủ 3 record LKD-9001/9002/9003 vẫn hiển thị nguyên vẹn, đúng trạng thái và THÔNG TIN XỬ LÝ như trước khi tắt flag | High | tenant 5099 |
| GARA_DPL_TC_200 | M8 | High | Kiểm tra chức năng Liên kết hoạt động bình thường trở lại thành công sau khi bật lại flag | Đăng nhập garage-owner của tenant 5099; đang ở Trang chủ GMS; tenant 5099 vừa được bật lại `PartnerLink:DriverPlus=on`, có sẵn LKD-9004 (PENDING) | 1. Đăng nhập garage-owner của tenant 5099<br>2. Mở menu "Liên kết", chọn LKD-9004, Duyệt thành công (cuộn hết, tick, xác nhận) | 1. Menu "Liên kết" hiển thị bình thường<br>2. Duyệt LKD-9004 thành công, chuyển "Đã liên kết" — chức năng hoạt động lại như bình thường | High | tenant 5099 |
| **NHÓM UI & BEHAVIOR — M8 PL-PERM-ERROR** | | | | | | | | |
| GARA_DPL_TC_201 | M8 | Medium | Kiểm tra hiển thị đúng thông báo phù hợp thành công khi cố truy cập route Liên kết lúc flag tắt (Web) | Đăng nhập garage-owner của tenant 5099 (`PartnerLink:DriverPlus=off`); đang ở Trang chủ GMS | 1. Truy cập trực tiếp URL route `/partner-links` | 1. Hiển thị trang lỗi/thông báo phân quyền rõ ràng cho user hiểu — không phải màn trắng hoặc lỗi kỹ thuật (500/stack trace) | Medium | tenant 5099 |
| GARA_DPL_TC_202 | M8 | Medium | Kiểm tra chặn truy cập tab Liên kết thành công giống hệt Web khi flag đang tắt (Mobile) | Đăng nhập garage-owner của tenant 5099 (`PartnerLink:DriverPlus=off`) trên Mobile; đang ở Trang chủ | 1. Quan sát bottom-navigation | 1. Tab "Liên kết" không hiển thị, giống hệt hành vi Web (TC_197) | Medium | tenant 5099 |
| **NHÓM PHÂN QUYỀN — M8 PL-PERM-ERROR** | | | | | | | | |
| GARA_DPL_TC_203 | M8 | High | Kiểm tra ma trận đầy đủ garage-owner nhìn thấy đúng nút hành động theo cả 4 trạng thái không có ngoại lệ | Đăng nhập garage-owner của tenant 5001; đang ở màn "Liên kết"; tenant 5001 có sẵn đủ 4 record 4 trạng thái LKD-2026-001(PENDING)/002(LINKED)/003(REJECTED)/004(UNLINKED) | 1. Lần lượt chọn từng record, quan sát nút hành động header | 1. PENDING → Duyệt+Từ chối; LINKED → Đồng bộ lại+Hủy liên kết; REJECTED → không nút; UNLINKED → không nút — đúng 100% theo state guard, không có ngoại lệ nào phát sinh cho role garage-owner | High | tenant 5001 |
| GARA_DPL_TC_204 | M8 | High | Kiểm tra ma trận đầy đủ accountant nhìn thấy đúng nút hành động giống hệt garage-owner theo cả 4 trạng thái | Đăng nhập accountant của tenant 5001; đang ở màn "Liên kết"; tenant 5001 có sẵn đủ 4 record 4 trạng thái LKD-2026-001(PENDING)/002(LINKED)/003(REJECTED)/004(UNLINKED) | 1. Lần lượt chọn từng record, quan sát nút hành động header | 1. Kết quả giống hệt TC_203 (garage-owner) — không có bất kỳ khác biệt phân quyền nào giữa 2 role | High | tenant 5001 |
| **NHÓM CÁC PHẦN ẢNH HƯỞNG CHỨC NĂNG LIÊN QUAN — M8 PL-PERM-ERROR** | | | | | | | | |
| GARA_DPL_TC_205 | M8 | High | Kiểm tra danh sách hiển thị đầy đủ dữ liệu cũ thành công sau khi bật lại flag, không mất record nào | Đăng nhập garage-owner của tenant 5099; đang ở Trang chủ GMS; tenant 5099 vừa được bật lại `PartnerLink:DriverPlus=on`, có sẵn 3 record từ trước khi tắt flag | 1. Đăng nhập garage-owner của tenant 5099, mở menu "Liên kết", tick đủ 4 checkbox filter | 1. Đủ 3 record cũ hiển thị lại, không mất dữ liệu, sort đúng theo ngày gửi | High | tenant 5099 |
| GARA_DPL_TC_206 | M8 | Medium | Kiểm tra thao tác action hoạt động lại bình thường thành công trên record cũ sau khi bật lại flag | Đăng nhập garage-owner của tenant 5099; đang ở Trang chủ GMS; tenant 5099 vừa được bật lại `PartnerLink:DriverPlus=on`, có sẵn LKD-9005 (PENDING, tạo từ trước khi tắt flag) | 1. Đăng nhập garage-owner của tenant 5099<br>2. Chọn LKD-9005, Từ chối thành công (nhập lý do, xác nhận) | 1. LKD-9005 chuyển "Từ chối" thành công — record cũ vẫn thao tác được bình thường sau khi bật lại flag | Medium | tenant 5099 |
| **NHÓM FUNCTION — BỔ SUNG (Coverage Gap-fill audit: AC-28, AC-33)** | | | | | | | | |
| GARA_DPL_TC_207 | M3 | Medium | Kiểm tra cascade auto-reject bỏ qua đúng record đã có trạng thái terminal thành công khi trùng thời điểm bị Từ chối thủ công | Đăng nhập garage-owner của tenant 5001; đang ở màn "Liên kết", đã mở modal Duyệt cho LKD-2026-001, đã cuộn hết + tick checkbox; tenant 5001 có thêm LKD-2026-016 (PENDING) khác cùng garage; ngay trước khi User A bấm xác nhận Duyệt, accountant (User B) đã Từ chối thủ công xong LKD-2026-016 | 1. User A bấm "Đồng ý liên kết" cho LKD-2026-001 | 1. LKD-2026-001 chuyển "Đã liên kết" thành công<br>2. LKD-2026-016 giữ nguyên trạng thái "Từ chối" đã có từ trước (do User B), THÔNG TIN XỬ LÝ KHÔNG bị cascade ghi đè thành lý do hệ thống — cascade chỉ áp dụng cho record còn thực sự "Chờ liên kết" tại thời điểm xử lý (AC-28) | Medium | LKD-2026-001, LKD-2026-016 |
| GARA_DPL_TC_208 | M7 | High | Kiểm tra danh sách và chi tiết cập nhật đúng thành công sau khi Driver Plus tự hủy yêu cầu đang Chờ liên kết | Đăng nhập garage-owner của tenant 5001; đang ở màn "Liên kết"; tenant 5001 có sẵn LKD-2026-017 (PENDING); D+ gửi inbound event `PARTNER_LINK.REQUEST.WITHDRAW` hủy LKD-2026-017 kèm lý do "Khách không còn nhu cầu" | 1. Không thao tác gì tại thời điểm event tới (quan sát không có toast)<br>2. Nhấn F5, chọn xem LKD-2026-017 | 1. Trước F5: KHÔNG có toast nào xuất hiện<br>2. Sau F5: badge LKD-2026-017 chuyển "Đã hủy liên kết"; section "THÔNG TIN XỬ LÝ" ghi Người thực hiện = "Driver Plus", Lý do = "Khách không còn nhu cầu"; header hết nút hành động (AC-33, bổ sung mirror đầy đủ theo AC-35 đã kiểm ở M7) | High | LKD-2026-017 |
| GARA_DPL_TC_209 | M7 | Medium | Kiểm tra Lý do hiển thị đúng text mặc định thành công khi Driver Plus tự hủy yêu cầu Chờ liên kết không kèm lý do [ASSUMPTION: AC-33] | Đăng nhập garage-owner của tenant 5001; đang ở màn "Liên kết"; tenant 5001 có sẵn LKD-2026-018 (PENDING); D+ gửi inbound event `PARTNER_LINK.REQUEST.WITHDRAW` hủy LKD-2026-018, payload KHÔNG kèm lý do | 1. Nhấn F5, chọn xem LKD-2026-018 | 1. Badge chuyển "Đã hủy liên kết"; section "THÔNG TIN XỬ LÝ" ghi Lý do = "Hủy từ ứng dụng Driver Plus." [ASSUMPTION: AC-33 không chốt rõ text mặc định riêng cho case Withdraw-pending — tái dùng cùng fallback đã chốt ở AC-35/BR-DPL-CAN-005, cần Backend Lead xác nhận thống nhất] | Medium | LKD-2026-018 |
