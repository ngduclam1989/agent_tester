# TÀI LIỆU TEST CASES: CHỨC NĂNG ONBOARD TENANT (SAAS TENANT ONBOARDING) - PHIÊN BẢN CẬP NHẬT CHI TIẾT CỰC HẠN (V4)

---

## 1. THÔNG TIN CHUNG
*   **Dự án:** Cardoctor SaaS (control-plane SaaS Tenant)
*   **Module:** Tenant Management / Onboard Tenant
*   **Mã Feature:** `FEAT-TEN-001`
*   **Tài liệu tham chiếu:** [FEAT-TEN-001-tenant-onboarding.md](file:///f:/lam_demo/antigravity-testing-kit/practices/requirements/cardoctor/FEAT-TEN-001-tenant-onboarding.md), [ct-saas-tenant-service-api.md](file:///f:/lam_demo/antigravity-testing-kit/practices/requirements/cardoctor/ct-saas-tenant-service-api.md)
*   **Kỹ thuật thiết kế test case:** Equivalence Partitioning (Phân vùng tương đương), Boundary Value Analysis (Phân tích giá trị biên), Decision Table (Bảng quyết định), State Transition (Chuyển đổi trạng thái).
*   **Tổng số Test Cases:** 88 TCs

---

## 2. BẢNG TỔNG HỢP RISK LEVEL THEO TÍNH NĂNG
| Sub-module / Tính năng | Mức độ Rủi ro (Risk Level) | Chiến lược kiểm thử |
| :--- | :---: | :--- |
| **Sub-module 1:** Đăng ký Thông tin cơ bản | **High** | Kiểm tra kỹ Happy Path (Full/Minimal), Tổ hợp bỏ trống trường tùy chọn, Validate chi tiết từng trường dữ liệu (bao gồm cả XSS/SQL Injection/Trim), UI & Behavior form, Phân quyền và Database integrity. |
| **Sub-module 2:** Cấu hình Thông tin hóa đơn | **High** | Kiểm tra logic rẽ nhánh theo Feature Flag `Purchase:PurchaseV02`, validate bắt buộc các trường hóa đơn cho GARAGE, publish event Kafka. |
| **Sub-module 3:** Gán Nhân viên tư vấn | **Medium** | Kiểm tra validate bắt buộc khi flag `Marketplace:QuotationConsultant` ON, verify gán đúng ID tư vấn viên. |
| **Sub-module 4:** Khởi tạo Chi nhánh & Kho mặc định | **High** | Kiểm tra validate bắt buộc trụ sở chính khi flag `Inventory:InventoryStockV01` ON, verify event Kafka provisioning. |
| **Sub-module 5:** Tạo tài khoản quản trị Owner IAM | **High** | Kiểm tra tích hợp Keycloak, trả credentials tạm, event Kafka và logic retry khi Keycloak lỗi. |
| **Sub-module 6:** Hoàn tất & Kích hoạt Tenant | **High** | Kiểm tra API completed, đổi trạng thái stage/status, tích hợp Jenkins kích hoạt subdomain, event Kafka đồng bộ kế toán. |

---

## 3. DANH SÁCH TÀI KHOẢN TEST / TEST DATA THIẾT YẾU
*   **Tài khoản Test:**
    *   `SYSTEM_ADMIN`: Quyền tối cao trong hệ thống (`saasControlOnboarding:create`, `saasControlTenant:updateInformation`).
    *   `CD_BD_HEAD` / `CD_BD_LEAD`: Quyền tạo và cập nhật thông tin Tenant.
    *   `USER_THUONG` / `GARAGE_USER`: Không có quyền thao tác Tenant control-plane.
*   **Test Data mặc định:**
    *   `subdomain` hợp lệ: `gara-cardoctor-th`, `gara-toithieu`, `vendor-phutung`.
    *   `companyPhoneNumber` hợp lệ: `0987123456`, `0987654321`, `0912345678`.
    *   `cityCode` / `wardCode` hợp lệ: Hà Nội (`01`) / Láng Thượng (`00001`).

---

## 4. TRACEABILITY MATRIX
| Yêu cầu (REQ / AC) | Sub-module | Test Case ID |
| :--- | :--- | :--- |
| `FEAT-TEN-001` / `AC-2` | Sub-module 1 | `CARDOCTOR_ONBOARD_TC_001` -> `CARDOCTOR_ONBOARD_TC_043_6` |
| `Purchase:PurchaseV02` | Sub-module 2 | `CARDOCTOR_ONBOARD_TC_044` -> `CARDOCTOR_ONBOARD_TC_056_1` |
| `Marketplace:QuotationConsultant` | Sub-module 3 | `CARDOCTOR_ONBOARD_TC_057` -> `CARDOCTOR_ONBOARD_TC_059_1` |
| `Inventory:InventoryStockV01` | Sub-module 4 | `CARDOCTOR_ONBOARD_TC_060` -> `CARDOCTOR_ONBOARD_TC_065_1` |
| `AC-2` (IAM Integration) | Sub-module 5 | `CARDOCTOR_ONBOARD_TC_066` -> `CARDOCTOR_ONBOARD_TC_072` |
| `FEAT-TEN-001` / `AC-1`, `AC-3` | Sub-module 6 | `CARDOCTOR_ONBOARD_TC_073` -> `CARDOCTOR_ONBOARD_TC_078` |

---

## 5. BẢNG TỔNG HỢP AMBIGUITIES & CÂU HỎI Q&A (ASSUMPTIONS)
| ID | Điểm mờ / Câu hỏi | Giả định của QA (QA Assumption) | Đề xuất giải pháp |
| :---: | :--- | :--- | :--- |
| **Q1** | Định dạng trường Subdomain | Subdomain chỉ chấp nhận chữ thường không dấu, số và dấu `-`. | Áp dụng regex `^[a-z0-9-]+$` để validate. |
| **Q2** | Trạng thái ban đầu sau khi onboard | Tenant ban đầu có stage/status là `PROVISIONED` / `PROVISIONED`. | Xác nhận trạng thái lưu DB khi chưa kích hoạt chạy thực tế. |
| **Q3** | Keycloak down khi đang onboard | Không rollback DB Tenant, lưu user lỗi và cho phép retry qua `/retry-iam`. | Trả thông báo tạo Tenant thành công nhưng IAM lỗi. |
| **Q4** | Định dạng số điện thoại công ty | Phải là số điện thoại Việt Nam hợp lệ (9-11 số, đầu số chuẩn). | Áp dụng regex validate định dạng SĐT Việt Nam. |

---

## 6. BẢNG THỐNG KÊ SỐ LƯỢNG TEST CASES
*   **Theo Mức độ Ưu tiên (Priority):**
    *   `Critical`: 18 TCs
    *   `High`: 48 TCs
    *   `Medium`: 18 TCs
    *   `Low`: 4 TCs
*   **Theo Nhóm rủi ro (Risk Group):**
    *   `Function`: 23 TCs
    *   `Validation`: 45 TCs
    *   `UI & Behavior`: 8 TCs
    *   `Phân quyền`: 4 TCs
    *   `Ảnh hưởng liên quan`: 8 TCs

---

## 7. BẢNG TEST CASES CHI TIẾT

| TC ID | Module | Risk Level | Test Title | Pre-Condition | Test Steps | Expected Result | Priority | Test Data |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **PHẦN 1: ĐĂNG KÝ THÔNG TIN CƠ BẢN** | | | | | | | | |
| **NHÓM FUNCTION** | | | | | | | | |
| `CARDOCTOR_ONBOARD_TC_001` | Sub-module 1 | High | Khởi tạo Tenant loại GARAGE thành công với đầy đủ các trường thông tin (Full Fields) | Đăng nhập bằng `SYSTEM_ADMIN`. Các Feature Flags đang ON. | 1. Truy cập form Khởi tạo Tenant.<br>2. Nhập đầy đủ thông tin hợp lệ vào tất cả các trường (bao gồm cả các trường tùy chọn và thông tin mở rộng).<br>3. Click "Lưu". | 1. Form hiển thị đầy đủ.<br>2. Các trường hiển thị dữ liệu chính xác.<br>3. Hệ thống báo tạo mới thành công. Bản ghi được lưu DB với stage/status là PROVISIONED/PROVISIONED. | Critical | `name`: "Gara Ô tô Cardoctor Tây Hồ", `type`: "GARAGE", `tenantBusinessModel`: "REPAIRING", `subdomain`: "gara-cardoctor-th", `saasTier`: "STANDARD", `saasSolution`: "GMS", `companyPhoneNumber`: "0987123456", `operationAreaCodes`: ["Hà Nội"] |
| `CARDOCTOR_ONBOARD_TC_002` | Sub-module 1 | High | Khởi tạo Tenant loại GARAGE thành công với các trường bắt buộc tối thiểu (Minimal Fields) | Đăng nhập bằng `SYSTEM_ADMIN`. Các Feature Flags đang ON. | 1. Truy cập form Khởi tạo Tenant.<br>2. Nhập đầy đủ thông tin vào các trường bắt buộc.<br>3. Bỏ trống toàn bộ các trường tùy chọn.<br>4. Click "Lưu". | 1. Form hiển thị đúng.<br>2. Dữ liệu bắt buộc được điền.<br>3. Các trường tùy chọn trống.<br>4. Hệ thống báo tạo mới thành công. Các trường tùy chọn nhận giá trị null hoặc mặc định trong DB. | Critical | `name`: "Gara Cardoctor Tối Thiểu", `type`: "GARAGE", `tenantBusinessModel`: "REPAIRING", `subdomain`: "gara-toithieu", `saasTier`: "STANDARD", `saasSolution`: "GMS", `companyPhoneNumber`: "0987654321", `operationAreaCodes`: ["Hà Nội"] |
| `CARDOCTOR_ONBOARD_TC_003` | Sub-module 1 | High | Khởi tạo Tenant loại VENDOR thành công với các trường bắt buộc tối thiểu | Đăng nhập bằng `SYSTEM_ADMIN`. Flag Inventory ON. | 1. Truy cập form Khởi tạo Tenant.<br>2. Chọn loại Tenant là VENDOR.<br>3. Nhập đầy đủ các trường bắt buộc của VENDOR.<br>4. Bỏ trống thông tin hóa đơn và consultant.<br>5. Click "Lưu". | 1. Form hiển thị đúng.<br>2. Chọn VENDOR thành công.<br>3. Các trường hóa đơn/consultant tự động ẩn hoặc không bắt buộc.<br>4. Không báo lỗi thiếu hóa đơn.<br>5. Hệ thống báo tạo mới thành công. | High | `name`: "Nhà cung cấp phụ tùng Cardoctor", `type`: "VENDOR", `tenantBusinessModel`: "SPARE_PARTS", `subdomain`: "vendor-phutung", `saasTier`: "STANDARD", `saasSolution`: "VMS", `companyPhoneNumber`: "0912345678", `operationAreaCodes`: ["Hà Nội"] |
| `CARDOCTOR_ONBOARD_TC_004` | Sub-module 1 | High | Tổ hợp Happy Path: Khởi tạo Tenant thành công khi chỉ bỏ trống email công ty (companyEmailAddress) | Đăng nhập bằng `SYSTEM_ADMIN`. | 1. Nhập đầy đủ tất cả các trường dữ liệu hợp lệ.<br>2. Chỉ bỏ trống trường `companyEmailAddress`.<br>3. Click "Lưu". | 3. Hệ thống lưu thành công Tenant. Trường email trong DB là null. | High | `companyEmailAddress` = `""` |
| `CARDOCTOR_ONBOARD_TC_005` | Sub-module 1 | High | Tổ hợp Happy Path: Khởi tạo Tenant thành công khi chỉ bỏ trống tên kế toán trưởng (chiefAccountantName) | Đăng nhập bằng `SYSTEM_ADMIN`. | 1. Nhập đầy đủ tất cả các trường dữ liệu hợp lệ.<br>2. Chỉ bỏ trống trường `chiefAccountantName`.<br>3. Click "Lưu". | 3. Hệ thống lưu thành công Tenant. Trường kế toán trưởng trong DB là null. | High | `chiefAccountantName` = `""` |
| `CARDOCTOR_ONBOARD_TC_006` | Sub-module 1 | High | Tổ hợp Happy Path: Khởi tạo Tenant thành công khi chỉ bỏ trống URL logo (logoUrl) | Đăng nhập bằng `SYSTEM_ADMIN`. | 1. Nhập đầy đủ tất cả các trường dữ liệu hợp lệ.<br>2. Chỉ bỏ trống trường `logoUrl`.<br>3. Click "Lưu". | 3. Hệ thống lưu thành công Tenant. LogoUrl trong DB là null. | High | `logoUrl` = `""` |
| `CARDOCTOR_ONBOARD_TC_007` | Sub-module 1 | High | Tổ hợp Happy Path: Khởi tạo Tenant thành công khi bỏ trống toàn bộ các trường tùy chọn cùng lúc | Đăng nhập bằng `SYSTEM_ADMIN`. | 1. Nhập đầy đủ thông tin bắt buộc.<br>2. Bỏ trống đồng thời: `companyEmailAddress`, `chiefAccountantName`, `logoUrl`, `opsNote`, `ops1`..`ops5`.<br>3. Click "Lưu". | 3. Hệ thống lưu thành công, verify các trường tùy chọn đều nhận giá trị null hoặc rỗng trong database. | Critical | Các trường tùy chọn = `""` |
| `CARDOCTOR_ONBOARD_TC_008` | Sub-module 1 | High | Rẽ nhánh Enum: Khởi tạo Tenant loại GARAGE với mô hình SPA và phân hạng FREE | Đăng nhập bằng `SYSTEM_ADMIN`. | 1. Chọn type: GARAGE.<br>2. Chọn model: SPA.<br>3. Chọn tier: FREE.<br>4. Nhập đầy đủ thông tin hợp lệ khác.<br>5. Click "Lưu". | 5. Lưu thành công. Trạng thái subscription và phân hạng dịch vụ trong DB ghi nhận chính xác SPA và FREE. | High | `tenantBusinessModel`: "SPA", `saasTier`: "FREE" |
| `CARDOCTOR_ONBOARD_TC_009` | Sub-module 1 | High | Rẽ nhánh Enum: Khởi tạo Tenant loại GARAGE với mô hình ALL_IN_ONE, giải pháp EXPRESS và phân hạng ENTERPRISE | Đăng nhập bằng `SYSTEM_ADMIN`. | 1. Chọn type: GARAGE.<br>2. Chọn model: ALL_IN_ONE.<br>3. Chọn solution: EXPRESS.<br>4. Chọn tier: ENTERPRISE.<br>5. Click "Lưu". | 5. Lưu thành công Tenant với tổ hợp cấu hình đặc biệt. | High | `tenantBusinessModel`: "ALL_IN_ONE", `saasSolution`: "EXPRESS", `saasTier`: "ENTERPRISE" |
| **NHÓM VALIDATE** | | | | | | | | |
| `CARDOCTOR_ONBOARD_TC_010` | Sub-module 1 | Medium | Validate trường Tên Tenant (name) bỏ trống | Form khởi tạo đang mở. | 1. Nhập các trường khác hợp lệ.<br>2. Bỏ trống trường `name`.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và hiển thị thông báo lỗi ngay dưới trường Tên Tenant: "Tên Tenant không được để trống". | High | `name` = `""` |
| `CARDOCTOR_ONBOARD_TC_011` | Sub-module 1 | Medium | Validate trường Tên Tenant (name) chứa toàn khoảng trắng (whitespace-only) | Form khởi tạo đang mở. | 1. Nhập các trường khác hợp lệ.<br>2. Nhập `name` chứa toàn khoảng trắng.<br>3. Click "Lưu". | 3. Hệ thống tự động trim, chặn lưu và báo lỗi: "Tên Tenant không được để trống". | High | `name` = `"      "` |
| `CARDOCTOR_ONBOARD_TC_012` | Sub-module 1 | Medium | Validate trường Tên Tenant (name) vượt quá độ dài tối đa 255 ký tự | Form khởi tạo đang mở. | 1. Nhập các trường khác hợp lệ.<br>2. Nhập `name` dài 256 ký tự.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và báo lỗi: "Tên Tenant không được vượt quá 255 ký tự". | Medium | Chuỗi 256 ký tự |
| `CARDOCTOR_ONBOARD_TC_013` | Sub-module 1 | Medium | Bảo mật & Validate: Nhập mã độc XSS Script vào trường Tên Tenant (name) | Form khởi tạo đang mở. | 1. Nhập các trường khác hợp lệ.<br>2. Nhập `name` chứa script XSS.<br>3. Click "Lưu". | 3. Hệ thống chặn hoặc tự động mã hóa/sanitize HTML entities, không chạy đoạn script độc hại. | High | `name` = `"<script>alert('XSS')</script>"` |
| `CARDOCTOR_ONBOARD_TC_014` | Sub-module 1 | Medium | Bảo mật & Validate: Nhập mã SQL Injection vào trường Tên Tenant (name) | Form khởi tạo đang mở. | 1. Nhập các trường khác hợp lệ.<br>2. Nhập `name` chứa ký tự SQL Injection.<br>3. Click "Lưu". | 3. Hệ thống xử lý an toàn bằng Prepared Statement, lưu chuỗi text thuần túy vào DB, không gây lỗi SQL. | High | `name` = `"' OR 1=1--"` |
| `CARDOCTOR_ONBOARD_TC_015` | Sub-module 1 | Medium | Validate: Nhập ký tự đặc biệt tiếng Việt / Unicode / Emoji vào trường Tên Tenant (name) | Form khởi tạo đang mở. | 1. Nhập các trường khác hợp lệ.<br>2. Nhập `name` có dấu tiếng Việt và Emoji.<br>3. Click "Lưu". | 3. Hệ thống lưu thành công, hiển thị chính xác đầy đủ ký tự tiếng Việt có dấu và Emoji trong DB và UI. | Medium | `name` = `"Gara Ôtô Việt Nam 🌟🚗"` |
| `CARDOCTOR_ONBOARD_TC_016` | Sub-module 1 | Medium | Validate: Tự động Trim khoảng trắng đầu/cuối của trường Tên Tenant (name) | Form khởi tạo đang mở. | 1. Nhập các trường khác hợp lệ.<br>2. Nhập `name` có khoảng trắng ở đầu và cuối.<br>3. Click "Lưu". | 3. Hệ thống lưu thành công. Kiểm tra DB thấy giá trị trường name đã bị cắt bỏ khoảng trắng thừa đầu cuối. | Medium | `name` = `"   Gara Tây Hồ   "` (lưu thành `"Gara Tây Hồ"`) |
| `CARDOCTOR_ONBOARD_TC_017` | Sub-module 1 | Medium | Validate trường Loại Tenant (type) không truyền giá trị (null) | Thực hiện qua gọi API POST. | 1. Gửi request body thiếu trường `type` hoặc `type` = null.<br>2. Gửi request. | 2. API trả về mã lỗi 400 Bad Request: "Loại Tenant không được để trống". | High | `type` = `null` |
| `CARDOCTOR_ONBOARD_TC_018` | Sub-module 1 | Medium | Validate trường Mô hình kinh doanh (tenantBusinessModel) không truyền giá trị (null) | Thực hiện qua gọi API POST. | 1. Gửi request body thiếu trường `tenantBusinessModel`.<br>2. Gửi request. | 2. API trả về mã lỗi 400 Bad Request: "Mô hình kinh doanh không được để trống". | High | `tenantBusinessModel` = `null` |
| `CARDOCTOR_ONBOARD_TC_019` | Sub-module 1 | Medium | Validate trường Phân hạng dịch vụ (saasTier) không truyền giá trị (null) | Thực hiện qua gọi API POST. | 1. Gửi request body thiếu trường `saasTier`.<br>2. Gửi request. | 2. API trả về mã lỗi 400 Bad Request: "Phân hạng dịch vụ không được để trống". | High | `saasTier` = `null` |
| `CARDOCTOR_ONBOARD_TC_020` | Sub-module 1 | Medium | Validate trường Giải pháp SaaS (saasSolution) không truyền giá trị (null) | Thực hiện qua gọi API POST. | 1. Gửi request body thiếu trường `saasSolution`.<br>2. Gửi request. | 2. API trả về mã lỗi 400 Bad Request: "Giải pháp SaaS không được để trống". | High | `saasSolution` = `null` |
| `CARDOCTOR_ONBOARD_TC_021` | Sub-module 1 | Medium | Validate trường Subdomain (subdomain) bỏ trống | Form khởi tạo đang mở. | 1. Nhập các trường khác hợp lệ.<br>2. Bỏ trống trường `subdomain`.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và báo lỗi: "Subdomain không được để trống". | High | `subdomain` = `""` |
| `CARDOCTOR_ONBOARD_TC_022` | Sub-module 1 | Medium | Validate trường Subdomain (subdomain) chứa ký tự không hợp lệ | Form khởi tạo đang mở. | 1. Nhập các trường khác hợp lệ.<br>2. Nhập `subdomain` chứa ký tự hoa và ký tự đặc biệt.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và báo lỗi: "Subdomain chỉ được chứa ký tự chữ thường không dấu, chữ số và dấu gạch ngang". | High | `subdomain` = `"Gara_TayHo!"` |
| `CARDOCTOR_ONBOARD_TC_023` | Sub-module 1 | Medium | Validate trường Subdomain (subdomain) vượt quá độ dài tối đa 20 ký tự | Form khởi tạo đang mở. | 1. Nhập các trường khác hợp lệ.<br>2. Nhập `subdomain` dài 21 ký tự.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và báo lỗi: "Subdomain không được vượt quá 20 ký tự". | Medium | `subdomain` = `"gara-cardoctor-tay-ho"` |
| `CARDOCTOR_ONBOARD_TC_024` | Sub-module 1 | Medium | Validate trường Subdomain (subdomain) trùng lặp (Unique Constraint) | Đã có một Tenant tồn tại với subdomain `gara-cardoctor-th`. | 1. Nhập các trường khác hợp lệ.<br>2. Nhập `subdomain` là `gara-cardoctor-th`.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và báo lỗi: "Subdomain này đã tồn tại trên hệ thống, vui lòng nhập subdomain khác". | Critical | `subdomain` = `"gara-cardoctor-th"` |
| `CARDOCTOR_ONBOARD_TC_025` | Sub-module 1 | Medium | Validate trường Số điện thoại (companyPhoneNumber) bỏ trống | Form khởi tạo đang mở. | 1. Nhập các trường khác hợp lệ.<br>2. Bỏ trống trường `companyPhoneNumber`.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và báo lỗi: "Số điện thoại không được để trống". | High | `companyPhoneNumber` = `""` |
| `CARDOCTOR_ONBOARD_TC_026` | Sub-module 1 | Medium | Validate trường Số điện thoại (companyPhoneNumber) chứa chữ cái hoặc ký tự đặc biệt | Form khởi tạo đang mở. | 1. Nhập các trường khác hợp lệ.<br>2. Nhập `companyPhoneNumber` chứa chữ.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và báo lỗi: "Số điện thoại không đúng định dạng". | High | `companyPhoneNumber` = `"0987.123.abc"` |
| `CARDOCTOR_ONBOARD_TC_027` | Sub-module 1 | Medium | Validate trường Số điện thoại (companyPhoneNumber) sai độ dài Việt Nam | Form khởi tạo đang mở. | 1. Nhập các trường khác hợp lệ.<br>2. Nhập `companyPhoneNumber` có 8 số hoặc 12 số.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và báo lỗi: "Số điện thoại phải từ 9 đến 11 số". | Medium | `companyPhoneNumber` = `"09871234"` và `"098712345678"` |
| `CARDOCTOR_ONBOARD_TC_028` | Sub-module 1 | Medium | Validate trường Số điện thoại (companyPhoneNumber) chứa ký tự dấu gạch ngang / khoảng trắng | Form khởi tạo đang mở. | 1. Nhập các trường khác hợp lệ.<br>2. Nhập `companyPhoneNumber` chứa khoảng trắng/dấu gạch.<br>3. Click "Lưu". | 3. Hệ thống tự động loại bỏ các ký tự dấu cách/gạch nối, chuẩn hóa lưu số thuần túy vào database và báo thành công. | Medium | `companyPhoneNumber` = `"0987-123 456"` (lưu thành `"0987123456"`) |
| `CARDOCTOR_ONBOARD_TC_029` | Sub-module 1 | Medium | Validate trường Số điện thoại (companyPhoneNumber) bắt đầu bằng đầu số không hợp lệ tại Việt Nam | Form khởi tạo đang mở. | 1. Nhập các trường khác hợp lệ.<br>2. Nhập `companyPhoneNumber` bắt đầu bằng đầu số sai (ví dụ: `012...`).<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và báo lỗi: "Số điện thoại không đúng định dạng đầu số Việt Nam". | Medium | `companyPhoneNumber` = `"0123456789"` |
| `CARDOCTOR_ONBOARD_TC_030` | Sub-module 1 | Medium | Validate trường Số điện thoại (companyPhoneNumber) trùng lặp (Unique Constraint) | Đã có một Tenant tồn tại với SĐT `0987123456`. | 1. Nhập các trường khác hợp lệ.<br>2. Nhập `companyPhoneNumber` là `0987123456`.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và báo lỗi: "Số điện thoại này đã tồn tại trên hệ thống". | Critical | `companyPhoneNumber` = `"0987123456"` |
| `CARDOCTOR_ONBOARD_TC_031` | Sub-module 1 | Medium | Validate trường Email (companyEmailAddress) thiếu ký tự @ | Form khởi tạo đang mở. | 1. Nhập các trường khác hợp lệ.<br>2. Nhập `companyEmailAddress` thiếu `@`.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và báo lỗi: "Email không đúng định dạng". | High | `companyEmailAddress` = `"contactdomain.com"` |
| `CARDOCTOR_ONBOARD_TC_032` | Sub-module 1 | Medium | Validate trường Email (companyEmailAddress) thiếu tên miền sau ký tự @ | Form khởi tạo đang mở. | 1. Nhập các trường khác hợp lệ.<br>2. Nhập `companyEmailAddress` thiếu tên miền.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và báo lỗi: "Email không đúng định dạng". | High | `companyEmailAddress` = `"contact@"` |
| `CARDOCTOR_ONBOARD_TC_033` | Sub-module 1 | Medium | Validate trường Email (companyEmailAddress) chứa nhiều ký tự @ | Form khởi tạo đang mở. | 1. Nhập các trường khác hợp lệ.<br>2. Nhập `companyEmailAddress` chứa 2 ký tự `@`.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và báo lỗi: "Email không đúng định dạng". | High | `companyEmailAddress` = `"contact@@domain.com"` |
| `CARDOCTOR_ONBOARD_TC_034` | Sub-module 1 | Medium | Validate trường Người đại diện (representativeName) bỏ trống | Form khởi tạo đang mở. | 1. Nhập các trường khác hợp lệ.<br>2. Bỏ trống trường `representativeName`.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và báo lỗi: "Tên người đại diện không được để trống". | High | `representativeName` = `""` |
| `CARDOCTOR_ONBOARD_TC_035` | Sub-module 1 | Medium | Validate trường Khu vực hoạt động (operationAreaCodes) không chọn giá trị | Form khởi tạo đang mở. | 1. Nhập các trường khác hợp lệ.<br>2. Không chọn khu vực nào.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và báo lỗi: "Khu vực hoạt động không được để trống". | High | `operationAreaCodes` = `[]` |
| `CARDOCTOR_ONBOARD_TC_036` | Sub-module 1 | Medium | Validate Tổ hợp: Thiếu đồng thời 2 trường bắt buộc (name và subdomain) | Form khởi tạo đang mở. | 1. Nhập các trường khác hợp lệ.<br>2. Bỏ trống cả `name` và `subdomain`.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu, hiển thị đồng thời cả 2 thông báo lỗi dưới 2 trường tương ứng: "Tên Tenant không được để trống" và "Subdomain không được để trống". | High | `name` = `""`, `subdomain` = `""` |
| `CARDOCTOR_ONBOARD_TC_037` | Sub-module 1 | Medium | Validate Tổ hợp: Thiếu đồng thời 3 trường bắt buộc (name, subdomain và companyPhoneNumber) | Form khởi tạo đang mở. | 1. Nhập các trường khác hợp lệ.<br>2. Bỏ trống cả 3 trường bắt buộc trên.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu, hiển thị đồng thời các thông báo lỗi tương ứng dưới cả 3 trường. | High | Các trường bắt buộc = `""` |
| `CARDOCTOR_ONBOARD_TC_037_1`| Sub-module 1 | Medium | Validate Tổ hợp: Thiếu đồng thời 4 trường bắt buộc (name, subdomain, companyPhoneNumber và representativeName) | Form khởi tạo đang mở. | 1. Nhập các trường khác hợp lệ.<br>2. Bỏ trống cả 4 trường bắt buộc trên.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu, hiển thị đồng thời các thông báo lỗi tương ứng dưới cả 4 trường. | High | `name`="", `subdomain`="", `companyPhoneNumber`="", `representativeName`="" |
| `CARDOCTOR_ONBOARD_TC_038` | Sub-module 1 | Medium | Validate Tổ hợp: Để trống hoàn toàn form và click Lưu | Form khởi tạo đang mở. | 1. Mở form, không nhập bất kỳ trường nào.<br>2. Click "Lưu". | 2. Hệ thống chặn lưu, bôi đỏ và hiển thị tất cả các lỗi validate bắt buộc của toàn bộ form biểu mẫu. | High | Form trống |
| **NHÓM UI & BEHAVIOR** | | | | | | | | |
| `CARDOCTOR_ONBOARD_TC_039` | Sub-module 1 | Medium | Kiểm tra giao diện hiển thị các trường nhập liệu và nhãn (Label & Starmark) | Form khởi tạo đang mở. | 1. Quan sát trực quan giao diện form biểu mẫu.<br>2. Kiểm tra các nhãn, placeholder và ký hiệu `*` đỏ cạnh các trường bắt buộc. | 1. Giao diện hiển thị đúng layout, font chữ.<br>2. Các trường bắt buộc có dấu `*` đỏ nổi bật. | Medium | N/A |
| `CARDOCTOR_ONBOARD_TC_040` | Sub-module 1 | Medium | Kiểm tra di chuyển focus bằng phím Tab | Form khởi tạo đang mở. | 1. Đặt con trỏ focus vào trường đầu tiên `Tên Tenant`.<br>2. Nhấn phím `Tab` liên tục để di chuyển qua các trường tiếp theo. | 2. Con trỏ focus di chuyển tuần tự từ trên xuống dưới, từ trái qua phải theo đúng thiết kế của form. | Low | N/A |
| `CARDOCTOR_ONBOARD_TC_041` | Sub-module 1 | Medium | Kiểm tra di chuyển focus bằng phím Shift + Tab | Form khởi tạo đang mở. | 1. Đặt con trỏ focus ở nút `Lưu` ở cuối form.<br>2. Nhấn tổ hợp `Shift + Tab` liên tục. | 2. Con trỏ focus di chuyển tuần tự ngược lại từ dưới lên trên chính xác. | Low | N/A |
| `CARDOCTOR_ONBOARD_TC_042` | Sub-module 1 | Medium | Kiểm tra hành vi focus vào input field | Form khởi tạo đang mở. | 1. Click chuột vào input field bất kỳ (ví dụ: `Subdomain`). | 1. Viền của input field đổi màu làm nổi bật và con trỏ nhấp nháy xuất hiện. | Low | N/A |
| `CARDOCTOR_ONBOARD_TC_043` | Sub-module 1 | Medium | Kiểm tra thay đổi kích thước trình duyệt (Resize) và Zoom In/Out | Form khởi tạo đang mở. | 1. Resize trình duyệt xuống các kích thước nhỏ.<br>2. Zoom In/Out từ 50% đến 200%. | 1-2. Giao diện tự động co giãn tương thích (Responsive), các trường không bị vỡ khung, text không bị chịu cắt, các nút bấm vẫn hiển thị và tương tác được. | Medium | N/A |
| `CARDOCTOR_ONBOARD_TC_043_1` | Sub-module 1 | Medium | Kiểm tra hủy bỏ thao tác bằng nút Hủy | Form khởi tạo đang mở. | 1. Nhập một số thông tin tạm thời vào form.<br>2. Click nút "Hủy". | 2. Hệ thống đóng form hoặc redirect về trang danh sách, thông tin vừa nhập bị xóa sạch, không có Tenant nào được tạo. | High | N/A |
| **NHÓM PHÂN QUYỀN** | | | | | | | | |
| `CARDOCTOR_ONBOARD_TC_043_2` | Sub-module 1 | High | Kiểm tra quyền tạo mới Tenant với tài khoản có quyền hợp lệ | Đăng nhập bằng tài khoản có role `SYSTEM_ADMIN`. | 1. Truy cập màn hình Khởi tạo Tenant.<br>2. Nhập đầy đủ thông tin hợp lệ và nhấn "Lưu". | 1. Truy cập form thành công.<br>2. Tạo mới Tenant thành công, audit log ghi nhận chính xác actor. | Critical | N/A |
| `CARDOCTOR_ONBOARD_TC_043_3` | Sub-module 1 | High | Kiểm tra hệ thống từ chối truy cập khi tài khoản không có quyền | Đăng nhập bằng tài khoản `GARAGE_USER`. | 1. Cố tình truy cập trực tiếp URL trang khởi tạo hoặc gọi API POST tạo mới. | 1. Hệ thống từ chối truy cập, hiển thị thông báo lỗi 403 Forbidden. Không ghi nhận dữ liệu vào DB. | Critical | N/A |
| **NHÓM CÁC PHẦN ẢNH HƯỞNG CHỨC NĂNG LIÊN QUAN** | | | | | | | | |
| `CARDOCTOR_ONBOARD_TC_043_4` | Sub-module 1 | High | Kiểm tra dữ liệu Tenant hiển thị chính xác trên Màn hình Danh sách (List) sau khi tạo thành công | Tạo mới Tenant thành công. | 1. Quay lại Màn hình Danh sách Tenant.<br>2. Kiểm tra dòng đầu tiên trong bảng danh sách. | 2. Tenant mới tạo hiển thị ở dòng đầu tiên, các thông tin tên, subdomain, SĐT, loại, stage/status (PROVISIONED) hiển thị chính xác. | High | N/A |
| `CARDOCTOR_ONBOARD_TC_043_5` | Sub-module 1 | High | Kiểm tra dữ liệu hiển thị chính xác trên Màn hình Chi tiết (Detail) | Tạo mới Tenant thành công. | 1. Click vào Tenant mới tạo trên danh sách để xem chi tiết. | 1. Màn hình chi tiết hiển thị đúng 100% tất cả các trường dữ liệu đã nhập ở form tạo mới. | High | N/A |
| `CARDOCTOR_ONBOARD_TC_043_6` | Sub-module 1 | High | Kiểm tra tính toàn vẹn dữ liệu trong Database (Database Record Integrity) | Tạo mới Tenant thành công. | 1. Thực hiện truy vấn trực tiếp bảng `tenant` trong Database PostgreSQL. | 1. Bản ghi được tạo đầy đủ các cột: id tự tăng, code tự sinh theo format, stage/status là PROVISIONED, createdAt/updatedAt lưu đúng timestamp. | Critical | N/A |
| | | | | | | | |
| **PHẦN 2: CẤU HÌNH HÓA ĐƠN** | | | | | | | | |
| **NHÓM FUNCTION** | | | | | | | | |
| `CARDOCTOR_ONBOARD_TC_044` | Sub-module 2 | High | Khởi tạo Tenant loại GARAGE thành công khi điền thông tin hóa đơn khi flag PurchaseV02 bật | Đăng nhập bằng `SYSTEM_ADMIN`. Flag `Purchase:PurchaseV02` đang ON. | 1. Nhập thông tin cơ bản hợp lệ của GARAGE.<br>2. Điền đầy đủ thông tin hóa đơn (`taxCode`, `invoiceCompanyName`, `invoiceCompanyAddress`).<br>3. Click "Lưu". | 3. Hệ thống báo tạo mới Tenant thành công. Thông tin hóa đơn được lưu vào bảng `tenant_invoice_info`. | Critical | `taxCode`: "0102030405", `invoiceCompanyName`: "Công ty Gara Tây Hồ", `invoiceCompanyAddress`: "Tây Hồ, Hà Nội" |
| `CARDOCTOR_ONBOARD_TC_045` | Sub-module 2 | High | Khởi tạo Tenant loại GARAGE thành công và bỏ trống hóa đơn khi flag PurchaseV02 tắt | Đăng nhập bằng `SYSTEM_ADMIN`. Flag `Purchase:PurchaseV02` đang OFF. | 1. Nhập thông tin cơ bản hợp lệ của GARAGE.<br>2. Bỏ trống toàn bộ thông tin hóa đơn.<br>3. Click "Lưu". | 3. Hệ thống báo tạo mới Tenant thành công mà không hiển thị lỗi bắt buộc điền thông tin hóa đơn. | High | N/A |
| `CARDOCTOR_ONBOARD_TC_045_1`| Sub-module 2 | High | Khởi tạo Tenant loại GARAGE thành công khi flag PurchaseV02 TẮT nhưng client vẫn cố tình truyền dữ liệu hóa đơn | Đăng nhập bằng `SYSTEM_ADMIN`. Flag `Purchase:PurchaseV02` đang OFF. | 1. Nhập thông tin cơ bản hợp lệ của GARAGE.<br>2. Vẫn điền đầy đủ dữ liệu hóa đơn.<br>3. Click "Lưu". | 3. Hệ thống lưu thành công và vẫn lưu các trường hóa đơn này vào DB (hệ thống xử lý bỏ qua an toàn, không báo lỗi). | High | Dữ liệu hóa đơn hợp lệ |
| **NHÓM VALIDATE** | | | | | | | | |
| `CARDOCTOR_ONBOARD_TC_046` | Sub-module 2 | Medium | Validate trường Mã số thuế (taxCode) bỏ trống khi flag PurchaseV02 bật | Flag `Purchase:PurchaseV02` đang ON. Tạo GARAGE. | 1. Nhập thông tin khác hợp lệ.<br>2. Bỏ trống trường `taxCode`.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và báo lỗi: "Mã số thuế không được để trống". | High | `taxCode` = `""` |
| `CARDOCTOR_ONBOARD_TC_047` | Sub-module 2 | Medium | Validate trường Mã số thuế (taxCode) vượt quá độ dài tối đa 50 ký tự | Flag `Purchase:PurchaseV02` đang ON. Tạo GARAGE. | 1. Nhập thông tin khác hợp lệ.<br>2. Nhập `taxCode` dài 51 ký tự.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và báo lỗi: "Mã số thuế không được vượt quá 50 ký tự". | Medium | Chuỗi 51 ký tự |
| `CARDOCTOR_ONBOARD_TC_048` | Sub-module 2 | Medium | Validate trường Tên công ty hóa đơn (invoiceCompanyName) bỏ trống khi flag PurchaseV02 bật | Flag `Purchase:PurchaseV02` đang ON. Tạo GARAGE. | 1. Nhập thông tin khác hợp lệ.<br>2. Bỏ trống trường `invoiceCompanyName`.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và báo lỗi: "Tên công ty xuất hóa đơn không được để trống". | High | `invoiceCompanyName` = `""` |
| `CARDOCTOR_ONBOARD_TC_049` | Sub-module 2 | Medium | Validate trường Địa chỉ hóa đơn (invoiceCompanyAddress) bỏ trống khi flag PurchaseV02 bật | Flag `Purchase:PurchaseV02` đang ON. Tạo GARAGE. | 1. Nhập thông tin khác hợp lệ.<br>2. Bỏ trống trường `invoiceCompanyAddress`.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và báo lỗi: "Địa chỉ xuất hóa đơn không được để trống". | High | `invoiceCompanyAddress` = `""` |
| `CARDOCTOR_ONBOARD_TC_050` | Sub-module 2 | Medium | Validate trường Email nhận hóa đơn (invoiceCompanyEmailAddress) sai định dạng | Form cấu hình đang mở. | 1. Nhập thông tin khác hợp lệ.<br>2. Nhập `invoiceCompanyEmailAddress` sai cấu trúc.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và báo lỗi: "Email nhận hóa đơn không đúng định dạng". | High | `invoiceCompanyEmailAddress` = `"invoice@tayho..vn"` |
| `CARDOCTOR_ONBOARD_TC_051` | Sub-module 2 | Medium | Validate Tổ hợp: Thiếu toàn bộ các trường trong nhóm hóa đơn (PurchaseV02 bật) | Flag PurchaseV02 đang ON. Tạo GARAGE. | 1. Nhập thông tin khác hợp lệ.<br>2. Bỏ trống cả 3 trường: `taxCode`, `invoiceCompanyName`, `invoiceCompanyAddress`.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu, bôi đỏ và hiển thị đồng thời cả 3 thông báo lỗi bắt buộc của nhóm hóa đơn. | High | Cả 3 trường hóa đơn = `""` |
| `CARDOCTOR_ONBOARD_TC_052` | Sub-module 2 | Medium | Validate Logic: Chọn loại VENDOR nhưng điền thông tin hóa đơn (Verify hệ thống xử lý bỏ qua) | Flag PurchaseV02 đang ON. Tạo VENDOR. | 1. Chọn loại Tenant là VENDOR.<br>2. Nhập thông tin hóa đơn hợp lệ.<br>3. Click "Lưu". | 3. Hệ thống lưu thành công Tenant VENDOR, nhưng không bắt buộc mapping hay kiểm tra chặt chẽ các trường hóa đơn này đối với luồng VENDOR. | Medium | `type`: "VENDOR", `taxCode`: "010203" |
| **NHÓM UI & BEHAVIOR** | | | | | | | | |
| `CARDOCTOR_ONBOARD_TC_053` | Sub-module 2 | Medium | Kiểm tra starmark (*) đỏ hiển thị ở các trường hóa đơn khi flag PurchaseV02 bật | Flag `Purchase:PurchaseV02` đang ON. Chọn loại Tenant là GARAGE. | 1. Quan sát các trường thông tin hóa đơn trên form. | 1. Các trường `Mã số thuế`, `Tên công ty xuất hóa đơn`, `Địa chỉ xuất hóa đơn` có dấu `*` đỏ. Khi chọn loại VENDOR, các dấu `*` đỏ này biến mất. | Medium | N/A |
| **NHÓM PHÂN QUYỀN** | | | | | | | | |
| `CARDOCTOR_ONBOARD_TC_054` | Sub-module 2 | High | Kiểm tra quyền cập nhật thông tin hóa đơn qua API riêng | Đăng nhập bằng `CD_BD_HEAD` (Role được phép cập nhật). Flag PurchaseV02 đang ON. | 1. Gọi API PUT `/api/v1/saas-tenant/{tenantId}/invoice-info` để thay đổi thông tin hóa đơn. | 1. API xử lý thành công, trả về HTTP 200 OK. Dữ liệu hóa đơn được cập nhật. | High | N/A |
| **NHÓM CÁC PHẦN ẢNH HƯỞNG CHỨC NĂNG LIÊN QUAN** | | | | | | | | |
| `CARDOCTOR_ONBOARD_TC_055` | Sub-module 2 | High | Kiểm tra Kafka event publish sau khi cập nhật thông tin hóa đơn thành công | Tenant đã ở trạng thái ACTIVE / ACTIVATED. | 1. Thực hiện cập nhật thông tin hóa đơn thành công.<br>2. Kiểm tra Message Broker Kafka. | 2. Sự kiện `TENANT_INVOICE_INFO_UPSERT_REQUESTED` được publish thành công ra topic `AC-DEV-TENANT-INVOICE-INFO` sau khi transaction commit. | High | N/A |
| `CARDOCTOR_ONBOARD_TC_056` | Sub-module 2 | High | Kiểm tra không cho phép cập nhật hóa đơn khi Tenant ở trạng thái tạm khóa | Tenant có stage = INACTIVE và status = DEACTIVATING. | 1. Gọi API PUT `/api/v1/saas-tenant/{tenantId}/invoice-info` để cập nhật hóa đơn. | 1. Hệ thống từ chối cập nhật, trả về mã lỗi 400 Bad Request: "Không thể cập nhật hóa đơn cho Tenant đang tạm khóa". | High | N/A |
| `CARDOCTOR_ONBOARD_TC_056_1`| Sub-module 2 | High | Kiểm tra tính toàn vẹn dữ liệu trong bảng `tenant_invoice_info` sau khi lưu | Điền đầy đủ thông tin hóa đơn thành công. | 1. Truy vấn trực tiếp bảng `tenant_invoice_info` trong Database. | 1. Bản ghi được tạo đầy đủ, trường `tax_code`, `company_name`, `company_address` khớp 100% dữ liệu đã nhập. | High | N/A |
| | | | | | | | |
| **PHẦN 3: GÁN NHÂN VIÊN TƯ VẤN** | | | | | | | | |
| **NHÓM FUNCTION** | | | | | | | | |
| `CARDOCTOR_ONBOARD_TC_057` | Sub-module 3 | High | Gán tư vấn viên thành công cho GARAGE khi flag QuotationConsultant bật | Đăng nhập bằng `SYSTEM_ADMIN`. Flag `Marketplace:QuotationConsultant` đang ON. | 1. Nhập thông tin Tenant GARAGE hợp lệ.<br>2. Chọn một nhân viên tư vấn (`consultantId` = 1001).<br>3. Click "Lưu". | 3. Hệ thống lưu thành công. Trong bảng `tenant` ghi nhận cột `consultant_id = 1001`. | High | `consultantId` = 1001 |
| `CARDOCTOR_ONBOARD_TC_057_1`| Sub-module 3 | High | Khởi tạo Tenant loại GARAGE thành công khi flag QuotationConsultant TẮT nhưng client vẫn cố tình truyền consultantId | Đăng nhập bằng `SYSTEM_ADMIN`. Flag `Marketplace:QuotationConsultant` đang OFF. | 1. Nhập thông tin Tenant GARAGE hợp lệ.<br>2. Truyền `consultantId = 1001` trong API request.<br>3. Click "Lưu". | 3. Hệ thống lưu thành công, bỏ qua bước gọi HRMS validate consultantId, và trường consultant_id trong DB là null hoặc lưu 1001 tùy hành vi legacy. | High | `consultantId` = 1001 |
| **NHÓM VALIDATE** | | | | | | | | |
| `CARDOCTOR_ONBOARD_TC_058` | Sub-module 3 | Medium | Validate trường Nhân viên tư vấn (consultantId) bỏ trống khi flag QuotationConsultant bật | Flag `Marketplace:QuotationConsultant` đang ON. Tạo GARAGE. | 1. Nhập thông tin khác hợp lệ.<br>2. Bỏ trống trường `consultantId`.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và báo lỗi: "Vui lòng chọn nhân viên tư vấn gán cho Garage". | High | `consultantId` = `null` |
| `CARDOCTOR_ONBOARD_TC_059` | Sub-module 3 | Medium | Validate trường Nhân viên tư vấn (consultantId) không tồn tại trong hệ thống | Flag QuotationConsultant đang ON. Tạo GARAGE. | 1. Nhập thông tin khác hợp lệ.<br>2. Nhập `consultantId` là mã không tồn tại.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và báo lỗi: "Mã nhân viên tư vấn không tồn tại trên hệ thống". | High | `consultantId` = 999999 |
| `CARDOCTOR_ONBOARD_TC_059_1`| Sub-module 3 | Medium | Validate trường Nhân viên tư vấn (consultantId) đang ở trạng thái INACTIVE trong HRMS | Flag QuotationConsultant đang ON. Tạo GARAGE. | 1. Nhập thông tin khác hợp lệ.<br>2. Nhập `consultantId` của nhân viên có status INACTIVE trong HRMS.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và báo lỗi: "Nhân viên tư vấn này không còn hoạt động". | High | `consultantId` = 1002 (InActive) |
| | | | | | | | |
| **PHẦN 4: KHỞI TẠO CHI NHÁNH & KHO MẶC ĐỊNH** | | | | | | | | |
| **NHÓM FUNCTION** | | | | | | | | |
| `CARDOCTOR_ONBOARD_TC_060` | Sub-module 4 | High | Khởi tạo Tenant thành công và tự động kích hoạt luồng tạo Branch/Warehouse khi flag InventoryStockV01 bật | Đăng nhập bằng `SYSTEM_ADMIN`. Flag `Inventory:InventoryStockV01` đang ON. | 1. Nhập thông tin Tenant hợp lệ.<br>2. Điền đầy đủ địa chỉ trụ sở (`companyHoAddress`, `city`, `ward`).<br>3. Click "Lưu". | 3. Hệ thống báo tạo mới thành công. | Critical | `companyHoAddress`: "Xuân Diệu, Hà Nội", `city`: "01", `ward`: "00015" |
| `CARDOCTOR_ONBOARD_TC_060_1`| Sub-module 4 | High | Khởi tạo Tenant thành công khi flag InventoryStockV01 TẮT nhưng client vẫn cố tình truyền thông tin địa chỉ | Đăng nhập bằng `SYSTEM_ADMIN`. Flag `Inventory:InventoryStockV01` đang OFF. | 1. Nhập thông tin Tenant hợp lệ.<br>2. Vẫn truyền `companyHoAddress`, `city`, `ward`.<br>3. Click "Lưu". | 3. Hệ thống lưu thành công (hệ thống xử lý bỏ qua an toàn, không báo lỗi). | High | Dữ liệu địa chỉ hợp lệ |
| **NHÓM VALIDATE** | | | | | | | | |
| `CARDOCTOR_ONBOARD_TC_061` | Sub-module 4 | Medium | Validate trường Địa chỉ trụ sở (companyHoAddress) bỏ trống khi flag InventoryStockV01 bật | Flag `Inventory:InventoryStockV01` đang ON. | 1. Nhập thông tin khác hợp lệ.<br>2. Bỏ trống trường `companyHoAddress`.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và báo lỗi: "Địa chỉ trụ sở chính không được để trống". | High | `companyHoAddress` = `""` |
| `CARDOCTOR_ONBOARD_TC_062` | Sub-module 4 | Medium | Validate trường Tỉnh/Thành phố (city) bỏ trống khi flag InventoryStockV01 bật | Flag `Inventory:InventoryStockV01` đang ON. | 1. Nhập thông tin khác hợp lệ.<br>2. Bỏ trống trường `city`.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và báo lỗi: "Vui lòng chọn Tỉnh/Thành phố trụ sở". | High | `city` = `""` |
| `CARDOCTOR_ONBOARD_TC_063` | Sub-module 4 | Medium | Validate trường Phường/Xã (ward) bỏ trống khi flag InventoryStockV01 bật | Flag `Inventory:InventoryStockV01` đang ON. | 1. Nhập thông tin khác hợp lệ.<br>2. Bỏ trống trường `ward`.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và báo lỗi: "Vui lòng chọn Phường/Xã trụ sở". | High | `ward` = `""` |
| `CARDOCTOR_ONBOARD_TC_063_1` | Sub-module 4 | Medium | Validate Tổ hợp: Thiếu toàn bộ nhóm địa chỉ (companyHoAddress, city, ward) khi flag Inventory bật | Flag InventoryStockV01 đang ON. | 1. Nhập các trường khác hợp lệ.<br>2. Bỏ trống cả 3 trường địa chỉ.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu, bôi đỏ và báo lỗi đồng thời cả 3 trường thuộc nhóm địa chỉ. | High | Cả 3 trường địa chỉ = `""` |
| **NHÓM CÁC PHẦN ẢNH HƯỞNG CHỨC NĂNG LIÊN QUAN** | | | | | | | | |
| `CARDOCTOR_ONBOARD_TC_064` | Sub-module 4 | High | Kiểm tra Kafka event publish yêu cầu provisioning default branch/warehouse | Onboard Tenant thành công và bật flag Inventory. | 1. Kiểm tra Message Broker Kafka. | 1. Hệ thống gửi thành công event `TENANT_PROVISIONED` ra topic `AC-DEV-TENANT-PROVISIONING` với headers và payload chứa thông tin địa chỉ, max chi nhánh/kho. | Critical | N/A |
| `CARDOCTOR_ONBOARD_TC_065` | Sub-module 4 | High | Kiểm tra đồng bộ dữ liệu default branch/warehouse từ gf-system và gf-inventory | Event `TENANT_PROVISIONED` đã được publish. | 1. Hệ thống downstream xử lý event và gửi lại event `BRANCH_CREATED.1` và `WAREHOUSE_CREATED.1`.<br>2. Kiểm tra API `GET /api/v1/saas-tenant/{tenantId}/default-branch`. | 2. Dữ liệu default branch và warehouse của Tenant được lưu thành công vào master data nội bộ và hiển thị trên tab Chi nhánh/Kho. | High | N/A |
| `CARDOCTOR_ONBOARD_TC_065_1`| Sub-module 4 | High | Kiểm tra logic không tự động tạo Branch/Warehouse mới từ COP mà chỉ kế thừa/sync từ event | Nhận sự kiện tạo chi nhánh bên ngoài. | 1. Verify code backend trong service xem có thực hiện CRUD trực tiếp bảng Branch/Warehouse hay không. | 1. Hệ thống chỉ lưu master data sync từ event Kafka bên ngoài, không tự CRUD trực tiếp (đảm bảo tính toàn vẹn dữ liệu legacy). | High | N/A |
| | | | | | | | |
| **PHẦN 5: TẠO TÀI KHOẢN QUẢN TRỊ OWNER IAM** | | | | | | | | |
| **NHÓM FUNCTION** | | | | | | | | |
| `CARDOCTOR_ONBOARD_TC_066` | Sub-module 5 | High | Tạo thành công local user và trigger tạo tài khoản Owner trên Keycloak khi onboard Tenant | Đăng nhập bằng `SYSTEM_ADMIN`. | 1. Khởi tạo Tenant thành công.<br>2. Hệ thống gọi API POST `/api/v1/saas-tenant/create-user` để tạo user với role `OWNER`. | 2. Bản ghi local user được tạo với role `OWNER`. Hệ thống tự động gọi Keycloak tạo tài khoản IAM và trả về username/temporaryPassword. | Critical | `fullName`: "Nguyễn Owner", `phoneNumber`: "0987654321", `role`: "OWNER" |
| **NHÓM VALIDATE** | | | | | | | | |
| `CARDOCTOR_ONBOARD_TC_067` | Sub-module 5 | Medium | Validate trường Họ và tên user (fullName) bỏ trống | Gọi API create-user. | 1. Nhập các trường khác hợp lệ.<br>2. Bỏ trống `fullName`.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và báo lỗi: "Họ và tên người dùng không được để trống". | High | `fullName` = `""` |
| `CARDOCTOR_ONBOARD_TC_068` | Sub-module 5 | Medium | Validate trường Số điện thoại user (phoneNumber) sai định dạng hoặc bỏ trống | Gọi API create-user. | 1. Nhập các trường khác hợp lệ.<br>2. Nhập `phoneNumber` trống hoặc chứa ký tự chữ.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và báo lỗi: "Số điện thoại không được để trống hoặc sai định dạng". | High | `phoneNumber` = `"0987.ABC"` |
| `CARDOCTOR_ONBOARD_TC_069` | Sub-module 5 | Medium | Validate trường Vai trò user (role) không hợp lệ | Gọi API create-user. | 1. Nhập các trường khác hợp lệ.<br>2. Nhập `role` là mã không tồn tại.<br>3. Click "Lưu". | 3. Hệ thống chặn lưu và báo lỗi: "Vai trò người dùng không hợp lệ". | Medium | `role` = `"INVALID_ROLE"` |
| **NHÓM PHÂN QUYỀN** | | | | | | | | |
| `CARDOCTOR_ONBOARD_TC_070` | Sub-module 5 | High | Kiểm tra quyền tạo user thuộc Tenant | Đăng nhập bằng tài khoản có role `SaasControlTenantUser: saasControlTenantUser:create`. | 1. Thực hiện gọi API POST `/api/v1/saas-tenant/create-user`. | 1. Tạo user thành công. Tài khoản không có quyền này sẽ bị trả lỗi 403 Forbidden. | High | N/A |
| **NHÓM CÁC PHẦN ẢNH HƯỞNG CHỨC NĂNG LIÊN QUAN** | | | | | | | | |
| `CARDOCTOR_ONBOARD_TC_071` | Sub-module 5 | High | Kiểm tra Kafka event publish TenantUserCreatedEvent sau khi tạo IAM user thành công | Tạo IAM user thành công trên Keycloak. | 1. Kiểm tra Message Broker Kafka. | 1. Sự kiện `TenantUserCreatedEvent` được publish ra topic `AC-DEV-TENANT-USER-CREATED-RESULTS` chứa tempPassword và iamUserId. | High | N/A |
| `CARDOCTOR_ONBOARD_TC_072` | Sub-module 5 | High | Kiểm tra cơ chế Thử lại tạo IAM Tenant (Retry IAM) khi Keycloak gặp lỗi | Khởi tạo Tenant thành công nhưng Keycloak down, bản ghi local user chưa có `iamUserId`. | 1. Bật lại dịch vụ Keycloak.<br>2. Gọi API POST `/api/v1/saas-tenant/{tenantId}/retry-iam` bằng tài khoản System Admin. | 2. Hệ thống gọi lại Keycloak tạo tài khoản thành công, cập nhật `iamUserId` cho user trong DB và hoàn tất. | Critical | N/A |
| | | | | | | | |
| **PHẦN 6: HOÀN TẤT THÔNG TIN & KÍCH HOẠT TENANT** | | | | | | | | |
| **NHÓM FUNCTION** | | | | | | | | |
| `CARDOCTOR_ONBOARD_TC_073` | Sub-module 6 | High | Đánh dấu Tenant hoàn tất thông tin (completed) thành công | Tenant đang ở stage = PROVISIONED. Dữ liệu đã đầy đủ. | 1. Gọi API POST `/api/v1/saas-tenant/completed` với body chứa ID của Tenant. | 1. Hệ thống cập nhật trạng thái Tenant sang `status = ACTIVATING` và bắt đầu luồng kích hoạt/gọi Jenkins tạo subdomain. | Critical | Body: `[123]` (ID Tenant) |
| `CARDOCTOR_ONBOARD_TC_074` | Sub-module 6 | High | Kích hoạt Tenant thành công sau khi subdomain hoạt động (Terminal State ACTIVE) | Tenant đang ở status = ACTIVATING. Job update status quét qua và thấy domain ping thông. | 1. Hệ thống Jenkins hoàn tất tạo DNS và subdomain hoạt động.<br>2. Job update status chạy.<br>3. Kiểm tra DB. | 3. Bản ghi Tenant chuyển trạng thái cuối cùng thành `stage = ACTIVE` và `status = ACTIVATED`. | Critical | N/A |
| **NHÓM VALIDATE** | | | | | | | | |
| `CARDOCTOR_ONBOARD_TC_075` | Sub-module 6 | Medium | Validate gọi completed với danh sách Tenant IDs trống hoặc ID không tồn tại | Đăng nhập bằng `SYSTEM_ADMIN`. | 1. Gọi API completed với body rỗng `[]` hoặc ID `999999` không tồn tại.<br>2. Nhấn nút "Hoàn tất". | 2. Hệ thống chặn và trả lỗi: "Danh sách Tenant ID không hợp lệ hoặc không tìm thấy Tenant". | High | Body: `[]` và `[999999]` |
| **NHÓM CÁC PHẦN ẢNH HƯỞNG CHỨC NĂNG LIÊN QUAN** | | | | | | | | |
| `CARDOCTOR_ONBOARD_TC_076` | Sub-module 6 | High | Kiểm tra Kafka event publish TENANT_ACTIVATED sau khi Tenant chuyển trạng thái ACTIVATED thành công | Tenant chuyển trạng thái ACTIVATED thành công. | 1. Kiểm tra Message Broker Kafka. | 1. Sự kiện `TENANT_ACTIVATED` được gửi ra topic `AC-DEV-TENANT-ACTIVATION` để thông báo cho các service hạ nguồn. | Critical | N/A |
| `CARDOCTOR_ONBOARD_TC_077` | Sub-module 6 | High | Kiểm tra Kafka event publish TENANT_SYNC_TO_ACCOUNTING để đồng bộ kế toán ERP | Flag sync-to-accounting = true. Tenant chuyển ACTIVATED. | 1. Kiểm tra Message Broker Kafka. | 1. Sự kiện `TENANT_SYNC_TO_ACCOUNTING` được publish ra topic `AC-DEV-TENANT-ACTIVATION` dạng JSON array chứa thông tin Tenant. | High | N/A |
| `CARDOCTOR_ONBOARD_TC_078` | Sub-module 6 | High | Kiểm tra hoạt động của Job TenantActivationStatusProbeJob quét trạng thái tự động | Job được kích hoạt định kỳ. | 1. Hệ thống scan các Tenant đang ở trạng thái `ACTIVATING` hoặc `DEACTIVATING`.<br>2. Build URL dạng `https://{subdomain}.cardoctor.vn` và dùng RestClient để ping.<br>3. Cập nhật Tenant sang trạng thái cuối cùng (ACTIVE/INACTIVE) tương ứng kết quả reachability. | 3. Trạng thái Tenant được cập nhật chính xác và tự động mà không cần can thiệp thủ công. | High | N/A |
